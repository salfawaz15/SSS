# بوابة وحدة الإرشاد الأكاديمي والخريجين - كلية إدارة الأعمال - جامعة الطائف

تطبيق ويب Flutter/Firebase لإدارة عمليات الإرشاد الأكاديمي: الجداول الدراسية، توزيع الأعباء، متابعة حالات الإرشاد، التقارير، وحسابات المستخدمين.

- Firebase Hosting: https://sss-advising-tu.web.app
- دورة النشر المعتادة: `flutter analyze` → `flutter build web --release --pwa-strategy=none` → `firebase deploy --only hosting` (وقواعد Firestore عبر `firebase deploy --only firestore:rules` عند تغييرها).

## سجل الإنجاز

### إحصائية عدد الحالات (لا نسبة إنجاز) حسب القسم/الشطر والعضو (2026-08-30)

بطلب سليمان صراحةً: "عدد الحالات فقط قبل العمل عليها" - مؤشر خام يجيب مباشرة "أكثر عضو/قسم لديه حالات؟" بصرف النظر عن حالة الإنجاز (بخلاف جداول نسبة الإنجاز الموجودة أصلًا).

- **تطبيق الجوال** (`admin_summary_screen.dart`، تبويب "الحذف والإضافة"): قسمان جديدان "عدد الحالات حسب القسم والشطر" (`_DepartmentCaseCountTable`) و"عدد الحالات حسب العضو" (`_AdvisorCaseCountTable`)، كلاهما مرتَّب تنازليًا حسب العدد.
- **لوحة إدارة الموقع** (`admin_executive_dashboard_screen.dart`، تبويب "مؤشرات الحذف والإضافة"): قسم جديد `_CaseCountBreakdownSection` بنفس الفكرة، يستجيب لفلاتر القسم/الشطر أعلى الصفحة (`_computeDeptCaseCounts`/`_computeAdvisorCaseCounts`).
- لم يُنشَر بعد - بانتظار مراجعة سليمان على المعاينة المحلية.

### استثناء مرشد محدد من استقبال حالات التفريغ (2026-08-30)

بطلب سليمان صراحةً: عند توزيع حالات منسّق قسم (أو مرشد في إجازة) بالتساوي على بقية مرشدي القسم/الشطر عند بناء ملفات "الحذف والإضافة" (`AdvisorZipService`)، يمكن الآن استثناء عضو محدد من استقبال أي حالات مُعاد توزيعها، مع بقائه مرشدًا عاديًا يستلم ملفه بحالاته الخاصة فقط.

- حقل جديد `excludedFromRelief` في `AdvisorRosterEntry` (Firestore: `excluded_from_relief`).
- خيار "استثناء من استقبال حالات التفريغ" بشاشة "قائمة مرشدي القسم" (`advisor_roster_screen.dart`).
- `AdvisorZipService._groupWithCoordinatorRelief` يستثني هذا العضو من `regularsByGroup` (نفس معاملة المنسّق/المُجاز، لكن بلا نقل حالاته هو).
- أول استخدام: استثناء زياد حمود عبدالعزيز العضيله المطيري من توزيع حالات منسّق قسم نظم المعلومات الإدارية صالح حامد احمد العريفي.
- نُشر على الموقع الحي (2026-08-30).

### تغيير بريد إرسال تقارير المنسّقين إلى البريد الجامعي (2026-08-30)

`Secrets.senderEmail`/`recipientEmail` بـ`lib/config/secrets.dart` أصبحا `salfawaz@tu.edu.sa` بدل `tualfawaz@gmail.com` - بعد توثيق البريد الجامعي كـ"Single Sender" بحساب SendGrid. لا يغيّر هذا من يستقبل التقرير (يبقى منسّق القسم فقط عبر `MailService.sendDepartmentReport`)، فقط عنوان "من" الظاهر له. نُشر على الموقع الحي (2026-08-30).

### حذف عمود "إجباري/اختياري" من ملف مواد الطلاب/الطالبات (2026-08-29)

بطلب سليمان صراحةً: التصنيف كان غير صحيح إطلاقًا (بيانات `elective` بـ`course_catalog.dart` مُستخرَجة يدويًا من خطط الأقسام وناقصة/خاطئة لمقررات كثيرة). أُزيل العمود بالكامل من جدول `_coursesTable` بـ`course_schedule_student_pdf_service.dart` (الدالة `_mandatoryLabel` والمنطق المرتبط بها حُذفا)، مع إبقاء حقل `elective` نفسه بـ`course_catalog.dart` لاستخدامات أخرى (دليل الوحدة، سجل الكلية).

### فصل يومي حقيقي بملفات "الحذف والإضافة" المُصدَّرة (2026-08-29)

بطلب سليمان صراحةً بعد نقاش مطوَّل حول آلية عمل المرشد بين الأيام الثلاثة: الملفات المُصدَّرة لأي مرحلة (المرشد/منسّق القسم/منسّق الكلية/ذوي الإعاقة/تقرير البريد) أصبحت مقسَّمة بفواصل يومية فعلية بدل التداخل الكامل السابق (كل الحالات مرتَّبة فقط بالساعات المتبقية بلا أي اعتبار ليوم الرفع).

- **`FirestoreTicketService`**: كل تذكرة تُسجَّل الآن بحقل `uploaded_date` (تاريخ يومها بصيغة `yyyy-MM-dd`) لحظة إدخالها لأول مرة (سواء عبر "دورة جديدة" أو "رفعة يوم تالٍ") - لا يُعاد كتابته لاحقًا.
- **`ExcelExportService.buildDepartmentWorkbook`**: الفرز أصبح باليوم أولًا (الأقدم أعلى الملف - الحالات القديمة بلا `uploaded_date` تُعامَل كأقدم فتظهر أولًا)، ثم نفس منطق الساعات المتبقية/الرقم الجامعي كالسابق داخل كل يوم. فوق كل مجموعة يوم صف عنوان ملوَّن (يتكرر اللون كل 3 أيام) بتاريخ اليوم، يليه صف فارغ كامل يفصله عن اليوم التالي. تلوين أولوية التخرّج (وردي/أصفر) بقي كما هو على صفوف البيانات نفسها - لم يُستبدَل بلون اليوم حتى لا يطغى عليه (تمييز وظيفي أهم).
- الدالة أصبحت تُرجع `DepartmentWorkbookResult` (البايتات + `totalDataRowCount` الفعلي شاملاً صفوف العنوان/الفواصل) بدل بايتات فقط - ضروري لضبط نطاق القوائم المنسدلة بشكل صحيح عند الحماية (`ExcelProtectionService.protect`)؛ حُدِّثت كل نقاط الاستدعاء الخمس (`advisor_zip_service.dart`, `escalation_file_service.dart`, `disability_file_service.dart`, `mail_service.dart`, واختبار `ticket_merge_test.dart`).
- تحقَّقتُ أن 3 اختبارات فاشلة بـ`ticket_merge_test.dart` (خاصة بدمج `TicketRepository` المحلي لتطبيق الجوال) فاشلة أصلًا على الكود الأصلي قبل هذا التعديل (تحقُّق مباشر عبر `git stash`) - لا علاقة لها بهذا التغيير.
- تُطابق الآن صفحة 8 من "الدليل الإجرائي للمرشدين الأكاديميين" (PDF رسمي أعدَّه سليمان) فعليًا شكل الملف الحقيقي المُصدَّر، بعد أن كانت تصف تصميمًا مقترَحًا فقط.

### تنزيل ملف مواد الطلاب/الطالبات الدراسية - نسخة طالب مبسّطة (2026-08-27)

أُضيف زر تنزيل تحت كل من بطاقتَي "مقررات شطر الطلاب"/"مقررات شطر الطالبات" بصفحة "رفع الملفات" (`upload_hub_screen.dart`) ينتج ملفًا مضغوطًا (ZIP) بـ5 ملفات PDF (ملف لكل قسم من أقسام الكلية الخمسة) - 10 ملفات إجمالاً بين الشطرين. كل ملف يعرض فقط ما يحق للطالب رؤيته من بيانات المقرر: رمز المقرر، اسمه، نوعه (نظري/عملي)، رقم الشعبة، اليوم، الوقت، الحضور (حضوري/عن بعد - بلا رقم القاعة نفسه)، وتصنيف إجباري/اختياري - **بلا** اسم عضو هيئة التدريس أو عدد المسجلين (بيانات تشغيلية داخلية). ينتهي كل ملف بقسم منفصل واضح "مقررات من خارج الكلية".

المصدر: نفس ملف CSV الحويّة المرفوع أصلاً لتسكين المقررات (`CourseScheduleRepository`) + `OutsideCourseRepository`، بنفس منطق تصنيف "المقرر متاح لأي قسم" المستخدَم أصلاً بشاشة إدارة المقررات (القسم المالك + المقررات المشتركة بين كل الأقسام + المقررات "المتاحة أيضًا" لقسم إضافي).

تصنيف إجباري/اختياري: أُضيف حقل `elective` لـ`CourseCatalogEntry` (`lib/data/course_catalog.dart`)، مُستخرَج يدويًا من ملفات الخطط الدراسية الرسمية - أولاً 4 أقسام (الإدارة، التسويق، المحاسبة، نظم المعلومات الإدارية)، ثم أُكمِل قسم الاقتصاد والتمويل (اقتصاد/تمويل/تأمين) بعد أن رفع سليمان ملفَي "تقرير خطط 38/47" الشاملين للكلية. عند تعارض التصنيف بين برنامجين فرعيين لنفس رمز المقرر اعتُمد "اختياري" كخيار أكثر تحفّظًا.

خدمة جديدة: `lib/services/course_schedule_student_pdf_service.dart` (بنفس الهوية البصرية الموحَّدة لملفات PDF عبر `PdfBrandKit`).

### نشر دفعي شامل - كل الملاحظات المعلَّقة المؤكَّدة من سليمان (2026-08-27)

بعد سلسلة إصلاحات/ميزات اليوم (قارّاءا CSV، إصلاح NBSP، إصلاح تجمُّد صفحة الرفع، إصلاح فلتر بيانات الطلبة الأكاديمية، ترتيب ملف طلبات القسم، تبويب بيانات الطلبة الجوّال)، أكّد سليمان صراحةً: "انشر جميع الملاحظات تم تجربتها وهي تعمل ماعدا تنزيل فترات الإرشاد سيتم تأجيله لاحقًا".

نُشر الجميع دفعة واحدة **باستثناء إصلاح أزرار ZIP لجدول "توزيع فترات الإرشاد"** (استُبعد عمدًا بطلبه) - عبر `git stash` مؤقت لملف `advising_schedule_admin_screen.dart` وقت البناء، ثم إعادته لشجرة العمل (غير مُلتزَم) بعد نجاح النشر، حتى يبقى الإصلاح جاهزًا للنشر لاحقًا بلا إعادة عمل. تحقَّق من مطابقة الملف المنشور فعليًا (`main.dart.js`) بايتًا-لبايت مع البناء المحلي بعد كل نشر (`curl` + `diff`) - نفس الاحتراز المتَّبع بعد حادثة نشر سابق لم يعكس آخر تعديل بنفس اليوم (راجع القسم أعلاه).

### تبويب "بيانات الطلبة" (بحث سريع) بشاشة "الجداول الدراسية" - تطبيق بوابة الإرشاد الجوّالة (2026-08-27)

طلب سليمان صراحةً ميزة معاينة سريعة أثناء الإرشاد الحي: "لو طالب خلال عملية الإرشاد ذكر ليس له جدول، بمجرد البحث برقمه أو اسمه يظهر وضعه كامل (مين مرشده ومعدله وحالته مفصول أو أي بيانات)" - بحث فقط بلا أي إجراء (لا تعديل ولا تصدير).

أُضيف تبويب ثالث "بيانات الطلبة" بشاشة `course_schedules_screen.dart` (بجانب "المقررات الدراسية"/"الجدول الدراسي" الموجودين - بطلب سليمان صراحةً وضعها هناك لا شاشة/تبويب "المزيد" رغم أن الأخير كان يحمل ملاحظة "قيد التطوير" لميزة بحث). التبويب الجديد ([student_lookup_screen.dart](lib/mobile_portal/screens/student_lookup/student_lookup_screen.dart)، `StudentLookupTab`) يحمّل نفس مصدر بيانات "بحث عن مرشد" بالموقع (تقرير "كل الكليات" مدمَجًا مع "بيانات الطلبة الأكاديمية" عبر `AdvisingCaseAnalyzer.mergeAcademicData`)، وشريط بحث واحد يقبل رقم الطالب الجامعي أو اسمه (بحث جزئي، تطبيع الاسم كالمعتمَد بـ"بحث عن مرشد": توحيد الألف/التاء المربوطة). كل نتيجة مطابقة تظهر كبطاقة معاينة كاملة: القسم، الشطر، المرشد الأكاديمي، الحالة الصحية إن وُجدت، شارة الحالة الدراسية (منتظم/مفصول أكاديميًا/منقطع عن الدراسة بلون أحمر للحالتين الأخيرتين)، والمعدل التراكمي/الساعات المتبقية بنفس تلوين النطاق المعتمَد بالموقع.

**لم يُختبَر حيًّا بعد** (لا `flutter` بهذه البيئة) - يحتاج تحميلاً فعليًا عبر التطبيق للتأكد أن البحث يعمل بسلاسة وأن البطاقة تعرض بيانات صحيحة قبل أي نشر [[feedback_no_deploy_until_all_notes]].

سليمان طلب أيضًا (نفس الجلسة) إعادة تسمية تبويب التنقّل السفلي نفسه من "الجداول الدراسية" إلى "البيانات الأكاديمية" (بديهي الآن أنه يحوي بيانات طلبة لا جداول مقررات فقط) - عُدِّل `NavigationDestination` بـ[portal_shell.dart](lib/mobile_portal/app/portal_shell.dart).

### تنبيه بتغييرات جدول المقررات (الحويّة) بعد كل رفعة + تنزيل PDF (2026-08-27)

طلب سليمان صراحةً: عند رفع ملف جديد، تنبيه يوضّح ما تغيّر مقارنة بالرفعة السابقة، مع إمكانية تنزيل ملف بهذه التغييرات - حدَّد النطاق لاحقًا بأنه لجدول المقررات (الحويّة) فقط حاليًا (لا كل رفعات الموقع).

منطق اكتشاف التغييرات (إضافة/حذف شعبة، تغيير محاضر) كان موجودًا مسبقًا فعليًا (`CourseScheduleDiffService`، يُخزَّن تراكميًا بـ`CourseScheduleChangeRepository`) لكن دون أي واجهة تعرضه فور الرفع. أُضيف [course_schedule_change_pdf_service.dart](lib/services/course_schedule_change_pdf_service.dart) (بنفس هوية `PdfBrandKit`/`ReportPdfService` - جدول ملوَّن حسب نوع التغيير: أخضر=إضافة، أحمر=حذف، ذهبي=تغيير محاضر)، ودالة `_showCourseScheduleChangesDialog` جديدة بـ[upload_flows.dart](lib/web_portal/upload_flows.dart) تظهر تلقائيًا بعد نجاح اعتماد رفعة "الحويّة" - فقط إن وُجدت رفعة سابقة فعلية للمقارنة (أول رفعة على الإطلاق لا معنى لتغييراتها) - تعرض ملخص الأعداد الثلاثة + أول 30 تغييرًا نصيًا، مع زر "تنزيل PDF" يبني الملف الكامل بكل التغييرات.

### زر ZIP منظَّم (شطر ← قسم ← ملف PDF واحد لكل الأيام) لجدول توزيع فترات الإرشاد (2026-08-27)

طلب مباشر من إدارة الكلية عبر سليمان: تنظيم ملفات PDF لجدول توزيع فترات الإرشاد بحيث يسهل إرسال ملف قسم واحد بعينه لمرشديه، بدل البحث داخل تقرير "كل الأقسام" الكبير.

أُضيفت `AdvisingSchedulePdfService.buildDepartmentFolderZip` ([advising_schedule_pdf_service.dart](lib/services/advising_schedule_pdf_service.dart)) التي تبني ملفًا مضغوطًا بمجلد لكل شطر (شطر الطلاب/شطر الطالبات)، وداخل كل شطر مجلد فرعي باسم كل قسم يحوي **ملفًا واحدًا فقط** يجمع كل أيام ذلك القسم - بنفس تصميم/مقاس/خط دالة `build()` الحالية حرفيًا (لا تصميم جديد)، فقط تكرار الدالة الموجودة أصلاً لقسم/شطر واحد على كل تركيبة قسم×شطر متوفرة، وتجميعها بمكتبة `archive` (نفس نمط `AdvisorZipService.buildZip` الموجود مسبقًا) بمسارات تحتوي `/` كفواصل مجلدات.

زر جديد ("تنزيل ملف مضغوط منظّم") أُضيف لبطاقة التقرير بشاشة `advising_schedule_admin_screen.dart` بجانب أزرار PDF/الطباعة/ZIP الصور/PowerPoint الحالية - يعمل على أي نطاق فلتر مختار (قسم/شطر محدَّدين أو "الكل").

**طلب إضافي منفصل (نفس اليوم)**: سليمان وضّح لاحقًا أنه يريد ميزة أخرى بجانب هذه - وليست بديلاً عنها - لأيقونة "شاشات العرض" تحديدًا (لا التقرير الرسمي PDF): صور الشرائح (PNG) التي تُرسَل حاليًا لسكرتارية الكلية تنزل بملف مضغوط **مسطَّح** (كل الأقسام/الأيام مختلطة معًا بلا فواصل)، ويريد لكل قسم "شاشته" الخاصة بفتراته فقط بلا اختلاط بأي قسم آخر. أُضيفت `AdvisingScheduleSignageImageService.buildZipByDepartment` (نفس تصميم/دقة الصور الحالية حرفيًا عبر `buildZip` - فقط إعادة تنظيم داخل الملف المضغوط بمجلد شطر ← مجلد قسم)، وحقلا `department`/`shatr` جديدان أُضيفا لسجل `AdvisingSchedulePdfService.buildSignageCards` (كانا يُشتَقان سابقًا بتحليل نص `label` فقط) لبناء مسار المجلد مباشرة. زر جديد ("تنزيل صور الشرائح حسب القسم") أُضيف بجانب زر "تنزيل صور الشرائح (ZIP)" الحالي.

**لم يُختبَر حيًّا بعد** (لا `flutter` بهذه البيئة) - يحتاج تحميلاً فعليًا عبر الموقع للتأكد أن هيكل المجلدات والتصميم مطابقان للمطلوب قبل أي نشر [[feedback_no_deploy_until_all_notes]].

### قارئا CSV جديدان لملفي "طلاب تابعين لمرشد" و"الحويّة" - بديل أسرع وأدق من PDF (2026-08-27)

سليمان اكتشف أن المنظومة الداخلية تتيح تنزيل هذين الملفين مباشرة بصيغة CSV (لا PDF فقط كما كان معتمَدًا)، وسأل هل هذا سيجعل القراءة أسهل وأسرع - جُرِّب الأمر عمليًا قبل أي كود إنتاجي: بُنيت أداتا معاينة مستقلة (Artifacts، لا تلمسان بيانات الموقع) تقرآن الترميز الفعلي للملفين (Windows-1256، فاصل ";") وتعرضانهما كجدول، اختُبرتا على ملفين حقيقيين ("طلاب تابعين لمرشد عصر 14-03-1448"، "دراسي طلاب - المستفيد 102-481-14-03-1448") وطابقتا المحتوى الخام تمامًا (1744 مرشدًا/42479 طالبًا، 127 مقررًا/321 شعبة/12581 مسجَّلًا) - بما في ذلك حالات معقّدة مثل الشُّعب متعددة الأيام (موعد ثانٍ بصف منفصل بلا رقم شعبة) وتكرار رأس الصفحة بين صفحات ملف الحويّة.

بعد تأكيد سليمان صحة البنية، بُني القارّاءان الفعليان بـDart: [windows1256_decoder.dart](lib/services/windows1256_decoder.dart) (مفكّك ترميز يدوي - `dart:convert` لا يحويه جاهزًا)، [advising_report_csv_parser_service.dart](lib/services/advising_report_csv_parser_service.dart) (يحوّل كل صف لنفس الصيغة المضغوطة التي ينتجها قارئ docx الحالي، ثم يُمرَّرها لنفس `AdvisingReportParserService.parseRows` الموجود - يرث تلقائيًا كل قواعد الأعمال المتراكمة هناك: استبعاد فروع المحافظات، الدراسات العليا، استنتاج الشطر... بدل تكرارها)، و[csv_schedule_parser_service.dart](lib/services/csv_schedule_parser_service.dart) (فهرسة أعمدة ثابتة موثوقة - أبسط بكثير من قارئ PDF المكافئ الذي يستنتج الأعمدة هندسيًا من مواضع الكلمات).

تحقّق مؤقت (اختبار محذوف بعد التأكد) أثبت أن القارئ الجديد يطبّق فعليًا كل قواعد الاستبعاد الحالية بشكل صحيح (فروع الخرمة/تربة/رنية، كليات الجامعة الأخرى، دراسات عليا)، والشُّعب متعددة الأيام تُربَط بمواعيدها الصحيحة.

رُبط القارئان بشاشة الرفع الفعلية (`runUploadAllColleges`/`runUploadCourses` بـ[upload_flows.dart](lib/web_portal/upload_flows.dart)) - يظهر خيار `.csv` الآن بجانب `.pdf`/`.docx` بكلا الزرّين. ملف CSV لجدول الحويّة لا يذكر الشطر بمحتواه (خلافًا لـPDF/docx)، فيُستدَل عليه من اسم الملف نفسه ("دراسي طلاب"/"دراسي طالبات") عبر `_shatrFromFileName` الجديدة.

**نُشر على الموقع الفعلي بعد "انشر" صريحة من سليمان (2026-08-27).**

**خلل حقيقي اكتُشف فور أول رفع حي (نفس اليوم) وأُصلح فورًا**: سليمان رفع ملفي حويّة أحدث وأكبر (709 شعبة إجمالاً) فظهر خطأ "لم يُعثر على أي شعبة (مستفيد) منها كلية إدارة الأعمال". السبب: خلايا CSV مثل عمود "المستفيد" تستخدم أحيانًا **مسافة غير فاصلة (NBSP، U+00A0)** بين الكلمات بدل المسافة العادية (مثال: "كلية[NBSP]إدارة[NBSP]الأعمال") - فمطابقة `.contains('كلية إدارة الأعمال')` (بمسافات عادية بالكود) كانت تفشل صامتًا رغم تطابق النص بصريًا تمامًا. أُصلح بتوحيد كل مسافات NBSP لمسافة عادية فور تقسيم كل خلية (`replaceAll(' ', ' ')`) في كلا القارئين الجديدين ([csv_schedule_parser_service.dart](lib/services/csv_schedule_parser_service.dart)، [advising_report_csv_parser_service.dart](lib/services/advising_report_csv_parser_service.dart) وقائيًا). تحقَّق تحقّق مؤقت (اختبار محذوف بعد التأكد) أن 677 من 709 شعبة أصبحت تُطابَق بنجاح بعد الإصلاح. **نُشر على الموقع الفعلي مباشرة (خلل حي يمنع رفعًا فعليًا طلبه سليمان للتو).**

**مشكلة نشر إضافية اكتُشفت بعد ذلك مباشرة**: سليمان أبلغ أن نافذة اختيار الملف بالمتصفح ما زالت تعرض "*.docx;*.pdf" فقط بلا CSV، حتى بعد تحديث قسري وتسجيل خروج ومتصفح خفي (استُبعد الكاش كسبب فعليًا). تحقّقتُ مباشرة من `main.dart.js` المنشور فعليًا (`curl`) ولم يحتوِ سلسلة "csv" بامتدادات الرفع إطلاقًا رغم أن الكود المصدري ونتيجة `flutter build` السابقة كانا يحتويانها - أي أن نشرًا سابقًا (أو بناءً سابقًا) لم يعكس آخر تعديل فعليًا لسبب غير محدَّد بثقة (احتمال تعارض بناء متزامن مع جلسة أخرى تستخدم نفس المستودع، انظر [[project_concurrent_sessions_same_repo]]). الحل: `flutter clean` كامل (حذف `build/`+`.dart_tool/`) ثم `flutter pub get` وإعادة بناء من الصفر، مع تحقّق يدوي (`grep` على `main.dart.js` المبني محليًا) أن سلسلة `"docx","pdf","csv"` موجودة فعليًا **قبل** النشر - ثم `firebase deploy` وتحقّق ثانٍ بعده أن الملف المنشور مطابق بايتًا لبايت لما بُني محليًا (`diff` بين `build/web/main.dart.js` والنسخة المنزَّلة من الموقع الفعلي عبر `curl` - النتيجة الآن مطابقة تمامًا).

### دعم رفع الملفات الستة "بيانات الطلبة الأكاديمية" (CSV) + استبعاد غير المنتظم من الإرشاد + شاشة إحصائيات (2026-08-27)

سليمان يُنزِّل دوريًا من المنظومة الداخلية 6 ملفات CSV (منتظم/مفصول أكاديمي/منقطع عن الدراسة × طلاب/طالبات) تغطي كل طلبة الكلية، ويرغب برفعها دفعة واحدة كأساس لبيانات الطلبة، مع استبعاد كل من حالته ليست "منتظم" من الإرشاد حتى تُصحَّح حالته، وشاشة إحصائيات مستقلة تعرض كل الحالات معًا بفلتر قسم/شطر/حالة.

أُضيف قارئ جديد [academic_data_csv_parser_service.dart](lib/services/academic_data_csv_parser_service.dart) (ترميز Windows-1256 عبر `Windows1256Decoder` الموجود مسبقًا، فاصل `;`) بجانب قارئ xlsx القديم - كلاهما يقرآن الآن من زر "بيانات الطلبة الأكاديمية" نفسه (`runUploadAcademicData` بـ[upload_flows.dart](lib/web_portal/upload_flows.dart))، الذي يقبل حتى 6 ملفات معًا ويستنتج الشطر وحالة القيد من اسم كل ملف تلقائيًا (عمود "الوضع في الفصل" بالملف نفسه فارغ دومًا).

اكتُشفت أثناء التنفيذ فجوة فعلية: `AdvisingCaseAnalyzer.mergeAcademicData` كان يدمج المعدل/الساعات من "بيانات الطلبة الأكاديمية" لكن **لا يدمج حالة القيد** (`enrollmentStatus`) - يعني أن استبعاد "مفصول أكاديميًا" (الموجود مسبقًا) لم يكن ليعمل فعليًا على القوائم المستخدَمة بكل شاشات الإرشاد. أُضيف `enrollmentStatus` لوسائط `copyWith` بـ[advising_case_record.dart](lib/models/advising_case_record.dart) وأُصلح الدمج، وعُمِّم شرط الاستبعاد (`isRegularlyEnrolled` الجديد) بنقطتَي `advising_case_analyzer.dart` ليشمل "منقطع عن الدراسة" أيضًا لا "مفصول أكاديميًا" فقط.

شاشة جديدة [student_data_stats_screen.dart](lib/web_portal/student_data_stats_screen.dart) (متاحة من "تقارير" - "إحصائيات بيانات الطلبة") تعرض **كل** طلبة الكلية بكل الحالات معًا (بخلاف شاشات الإرشاد التي تستبعد غير المنتظم)، بفلاتر قسم/شطر/حالة بنفس نمط `admin_executive_dashboard_screen.dart` المعتمَد (الخيار الافتراضي اسم الحقل نفسه لا "الكل")، مع بطاقات إحصائية وإخفاء عمود القسم تلقائيًا عند تحديد قسم واحد.

**نُشر على الموقع الفعلي وأكّد سليمان نجاحه فعليًا بعد رفع الملفات الستة الحقيقية.** طلب إضافيًا (2026-08-27) إظهار عدّاد الحالات الثلاث (منتظم/مفصول أكاديميًا/منقطع عن الدراسة) مباشرة بجانب زر "بيانات الطلبة الأكاديمية" بصفحة "رفع وتنزيل الملفات" نفسها، لا شاشة الإحصائيات المنفصلة فقط. عُدِّلت `_loadDates` بـ[upload_hub_screen.dart](lib/web_portal/upload_hub_screen.dart) لتحميل قائمتَي الشطرين كاملتين (بدل `currentRecordsCount` السريع فقط) واستخراج توزيع الحالات الثلاث منهما (`isRegularlyEnrolled`/`isAcademicallyDismissed`/`enrollmentStatus.contains('منقطع')`)، وأُضيف صف بطاقات ثانٍ لمربّع "بيانات الطلبة الأكاديمية" يعرض العدّادات الثلاثة بجانب صف "طلبة شطر الطلاب/الطالبات" الموجود مسبقًا. نُشر على الموقع الفعلي بعد `flutter analyze` نظيف.

طلب إضافيًا (نفس اليوم) إظهار الطلبة غير المنتظمين (المستبعَدين من كل حسابات الإرشاد) داخل شاشة "متابعة حالات الإرشاد" نفسها، بنفس أسلوب تبويب "الطلبة المستجدون" الموجود - لا الاكتفاء بشاشة الإحصائيات المنفصلة. أُضيف تبويب/بطاقة "طلبة غير منتظمين" جديد ([advising_cases_admin_screen.dart](lib/web_portal/advising_cases_admin_screen.dart)) يعرض حقل `dismissedStudents` (كان محسوبًا مسبقًا بـ`CollegeAdvisingClassification` لكن بلا أي واجهة تستهلكه) بعمود "الحالة" يوضّح السبب (مفصول/منقطع)، وفلتر "الحالة" جديد بشريط الفلترة المشترك أعلى كل التبويبات (منتظم/مفصول أكاديميًا/منقطع عن الدراسة) يعمل فعليًا على هذا التبويب تحديدًا (التبويبات الأخرى لا تحتوي أصلًا طلبة غير منتظمين، فلا أثر ظاهر للفلتر عليها). نُشر على الموقع الفعلي بعد `flutter analyze` نظيف.

**عدَّلها سليمان بعد المعاينة الحية (نفس اليوم)**: (أ) بطاقة/تبويب "طلبة غير منتظمين" المدموج **غير مطلوب** - قُسِّم لبطاقتين/تبويبين منفصلين "مفصولون أكاديميًا" و"منقطعون عن الدراسة" (كل سبب ببطاقته الخاصة)، فحُذف فلتر "الحالة" المشترك بالكامل (لم يعد له داعٍ مع الفصل، وكان بلا خيار "الكل" أصلًا - ملاحظة سليمان). (ب) "الفلتر هو الأساس في إظهار أي نتيجة بالصفحة" - أُضيفت `_dismissedFiltered`/`_withdrawnFiltered` (getters جديدة) تطبّق فلاتر الشريط العلوي (شطر/قسم/مرشد/بحث) فعليًا على البطاقتين الجديدتين وعدّاديهما معًا (لا الجدول التفصيلي فقط كبقية البطاقات القديمة) - `advisorNameRaw` لهؤلاء الطلبة فارغ دومًا (مصدرهم "بيانات الطلبة الأكاديمية" فقط)، فاختيار مرشد محدَّد يُصفِّرهما عمدًا (صحيح فعليًا لا خللًا). **نُشر على الموقع الفعلي بعد "انشر" صريحة من سليمان.**

طلب إضافيًا (نفس الجلسة): تبويب جديد بـ"الخدمات السريعة" (شاشة `academic_services_hub_screen.dart`) باسم "بيانات الطلبة الأكاديمية" يفتح جدولًا كاملاً بفلتر الكل/شطر/قسم/مرشد/حالة وتصدير Excel/PDF "كالمعتاد". وسَّعت [student_data_stats_screen.dart](lib/web_portal/student_data_stats_screen.dart) (بدل بناء شاشة جديدة): أُضيف فلتر "المرشد" (غير موجود أصلًا ببيانات "بيانات الطلبة الأكاديمية" - يُثرى بمطابقة الرقم الجامعي مع تقرير "كل الكليات" `AdvisingReportKind.allColleges`، بنفس مبدأ `mergeAcademicData` بالاتجاه المعاكس)، واستُبدلت شرائح الفلتر المحلية الخاصة بالشاشة بمكوّنات `FilterResetChip`/`FilterPillDropdown` المشتركة (`theme/filter_pills.dart`) لتوحيد الهوية مع بقية الموقع (تضمّنت تلقائيًا شريحة "الكل")، وأُضيف تصدير Excel/PDF بنفس خدمتَي `AdvisingCaseExcelService`/`AdvisingCasePdfService` المستخدَمتين بشاشة "بحث عن مرشد". بطاقة جديدة بـ"الخدمات السريعة" تفتح نفس الشاشة.

سليمان لاحظ على المعاينة أن الجدول لا يعكس الفلتر المختار أحيانًا (عدّاد النتائج يتحدَّث لكن صفوف الجدول تبقى كما هي)، وطلب توسيط عمود "اسم الطالب" وعرض "النطاق" الملوَّن بدل رقم "المعدل" المجرَّد. **الأخيران نُفِّذا بثقة** (`textAlign: TextAlign.center` لعمود الاسم، واستبدال عمود `gpa` بـ`DashProgressCell` الملوَّن - نفس مكوّن "النطاق" بشاشة "بحث عن مرشد" حرفيًا). **أما خلل عدم انعكاس الفلتر فلم يُحدَّد سببه الجذري بثقة** رغم مراجعة الكود كاملاً (منطق الفلترة يستخدم نفس المتغيّر المحسوب مرة واحدة لعدّاد النتائج وجدول الصفوف معًا - لا تناقض واضح بالكود) - طُبِّق إصلاحان احترازيان معًا: (أ) مطابقة `.trim()` على مقارنتَي المرشد/الحالة (احتمال عدم تطابق نص خام غير مُهذَّب من تقرير "كل الكليات" مع القيمة المختارة بالفلتر)، (ب) `KeyedSubtree` بمفتاح يضم كل قيم الفلاتر يُغلِّف عدّاد النتائج والجدول معًا، يُجبر Flutter على إعادة بناء الشجرة بالكامل عند أي تغيير فلتر بدل الاعتماد على مطابقته الداخلية المعتادة. **نُشر على الموقع الفعلي بعد "انشر" صريحة من سليمان - يحتاج تأكيده الحي بعد تحديث المتصفح (Ctrl+Shift+R) هل الخلل زال فعليًا أم لا يزال قائمًا** - إن استمر رغم هذين الإصلاحين فالسبب مختلف تمامًا عمّا افتُرض ويحتاج تشخيصًا حيًّا مباشرًا (لا تتوفر أداة تشغيل/تصحيح تفاعلي بهذه البيئة).

**سليمان سأل لاحقًا (2026-08-27) لماذا بطاقة "بيانات الطلبة الأكاديمية" غير ظاهرة بـ"الخدمات السريعة" رغم ما سبق**: تبيّن أن تعديل الكود (البطاقة الثالثة بـ`academic_services_hub_screen.dart`) كان لا يزال **غير مُلتزَم بـgit** (working tree فقط) على فرع `preview/department-file-priority-sort` - لم يُدفع فعليًا رغم إشارات "نُشر" أعلاه (على الأرجح نُشر يدويًا مرة عبر `firebase deploy` مباشرة بجلسة سابقة بلا `git commit` مرافق، ثم فُقد الأثر). عند "انشر" التالية اكتُشف أيضًا أن **مجلد "المرفقات" الشخصي بأكمله (ملفات عمل، سير ذاتية، ومجلد "كلمات المرور") كان متتبَّعًا بـgit خطأً** منذ البداية - أُضيف `المرفقات/` لـ[.gitignore](.gitignore) وأُزيل من تتبّع git نهائيًا (`git rm --cached`، الملفات نفسها لم تُمَس على القرص)، ثم التُزمت التعديلات البرمجية الفعلية فقط ودُفعت لفرع `preview/department-file-priority-sort` (commit `86f18cb`) - يُشغِّل هذا تلقائيًا نشر **قناة معاينة Firebase منفصلة** (`preview-forms-import-fix`، صلاحيتها 7 أيام) لا الموقع الفعلي `sss-advising-tu.web.app` مباشرة. **النشر للموقع الفعلي بعد تأكيد سليمان الحي على قناة المعاينة يتطلب خطوة يدوية منفصلة لاحقًا.**

### خلل تعليق كبير بصفحة "رفع وتنزيل الملفات" + قصّ جدول "بيانات الطلبة الأكاديمية" لـ120 صفًا (2026-08-27)

سليمان أبلغ عن "تعليق كبير جدًا" يستمر بالموقع، وحدَّده لاحقًا بدقة: يحدث فقط بصفحة "رفع وتنزيل الملفات" ("تظهر صفحة في الانتظار")، وخمَّن بنفسه صراحةً أن "بيانات عالقة" هي السبب - تخمين صحيح تمامًا.

السبب الجذري: تعديل سابق بنفس اليوم (إضافة عدّادات منتظم/مفصول/منقطع بجانب زر "بيانات الطلبة الأكاديمية") غيَّر `_loadDates` بـ[upload_hub_screen.dart](lib/web_portal/upload_hub_screen.dart) من قراءة خفيفة (`currentRecordsCount`) إلى **تحميل كل سجلات الشطرين كاملة** (`AdvisingReportRepository.load`) في كل زيارة للصفحة - مع أكثر من 10,000 سجل حاليًا (9174 منتظم + 222 مفصول + 687 منقطع)، هذا يعني تحميل + فك JSON لآلاف السجلات (عبر عشرات مستندات `chunks`) على كل تحميل للصفحة، يُجمِّد الواجهة فعليًا.

الإصلاح الجذري: أُضيفت حقول `regularCount`/`dismissedCount`/`withdrawnCount` تُحسَب وتُخزَّن **وقت الرفع نفسه** بـ[advising_report_repository.dart](lib/services/advising_report_repository.dart) (`save()`)، مع دالة قراءة خفيفة جديدة `currentStatusCounts()` (قراءة مستند واحد فقط، بلا `chunks`) - نفس مبدأ `currentRecordsCount` الموجود مسبقًا بالضبط. `upload_hub_screen.dart` يقرأ الآن هذه الحقول مباشرة بدل تحميل السجلات. البيانات المرفوعة مسبقًا (قبل هذا الإصلاح) لم تكن تحمل هذه الحقول - شُغِّل سكربت هجرة لمرة واحدة [tool/admin_cli/backfill_academic_status_counts.js](tool/admin_cli/backfill_academic_status_counts.js) يحسبها من `chunks` الحالية ويكتبها مباشرة على المستندين (تحقَّق تطابق الأرقام تمامًا مع ما رُفع أصلًا: 3550/193/596 طلاب، 5624/29/91 طالبات).

سليمان اقترح أيضًا حلًا وقائيًا مستقلاً لشاشة "بيانات الطلبة الأكاديمية" (الخدمات السريعة) قد تعاني من نفس فئة الخلل مستقبلاً مع نمو البيانات: رسم آلاف الصفوف دفعة واحدة بجدول `DashTable` (يبني كل صف كـWidget فعلي فورًا، بلا Virtualization) قد يُجمِّد الصفحة تلك أيضًا. طُبِّق اقتراحه حرفيًا بـ[student_data_stats_screen.dart](lib/web_portal/student_data_stats_screen.dart): الجدول المعروض يُقصّ لأول 120 نتيجة فقط (مع ملاحظة نصية توضّح ذلك)، بينما تصدير Excel/PDF يبقى على القائمة الكاملة غير المقصوصة كما هي.

نُشر على الموقع الفعلي مباشرة (خلل حي يعطّل صفحة أساسية).

توسيط إضافي بجدول "بيانات الطلبة الأكاديمية" (طلب سليمان صراحةً): عمودا "القسم"/"المرشد" كانا محاذَيَين لليمين بخلاف بقية الأعمدة الممركزة - وُحِّدا لتوسيط `TextAlign.center` مثل الجميع. نُشر على الموقع الفعلي بعد تأكيد "انشر".

### إصلاح "0 بيانات" عند رفع ملف فورمز نُزِّل من تابلت أندرويد (2026-08-26)

سليمان حمّل رابط الفورمز من تابلت أندرويد (متصفح Chrome) ونزّل ملف النتائج، فرفعه على الموقع وظهر "0 بيانات" رغم نجاح نفس الرابط عند التنزيل من حاسوب عادي. أرسل الملف الفاشل ففُحص بتشغيل الدالة الحقيقية `ExcelParserService.parseTickets` مباشرة عليه (سكربت Dart مؤقت عبر `dart run`، ثم حُذف) - **أعاد فعليًا 0 تذكرة بلا أي استثناء** (فشل صامت تمامًا)، فأكّد المشكلة قبل أي افتراض.

السبب الجذري: عمودا Forms النظاميان "البريد الإلكتروني"/"الاسم" يصلان بمسمّاهما **الإنجليزي الخام** ("Email"/"Name") في ملفات مُنزَّلة من بعض الأجهزة (تابلت أندرويد هنا)، بدل الترجمة العربية المعتادة من تنزيل الحاسوب. الكود بـ[excel_parser_service.dart](lib/services/excel_parser_service.dart) كان يبحث حصرًا عن "البريد الإلكتروني"، فيفشل `emailCol` بصمت (-1)، فيُقرأ `email` فارغًا لكل صف، فتُتجاهَل كل الصفوف (`if (email.isEmpty) continue;`) - هذا هو حاجز "0 بيانات" الوحيد؛ عمود "الاسم" يحمل نفس العلة (يصل كـ"Name") فتُخزَّن التذاكر بلا اسم لو لم يُصلَح أيضًا. أُضيف بديل مطابقة إنجليزي ("Email"/"Name") لكلا العمودين - أعاد تشغيل نفس السكربت 4 تذاكر بأسماء صحيحة كاملة بعد الإصلاح.

### إصلاح عرض/توسيط جدول عضو هيئة التدريس (2026-08-26)

سليمان لاحظ أن جدول المقررات المسكَّنة لعضو هيئة التدريس ([course_schedule_admin_screen.dart](lib/web_portal/course_schedule_admin_screen.dart)) يتطلب تمريرًا أفقيًا لإظهار عمود "القاعة" رغم وجود مساحة فارغة على جانبي الصفحة، وأن عمود "الساعات" غير متوسِّط بشكل موحَّد بين الصفوف.

الإصلاح الأول: التفاف `DataTable` بـ `LayoutBuilder` + `ConstrainedBox(minWidth: constraints.maxWidth)` بدل تمرير أفقي حر، فتمتد الأعمدة لتملأ عرض الشاشة المتاح بدل الانكماش لعرض المحتوى فقط. كل عناوين الأعمدة وخلاياها (غير المقسومة نظري/عملي) صارت داخل `Expanded(child: Center(...))`/`Center(...)` للتوسيط الموحَّد. كذلك أُعطي الخط الفاصل داخل الخلايا المقسومة (نظري/عملي) عرضًا ثابتًا (30) بدل الاعتماد على عرض النص، لأن اختلاف طول الأرقام (مثلًا "2" مقابل "1") كان يُنتج خطًا بعرض مختلف فيبدو غير متوسِّط بصريًا.

تعديل تكميلي بعد أن أظهر التوزيع المتساوي (Expanded بلا نسب) تشوّهًا آخر: عمود "اسم المقرر" ينكسر لسطرين، وعمود "الوقت" ينكسر بمنتصف "ص/م" لأن كل الأعمدة أخذت عرضًا متساويًا رغم اختلاف طول محتواها الطبيعي. الحل: نسب `flex` مختلفة حسب طول المحتوى المعتاد (اسم المقرر=3، الوقت=2، البقية=1)، مع `maxLines: 1` و`overflow: TextOverflow.ellipsis` و`softWrap: false` على نص اسم المقرر والوقت لمنع الانكسار لسطرين نهائيًا.

تعديل جذري ثالث بعد ملاحظة سليمان أن الخلايا القصيرة (رمز/ساعات/قاعة) تلتصق بأعلى الصف بدل توسّطها رأسيًا عندما يطول الصف (مقرر بعدة مواعيد أسبوعية مفصولة بسطر جديد داخل `theoryDayName`/`theoryTimeRange`، انظر `_joinMeetings` بـ [instructor_schedule_table.dart](lib/services/instructor_schedule_table.dart)) - اكتُشف أن `DataTable` في Flutter يُثبِّت محاذاة خلاياه أعلى الصف (`TableCellVerticalAlignment.top` ضمنيًا) بلا أي معامل متاح لتغييرها، فالتفاف الخلية بـ`Center` لا ينفع لأن ارتفاع الصف الفعلي متغيّر. استُبدل `DataTable` بالكامل بـ`Table` خام (دالة `_instructorScheduleTable` الجديدة) مضبوطًا بـ`defaultVerticalAlignment: TableCellVerticalAlignment.middle` - يحل المشكلة جذريًا لأي ارتفاع صف ديناميكي مهما زاد عدد المواعيد، بخلاف أي حل بارتفاع صف ثابت كان سيفشل مع مقرر له مواعيد أكثر من المعتاد. نُشر هذا الإصلاح على الموقع الحي بعد تأكيد سليمان.

بعد النشر ظهر خلل بصري جديد بعمود "الشعبة": قيمة نصية تحتوي حروفًا عربية وأرقامًا لاتينية معًا (مثل "أونلاين50" لشعبة تُعقَد أونلاين) تنكسر في المنتصف داخل عمود ضيق، فيُعيد خوارزمية الاتجاه الثنائي (bidi) ترتيب الشظيتين بصريًا فتظهر مشوَّهة (مثال: "أون" بسطر ثم "50لاي" بالسطر التالي بدل "أونلاين" و"50"). الحل: أُضيف `softWrap: false` (+`maxLines: 1`/`ellipsis` لحقل الشعبة تحديدًا لأنه قيمة مفردة لا تُجمَع بعدة مواعيد، بخلاف عمودي الوقت والقاعة اللذين يُبقيان multiline لاحتوائهما أحيانًا على أكثر من موعد مفصول بسطر جديد فلا يصح تحديدهما بسطر واحد).

### تصدير Excel بأسلوب "كتلة لكل مرشد" في شاشة متابعة حالات الإرشاد (2026-08-25)

سليمان قارن تصدير Excel الحالي (قائمة مسطَّحة مرتَّبة أبجديًا) بتقرير المنظومة الجامعية الأصلي (كتلة مستقلة لكل مرشد: عنوان ممتد بلون مميّز يحمل اسمه وعدد طلابه، ثم طلابه تحته) ووصف تنظيم الموقع بـ"غير احترافي" بالمقارنة. طلب صراحةً تطبيق نفس أسلوب المنظومة على التبويبات الاثني عشر كلها.

أُضيف دعم تجميع اختياري إلى [advising_case_excel_service.dart](lib/services/advising_case_excel_service.dart) (`groupColumnIndex`): يُدرَج صف عنوان ممتد (merge) بلون مميّز قبل كل كتلة، يحمل اسم القيمة (المرشد) وعدد صفوفها. في [advising_cases_admin_screen.dart](lib/web_portal/advising_cases_admin_screen.dart)، دالة `_groupColumnIndexFor` تكتشف تلقائيًا عمود "المرشد" (بأي من مسمّياته الخمسة المختلفة حسب التبويب) وتُفعِّل التجميع فقط إن كان مفيدًا فعليًا (أكثر من صف واحد للقيمة نفسها بالمتوسط) - فتبويبات مثل "تقرير النصاب" (صف واحد أصلًا لكل مرشد) تبقى مسطَّحة كالسابق بلا فائدة من كتلة لصف منفرد.

### إصلاح انقسام خلية "الاسم" أثناء استخراج PDF (فارق 25 طالبة مقارنةً بملف يدوي مرجعي) (2026-08-25)

سليمان قارن ملف "بيانات الطلبه.xlsx" (تصدير يدوي من المنظومة، 8749 رقمًا جامعيًا) بما هو مخزَّن فعليًا بالموقع من ملف "طلاب تابعين لمرشد - كل الكليات" (8895 رقمًا بالشطرين). أداة مقارنة مباشرة على قاعدة البيانات الحية ([tool/admin_cli/check_advising_record.js](tool/admin_cli/check_advising_record.js) نمط) كشفت **25 رقمًا موجودين بالملف اليدوي وغائبين عن الموقع** بدقة.

تتبُّع الصفوف الخام لهذه الـ25 حالة كشف نمطًا واحدًا متكررًا بدقة: خلية "اسم الطالبة" انقسمت خطأً لخليتين متجاورتين أثناء استخراج PDF (مثال حقيقي: `[الإدارة, المالكي, انتصار منصور بن محمد, 44450079, 8]` بدل 4 خلايا متوقَّعة) - فجوة أفقية غير معتادة بين اسم العائلة/القبيلة وبقية الاسم، تُزيح كل الأعمدة اللاحقة (رقم الطالب، م) خانة واحدة فيفشل الاستخراج الصحيح.

أُضيفت دالة `_repairSplitNameCell` جديدة إلى [advising_report_parser_service.dart](lib/services/advising_report_parser_service.dart) - عكس دالة `_splitMergedNameAndDepartment` الموجودة مسبقًا (تلك تُصلح اندماج خليتين في واحدة، هذه تُصلح انقسام خلية واحدة لاثنتين): عند اكتشاف صف بعدد أعمدة أكبر بواحد من صف العناوين المكتشَف لنفس الكتلة، تُدمَج خليتا الاسم المتجاورتان تلقائيًا قبل استخراج بقية الحقول. تحقُّق فعلي على الملف الحقيقي (بتحميل روستر المرشدين الحقيقي من قاعدة البيانات عبر اختبار Dart مخصَّص): **استُرجعت 18 من الـ25 طالبة**. الـ7 المتبقيات سببهن منفصل تمامًا (لا علاقة باستخراج PDF): أسماء مرشديهن بإضافات قبلية غير معتادة لا تُطابَق بدقة مع ملف "منسوبي الكلية" فتُستبعَد كتلهن كـ"خارج الكلية" - يحتاج حلًا منفصلًا لمطابقة الأسماء لاحقًا إن احتاج الأمر.

**تصحيح فوري لترتيب الدمج**: سليمان أرسل الأسماء الصحيحة الكاملة للـ25 حالة من مصدر رسمي - تبيَّن أن الإصلاح الأول دمج الخليتين بترتيب معكوس ("اسم العائلة" ثم "بقية الاسم" بدل العكس، مثال: "المالكي انتصار منصور بن محمد" بدل "انتصار منصور بن محمد المالكي" الصحيح). قارنتُ الأسماء الصحيحة الـ25 يدويًا مقابل الصفوف الخام فتأكَّد النمط الصحيح 100%: بقية الاسم دومًا أولًا، ثم اسم العائلة/القبيلة. صُحِّح ترتيب الدمج ونُشر فورًا.

### عرض إحصائية الصفوف المستبعَدة عند رفع ملف "كل الكليات" (2026-08-25)

سليمان لاحظ أن ملف "حالات الإرشاد" الكبير الذي رفعه لم تُقرأ كل بياناته (لوحة "نظرة عامة على الإرشاد" أظهرت أرقامًا أقل من المتوقَّع). تتبُّع الكود كشف أن [advising_report_parser_service.dart](lib/services/advising_report_parser_service.dart) يستبعد صفوفًا فعليًا لأسباب متعددة (رقم جامعي غير صالح - غالبًا ناتج عن انزياح استخراج أعمدة PDF بملفات كبيرة، قسم غير معروف، دراسات عليا، بلا اسم/رقم) ويُحصيها في `exclusionCounts`، لكن مسار رفع "كل الكليات" في [upload_flows.dart](lib/web_portal/upload_flows.dart) (`runUploadAllColleges`) كان يستقبل هذه الإحصائية من `AdvisingReportPdfParserService` **ولا يعرضها للمستخدم إطلاقًا** - فلا توجد أي وسيلة كانت تُظهر عدد أو سبب الصفوف المفقودة.

أُضيف حوار جديد يظهر بعد المعالجة مباشرة (قبل حوار التأكيد) يسرد كل سبب استبعاد وعدد صفوفه إن وُجدت استبعادات - الآن يعرف سليمان فورًا لماذا عدد السجلات المعتمَد أقل من عدد صفوف الملف الخام، بدل استبعاد صامت بلا تفسير.

### توحيد هوية أزرار الرفع بشاشة الجوال + إصلاح خلل "آخر تحديث" الوهمي بالمقررات الدراسية (2026-08-25)

استكمالًا لتصميم شاشة "رفع الملفات" بالجوال، وحَّد سليمان هوية كل أزرار الرفع: حُوِّلت أزرار "المقررات الدراسية" و"الإرشاد الكامل" من زر كبير (`ElevatedButton.icon`) إلى نفس الأيقونة الدائرية الصغيرة المستخدَمة بباقي الشريط، ونُقلت لتكون بنفس صفّ العنوان مباشرة (لا صفّ منفصل أسفله) بـ[portal_uploads_screen.dart](lib/mobile_portal/screens/uploads/portal_uploads_screen.dart) - محافظةً على أن الصفحة كاملة بلا أي تمرير. حُذف أيضًا السطر الوصفي الثابت أسفل كل بطاقة ("ملف الحويّة (Word .docx)..."، "تقرير كل الكليات (PDF)...") لتبسيط أكبر.

**خلل حقيقي اكتُشف أثناء المعاينة على جهاز سليمان**: بعد تفعيل عرض "آخر تحديث" الديناميكي، ظهرت عبارة "لم يُرفع أي ملف بعد" تحت "المقررات الدراسية" رغم وجود بيانات مرفوعة فعليًا وظاهرة بالجدول. السبب الجذري: الشاشة (وأيضًا [upload_hub_screen.dart](lib/web_portal/upload_hub_screen.dart) بالموقع) كانت تقرأ حقل `exportDate` من [course_schedule_repository.dart](lib/services/course_schedule_repository.dart) - وهو حقل مخصَّص فقط لـ"تاريخ سحب البيانات من المنظومة الداخلية" ولا يُمرَّر إطلاقًا من `runUploadCourses`، فيبقى فارغًا دائمًا بغضّ النظر عن نجاح الرفع. أُضيفت دالة جديدة `currentUploadedAt` تقرأ حقل `uploadedAt` (المكتوب فعليًا بكل رفعة عبر `FieldValue.serverTimestamp()`)، واستُبدلت بها في كلا الملفين - الآن يظهر تاريخ آخر رفع فعلي بدقة بالجوال وبالموقع معًا.

### إعادة تصميم بطاقة "حالات الحذف والإضافة" بشاشة رفع الجوال - سطر واضح لكل مرحلة (2026-08-25)

سليمان لاحظ (بلقطة شاشة فعلية) أن الشريط الموحَّد الجديد بشاشة الجوال ظهر كـ9 أيقونات متلاصقة بصفّين بلا تمييز - "شكلها غير واضح". طلب تصميمًا احترافيًا: سطر مستقل لكل إجراء/مرحلة، مع سطر شرح خفيف اللون تحت كل مجموعة، واستغلال المساحة البيضاء أسفل البطاقة بلا أي تمرير.

أُعيد تصميم البطاقة بالكامل بـ[portal_uploads_screen.dart](lib/mobile_portal/screens/uploads/portal_uploads_screen.dart): بطاقة جديدة `_CycleStagesCard` بأربعة أسطر مستقلة (الملف الأساسي/مرحلة المرشد/مرحلة منسّق القسم/مرحلة منسّق الكلية) - كل سطر: عنوان المرحلة بخط عريض + أيقونات إجراءاتها على اليمين، ثم سطر شرح رمادي فاتح تحته مباشرة يوضّح وظيفتها بلا حاجة لتمرير الماوس (الجوال لا يملك hover أصلاً). **تحقُّق فعلي مباشر على جهاز سليمان**: شُغِّل `flutter run --flavor advisingPortal` مباشرة على هاتفه المتصل بالكيبل (Hot Reload)، وأكّد بصريًا أن التصميم الجديد يظهر بشكل صحيح ("ممتاز جدًا") قبل النشر النهائي.

**اكتُشف وأُصلح أثناء نفس الجلسة**: تعارض تثبيت "package conflicts with an existing package" استمر رغم إلغاء التثبيت وإعادة التشغيل - تشخيص فعلي بـ`adb`/`dumpsys package` كشف سجل توقيع متبقٍّ (Ghost Entry) بذاكرة النظام لم يُزله أي من الإجراءين، وحلّه أمر `adb uninstall` مباشر عبر الكيبل ثم إعادة التثبيت - هذا كان انتقالًا لمرة واحدة فقط (بسبب تغيير مفتاح التوقيع بالبند التالي أدناه)، لن يتكرر مستقبلًا.

### إصلاح جذري: توقيع APK ثابت - كان يمنع تثبيت أي تحديث ("package conflicts") (2026-08-25)

بعد أول وصول لتطبيق "بوابة الإرشاد" (البند التالي أدناه)، حاول سليمان تثبيته فظهر خطأ أندرويد "App not installed as package conflicts with an existing package". السبب الجذري: `android/app/build.gradle.kts` كان يوقّع كل بناء release بمفتاح "debug" **الافتراضي العشوائي** (`signingConfigs.getByName("debug")`) بلا keystore ثابت مُعرَّف - كل مُشغِّل GitHub Actions (Runner) يُنشئ `~/.android/debug.keystore` عشوائيًا جديدًا بنفسه (لا يُخزَّن بين تشغيلات CI المنفصلة)، فيختلف توقيع كل بناء عن سابقه، ويرفض أندرويد تثبيت تحديث موقَّع بمفتاح مختلف عن النسخة المثبَّتة أصلاً - هذا كان سيتكرر مع **كل** تحديث مستقبلي لا مرة واحدة فقط.

**الحل**: وُلِّد keystore ثابت واحد (`keytool -genkeypair`)، خُزِّن كـ4 أسرار GitHub جديدة (`ANDROID_UPLOAD_KEYSTORE_BASE64` وكلمات المرور/الاسم المستعار - القيم الحساسة لا تُحفَظ بالمستودع، `android/.gitignore` يستثنيها أصلاً). [build.gradle.kts](android/app/build.gradle.kts) يقرأ الآن `key.properties` (يُكتَب فقط وقت تشغيل CI من الأسرار، انظر [deploy.yml](.github/workflows/deploy.yml)) ويستخدمه للتوقيع إن وُجد، وإلا يبقى توقيع "debug" الافتراضي كما كان (لا كسر لأي تطوير محلي حالي).

**تنبيه مهم لسليمان**: بما أن مفتاح التوقيع تغيَّر عن كل النسخ المثبَّتة سابقًا (سواء بُنيت محليًا أو عبر CI قبل هذا الإصلاح)، **يجب إلغاء تثبيت التطبيقين الحاليين من الجهاز مرة واحدة** ("CBA Advising" و"بوابة الإرشاد") ثم تثبيت النسخة الجديدة من رسالة Firebase App Distribution القادمة - بعدها ستُثبَّت كل التحديثات المستقبلية بسلاسة تلقائيًا بلا أي تعارض.

### إصلاح: تطبيق "بوابة الإرشاد" لم يكن يُبنى/يُوزَّع تلقائيًا عبر CI إطلاقًا (2026-08-25)

بعد نشر ميزات اليوم كلها (الشريط الموحَّد بالموقع وبالجوال)، سليمان لاحظ أن رسالة تحديث التطبيق الواصلة بريديًا (Firebase App Distribution) هي لتطبيق **"CBA Advising"** القديم المجمَّد (`lib/main_advising_app.dart`، بنية تذاكر محلية منفصلة قديمة)، وليست لتطبيق **"بوابة الإرشاد"** الجديد (`lib/main_advising_portal.dart` ← `lib/mobile_portal/` - المعمارية الفعلية التي تحمل كل تحديثات اليوم).

السبب: `.github/workflows/deploy.yml` كان يبني ويوزّع flavor "advising" (القديم) فقط - flavor "advisingPortal" (`com.taif.cba.advisingportal`) كان معرَّفًا فعليًا بـ`android/app/build.gradle.kts` لكن **لم يُستدعَ من CI إطلاقًا**، فكل تحديث لبوابة الإرشاد الجوّالة لم يكن يصل سليمان أبدًا رغم نجاح النشر ظاهريًا. أُضيفت 3 خطوات مطابقة تمامًا لخطوات flavor "advising" (بناء APK، رفع كأثر، توزيع عبر Firebase App Distribution بمعرّف تطبيق Firebase مختلف `...b13a15e0e42a7b19ea97af`) - الآن يصل التطبيقان معًا محدَّثين بكل نشر.

### نفس الشريط الموحَّد على النسخة المتجاوبة للجوال (2026-08-25)

بعد إضافة الشريط الموحَّد بالموقع (البند التالي أدناه)، طلب سليمان تطبيق نفس الآلية على شاشة "رفع الملفات" بالنسخة المتجاوبة للجوال ([portal_uploads_screen.dart](lib/mobile_portal/screens/uploads/portal_uploads_screen.dart) - جزء من الموقع نفسه بواجهة جوال، **وليس** تطبيق `main_advising_app.dart` المنفصل ببنية تذاكر مختلفة تمامًا الذي فُحص أولاً بالخطأ). بطاقة "حالات الحذف والإضافة" صار بها الآن نفس مجموعات الأزرار الأربع (أساسي/مرشد/منسّق قسم/منسّق كلية) بنفس الدوال المستخدَمة بالموقع تمامًا.

استُخرجت دالة رفع الملفات المعالجة المكرَّرة ثلاث مرات بـ[upload_hub_screen.dart](lib/web_portal/upload_hub_screen.dart) إلى `runUploadProcessedFiles` جديدة بـ[upload_flows.dart](lib/web_portal/upload_flows.dart) (يشترك بها الآن الموقع وشاشة الجوال معًا بلا تكرار منطق) - تصحيح مسار: الحوار البدائي بالجلسة نفسها افترض أن "التطبيق" يقصد `main_advising_app.dart` (بنية تذاكر محلية منفصلة `TicketRepository`، إرسال بريد مباشر بلا مراحل تصعيد) وبدأ استكشافه، حتى أرسل سليمان لقطة شاشة صحّحت الفهم لشاشة الجوال المتجاوبة الصحيحة.

### شريط موحَّد واحد لكل عمليات دورة الحذف/الإضافة (رفع وتنزيل كل المراحل) (2026-08-25)

سليمان أوضح أنه هذا الفصل تحديدًا (بداية دراسي حرجة، لا وقت لتدريب كل منسّق) سيتولى **هو بنفسه** كل عمليات الرفع والتنزيل نيابة عن المرشدين ومنسّقي الأقسام والكلية بالكامل: يرفع الملف الأساسي، ينزّل ملفات كل مرحلة، يرسلها يدويًا، يستلم المعالَج، يرفعه، وهكذا حتى النهاية. طلب أن يكون شريط "الملف الأساسي" وحده كافيًا لفعل كل شيء - أيقونات فقط مع تلميح (tooltip) يشرح كل زر.

أُعيد تصميم شريط الأزرار بـ[upload_hub_screen.dart](lib/web_portal/upload_hub_screen.dart) بالكامل: بدل الأزرار الثلاثة الأصلية (تنزيل/تنزيل الكل/إفراغ/رفع)، صار شريطًا واحدًا مقسَّمًا بفواصل ذهبية رفيعة إلى 4 مجموعات (الملف الأساسي، مرحلة المرشد، مرحلة منسّق القسم، مرحلة منسّق الكلية) - كل مجموعة زرّا تنزيل (ذهبي) ورفع (ذهبي فاتح) بتلميح واضح. أُضيفت دالتان جديدتان بـ[upload_flows.dart](lib/web_portal/upload_flows.dart): `runDownloadAllStage2Zip` (كل ملفات منسّقي الأقسام العشرة دفعة واحدة) و`runDownloadAllStage3Zip` (ملفا منسّق الكلية دفعة واحدة) - تكمل `runDownloadAllAdvisorsZip` الموجودة أصلاً لمرحلة المرشدين. أزرار الرفع الثلاثة (مرشدين/منسّقي أقسام/منسّق كلية) كانت موجودة أصلاً كبطاقات كبيرة منفصلة أسفل الصفحة - نُقلت لنفس الشريط الموحَّد (الدالة القديمة `_bulkStageCard` أُبقيت غير مستخدَمة تحسبًا لحاجة مستقبلية).

**إصلاح خلل حقيقي مؤكَّد أثناء نفس الجلسة**: أثناء التحقق من ميزة توزيع حالات المنسّقين على المرشدين (Round-robin)، اكتُشفت أنها لا تعمل فعليًا رغم وجود الكود - محاكاة حقيقية على بيانات Firestore الحية (1000 تذكرة، 143 مرشدًا) كشفت اختلافي تهجئة حقيقيين لحقل "القسم" بين `advisor_roster` والتذاكر (بادئة "قسم " الزائدة، ومسافة إضافية حول "و" بـ"الاقتصاد والتمويل") يمنعان تطابق المفتاح بصمت. أُصلح بدالة `_normalizeDept` جديدة بـ[advisor_zip_service.dart](lib/services/advisor_zip_service.dart)، وتحقَّق الإصلاح فعليًا بإعادة نفس المحاكاة: صفر منسّقين يحتفظون بملف بعد التوزيع (كان 6 قبل الإصلاح).

### ميزة جديدة: تنزيل كل ملفات المرشدين دفعة واحدة بمجلدات متداخلة (2026-08-25)

سليمان لاحظ أن زر "تنزيل الملف الأساسي" الحالي (أعلى الصفحة) ينتج ملفًا واحدًا لكل قسم بلا فرز حسب مرشد - أوضحت أن هذا سلوك مقصود أصلاً (لإرسال منسّق القسم يدويًا نسخة واحدة يراجعها بنفسه)، مختلف عن ميزة "مرحلة 1" الموجودة أصلاً (ملف مستقل لكل مرشد، لكن قسمًا/شطرًا واحدًا بكل ضغطة). سليمان أكّد أن الملف الأول صحيح كما هو لغرضه، وطلب ميزة **جديدة** إضافية: تنزيل كل الأقسام/الشطرين دفعة واحدة بملف ZIP رئيسي واحد بمجلدات متداخلة (شطر > قسم > ملف لكل مرشد).

- [advisor_zip_service.dart](lib/services/advisor_zip_service.dart): استُخرجت دالة `buildAdvisorFiles` (تُرجع خريطة اسم-ملف↔بايتات بلا ضغط) من `buildZip` (التي أصبحت تستدعيها الآن) - بلا تكرار منطق، ومتاحة لتجميع عدة أقسام معًا.
- [upload_flows.dart](lib/web_portal/upload_flows.dart): دالة جديدة `runDownloadAllAdvisorsZip` تلف على كل مجموعات (شطر، قسم)، تبني ملفات كل مرشد عبرها، وتضيفها لأرشيف واحد بمسارات `شطر_الطلاب/<القسم>/<المرشد>.xlsx`.
- زر جديد بأيقونة حقيبة مضغوطة بجانب زر "تنزيل" الحالي بـ[upload_hub_screen.dart](lib/web_portal/upload_hub_screen.dart) - بنفس بطاقة "الملف الأساسي" أعلى الصفحة.

### توسيع عمود "سبب الطلب" + إخفاء أعمدة المنسّق/الكلية غير المعنية بملف المرشد (2026-08-25)

بعد اختباره الملف الجديد، أكّد سليمان نجاح كل شيء وطلب تحسينين إضافيين لتبسيط الملف أكثر لأكثر من 100 مرشد أكاديمي: عمود "سبب الطلب" مقصوص بصريًا (نص طويل، أسباب متعددة مفصولة بـ";")، واقترح إخفاء عمودي حالة/ملاحظات منسّق القسم ومنسّق الكلية عن ملف المرشد تحديدًا (لا يملؤهما ولا يحتاج رؤيتهما).

- **عمود "سبب الطلب"**: وُسِّع (22→42) وفُعِّل التفاف النص (Word Wrap) له تحديدًا بـ[excel_export_service.dart](lib/services/excel_export_service.dart) - يظهر كاملاً بلا قص.
- **إخفاء الأعمدة**: أُضيفت قدرة جديدة `hiddenColumnIndexes` بـ[excel_protection_service.dart](lib/services/excel_protection_service.dart) (تُدرِج `<cols><col hidden="1".../></cols>` بمخطط OOXML قبل `<sheetData>` مباشرة) - تبقى الأعمدة موجودة فعليًا بالملف (ضرورية لمطابقة `ProcessedFileParserService` عند إعادة القراءة) لكن غير ظاهرة. مُطبَّقة حصرًا بملف المرشد ([advisor_zip_service.dart](lib/services/advisor_zip_service.dart)) على عمودي منسّق القسم ومنسّق الكلية الأربعة (حالة+ملاحظات لكل) - ملفات التصعيد (منسّق القسم/الكلية) لم تُمَس. سليمان اقترح لاحقًا (بنفس الجلسة) توسيع الإخفاء لأعمدة أخرى موحَّدة القيمة/غير مفيدة للمرشد - **بعد نقاش صريح حول رقم الجوال تحديدًا** (رأيي: يفيد التواصل المباشر، رأي سليمان الحاسم: "التواصل يكون عبر القنوات الرسمية فقط" - أُخفي بالنهاية وفق قراره). الأعمدة المخفية الآن بملف المرشد: الشطر، القسم، اسم المرشد الأكاديمي، رقم الجوال، رمز المقرر، بالإضافة لأعمدة منسّق القسم/الكلية الأربعة.

### إصلاح دائم لخلل numFmtId عند رفع ملفات اختبار مُولَّدة عبر SheetJS (2026-08-25)

بعد توليد ملف التجربة الواقعي (البند التالي أدناه) بسكربت Node/SheetJS، فشل رفعه على الموقع الرسمي فعليًا برسالة `Exception: custom numFmtId starts at 164 but found a value of 56` - عيب توافق معروف وموثَّق مسبقًا (مذكرة `project_sheetjs_dart_excel_compat`): `XLSX.writeFile` الافتراضي يكتب `<numFmts>` وهميًا بمعرّف غير صالح، وحزمة `excel` في Dart ترفضه بصرامة. أصلحت الملف يدويًا فورًا (فك ضغط، حذف `<numFmts>`، إعادة حزم بأداة `jar` لتفادي مشكلة فواصل مسار PowerShell)، ثم طبّقت الإصلاح **دائمًا** بـ[xlsx_sanitizer.dart](lib/utils/xlsx_sanitizer.dart) (يُطبَّق تلقائيًا بكل نقاط رفع Excel بالموقع) - أي ملف اختباري مشابه مستقبلاً يُصلَح تلقائيًا عند الرفع بلا تدخل يدوي.

### إعادة تصميم أعمدة ملف تصدير التذاكر لتبسيطها على المرشد الأكاديمي (2026-08-25)

سليمان طلب صراحةً أن يستطيع أي من 100+ مرشد أكاديمي (بينهم من لا يعرف إكسل إطلاقًا) تعبئة الملف "بدون أي تعقيد، واضح وسهل وبسيط جدًا جدًا". أعاد تصميم بنية الأعمدة بـ[excel_export_service.dart](lib/services/excel_export_service.dart) بالكامل حسب مواصفته الدقيقة:

- عمود "المقرر" المدمج (اسم+رمز+رقم أحيانًا) استُبدل بـ**6 أعمدة مستقلة واضحة**: اسم المقرر، رمز المقرر، رقم الشعبة المراد إضافتها، رقم الشعبة المراد حذفها، رقم الشعبة الحالية (تعديل)، رقم الشعبة المطلوبة (تعديل) - كل عمود يُملأ فقط لنوع الإجراء المناظر له ويبقى فارغًا للباقي، فلا التباس أبدًا حول أي رقم يخص أي إجراء.
- **تجميع الصفوف حسب نوع الإجراء** (كل صفوف "إضافة" أولاً، ثم "حذف"، ثم "تعديل") بدل الترتيب العشوائي السابق حسب الطالب - يسهّل المسح البصري كتلة كتلة.
- تنظيف اسم/رمز مقرري "حذف"/"تعديل" (لا قائمة منسدلة بالنموذج لهما، حقل حر بالكامل) عبر مطابقة تلقائية مع **جدول المقررات الفعلي المرفوع بالموقع (الحويّة)** - دالة جديدة `ExcelParserService.enrichWithCourseCatalog` في [excel_parser_service.dart](lib/services/excel_parser_service.dart)، مطبَّقة عند رفع كل ملف Forms في [upload_flows.dart](lib/web_portal/upload_flows.dart) (تحميل الجدولين الحاليين شطر طلاب/طالبات دومًا وقت الرفع - يعكس تلقائيًا أي تحديث مباشر للمقررات/الشعب بالموقع، لا نسخة مجمَّدة). فشل التحميل (لا جدول مرفوع بعد) لا يكسر الرفع - يبقى النص كما كتبه الطالب.
- توافق رجعي كامل مع تذاكر بلا `course_name`/`course_code` منفصلين (تُستخدم `action['course']` كاملاً كاسم احتياطيًا)، وتصنيف نوع الإجراء بات بـ`.contains()` بدل `==` الصارمة (يتحمّل تسميات أوسع من الثلاث القياسية، تحقَّق عبر اختبار الدمج القائم `test/ticket_merge_test.dart`).
- [processed_file_parser_service.dart](lib/services/processed_file_parser_service.dart) (يقرأ الملف العائد من المرشد) حُدِّث ليعيد بناء `course`/`section` بنفس منطق `FirestoreTicketService` بالضبط للمطابقة الصحيحة عند الدمج.
- **بيانات اختبار واقعية**: استخرجتُ 54 مقررًا تخصصيًا حقيقيًا (رمز+اسم+كل أرقام شعبه الفعلية) من ملف "الجدول الدراسي للمواد.docx" الفعلي المرفوع (فلترة بعمود "المستفيد" = كلية إدارة الأعمال)، وأعدت توليد `استجابات_Forms_اختبار_شامل_1000_حالة_24-08-2026.xlsx` بها بدل الأرقام العشوائية السابقة (كان الجدول الدراسي غير مُعلَن وقت إنشاء الاختبار الأصلي) - مع تعمّد حذف "ال" التعريف بنحو 20% من الحالات لاختبار متانة المطابقة التقريبية.

### إصلاح بقايا "من الشعبة/إلى الشعبة" بعمود المقرر + سماح بتوسيع الأعمدة بالملف المحمي (2026-08-25)

بعد أول نشر، لاحظ سليمان بلقطة شاشة فعلية أن عمود "المقرر" لصفوف "تعديل شعبة" ما زال يحوي بقايا نصية مثل "مقدمة في الشبكات - من الشعبة 1793 إلى الشعبة" (الرقمان حُذفا لكن كلمتي "من الشعبة"/"إلى الشعبة" بقيتا)، وأن الأعمدة ضيقة جدًا لدرجة تمنع القراءة، ولا يقدر عضو هيئة التدريس على تكبيرها لأن الملف محمي.

**السبب الأول**: `_parseSectionChange` بـ[excel_parser_service.dart](lib/services/excel_parser_service.dart#L450) كانت تحذف الرقم المستخرَج فقط من نص المقرر (`courseText.replaceAll(digit, '')`)، لا العبارة الدالة المحيطة به - فتبقى الكلمات عالقة. أُعيدت الدالة بالكامل: نمط جديد `_fromToSectionPattern` يلتقط العبارة **كاملة** ("من الشعبة X إلى الشعبة Y") ويحذفها دفعة واحدة من نص المقرر (`replaceRange` بموضع المطابقة الفعلي، لا حذف نص متكرر بالخطأ)، مع نمطين احتياطيين لصيغة "الشعبة الحالية/المطلوبة" المنفصلة. تحقُّق فعلي بمحاكاة Node.js على المثالين الحقيقيين من اللقطة - اسم المقرر نظيف تمامًا الآن.

**السبب الثاني**: عرض عمود "المقرر" الافتراضي بالملف المُصدَّر كان 18 وحدة فقط (لا يكفي لاسم مقرر كامل + رمزه) - وُسِّع إلى 42 بـ[excel_export_service.dart](lib/services/excel_export_service.dart). كذلك اكتُشف أن `sheetProtection` بـ[excel_protection_service.dart](lib/services/excel_protection_service.dart#L202) كانت تفتقد صراحة `formatColumns`/`formatRows` - القيمة الافتراضية بمواصفة OOXML لو غابا هي "محمي" (ممنوع)، فيمنع الملف أي عضو من توسيع عمود بنفسه حتى لو أراد ذلك يدويًا. أُضيف `formatColumns="0"` و`formatRows="0"` صراحة للسماح بالتحكم بحجم الأعمدة/الصفوف رغم بقاء الخلايا نفسها مقفلة.

### استخراج رقم الشعبة اعتمادًا على القائمة المنسدلة + إصلاح ملف تنزيل بلا حماية/قوائم منسدلة (2026-08-25)

سليمان رفع ملف تصدير حقيقي وأشار لمشكلتين: عمود "رقم الشعبة" فارغ بكل الصفوف، ولا حماية ولا قوائم منسدلة رغم الاتفاق السابق على وجودها.

**١. رقم الشعبة الفارغ** - فحص الملف الخام (نموذج Microsoft Forms) كشف أن حقل "المقرر ورقم الشعبة" حر تمامًا وغير موثوق (طلاب يكتبون اسم مقرر مختلف كليًا عن اختيارهم، أو رقمًا وهميًا "1234"، أو حتى النص التوضيحي "مثال: ..." حرفيًا بلا تعديل). القرار مع سليمان: الاعتماد الكلي على عمود القائمة المنسدلة الفعلي ("اختر المقرر أو المقررات المطلوب إضافتها") كمصدر الحقيقة الوحيد لاسم المقرر، واستخراج رقم الشعبة فقط من الحقل الحر عبر مطابقة نصية (احتواء متبادل بعد تجريد "ال" التعريف، أو مسافة Levenshtein محدودة لتحمّل خطأ إملائي بسيط) بلا اعتماد على ترتيب الحقول إطلاقًا - إن لم يوجد تطابق واثق يُترك رقم الشعبة فارغًا بدل تخمين خاطئ. التحقق تم فعليًا بمحاكاة Node.js على بيانات الملف الحقيقي (تطابق دقيق للحالات الصحيحة، فراغ صحيح للحالة التجريبية العشوائية). التنفيذ في [excel_parser_service.dart](lib/services/excel_parser_service.dart) (أعمدة `addDropdownCols` جديدة + دوال `_splitDropdownCourses`/`_matchDropdownSection`/`_stripDefiniteArticles`/`_levenshtein`)، مع توافق رجعي كامل للنماذج القديمة بلا عمود قائمة منسدلة.

**٢. لا حماية/قوائم منسدلة** - تبيّن أن `ExcelProtectionService` (حماية + قوائم منسدلة عبر معالجة XML يدوية لأن حزمة `excel` لا تدعمهما) مطبَّقة فعليًا بمعظم مسارات التصدير (ملف المرشد، ملفات التصعيد، البريد الإلكتروني، ملف ذوي الإعاقة) - لكن مسارًا واحدًا نُسي: `runDownloadFormsZip` في [upload_flows.dart](lib/web_portal/upload_flows.dart#L731) (تنزيل ملفات الأقسام لإرسالها يدويًا لمنسّقي الأقسام) كان يستدعي `ExcelExportService.buildDepartmentWorkbook` مباشرة بلا أي حماية. أُصلح باستبداله بـ`EscalationFileService.buildStage2File` (نفس حماية/قائمة "حالة الإنجاز من قبل منسق القسم" المستخدَمة بمسار التصعيد الفعلي). لا تشخيصات IDE على أي ملف مُعدَّل.

### إصلاح تشوّه الشعار بملف PowerPoint لشاشات العرض (2026-08-25)

سليمان لاحظ أن الشعار يظهر مضغوطًا/مشوَّهًا عند فتح ملف PowerPoint المُصدَّر (شاشات العرض)، ويضطر لسحبه يدويًا كل مرة ليتضح. السبب: `advising_schedule_pptx_service.dart` كان يضع صندوق الشعار بأبعاد مربّعة قسرية (`cx=520000, cy=520000`) بينما الشعار الحقيقي (`unit_logo_final.png`) مستطيل عريض فعليًا (378×124 بكسل، نسبة ~3:1) - فتضطر PowerPoint لضغط الصورة داخل صندوق لا يطابق شكلها. `noChangeAspect="1"` يمنع تغيير النسبة *لاحقًا* عند السحب لكنه لا يصحح الصندوق الأولي. الإصلاح: الارتفاع (`cy`) يُشتق الآن من العرض بنفس نسبة أبعاد الصورة الحقيقية (`cx × 124/378`) بدل قيمة ثابتة، ويُتمركز الصندوق رأسيًا داخل الشريط العلوي تلقائيًا. `flutter analyze` نظيف. لم يُنشر بعد - بانتظار تأكيد سليمان بعد رؤية النتيجة.

### معالجة تلقائية دائمة لعيوب توافق xlsx بكل نقاط رفع الملفات (2026-08-25)

بعد الإصلاح اليدوي لملفي الاختبار (البند أدناه)، طلب سليمان صراحةً معالجة تلقائية دائمة - العمل بالموقع يعتمد أساسًا على رفع ملف استجابات Forms، ولا يجوز أن يتكرر نفس الخلل يدويًا كل مرة. أُضيفت `lib/utils/xlsx_sanitizer.dart` (دالة `sanitizeXlsxBytes`) تُصلح تلقائيًا وبأمان تام (idempotent، تُرجع البايتات الأصلية دون تغيير لو الملف سليم أصلًا أو تعذّرت المعالجة) العيوب الثلاثة الموثَّقة أدناه: تفكّ ضغط الملف بحزمة `archive` (متوفرة أصلًا)، تُصلح مسارات `.rels` المطلقة وبادئة `x:` وخلايا `t="s"` الفارغة داخل XML، ثم تعيد الضغط. طُبِّقت في **كل** نقطة تستقبل ملف Excel مرفوعًا من مستخدم خارجي: `ExcelParserService.parseTickets` (استجابات Forms - الأهم)، `ProcessedFileParserService.parseProcessedRows` (ملف المرشد العائد)، `CollegeRosterParserService.parse`/`parseUnitCommittee` (منسوبو الكلية)، `AdvisingScheduleExcelService.parseTemplate` (نموذج فترات الإرشاد). لم تُطبَّق في `AdvisorZipService` لأنه يقرأ ملفًا من إنتاج الموقع نفسه (`ExcelExportService`) لا رفعًا خارجيًا.

**تحقُّق فعلي (لا افتراضي)**: أُعيد تصنيع نسخة تالفة بكل العيوب الثلاثة من الملف السليم (سكربت مؤقت بمجلد الـscratchpad)، ثم استُدعيت `ExcelParserService.parseTickets` الحقيقية من المشروع مباشرة عليها **بلا أي إصلاح يدوي مسبق** - نجحت القراءة تلقائيًا (999 حالة، الفارق البسيط عن 1000 متعمَّد ضمن الاختبار نفسه). `flutter analyze` نظيف على كل الملفات المعدَّلة.

### إصلاح ملفي Excel تالفين (استجابات Forms + النموذج الإلكتروني) رفضهما الموقع برسالة "Damaged Excel file: styles" (2026-08-25)

سليمان حاول رفع ملفي `.xlsx` (استجابة اختبار شاملة 1000 حالة، وملف بيانات حقيقية) فرفضهما الموقع فورًا. الفحص كشف 3 عيوب تراكمية بملفات الاستجابة الحقيقية من نفس مصدر تصدير Forms، تُصلَح جميعًا بفك ضغط الملف (zip) وتعديل الـXML الداخلي:

1. **مسارات مطلقة بـ`xl/_rels/workbook.xml.rels`**: `Target="/xl/styles.xml"` بدل `Target="styles.xml"` - حزمة `excel` بـDart تقرأ القيمة حرفيًا وتضيف `xl/` أمامها، فتصبح `xl//xl/styles.xml` (غير موجود) → استثناء "Damaged Excel file: styles" فورًا رغم أن `styles.xml` سليم تمامًا.
2. **بادئة نطاق أسماء `x:` على كل عنصر** بالملفات الرئيسية (`workbook.xml`, `styles.xml`, `sharedStrings.xml`, `worksheets/sheet1.xml`, `tables/table1.xml`) بدل استخدام `xmlns` كنطاق افتراضي - حزمة `excel` تبحث بأسماء غير مسبوقة (`findAllElements('sheet')` إلخ) فلا تجد شيئًا، فتفشل بصمت (`_sheetMap` يبقى فارغًا) دون أي رسالة خطأ حتى استدعاء `.tables`.
3. **خلايا فارغة موسومة زورًا `t="s"` بلا عنصر `<v>`** (138 خلية بملف البيانات الحقيقية) - يرمي `Bad state: No element` عند محاولة قراءة قيمتها كنص مشترك؛ الحل حذف `t="s"` من الخلايا المغلقة ذاتيًا (`<c .../>`) فتُعامَل كخلية فارغة عادية.

التحقق تم فعليًا (لا افتراضيًا) عبر سكربت Dart مؤقت يستدعي `ExcelParserService.parseTickets` الحقيقية من المشروع مباشرة على الملفين بعد الإصلاح: 1000 حالة اختبار و4 حالات بيانات حقيقية قُرئت بنجاح. الملفان الأصليان بنفس المسار بالمرفقات استُبدلا بالنسخ المُصلَحة (النسخة الاختبارية والنسخة الحقيقية معًا). راجع مذكرة `project_sheetjs_dart_excel_compat` (تُحدَّث بهذه الحالات الجديدة).

### إصلاح تقسيم صفحات "شاشات العرض" لتوزيع فترات الإرشاد + تصدير صور PNG مضغوطة + عرض وقت الفترة (2026-08-24)

سليمان لاحظ أن ملف "شاشات_العرض_توزيع_فترات_الإرشاد" (69 صفحة) غير منطقي: صفحات شبه فارغة (صف يتيم بصفحة كاملة)، وجداول قسم تنقسم منتصف القائمة. السبب: كل جدول فترة (`_periodTableSignage`) كان `pw.Table` واحدًا بلا أي تحكم بالانقسام، فتقسمه مكتبة `pdf` تلقائيًا بشكل عشوائي إن تجاوز ارتفاعه صفحة واحدة (وارد بخط شاشات العرض الضخم). الإصلاح في `advising_schedule_pdf_service.dart`: كل فترة تتجاوز 10 صفوف تُقسَّم صراحةً لعمودين/ثلاثة أعمدة جنبًا إلى جنب (كل عمود برأسه المكرر) بدل الاعتماد على الانقسام التلقائي، وتحسين "الفترتين جنبًا إلى جنب" (موجود سابقًا لوضع الطباعة العادي فقط) صار مطبَّقًا أيضًا بوضع شاشات العرض حين تكون الفترتان صغيرتين.

طلب سليمان أيضًا تصدير هذه "البطاقات" (يوم × قسم × شطر) كصور PNG داخل ملف ZIP لإرسالها لسكرتارية الكلية، تُعرض كشرائح على شاشات الإسياب (Corridor) بالكلية. أُضيفت `AdvisingSchedulePdfService.buildSignageCards` (كل بطاقة = PDF أحادي الصفحة مستقل تمامًا)، وخدمة جديدة `AdvisingScheduleSignageImageService` تحوّل كل بطاقة لصورة PNG عبر `Printing.raster` (حزمة `printing` الموجودة أصلاً، تعمل على الويب) وتضغطها بملف ZIP واحد (نفس نمط `AdvisorZipService.buildZip` بحزمة `archive`). زر جديد "تنزيل صور الشرائح (ZIP)" بجانب زر "عرض PDF - شاشات العرض" في `advising_schedule_admin_screen.dart`.

كذلك أُضيف عرض وقت كل فترة بجانب اسمها ("الفترة الأولى (8:00 ص - 11:00 ص)"، "الفترة الثانية (11:00 ص - 2:00 م)") - `AdvisingScheduleExcelService.periodDisplayLabel` جديدة، مطبَّقة في كل مكان يُعرض فيه اسم الفترة (بطاقات الواجهة وملفات PDF الرسمية وشاشات العرض).

كل هذا على الفرع الحالي فقط - لم يُختبَر حيًّا بعد (لا `flutter` بهذه البيئة)، يحتاج تأكيد سليمان بصريًا قبل أي نشر.

### اعتماد رفع PDF مباشرة لجدول المقررات الدراسية + إصلاح خلل انعكاس أرقام قاعات "شبكة" بـWord (2026-08-24)

سليمان طلب إمكانية رفع جدول المقررات (الحويّة) كملف **PDF مباشرة** بدل الاقتصار على Word، لأن رافع الملف مستقبلاً قد لا يعرف كيفية تحويل PDF إلى Word. بُنيت أولاً صفحة معاينة معزولة (`schedule_parse_preview_screen.dart` + `pdf_schedule_parser_service.dart`، بلا حفظ بيانات) لمقارنة قراءة PDF مباشرة بقراءة Word المعتمَدة، ثم قورنتا برمجيًا (سكربتات Node، مفتاح `رمز المقرر|التسلسل|الشطر`) على ملف حقيقي كامل (710 شعبة، عدة جولات اختبار حي من سليمان).

**اكتشاف مهم أثناء المقارنة**: قارئ Word المعتمَد نفسه كان يحمل خللاً حقيقيًا غير مُصحَّح - انعكاس أرقام قاعات "شبكة" (تحويل PDF→Word يعكس ترتيب الرقم عند وجود مسافة قبل "شبكة" بالمصدر، نفس نمط خلل انعكاس الوقت المُصحَّح سابقًا). أُصلح هذا في `docx_schedule_parser_service.dart` (دالة `_fixNetworkRoomReversal` جديدة) - يعالج فقط الحالة المفصولة بمسافة، ولا يمسّ الحالة الملتصقة بلا مسافة (كانت أصلًا سليمة).

**سلسلة إصلاحات بقارئ PDF التجريبي** (`pdf_schedule_parser_service.dart`) عبر عدة جولات اختبار حي:
- لقب "د." غير مُزال من اسم المحاضر، وكلمة "(حضوري)" غير منظَّفة من القاعة.
- اقتطاع "المستفيد" متعدد الكليات (يُصنَّف أحيانًا على 2-3 خلايا منفصلة) - أُصلح بثلاث جولات متتالية حتى وصل لاستشراف أمامي لخلية "جسر" وسطى بلا علامات.
- موعد أسبوعي وهمي إضافي كان يلتقط أحيانًا اسم المحاضر كـ"قاعة" لصف متابعة مُفسَّر خطأً - أُضيف تحقق `_looksLikeRoom` يعتبر القاعة فارغة بدل حقن نص خاطئ (وليس إسقاط الموعد بأكمله - تبيّن من مقارنة لاحقة أن Word نفسه يُظهر مواعيد ثانية حقيقية بقاعة فارغة).
- ترتيب معكوس مستقل لقاعات "شبكة" الملتصقة برقم داخل استخراج PDF نفسه (خلل مختلف عن خلل Word، مصدره ترتيب "كلمات" PDF لا معالجة bidi).
- محاولة إصلاح ترتيب قوس واحد شكلي (كورس نيم/المستفيد) **تراجَع عنها** لأنها زادت الخلل سوءًا - تُركت كوزمتيكية غير مُصلَحة، لا تغيّر المعنى، ولا تؤثر على منطق الفلترة الفعلي (تحقَّقتُ أن اسم الكلية الأساسي "كلية إدارة الأعمال" يبقى سليمًا بكل الحالات).

**القرار النهائي**: بدل مطاردة الخلل البنيوي المتبقي (~1% من الشعب: موعد وهمي إضافي، سببه طريقة تجميع كلمات PDF بلا معرفة حقيقية بحدود الجدول - غير مضمون حل كامل نهائي)، `runUploadCourses` (`upload_flows.dart`) أصبح يقبل **.docx أو .pdf مباشرة** (يتحقق من التوقيع الحقيقي لكل نوع: PK لـdocx، %PDF لـpdf، ويوجّه تلقائيًا للقارئ المناسب) مع **تنبيه تلقائي** (حوار تأكيد صريح يسرد الشعب المشبوهة) عند اكتشاف أي شعبة بـ3 مواعيد أسبوعية أو أكثر - شبكة أمان عملية تصمد أمام ملفات مستقبلية بدل إصلاح كود يعالج ملف اليوم فقط.

كل هذا على فرع `preview/forms-import-fix` فقط - يحتاج اختبار رفع PDF حقيقي عبر تدفّق الرفع الفعلي نفسه (لا صفحة المعاينة فقط) قبل أي نشر على الموقع الرسمي.

### إصلاح تجاهل صامت لحالات بلا رقم جامعي صالح + صفحة "الحذف والإضافة" لا تعود بيضاء بلا رسالة (2026-08-24)

سليمان رفع ملف اختباري بحالة واحدة، ظهر شريط "نجاح" لكن تبويب "الحذف والإضافة" ظهر بصفحة بيضاء تمامًا بلا أي بيانات ولا أي خطأ بالكونسول. السبب: `ExcelParserService` يستخرج الرقم الجامعي من بريد الطالب حصرًا (نمط `s<رقم>@...`)، وبريد الاختبار كان "anonymous" (لم يسجّل دخولاً جامعيًا حقيقيًا عند تعبئة النموذج) فخرج الرقم فارغًا - و`FirestoreTicketService.replaceAllTickets`/`addNewTickets` كانا يتجاهلان أي حالة برقم جامعي فارغ **بصمت تام** رغم أن `batch.commit()` ينجح ويُظهر شريط النجاح، فلم تُكتب أي بيانات فعليًا. إصلاحان:
- `replaceAllTickets` يرجّع الآن عدد الحالات المتجاهَلة، و`addNewTickets` يبلّغ عنها عبر `onSkippedNoId` - رسالة الرفع بـ`upload_flows.dart` (`runUploadForms`) تعرض الآن تحذيرًا صريحًا بالعدد والسبب بدل الصمت.
- تبويب "الحذف والإضافة" (`admin_workspace_screen.dart`) كان يعتمد على `TicketActionStatsPanel` الذي يُرجع مربعًا فارغًا (`SizedBox.shrink()`) عند عدم وجود بيانات - أصبح الآن يميّز صراحةً 3 حالات: تحميل (مؤشر دوّار)، خطأ فعلي بالتدفّق (رسالة حمراء بنص الخطأ)، لا توجد بيانات بعد (رسالة واضحة) - لا صفحة بيضاء صامتة بعد الآن مهما كان السبب.

### زر "تنزيل الملف" لـ"الملف الأساسي - طلبات الحذف والإضافة" + إصلاح نشر قواعد Firestore الناقص (2026-08-23)

سليمان أبلغ عن `[cloud_firestore/permission-denied]` عند رفع ملف الإرشاد رغم استخدام حساب salfawaz (super_admin) الذي يملك الصلاحية بالكود. اتضح أن نشر Hosting تلقائي عبر GitHub Actions (`deploy.yml`/`preview-deploy.yml`) لكن **نشر firestore.rules ليس جزءًا من هذه الأتمتة إطلاقًا** - القواعد المنشورة فعليًا على Firebase كانت أقدم من نسخة الكود الحالية. الحل: تشغيل `node tool/admin_cli/deploy_firestore_rules.js` يدويًا لنشر القواعد الحالية - نُشرت بنجاح (Ruleset جديد). سُجِّلت هذه النقطة بالذاكرة الدائمة حتى لا تتكرر مستقبلاً بعد أي تعديل على firestore.rules.

أُضيف أيضًا زر "تنزيل الملف" بجانب "رفع الملف" بشريط "الملف الأساسي - طلبات الحذف والإضافة" (`upload_hub_screen.dart`) - بطلب سليمان صراحةً: يحتاج ملفًا واحدًا يشمل 10 ملفات Excel منفصلة (5 أقسام علمية × شطرَي الطلاب/الطالبات) من بيانات "الملف الأساسي" الحالية كما هي (بلا فرز حسب مرشد)، ليتمكّن من إرساله يدويًا (بريد/واتساب) لأي منسّق قسم يتعذّر عليه الدخول للموقع نفسه. عند الضغط: تُجلَب كل التذاكر الحالية، تُجمَّع حسب (شطر، قسم) عبر `ExcelParserService.groupByShatrAndDepartment` الموجودة أصلاً، ويُبنى ملف Excel خام لكل مجموعة عبر `ExcelExportService.buildDepartmentWorkbook` (نفس الدالة المستخدَمة لبقية ملفات القسم)، ثم تُضغَط العشرة ملفات بملف ZIP واحد وتُنزَّل. لم يُختبَر حيًّا بعد (لا `flutter` بهذه البيئة).

### إصلاح خلل حقيقي بتصنيف "تقرير نصاب الإرشاد" (2026-08-23)

سليمان رصد حيًّا حالتين ("كريم سعيد ذيب عكور" و"السيد الحضري أحمد محمود"، قسم الإدارة) بعدد فعلي=71 وحصة عادلة معروضة=72 (فرق=1) لكن الحالة تظهر "دون النصاب" بدل "متوازن" - رغم أن قاعدة "فارق طالب واحد = متوازن" موجودة أصلًا بالكود منذ 2026-08-14. أُضيف تتبّع تشخيصي مؤقت وشُغِّل حيًّا للوصول للسبب الجذري: `advising_case_analyzer.dart` كان يقارن العدد الفعلي بالحصة العادلة **الخام** (مثال: 72.08) بينما الجدول يعرض القيمة **المقرَّبة** (72) - الفرق الظاهر للمستخدم = 1 (متوازن حسب القاعدة)، لكن الفرق الخام الفعلي = 1.08 (يتجاوز الحد فيصير "دون النصاب" رغم أن المعروض يوحي بالعكس تمامًا). الإصلاح: التصنيف يقارن الآن بالقيمة المقرَّبة نفسها المعروضة بالجدول - يطابق دائمًا ما يراه المستخدم فعليًا. حُذف التتبّع التشخيصي بعد التأكد.

تحقَّق `flutter analyze` بلا أخطاء جديدة، وتحقَّق حيًّا أن الحالتين المُبلَّغتين أصبحتا "متوازن" بشكل صحيح، وفُحصت عيّنة إضافية من الصفوف (فرق ≤1 لكل منها) وكلها سليمة الآن. **نُشر على الموقع الرسمي بموافقة سليمان الصريحة (2026-08-23)**.

### اعتماد نهائي: بطاقات "متابعة سير العمل" بسطر أفقي واحد + إعادة تصميم "متابعة حالات الإرشاد" (2026-08-23)

- **بطاقات "متابعة سير العمل" بلوحة الإدارة**: أُعيد تصميمها بمرجع صورة صريح من سليمان ("اعتماد نهائي، لا اجتهاد") - كل بطاقة (المرشدون الأكاديميون/منسّقو الأقسام العلمية/منسّق الكلية) أصبحت **سطرًا أفقيًا واحدًا** بدل الشبكة الرأسية: العنوان + الإجمالي يمينًا، ثم الحالات الثلاث جنبًا إلى جنب بفواصل رأسية رفيعة (رقم + نقطة ملوَّنة + تسمية + شريط تقدّم مختصر لكل حالة). البطاقات الثلاث ممتدة بعرض كامل ومكدَّسة رأسيًا (لا جنبًا إلى جنب). يقلّص ارتفاع القسم بشكل كبير جدًا مقارنة بالتصميم السابق.
- **صفحة "متابعة حالات الإرشاد" - إعادة تصميم شاملة بطلب سليمان المفصَّل (9 بنود)**: رأس الصفحة (العنوان + الوصف) بسطر واحد على Desktop بدل التكديس الرأسي (مكوّن محلي جديد خاص بهذه الصفحة فقط، لم يُمسّ `AdvisingPageHeader` المشترك مع بقية صفحات الإرشاد). حُذفت بطاقة "طلبة على غير مرشدهم – ذوي الإعاقة" المستقلة من شبكة المؤشرات (بلا حذف البيانات/الحسابات نفسها من الكود) وظهر رقمها كملاحظة ديناميكية "منهم X من ذوي الإعاقة" داخل بطاقة "طلبة على غير مرشدهم". المؤشرات التشغيلية أصبحت 8 بطاقات بتوزيع 4+4 (بدل 3×3). ضُغطت الفراغات الرأسية العلوية فأصبح الجدول يُظهر 8 صفوف طلاب كاملة عند فتح الصفحة بلا تمرير (الهدف المطلوب كان 4-5 فقط). بقيت الألوان والأيقونات والفلاتر وتصميم الجدول والفوتر بلا أي تغيير كما طُلب صراحةً.

تحقَّق `flutter analyze` بلا أخطاء جديدة، وتحقَّق حيًّا عبر Playwright أن كل البنود التسعة تعمل بدقة (بما فيها فحص دقيق لصورة المرجع المرفقة). أثناء المراجعة اكتُشف خلل محتمل منفصل بحساب "تقرير نصاب الإرشاد" (عدد فعلي أكبر من الحصة العادلة لكنه يُصنَّف "دون النصاب" لأحد المرشدين) - **لم يُحلّ بعد**، يحتاج تشخيصًا حيًّا مباشرًا (تتبّع القيم الخام قبل التقريب)، مسجَّل بـTODO.md. **نُشر على الموقع الرسمي بموافقة سليمان الصريحة (2026-08-23)**.

### ضغط رأسي للوحة الإدارة + تحسينات جداول متفرقة + حذف بيانات وهمية متبقية (2026-08-23)

- **لوحة الإدارة (مؤشرات الحذف والإضافة)**: جولة ضغط شاملة (تقليل الفراغات بين الأقسام وحشو البطاقات الداخلي، بلا أي تصغير خط أو `scale`/`zoom`) بطلب سليمان الصريح لإظهار الصفحة كاملة - من الشريط العلوي حتى الفوتر - بشاشة 1920×1080 واحدة بلا أي تمرير رأسي. تحقَّق حيًّا أن الفوتر يظهر ضمن حدود الشاشة مباشرة بلا فرق بين لقطة العرض المرئي والصفحة الكاملة.
- **خدمات أكاديمية → أعلى الأعباء التدريسية**: تحوَّلت من قائمة صفوف بسيطة إلى جدول `DashTable` حقيقي بعمودين (الاسم/عدد الشعب) بطلب سليمان ("اجعلها كجدول").
- **دليل المقررات → تسكين الشعب الرئيسي**: عمودا "المحاضر" و"الوقت" وُسِّعا ليظهرا دائمًا بسطر واحد؛ وأُضيفت دالة `_shortenLongName` تحذف الاسم قبل الأخير (اسم الجد عادةً) تلقائيًا من أسماء 4 كلمات فأكثر للعرض فقط (بلا أي أثر على مطابقة البيانات) - تحل مشكلة التفاف الأسماء الطويلة جدًا لسطرين حتى بعد التوسيع.
- **تنظيف بيانات وهمية متبقية بالموقع** (بطلب سليمان الصريح قبل النشر): حُذف قسم "اختبار مؤقّت" كامل من `unit_coordinator_workspace_screen.dart` (بطاقات "رقم تجريبي 1/2"، "أيقونة تجريبية 1/2") كان معلَّمًا صراحةً بتعليقه الخاص للحذف بعد التجربة (2026-08-09) لكنه نُسي ومكث منشورًا. فُحص بقية المشروع بحثًا عن أنماط مشابهة (نموذج/تجريبي/mock/dummy) - لم يُعثر على أي بيانات وهمية أخرى معروضة فعليًا على الموقع الحي (ملف `mail_test_screen.dart` يحتوي بيانات اختبار لكنه غير مربوط بأي مسار تنقّل، فلا يظهر للمستخدمين).

تحقَّق `flutter analyze` بلا أخطاء جديدة، وتحقَّق حيًّا عبر Playwright أن كل التعديلات تعمل بلا أخطاء console أو خلل بصري. **نُشر على الموقع الرسمي بموافقة سليمان الصريحة (2026-08-23)**.

### تطبيق "بوابة الإرشاد" الجوّالي الجديد - المرحلة الأولى (الأساس) (2026-08-23)

قرار معماري صريح من سليمان: التطبيق الجوّالي القديم "CBA Advising" (`lib/main_advising_app.dart`, `lib/web_portal/mobile_*`) أصبح **مجمَّدًا نهائيًا** لأنه يحاول عرض صفحات الموقع المصمَّمة لسطح المكتب بمقاسات جوال (تخطيطات مشوَّهة، بطاقات غير مناسبة). بدلًا من الاستمرار بترقيعه، بُني تطبيق جوال جديد كليًا "بوابة الإرشاد" (Mobile-First فعليًا، لا نسخة مصغَّرة من الموقع)، منفصل تمامًا في المعمارية عن القديم، لكن يشترك معه بنفس مشروع Firebase/Firestore ونفس خدمات منطق الأعمال (لا اختلاف أرقام بين الموقع والتطبيق).

- **نقطة دخول جديدة منفصلة**: `lib/main_advising_portal.dart` (Firebase app منفصل `DefaultFirebaseOptions.advisingPortal`، معرّف حزمة أندرويد جديد `com.taif.cba.advisingportal` - flavor ثالث أُضيف بـ`android/app/build.gradle.kts` + `android/app/src/advisingPortal/res/values/strings.xml` باسم "بوابة الإرشاد"). **appId الحقيقي مُسجَّل فعليًا الآن** (سليمان سجّله بـFirebase Console 2026-08-23) - `android/app/google-services.json` يحوي الإدخال الثالث، و`lib/firebase_options.dart` محدَّث بالـappId الحقيقي.
- **بنية جديدة كليًا** تحت `lib/mobile_portal/` (app/theme/widgets/screens) منفصلة عن `lib/web_portal/` القديم بلا أي استيراد لصفحات سطح المكتب أو أي WebView/تصغير.
- **شاشة الدخول** (`portal_login_screen.dart`): تخطيط رأسي أصيل للجوال (شعار + "بوابة الإرشاد" + حقلا بريد/كلمة مرور)، تظهر إلزاميًا عند عدم وجود جلسة صالحة (`auth_gate.dart` عبر `authService.authState`) - خلافًا للتطبيق القديم الذي كان يعرض الرئيسية العامة دائمًا بلا دخول إجباري.
- **الرئيسية أُعيد بناؤها كليًا بطلب سليمان الصريح بعد نقاش مباشر أثناء الاختبار الحي (2026-08-23)**: "فراغ كبير... شكلها غير احترافي" ثم مواصفة دقيقة بصور من الموقع - أصبحت **جزأين فقط بنفس الهوية البصرية للموقع حرفيًا، بلا أي إضافات** (لا بطاقات KPI مخترعة، لا أزرار وصول سريع - كلها أُزيلت بعد تجربتها):
  - **"مؤشرات رئيسية لحالات الإرشاد"**: 5 بطاقات (الكل/تابعين لمرشد/تابعين لمرشد – ذوي الإعاقة/بلا مرشد/على غير مرشدهم + ملاحظة "منهم X من ذوي الإعاقة") - **بيانات حقيقية 100%** عبر خدمة جديدة `lib/services/advising_overview_stats_service.dart` أُنشئت خصّيصًا؛ تستدعي حرفيًا نفس الدوال العامة المستخدَمة بشاشة "متابعة حالات الإرشاد" بالموقع (`AdvisingCaseAnalyzer.loadCollegeScopedStudents`/`classifyAllColleges`/`analyze`) بلا أي تعديل عليها ولا إعادة كتابة للحسابات - نفس الصيغ الحسابية بالضبط الموجودة بـ`advising_cases_admin_screen.dart` (`_buildStatsGrid`). هذا مصدر بيانات مختلف تمامًا عن تذاكر `tickets` (تقارير Excel مرفوعة + مطابقة مع ملف منسوبي الكلية)، فتُحمَّل بـ`Future` لا `Stream`.
  - **"إحصائيات طلبات الحذف والإضافة"**: 5 بطاقات (طلبات إضافة/حذف/تعديل + نسبة الإنجاز العامة + إجمالي الحالات) - أرقام إجمالية مباشرة من `ticket_action_stats_service.dart`/`ticket_workflow_stats_service.dart` (`Stream` حي من `tickets`).
  - كلا القسمين بشبكة عمودين (لا صف أفقي واحد بخمس بطاقات - لا يصلح لجوال ضيق)، ببطاقة `MobileKpiCard` المشتركة (أُضيف لها دعم `note`/`highlighted` اختياريين لمطابقة نمط "الكل"/"إجمالي الحالات" المميَّز حدوده بالموقع).
  - **لوحة الإدارة** (`admin_summary_screen.dart`) تبقى كما بُنيت: كل تفصيل قسم "الحذف والإضافة" (ثلاثية المرشد + نظرة عامة + توزيع الأقسام + حالة الإجراءات + سير العمل الثلاثي الكامل) بتبويبين فرعيين داخليين (`DefaultTabController`/`TabBar`) يحاكيان نفس تقسيم الموقع: "الحذف والإضافة" (جاهز) و"الإرشاد" (لا يزال placeholder "قيد التطوير" هنا تحديدًا - أرقام الإرشاد الحقيقية موجودة الآن بالرئيسية فقط عبر الخدمة الجديدة، لم تُربَط بعد بتبويب لوحة الإدارة الفرعي بنفس التفصيل).
- **تنقّل سفلي ثابت بخمس وجهات** (`portal_shell.dart`): الرئيسية/لوحة الإدارة/رفع الملفات/الجداول الدراسية فعّالة بالكامل، و"المزيد" فعّال جزئيًا (تسجيل الخروج فقط).
- **لا تكرار منطق أعمال**: أُنشئ `lib/services/ticket_workflow_stats_service.dart` (مصدر عام مشترك) باستخراج نفس حسابات لوحة الإدارة (`admin_executive_dashboard_screen.dart`: مراحل المسار، نتيجة كل إجراء، تقدّم كل دور، حالات تجاوزت المستوى، نسبة الإغلاق) بلا تغيير في النتيجة نفسها؛ التطبيق الجديد يستهلك هذا المصدر بدل إعادة كتابة الحسابات، إلى جانب إعادة استخدام مباشرة لـ`FirestoreTicketService`, `AuthService`, `ReportDataService`, `TicketActionStatsService`, وهوية `theme/app_theme.dart` كما هي (بلا لون/خط جديد).
- **لم يُلمَس إطلاقًا**: `lib/main_advising_app.dart`, `lib/web_portal/mobile_*` (التطبيق القديم مجمَّد فعليًا كما هو)، ولا أي صفحة سطح مكتب أخرى (باستثناء استخراج الحسابات المذكور أعلاه من `admin_executive_dashboard_screen.dart` بلا أي تغيير بصري).
- **تبويب "المزيد" فعّال جزئيًا الآن** (`portal_more_screen.dart`): تسجيل الخروج فقط (مع تأكيد قبل التنفيذ) - سليمان لاحظ حيًّا أنه لا يوجد مكان لتسجيل الخروج بالمرحلة الأولى؛ بقية عناصر "المزيد" (بحث/سجل عمليات/فتح النظام الكامل) لمراحل تالية.
- **تبويب "رفع الملفات" مُنجز بالكامل بثلاث فئات (2026-08-23)**: بطلب سليمان صراحةً - "ملف الفورم" (طلبات الحذف/الإضافة، مع زرَي رفع وتنزيل)، "المقررات الدراسية" (الحويّة)، "الإرشاد الكامل" (تقرير كل الكليات) - الباقي (ذوو الإعاقة/جداول المواعيد) نادر التغيير، مؤجَّل عمدًا. **إصلاح معماري مهم بالمناسبة**: منطق رفع الفورم والمقررات كان "مدمجًا" (private methods) داخل `_UploadHubScreenState` بـ`upload_hub_screen.dart` (1770+ سطرًا) بلا إمكانية إعادة استخدام - استُخرج الآن لدالتين عامتين جديدتين `runUploadForms`/`runDownloadFormsZip`/`runUploadCourses` بـ`upload_flows.dart` (نفس نمط الدوال الثلاث الموجودة أصلًا هناك: `runUploadHealth`/`runUploadAdvisingSchedule`/`runUploadAllColleges`)، **بلا أي تغيير في السلوك أو الحوارات على الموقع نفسه** (نقل كود بحت، اختُبر التطابق سطرًا بسطر) - يضمن هذا عدم وجود أي احتمال لاختلاف نتيجة رفع نفس الملف بين الموقع والتطبيق مستقبلاً. تنزيل ملف الفورم (`downloadBytes`) يعمل تلقائيًا وبشكل صحيح على الجوال (يحفظ بمجلد مؤقت ويفتح عبر `OpenFilex` للمشاركة) بلا أي كود جوّالي إضافي - كان مبنيًا أصلًا بتجريد Web/Android منذ تطبيق CBA Advising القديم.
- **لوحة الإدارة - تبويب "الإرشاد" الفرعي مُنجز بالكامل الآن (2026-08-23)**: نفس بطاقات "مؤشرات رئيسية لحالات الإرشاد" الأربع بالرئيسية + بطاقة "الكل" الخامسة (مناسبة هنا كصفحة تفصيل)، وجدولا "التوزيع حسب القسم العلمي" (طلاب/طالبات، عمودا تابعين لمرشد/على غير مرشدهم لكل قسم) - عبر دالة جديدة `AdvisingOverviewStatsService.loadDepartmentBreakdown()` (تجميع `classifyAllColleges` حسب قسم-شطر، بلا تكرار حساب). لا يزال خارج النطاق عمدًا: قوائم أسماء الطلاب، تقرير النصاب، إحصائيات المرشدين بالاسم (14 تبويبًا بالموقع - تفصيل ثقيل غير مناسب لجوال).
- **لوحة الإدارة - جدول "الإنجاز حسب القسم العلمي" أُعيد تصميمه (2026-08-23)**: كان قائمة واحدة مختلطة (شطرين معًا) بتسمية طويلة "قسم X - شطر Y" تُقصّ على عرض جوال ضيق - سليمان: "شكله غير احترافي". أصبح جدولين منفصلين (شطر الطلاب/شطر الطالبات) بعنوان واضح لكل جدول، وتسمية القسم مختصرة (`الإدارة`، `المحاسبة`...) بحذف بادئة "قسم" (`_DepartmentShatrTable`)، وترتيب "حالة الإجراءات" أصبح قبل "الإنجاز حسب القسم العلمي" بطلب سليمان.
- **شريط التقدّم (`MobileStatusRow`) متدرّج تلقائيًا حسب النسبة (2026-08-23)**: أحمر (منخفض) ← ذهبي (منتصف) ← أخضر داكن (100%)، ما لم يُمرَّر لون صريح - بطلب سليمان صراحةً، بلا لون جديد خارج هوية `AppColors` الثلاثة.
- **بطاقة `MobileKpiCard` أعيد تصميمها بتخطيط أفقي مضغوط + خلفية أيقونة صلبة (2026-08-23)**: سليمان: "لا تظهر عندي ملوّنة وحجمها كبير جدًا" - كانت تكديسًا رأسيًا بخلفية أيقونة شفافة باهتة؛ أصبحت رقم+تسمية على اليسار وأيقونة مربّعة بخلفية لونية صلبة على اليمين، أقصر بكثير عموديًا.
- **تبويب "التنبيهات" استُبدل بـ"الجداول الدراسية" (بطلب سليمان صراحةً 2026-08-23)**: تبويبان فرعيان (`DefaultTabController`) بنفس نمط "لوحة الإدارة": (١) **"المقررات الدراسية"** - فلتر شطر (`SegmentedButton`) + قسم علمي (`DropdownButtonFormField`) أعلى الصفحة، بطاقة لكل شعبة. (٢) **"الجدول الدراسي - عضو هيئة التدريس"** - فلتر شطر + بحث بالاسم، ثم **جدول** (`Table` حقيقي لا بطاقات) بعمودي المقرر/اليوم/الوقت فقط (بطلب سليمان: "عامل مساعد... يظهر الاسم فقط" لا كل التفاصيل)، اسم المحاضر يظهر مرة واحدة أعلى الجدول لا مكرَّرًا بكل صف. نفس بيانات `CourseScheduleRepository`/`CourseCatalog` (تحديد القسم من كود المقرر) المستخدَمة بالموقع - بلا تكرار حساب. **عمود "القاعة" غير متوفر حاليًا** (غير موجود إطلاقًا ببيانات الملف المرفوع - `DocxScheduleParserService` لا يقرأه بعد) - سيُضاف لاحقًا مع ملف حويّة جديد يتضمنه، مسجَّل بـTODO.md.
- **"المزيد" - تغيير كلمة المرور + بطاقة الدعم الفني + شعار ثابت بكل الشاشات (2026-08-23)**: "تغيير كلمة المرور" أُضيفت بإعادة استخدام مباشرة لـ`showChangePasswordDialog` الموجودة أصلًا بالموقع (`change_password_dialog.dart`) بلا أي تعديل - تعمل بنفس آلية Firebase Auth (تحقّق كلمة المرور الحالية أولًا). سليمان طلب أيضًا "الوصول التشغيلي" لكن اتضح أنها أداة اختبار خاصة بحساب `salfawaz` فقط تفتح صفحات سطح المكتب الكاملة (لا تصلح للجوال، غير موجودة أصلًا بمعمارية `mobile_portal`) - **تم تجاهلها بتأكيد سليمان صراحةً**. بطاقة "الدعم الفني" (تدرّج أخضر، اسم سليمان بالذهبي، بريد قابل للنقر) بنفس تصميم `_UnitLeaderCard` بصفحة "تواصل" حرفيًا. الشعار الرسمي النظيف أصبح ثابتًا بأعلى يمين كل شاشات التطبيق الخمس عبر مكوّن مشترك واحد (`PortalAppBarLogo`) بدل تكراره.
- **تفريغ بيانات اختبار من قاعدة البيانات الحية بطلب سليمان الصريح (2026-08-23)**: حُذفت 3 مجموعات Firestore بالكامل (كانت بيانات تجريبية): `tickets` (حالات الحذف والإضافة، 40 مستند)، `advisingAllCollegesReports`+`advisingAllCollegesReportsPrevious` (الإرشاد الكامل، شطرين + مجموعات chunks الفرعية)، `courseSchedules` (المقررات الدراسية، شطرين) - عبر سكريبت جديد `tool/admin_cli/clear_selected_data_2026_08_23.js` (نفس آلية مصادقة Firebase CLI المستخدَمة بـ`clear_test_data.js` الموجود أصلاً). لم تُمسّ أي مجموعة أخرى (منسوبو الكلية/ذوو الإعاقة/جداول المواعيد/الحالات الخاصة). الموقع والتطبيق سيعرضان أصفارًا لهذه الفئات الثلاث حتى يرفع سليمان بيانات حقيقية جديدة.
- **خلل حقيقي مُصحَّح: ازدواج عدّ الشعبة النظري/العملي (2026-08-24)**: سليمان رصد بلقطة شاشة حقيقية من "خدمات أكاديمية → أعلى الأعباء التدريسية" أن نفس الشعبة (نظري+عملي معًا) كانت تُحتسَب مرتين منفصلتين لنفس المحاضر بدل مرة واحدة (`_computeTopLoad` بـ`academic_services_hub_screen.dart` كانت تزيد العدّاد لكل من `instructorName`/`practicalInstructorName` بشكل مستقل بلا دمج). صُحِّح ببناء مجموعة أسماء فريدة لكل شعبة قبل العدّ - محاضر واحد لنظري وعملي نفس الشعبة يُحسَب له شعبة واحدة الآن، لا شعبتين. **نفس المشكلة الجذرية اكتُشفت أولًا بتطبيق "بوابة الإرشاد" الجوّالة** (بطاقة المقرر كانت تعرض نظري وعملي كأنهما شعبتان منفصلتان بالكامل - صُحِّحت بعرضهما معًا داخل بطاقة واحدة بصفَّين).
- **إصلاحان بعد الاختبار الحي الأول (سليمان بنى APK فعليًا وثبَّته على جواله)**: (أ) شاشة الدخول كانت تطلب "بريدًا إلكترونيًا" خامًا فيفشل الدخول بحساب `salfawaz` برسالة "بريد غير صحيح" - صُحِّحت لحقل "رقم المنسوب" الذي يُحوَّل تلقائيًا لبريد النظام الداخلي (`<رقم المنسوب>@${PortalAccounts.domain}`) بنفس آلية `staff_number_login_screen.dart` بالموقع بالضبط (تشمل `salfawaz` نفسه). (ب) الشعار كان أيقونة التطبيق المربَّعة (`app_icon.png`) بدل الشعار الرسمي النظيف المستخدَم بشاشة دخول الموقع (`unit_logo_clean_transparent_cropped.png`) - صُحِّح. كما أُضيف اسم سليمان لنص "جميع الحقوق محفوظة" مطابقًا لنص الموقع بالضبط.

**اجتاز الاختبار الحي الأول بنجاح (2026-08-23)**: بناء APK فعلي (`flutter build apk --target=lib/main_advising_portal.dart --flavor advisingPortal --release`)، تثبيت على جوال سليمان، وتسجيل دخول ناجح بحساب `salfawaz` بعد الإصلاحين أعلاه. التفاصيل الكاملة والمراحل التالية بـTODO.md.

### ترتيب جانبي للوحة الإدارة + فلتر "أعلى الأعباء التدريسية" (2026-08-23)

- **لوحة الإدارة**: "متابعة سير العمل" (3 بطاقات) و"حالات تجاوزت مستوى المعالجة دون إجراء" أصبحا جنبًا إلى جنب (يمين/يسار) بدل التكديس الرأسي، بطلب سليمان صراحةً (بمخطط توضيحي: "كل المحتويات بصفحة واحدة بلا تمرير") - كلا القسمين له ارتفاع مضغوط ثابت أصلًا فتناسبا أفقيًا بلا إشكال. بهذه المناسبة أُزيلت أيضًا **بيانات وهمية كانت منشورة فعليًا بالخطأ** ("أ. نموذج تجريبي"/"د. نموذج تجريبي 2" - fallback مخصَّص للمعاينة المحلية فقط، نُسي إزالته قبل نشر سابق).
- **خدمات أكاديمية → نظرة عامة**: أُضيف فلتر (الكل/الشطر/القسم) لقسم "أعلى الأعباء التدريسية" الجديد (سليمان: "اجعل فيها فلتر لكي يظهر أعلى الشعب حسب الشطر والقسم") - يعتمد مطابقة اسم المحاضر بملف منسوبي الكلية (`CollegeRosterRepository`) لتحديد قسمه، بنفس شرائح الفلتر الموحَّدة (`FilterResetChip`/`FilterPillDropdown`) المستخدَمة بكل الموقع. كما وُسِّع أقصى عرض حاوية الصفحة (1400→1600) ليطابق عرض لوحة الإدارة.

تحقَّق `flutter analyze` بلا أخطاء جديدة، وتحقَّق حيًّا عبر Playwright أن الترتيب الجانبي يعمل صحيحًا (كل المحتوى بصفحة واحدة بلا تمرير على 1920×1080)، البيانات الوهمية اختفت فعليًا، والفلتر الجديد يعمل بالهوية الصحيحة. **نُشر على الموقع الرسمي بموافقة سليمان الصريحة (2026-08-23)**.

### 4 ملاحظات تصميم بعد المراجعة الحية (2026-08-23)

سليمان راجع الموقع المنشور حديثًا وأرسل 4 ملاحظات متتابعة، عولجت جميعها:

1. **بطاقات "متابعة سير العمل" أصبحت أطول من السابق**: التصميم الأصلي (4 حالات قبل حذف "تنفيذ جزئي") كان شبكة 2×2 (صفّان)، لكن التحويل لعمود رأسي بثلاث حالات جعل البطاقة 3 صفوف - أطول رغم قلة الحالات - ما دفع قسم "حالات تجاوزت مستوى المعالجة" خارج الشاشة. أُصلح بإرجاعها لشبكة صفّين (2+1): "تم التنفيذ" و"لم يُعمَل عليه بعد" جنبًا لجنب بالصف الأول، و"تعذّر التنفيذ / تم التصعيد" وحدها بالصف الثاني - نفس ارتفاع الصفّين الأصلي، والقسم التالي ظاهر بلا تمرير إضافي على 1920×1080.
2. **قسم "الحالات والمتابعة" بصفحة "نظرة عامة" للإرشاد كان مقطوعًا نصفيًا** أسفل الشاشة (لا يظهر كاملًا لا بأول الصفحة ولا بعد التمرير). أُصلح بضغط الفراغات بين أقسام الصفحة (26px→18px بين الأقسام، 12px→10px بعد كل عنوان، 8px→6px حول تفريع ذوي الإعاقة) - القسم أصبح ظاهرًا بالكامل ضمن 1080px بلا تمرير.
3. **صفحة "نظرة عامة" للخدمات الأكاديمية كانت فارغة بصريًا** (4 بطاقات مؤشرات فقط ثم فراغ أبيض كبير حتى الفوتر) رغم أهمية الصفحة. بدل حل تجميلي بحت، أُضيف محتوى فعلي مفيد: قسم "شعب تحتاج تسكين" (قائمة شعب بلا محاضر مُسنَد، برابط مباشر لصفحة التسكين) وقسم "أعلى الأعباء التدريسية" (أعلى 5 أعضاء هيئة تدريس بعدد الشعب المُسنَدة)، معروضان جنبًا إلى جنب على الشاشات الواسعة. الفراغ تقلَّص بشكل كبير والصفحة أصبحت تبدو ممتلئة ومهنية.
4. **عمود "الاسم" بجدولَي المنسوبين (هيئة التدريس والإداريين) لم يكن متوسطًا** كبقية الأعمدة (كان `TextAlign.right` صريحًا بتعريف العمود وبنص الخلية). أُزيل التحويل الصريح في كلا الموضعين، فأصبح يتّبع الافتراضي `TextAlign.center` مثل عمود "المنصب" تمامًا.

تحقَّق `flutter analyze` بلا أي أخطاء جديدة على الملفات الأربعة، وتحقَّقت البنود 1-3 حيًّا عبر Playwright بلا أخطاء console أو خلل بصري. **نُشر على الموقع الرسمي بموافقة سليمان الصريحة (2026-08-23)**.

### تطبيق هوية DashTable على جدولَي "دليل المقررات" (2026-08-23)

جدول "تسكين الشعب الرئيسي" وجدول مواد خارج الكلية بـ`course_schedule_admin_screen.dart` حُوِّلا من `DataTable` القياسي لهوية `DashTable`/`DashTableCard` الموحَّدة (نفس الأسلوب المتَّبع بجدولَي منسوبي الكلية)، عبر دالة مشتركة جديدة `_scheduleDashTable` تبني كلا الجدولين بلا تكرار كود. سليمان اعتمد صراحةً (بعد معاينة HTML يدوية) تصميمين جديدين: عمود "أعلى حد" أصبح شريط تقدّم ملوَّن (نسبة المسجلين/أعلى حد - أخضر ≤50%، برتقالي بين 50% و100%، أحمر عند الاكتمال/التجاوز)، وعمود "النشاط" أصبح شارة ملوَّنة (`DashBadgeCell`: نظري=أخضر، عملي=ذهبي) بدل نص عادي. عمود "المسجلين" بقي رقمًا عاديًا كما هو. منطق "الخلية المقسومة" الحالي (شعبة عملية مرتبطة بنفس صف النظري، مقسومَين بخط فاصل عبر `_splitCell`) بقي محفوظًا بالكامل داخل كل دالة خلية.

جدول "المحاضر" (`_instructorTable`) - الذي استحسنه سليمان بصريًا كما هو - لم يُغيَّر داخليًا إطلاقًا (يبقى `DataTable` برأس أخضر والخلية المقسومة)، فقط بطاقته المحيطة استُبدلت بـ`DashTableCard` (حدود/ظل موحَّد بدل الحد الذهبي المستقل)، وحدود شارة "مجموع الساعات المعتمدة" أسفله وُحِّدت مع `DashTokens.border`.

**نُشر على الموقع الرسمي واعتمده سليمان صراحةً (2026-08-23)**. متبقٍ تعميم نفس الهوية على `_buildSectionsListCard`، `_buildQuotaReportCard`، و`advising_cases_admin_screen.dart`/`advisor_students_lookup_screen.dart` - مسجَّل بـTODO.md.

### إصلاح تراكب الفوتر بلوحة الإدارة + إعادة هيكلة "متابعة سير العمل" لثلاث حالات فقط (2026-08-22/23)

**إصلاح تراكب الفوتر**: تحقَّق حيًّا عبر Playwright (تسجيل دخول فعلي بحساب `salfawaz`) أن الصف الأخير بقسم "توزيع حالات الإرشاد حسب الحالة" ("تحتاج متابعة مستمرة") يظهر كاملًا 100% بلا أي تغطية من الفوتر الأخضر الداكن، بهامش أبيض طبيعي قبله - على المقاسات الثلاثة (1920×1080، 1366×768، 1280×720) وفي وضعي التبديل معًا ("مؤشرات الإرشاد" و"مؤشرات الحذف والإضافة"). لم يتغيّر أي كود متعلق بالفوتر نفسه أو `PortalScaffold` - الإصلاح السابق (2026-08-22، انظر أسفل) كان كافيًا فعليًا، هذه فقط جولة تحقّق مُصحَّحة (السكريبت السابق فشل تقنيًا بإحداثيات دخول خاطئة، فأعطى نتيجة مضلِّلة سابقًا).

**إعادة هيكلة "متابعة سير العمل" (`admin_executive_dashboard_screen.dart`, `report_data_service.dart`)**: بطلب سليمان الصريح - كل صف بملف Excel المرشد = إجراء/حالة مستقلة كاملة، فـ"تنفيذ جزئي" لا معنى له على مستوى الإجراء الواحد (كان يظهر سابقًا فقط بتجميع كل إجراءات الطالب معًا عبر `TicketAdvisorOutcome`/`ticketOutcomeForField` على مستوى "التذكرة" - هذا التجميع أُلغي كليًا لهذه الميزة). استُبدل بتصنيف مستقل لكل إجراء عبر `_ActionOutcome` جديد بثلاث حالات فقط: `complete` (تم التنفيذ، أخضر)، `escalated` (تعذّر التنفيذ / تم التصعيد، ذهبي - **لا** أحمر ولا يُسمَّى "مرفوض بالكامل")، `notStarted` (لم يُعمَل عليه بعد، رمادي). البطاقات الثلاث (المرشدون الأكاديميون/منسّقو الأقسام العلمية/منسّقو الكلية) تعرض الآن 3 صفوف بدل 4 بنفس حجم البطاقة المضغوط. مؤشر "طلاب تخطّى المرشد حالاتهم" أُعيدت تسميته "حالات تجاوزت مستوى المرشد دون إجراء" وأصبح يعدّ إجراءات/حالات مستقلة لا طلابًا فريدين. حُذفت `TicketAdvisorOutcome`/`ticketOutcomeForField`/`ticketAdvisorOutcome` من `report_data_service.dart` بعد التأكد أنها لم تعد مستخدَمة بأي مكان آخر بالمشروع - نظام `StatusCounts` المنفصل (يغذّي تصدير Excel/PDF ولوحة إحصاءات التذاكر، ويخزّن "جزئي" كنص حرفي) **لم يُمس إطلاقًا**، خارج نطاق هذا العمل عمدًا (مرحلة ثانية منفصلة مسجَّلة بـTODO.md).

تحقَّق `flutter analyze` بلا أي خطأ جديد (56 مشكلة، أقل من الأساس السابق 58 بعد حذف الكود الميت)، وتحقَّق حيًّا عبر Playwright أن البطاقات الثلاث تعرض حالاتها الصحيحة بالألوان الصحيحة على المقاسات الثلاثة وبلا أي خلل بصري (تراكب/قص/التفاف). **نُشر على الموقع الرسمي واعتمده سليمان صراحةً (2026-08-23)**.

### مكوّن جدول عام بهوية "أفضل 5 مرشدين إنجازًا" - أول تطبيق على جدول منسوبي الكلية (2026-08-22)

استُخرجت الهوية البصرية لبطاقة "أفضل 5 مرشدين إنجازًا" (`ticket_action_stats_panel.dart`: بطاقة بيضاء بحدود خفيفة وظل لطيف، رؤوس أعمدة قابلة للفرز، شرائط تقدّم ملوَّنة داخل الخلايا بدل أرقام مجرَّدة، تمييز الصف الأول بنجمة ذهبية) وحُوِّلت لمكوّن عام قابل لإعادة الاستخدام: `lib/theme/dashboard_table.dart` (`DashTableColumn`، `DashTable` - رسم يدوي Row/Container بدل `DataTable` القياسي لأنه لا يدعم شرائط تقدّم/بطاقات داخل الخلايا، مع واجهة استدعاء شبيهة بـDataTable عبر `onSort`؛ `DashTableCard` - الحاوية البيضاء المحيطة؛ `DashProgressCell`/`DashBadgeCell` - خلايا جاهزة لشريط تقدّم أو شارة ملوَّنة). يبني فوق `DashTokens` الموحَّد الموجود أصلًا (`dashboard_tokens.dart`).

طُبِّق أولاً على `college_roster_admin_screen.dart` (جدولا "منسوبي الكلية": أعضاء هيئة التدريس بفرزه الخماسي الكامل، والإداريون البسيط بلا فرز) بديلاً عن `DataTable` السابق، بلا أي مساس بالمنطق: نفس مفاتيح الفرز (`_sortKey`/`_onSort`)، نفس فلاتر البحث/القسم/الشطر/النوع، ونفس ألوان شارات عبء الإرشاد (أحمر=معفى، بنفسجي=حالات خاصة فقط، أزرق=إرشاد جزئي، أخضر=إرشاد كامل) وشارة العبء الدراسي. بقية جداول الموقع (متابعة حالات الإرشاد، بحث عن مرشد، النصاب التدريسي...) لا تزال تستخدم `DataTable` القياسي - تعميمها مسجَّل بـTODO.md. لم يُنشَر - تعديل كود محلي بانتظار مراجعة سليمان [[feedback_no_deploy_until_all_notes]].

### فحص حي كامل لـ"لوحة الإرشاد" بعد إصلاح خلل AdvisingSubNavigation (2026-08-22)

بعد إصلاح خلل `color: DashTokens.cardBg` (كان يُمرَّر منفصلًا عن `decoration` في `lib/web_portal/advising_workspace.dart` فيرمي استثناءً يمنع رسم الهيدر/الشريط الفرعي بالكامل)، شُغِّل خادم Flutter محليًا (`flutter run -d web-server --web-port 8765`) واختُبِرت 9 حالات حية عبر Playwright (3 مقاسات × 3 صفحات: متابعة الحالات، فترات الإرشاد، بحث عن مرشد) بحساب `salfawaz` الحقيقي.

**النتيجة: PortalScaffold/AdvisingSubNavigation نفسه سليم تمامًا الآن** - الإصلاح السابق نجح، الهيدر والشريط الفرعي يُرسَمان ويتنقّلان بشكل صحيح على كل المقاسات الثلاثة (1920×1080، 1366×768، 390×844) بلا استثناء واحد يتعلق بهما.

**لكن اكتُشفت 3 أعطال حقيقية أخرى، منفصلة تمامًا، داخل صفحات المحتوى نفسها (لا PortalScaffold)**:

1. **"متابعة الحالات" (`AdvisingCasesAdminScreen`) فارغة بالكامل على كل المقاسات المكتبية** (1920×1080 و1366×768) - استثناءات حقيقية بالـconsole: `SliverGeometry is not valid: The "layoutExtent" exceeds the "paintExtent"` و`Unexpected null value`. الهيدر والشريط الفرعي يظهران بشكل سليم، لكن منطقة المحتوى بيضاء تمامًا بلا أي عنصر - لم تُشخَّص السطر الدقيق المسبب (الملف 1465 سطرًا، يحتوي `SliverPersistentHeader`/`SliverToBoxAdapter` متعددة بمنطق فرز/تبويبات معقّد).
2. **خلل عرض بسيط (1px overflow) في `AdvisingMetricCard` بصفحة "نظرة عامة"** (`advising_hub_screen.dart` عبر `advising_workspace.dart:319`) - يظهر على كل المقاسات، شريط أصفر/أسود "BOTTOM OVERFLOWED BY 1.00 PIXELS" أسفل كل بطاقة مؤشر. `mainAxisExtent: 115` بالـ`GridView` لا يكفي بمقدار بكسل واحد لمحتوى `Column` الداخلي (`mainAxisAlignment: spaceBetween`).
3. **عطل حرج على الجوال (390×844) في نفس صفحة "نظرة عامة"**: استثناء `RenderFlex children have non-zero flex but incoming height constraints are unbounded` عند تحويل `_buildCasesSummary` بـ`advising_hub_screen.dart` (أسطر 350-374) من `Row` إلى `Column` بالوضع الضيق (`isNarrow = constraints.maxWidth < 700`) - على الأرجح لأن `_CaseSummaryCard` يحتوي `Expanded` داخليًا يفترض سياق `Row` محدود العرض، فيفشل حين يُوضَع بعمود غير محدود الارتفاع. **الأثر خطير**: هذا العطل يُسقِط أيضًا رسم أول 3 تبويبات بالشريط الفرعي (نظرة عامة، متابعة الحالات، بحث عن مرشد) على الجوال تحديدًا - فقط "الدعم النفسي والاجتماعي"، "الظروف الخاصة"، "فترات الإرشاد" تظهر وتُنقَر، ما يجعل الوصول لصفحتَي "متابعة الحالات" و"بحث عن مرشد" **مستحيلًا عبر الواجهة على الجوال إطلاقًا** حاليًا.

**النتائج التفصيلية للحالات التسع (قبل الإصلاح)**:
| الصفحة | 1920×1080 | 1366×768 | 390×844 |
|---|---|---|---|
| متابعة الحالات | FAIL (محتوى فارغ) | FAIL (محتوى فارغ) | FAIL (تبويبها غير قابل للنقر أصلًا) |
| فترات الإرشاد | PASS | PASS | لم يُختبَر (تعذّر الوصول بسبب عطل نظرة عامة الحاجب) |
| بحث عن مرشد | PASS | PASS (رسائل console متبقية من الصفحة السابقة، لا عطل فعلي بصريًا) | FAIL (تبويبها غير قابل للنقر أصلًا) |

**تحديث (2026-08-22، لاحقًا بنفس اليوم) - الأعطال الثلاثة أُصلحت وتحقَّقت حيًّا:**

1. **"متابعة الحالات" الفارغة**: السبب الفعلي المؤكَّد كان تعديلًا لي بنفس الجلسة (شريط فلاتر مثبَّت عبر `SliverPersistentHeader` داخل `CustomScrollView`، غير مُختبَر حيًّا وقتها) - **تراجعتُ عنه بالكامل** وأعدت بنية `Column`+`Expanded(ListView)` البسيطة المستقرة. تحقَّق حيًّا: الصفحة تعرض جدولًا حقيقيًا (8741 طالبًا) بلا استثناءات.
2. **تجاوز 1px بـ`AdvisingMetricCard`**: رُفع `mainAxisExtent` من 115 إلى 119 بـ`advising_hub_screen.dart`. تحقَّق حيًّا: لا نص OVERFLOWED ظاهر.
3. **انهيار الجوال (390px) بصفحة "نظرة عامة"**: السبب الفعلي كان `Expanded` داخل `_buildFairDistribution` (`advising_hub_screen.dart`) يُستخدَم أيضًا داخل `Column` بالوضع الضيق (بلا ارتفاع مُقيَّد من `SingleChildScrollView` المحيط) - أُزيل الاستخدام غير الصحيح واستُبدل بـ`Column(stretch)` بلا حاجة لـ`Expanded` إطلاقًا بالوضع الضيق. تحقَّق حيًّا: الصفحة تُرسَم كاملة بالتبويبات الستة على 390px.

**إصلاح رابع طارئ غير متعلق بالثلاثة أعلاه**: تجاوز 1px في `BrandPanel` (شعار صفحة الدخول المصغَّر بوضع الجوال، `staff_number_login_screen.dart`) - رُفع الارتفاع الثابت من 170 إلى 182. `flutter analyze` نظيف على كل الملفات الأربعة المعدَّلة؛ التحقق الحي على هذا الإصلاح تحديدًا أعطى نتيجتين متضاربتين (PASS/FAIL) عبر تشغيلين مختلفين، على الأرجح بسبب عدم التقاط أحد الفحصين لإعادة التجميع الكاملة بعد التعديل - لا خطورة منطقية فعلية بما أن التعديل رقمي بحت (زيادة مساحة لعنصر كان يفيض بمقدار بكسل واحد فقط) والتحذير المرئي أصلًا حصري لوضع Debug ولا يظهر بنسخة الإنتاج المنشورة.

**الخلاصة النهائية: `PortalScaffold` سليم تمامًا - لم يحتَج أي تعديل بتاتًا.** كل الأعطال كانت في صفحات المحتوى، واثنان منها كانا من تعديلات اليوم نفسها (غير منشورة سابقًا فلا خطر رجعي على المستخدمين).

### نص نطاق الفلتر ببطاقات المؤشرات + تصحيح مصطلح "حالة" بدل "ملف" (2026-08-21)

تحسينان دقيقان على قسم "الإغلاق الإداري النهائي" (`admin_executive_dashboard_screen.dart`):

- كل بطاقة مؤشر (طلبات تحتاج إجراء/طلاب تخطّى المرشد حالاتهم/نسبة الإغلاق
  النهائي) تعرض الآن نص نطاق الفلتر الحالي صراحةً في نهاية وصفها (مثال:
  "- قسم التسويق - شطر الطالبات" أو "- كل الأقسام والشطرين" لو لا فلتر) -
  حتى لا يبقى أي رقم بلا سياق واضح لمن يقرأه (سليمان: "إذا كان الفلتر عام
  يظهر للكل أو حسب القسم وهكذا").
- صُحِّح مصطلح "ملف/ملفاتهم" إلى "حالة/حالاتهم" في كل نصوص بطاقة "طلاب
  تخطّى المرشد حالاتهم" وقسم "من لم يعمل على حالاته" - "حالة" هو المصطلح
  الموحَّد المستخدَم بكل الصفحة (فلتر "الحالة"، "طلبات تحتاج إجراء"...)،
  و"ملف" لم يكن مصطلحًا مستخدَمًا بأي مكان آخر.

### إعادة تعريف "من لم يعمل على حالاته" + حذف مفهوم "التأخر الزمني" كليًا (2026-08-21)

توضيح جوهري من سليمان بعد سلسلة أمثلة عملية: **منسّق الكلية جهة تنفيذية
بصلاحيات كاملة (فتح/إغلاق شعب حسب توفر القاعات وأعضاء هيئة التدريس) - لا
يعمل يوميًا بل يجمع الحالات وينفّذها دفعة واحدة يومَي الأربعاء والخميس، وبمجرد
وصول الحالة إليه فهي "منجزة" من منظور إدارة الوحدة بصرف النظر متى ينفّذها**.
دوره الفعلي كإدارة وحدة: "متابعة الحالات التي لم يتم عليها أي إجراء إطلاقًا"
- بالاسم للمرشد، بمستوى القسم لمنسّق القسم - لا "من تأخّر بالوقت".

هذا ألغى مفهوم "طلبات متأخرة" (24 ساعة من التنزيل) بالكامل باعتباره غير
موضوعي (`admin_executive_dashboard_screen.dart`):

- **بطاقة KPI جديدة "طلاب تخطّى المرشد ملفاتهم"** بدل "طلبات متأخرة" - تُحسب
  من `_advisorSkippedTicket`: طالب لم يعمل عليه المرشد إطلاقًا (كل إجراءاته
  فارغة عند عمود المرشد) لكن أُنجز شيء منها لاحقًا عند منسّق القسم/الكلية.
- **قسم جديد "من لم يعمل على حالاته"** يستبدل "تحتاج تدخل إدارة الوحدة"
  القديم كليًا (كان مبنيًا على معيار الإعاقة/التخرج، ثم على "التأخر"، وكلاهما
  غير صحيح) - قائمتان: **مرشدون بالاسم** (تخطّتهم حالات وأُنجزت فوقهم،
  مجمَّعة حسب القسم/الشطر) و**أقسام تخطّى منسّقوها حالات** (بمستوى قسم-شطر
  فقط - لا يوجد حقل اسم منسّق قسم بالتذكرة حاليًا). منسّق الكلية غير مُتابَع
  بهذا القسم إطلاقًا وبشكل متعمَّد.
- كل هذا مربوط بنفس الفلتر الموحَّد أعلى الصفحة (شطر/قسم/حالة) - يدعم مباشرة
  طلب سليمان "أستطيع أرفع تقرير حسب الشطر والقسم للجهات العليا".
- تبسيط جذري لـ`_DashboardData`: حُذفت كل حقول/دوال حساب "التأخر" المعتمدة
  على توقيت التنزيل (`_classifyAction`، `_MutableStats`، `_DeptShatrStats`،
  `_DepartmentPerf`، الاتصال بـ`StageDownloadService`) لأنها لم تعد
  مستخدَمة بعد إلغاء المفهوم كليًا - الصنف أصبح يحمل قائمة التذاكر الخام فقط.

**مؤجَّل بالكامل (موثَّق في TODO.md):** تقرير "بحسب اليوم" (يوم 1/2/3: منجز/
محوَّل/لم يُعمل عليه) - يحتاج تسجيل توقيت فعلي لحظة كل تغيير حالة (غير متوفر
بقاعدة البيانات حاليًا، تُخزَّن الحالة الأخيرة فقط بلا تاريخ).

### فلتر واحد أعلى الصفحة يتحكم بكل شيء + إزالة قسم "النشاطات" الوهمي (2026-08-21)

استكمالًا لسلسلة تعديلات لوحة الإدارة اليوم، طلب سليمان (مع رسم توضيحي
بالرسّام) إعادة ترتيب الصفحة بحيث فلتر واحد فقط أعلى كل شيء يتحكم بالمؤشرات
+ حالة الإنجاز حسب نوع الإجراء + متابعة سير العمل معًا، بدل فلتر داخل كل قسم:

- **فلتر موحَّد جديد** (`_FilterableDashboardContent`) أعلى الصفحة مباشرة
  (تحت "البيانات حيّة")، بترتيب واضح يمينًا-ليسارًا: شريحة "الكل" (إعادة
  تعيين عامة، منفصلة عن الشطر) ← قائمة "الشطر" (كل الشطرين/شطر الطلاب/شطر
  الطالبات) ← قائمة الأقسام العلمية ← قائمة "الحالة" (الكل/الإعاقة/الخريجين).
  حُوِّلت شرائح الشطر من Chips نصية لقائمة منسدلة بنفس أسلوب الأقسام/الحالة
  للاتساق البصري.
- بطاقات المؤشرات الثلاث ("نسبة الإغلاق النهائي"/"طلبات متأخرة"/"طلبات تحتاج
  إجراء") أصبحت **قابلة للفلترة أيضًا** الآن (`_computeKpiStats`) - لم تكن
  كذلك سابقًا رغم إضافة الفلتر لبقية الأقسام.
- "نسبة الإنجاز" أُعيدت تسميتها "نسبة الإغلاق النهائي" (توضيحًا أنها تقيس
  الإغلاق الكامل عبر كل المستويات، لا نفس مقياس بطاقات "متابعة سير العمل").
- قسم "حالة الإنجاز حسب نوع الإجراء" أصبح قسمًا مستقلًا بحاويته الخاصة أعلى
  "متابعة سير العمل" مباشرة (لا مدمجًا داخلها كما كان بمحاولة سابقة اليوم).
- **حُذف المحتوى الوهمي (Mock) من قسم "التنبيهات/آخر النشاطات"** أسفل
  الصفحة - كان يعرض 5 عناصر ثابتة بالكود تتناقض بصريًا مع صندوق "تحتاج تدخل
  إدارة الوحدة" المجاور له (يعرض "0" حقيقية الآن). استُبدل بحالة "قيد
  التطوير" صريحة لحين توفّر سجل حركات لحظي حقيقي.

### حذف "حالات مصعدة لإدارة الوحدة" + توسيع فلتر متابعة سير العمل (شطر/قسم/حالة) ليشمل قسم أنواع الإجراءات (2026-08-21)

توضيح جوهري من سليمان: **منسّق الكلية جهة تنفيذية بصلاحيات كاملة (فتح/إغلاق
شعب) ويحتاج مرونة قرار عبر الأسبوع كامل - لا مهلة يومية ثابتة له**. وبالتالي
معيار "حالات مصعدة لإدارة الوحدة" السابق (ذوو إعاقة/متوقع تخرج بصرف النظر عن
العمل الفعلي) كان خاطئًا تمامًا - لا يوجد مؤشر زمني موضوعي لتدخل الوحدة على
مستوى منسّق الكلية أصلًا. التنفيذ (`admin_executive_dashboard_screen.dart`):

- حُذفت بطاقة "حالات مصعدة لإدارة الوحدة" من شريط المؤشرات العلوي (أصبح 3
  بطاقات بدل 4: طلبات تحتاج إجراء/طلبات متأخرة/نسبة الإنجاز).
- "تحتاج تدخل إدارة الوحدة" أسفل الصفحة أصبحت تعكس فقط "متأخرة" الحقيقية
  (تجاوزت مهلة المرشد أو منسّق القسم 24 ساعة) - إدارة الوحدة تراقب مستوى
  منسّق الكلية بنفسها عبر بطاقة "منسّقو الكلية" (خانة "لم يُعمَل عليه بعد")
  بلا مؤشر تلقائي جامد.
- فلتر "متابعة سير العمل" وُسِّع بخيار ثالث **"الحالة"** (الكل/الخريجون/ذوو
  الإعاقة) بجانب الشطر والقسم الموجودين - نفس آلية عمل الفلترة محليًا على
  قائمة التذاكر بلا استعلام Firestore إضافي.
- قسم "حالة الإنجاز حسب نوع الإجراء" (إضافة/حذف/تعديل) رُفع ليصبح **أسفل
  البطاقات الثلاث مباشرة داخل نفس حاوية "متابعة سير العمل"**، يشترك بنفس
  الفلتر الموحَّد الثلاثي بلا أي فلتر منفصل خاص به - استخراج منطق التصنيف
  الزمني-المستقل (`_stageOf`) من `_classifyAction` جعل إعادة حسابه لأي مجموعة
  تذاكر مُفلترة (`_computeActionTypeStats`) ممكنًا بلا الحاجة لسجلات تنزيل.

### فلتر شطر/قسم موحَّد لبطاقات "متابعة سير العمل" + حذف جدول "أداء الأقسام" القديم المكرِّر (2026-08-21)

بطلب سليمان صراحةً بعد رؤية البطاقات الثلاث الجديدة: "أريد إعادة باقي تصميم
الصفحة بنفس الفكرة، فلاتر تبيّن نفس الإحصائيات حسب الشطر والقسم، وتُطبَّق
على منسّقي الأقسام ومنسّقي الكلية أيضًا". التنفيذ (`admin_executive_dashboard_screen.dart`):

- فلتر واحد (شطر + قسم) أعلى قسم "متابعة سير العمل" مباشرة يتحكم بالبطاقات
  الثلاث معًا دفعة واحدة - اختيار "شطر الطالبات + قسم التسويق" يحدّث فورًا
  أرقام المرشدين ومنسّقي القسم ومنسّقي الكلية الثلاثة لتعكس هذا الاختيار فقط.
  الفلترة تحدث محليًا على قائمة التذاكر (`_computeRoleProgress`) بلا أي
  استعلام إضافي لـFirestore.
- حُذف قسم "أداء الأقسام العلمية" القديم بالكامل (كان يعرض نسبة إنجاز/متأخر
  بمقياس مختلف تمامًا عن التصنيف الرباعي الجديد، وأصبح مكرِّرًا ومربكًا بعد
  إضافة البطاقات الثلاث) - `departmentPerf`/`_DeptShatrStats`/`_DepartmentPerf`
  بقيت فقط لحساب إجماليات KPI الرئيسية (لا تُعرَض كجدول منفصل بعد الآن).
- قسما "حالة الإنجاز حسب نوع الإجراء" و"تحتاج تدخل إدارة الوحدة" بقيا بلا
  تغيير - موضوعهما مختلف (نوع الطلب/حالات عاجلة) لا صلة له بالتصنيف الجديد.

### إعادة تصميم ملف المرشد وإضافة متابعة سير عمل حقيقية بلوحة الإدارة (2026-08-21، فرع preview فقط - بانتظار اختبار سليمان)

بعد نقاش مطوَّل مع سليمان حول كيفية اختيار المرشد لحالة الإنجاز، أُعيد تصميم
ملف الإكسل الذي يستلمه كل مرشد (`AdvisorZipService.buildZip`) وربط النتائج
بلوحة الإدارة:

- عمود "حالة الإنجاز من قبل المرشد الأكاديمي": قائمة منسدلة حقيقية بخيارين
  فقط (تم التنفيذ / لم يتم التنفيذ) بدل نص حر يقارَن حرفيًا.
- عمود "ملاحظات المرشد الأكاديمي": قائمة منسدلة إلزامية بـ11 سببًا شائعًا
  لعدم التنفيذ (الشعبة مكتملة، متطلب سابق، تعارض جدول...) تنتهي بـ"سبب آخر"
  - القائمة موضوعة بورقة مرجعية مخفية (لا نصًّا مباشرًا بالصيغة) لأنها تتجاوز
  حد إكسل (~255 حرفًا) لقوائم Data Validation المكتوبة كنص حرفي.
- عمود جديد "يرجى كتابة السبب الآخر": نص حر، يُستخدم فقط عند اختيار "سبب آخر".
- صف تعليمات مدمَج أعلى الملف يشرح الخطوتين للمرشد.
- أثناء الاختبار الفعلي (توليد الملف وفتحه بإكسل) وُجِد وأُصلِح خللان حقيقيان
  كانا سيُفسدان أي ملف مُولَّد فعليًا: (1) عنصر `<drawing>` يتيم بملف الشيت
  كانت مكتبة `excel` تُصدره دومًا حتى بلا صور فعلية، يجعل إكسل يعرض "مشكلة في
  المحتوى" عند الفتح - أُصلِح في `ExcelProtectionService.protect` (`lib/services/excel_protection_service.dart`).
  (2) القوائم الطويلة (كأسباب عدم التنفيذ) تتجاوز حد الطول المسموح داخل صيغة
  `dataValidation` مباشرة - نُقلت لورقة مرجعية مخفية مع مرجع نطاق بدل نص حرفي
  (نفس أسلوب `AdvisingScheduleExcelService`).
- **تصنيف "تنفيذ جزئي" لا يظهر أبدًا كخيار يدوي بالملف** - هو حساب تلقائي على
  مستوى الطالب (لا الإجراء المفرد) في `report_data_service.dart`
  (`ticketOutcomeForField`): لو نُفِّذ بعض إجراءات الطالب ورُفض بعضها، يُصنَّف
  طلبه إجمالًا "جزئي".
- **لوحة الإدارة** (`admin_executive_dashboard_screen.dart`): قسم "متابعة سير
  العمل" أُعيد تصميمه بالكامل من شريط 6 مراحل بأرقام مجردة إلى ثلاث بطاقات
  دور واضحة (المرشدون الأكاديميون / منسّقو الأقسام العلمية / منسّقو الكلية)،
  كل بطاقة تعرض دائمًا 4 أرقام حقيقية بالألوان: منفَّذ بالكامل (أخضر)، تنفيذ
  جزئي (ذهبي)، مرفوض بالكامل (أحمر)، لم يُعمَل عليه بعد (رمادي) - بطلب سليمان
  صراحةً ليتابع تقدّم كل مستوى فعليًا بلا تعقيد أو حاجة للنقر.
- تنبيه عند رفع الملف المُعالَج (كل نقاط الرفع: بوابة الويب والتطبيق المحمول):
  أي حالة اختار فيها المرشد "لم يتم التنفيذ" بلا تحديد سبب تظهر كتنبيه برتقالي
  بدل أن تمر بصمت (`MergeResult.missingReasonCount`).

### تكبير خط بطاقات المؤشرات التشغيلية الثانوية (2026-08-21)

سليمان لاحظ أن خط بطاقات "مؤشرات تشغيلية" (الـ9 بطاقات الصغيرة أسفل
المؤشرات الأساسية بشاشة "متابعة حالات الإرشاد") صغير جدًا. كُبِّر خط
الرقم لنفس حجم البطاقات الأساسية (15)، خط التسمية من 9 إلى 10.5، وصندوق
الأيقونة من 24 إلى 26 مع تكبير ارتفاع البطاقة (56 ← 64) لاستيعاب الخط
الأكبر بلا قصّ - البطاقات الأساسية لم تتغيّر.

### تصحيح عاجل: محاذاة التنقّل الداخلي وتوازن الشبكات بلوحة الإرشاد (2026-08-21)

بعد نشر توحيد لوحة الإرشاد، رصد سليمان خللاً بصريًا حقيقيًا: شريط التنقّل
الداخلي الجديد كان يمتدّ بعرض الشاشة الكامل ومحاذًى بلا حاوية عرض مطابقة
لعرض المحتوى أسفله (كان كل قسم بعرضه الخاص - 1400 بلوحة الإرشاد الرئيسية،
1200 بالظروف الخاصة/الدعم النفسي، 1100 ببحث المرشد، وبلا أي حاوية إطلاقًا
بشاشة "متابعة حالات الإرشاد")، فبدا الشريط "منزاحًا" عن المحتوى، وظهرت
بطاقة إحصاء ثانوية معزولة بسطر ثالث بشاشة "متابعة حالات الإرشاد" بسبب
اعتماد `Wrap` بعرض بطاقة ثابت على عرض الحاوية الفعلي بدل عدد أعمدة محسوب.

**الإصلاح الجذري** (`lib/web_portal/advising_workspace.dart`):
- ثابت واحد جديد `kAdvisingWorkspaceMaxWidth = 1400` يُستخدَم الآن بكل
  الشاشات الست (شريط التنقّل الداخلي + هيدر الصفحة + المؤشرات + الفلاتر +
  الجداول) بدل عرض مختلف بكل قسم - "نظام عرض واحد" كما طلب سليمان صراحةً.
- شريط التنقّل الداخلي (`AdvisingSubNavigation`) أصبح مُمركزًا فعليًا داخل
  `Center` + `ConstrainedBox(maxWidth: kAdvisingWorkspaceMaxWidth)` (بدل
  تمرير أفقي حرّ بلا مركز)، والتبويبات تلتفّ (`Wrap`) لسطر ثانٍ على الشاشات
  الضيقة بدل تمرير أفقي - أبسط وأثبت مع 6 تسميات قصيرة.
- شاشة "متابعة حالات الإرشاد": أُضيفت حاوية `Center`+`ConstrainedBox` كانت
  غائبة كليًا (المحتوى كان يمتدّ حافة-لحافة)، فصارت محاذاة الهيدر/المؤشرات/
  الفلاتر/الجدول متطابقة مع شريط التنقّل أعلاها تمامًا.
- شبكة المؤشرات (أساسية + ثانوية) استُبدلت من `Wrap` بعرض بطاقة ثابت إلى
  `GridView` بعدد أعمدة محسوب يبقى دومًا قاسمًا لعدد البطاقات (5 أساسية:
  5/3/2/1 عمودًا حسب العرض؛ 9 ثانوية: 3 أو 1 عمودًا فقط - قاسمان لـ9 فلا
  تظهر بطاقة يتيمة بمعزل عن بقية الصفوف مهما كان عرض الشاشة).
- بقية الشاشات (الظروف الخاصة/الدعم النفسي/بحث عن مرشد/فترات الإرشاد)
  حُوِّلت لنفس الثابت الموحَّد بدل قيمها المتفرّقة السابقة.

فُحص `flutter analyze` (نظيف) ثم `flutter build web --release` و
`firebase deploy --only hosting` - منشور الآن.

### توحيد لوحة الإرشاد كمساحة عمل واحدة - المرحلة الأولى (2026-08-21)

بناءً على مواصفة تفصيلية من سليمان (56 بندًا: "GLOBAL HEADER = LOCKED،
ADVISING WORKSPACE = وحدة واحدة")، بدأ توحيد الشاشات الست ("نظرة عامة"،
"متابعة حالات الإرشاد"، "بحث عن مرشد"، "الظروف الخاصة"، "الدعم النفسي
والاجتماعي"، "توزيع فترات الإرشاد") كمساحة عمل واحدة، بدل صفحات منفصلة
بهويات متضاربة (المركزية `DashTokens`، الخمس الفرعية `AppColors` قديم).

**نظام تصميم مشترك جديد** `lib/web_portal/advising_workspace.dart` (بلا
مساس بالهيدر العام/الشعار/تبويب "لوحة الإرشاد" العلوي - مقفلة تمامًا حسب
طلب سليمان الصريح):
- `AdvisingSubNavigation`: شريط تنقّل داخلي ثابت (6 تبويبات) يظهر بكل
  الشاشات الست تحت الهيدر العام مباشرة - تبديل بـ`pushReplacement` (لا يكدّس
  شاشات)، تبويبا "متابعة الحالات"/"بحث عن مرشد" يظهران فقط لحساب المدير
  العام (نفس القيد الأصلي)، تمرير أفقي على الجوال.
- `AdvisingPageHeader`/`AdvisingBreadcrumb`: فتات تنقّل + عنوان + وصف قصير
  موحَّد أعلى كل شاشة.
- `AdvisingMetricCard`: بطاقة KPI يهيمن فيها الرقم (خط 32) بدل بطاقة كبيرة
  برقم صغير.
- `AdvisingEmptyState`: حالة فارغة موحَّدة (أيقونة + عنوان + شرح) بدل جملة
  رمادية معزولة.
- `AdvisingStatusChip`: شارة حالة دلالية اللون لحالات `HardshipStatus`.
- `AdvisingCaseCard`: بطاقة حالة موحَّدة (بعرض محكوم 1200px بدل تمدّد كامل
  الشاشة) لكل من "الظروف الخاصة"/"الدعم النفسي" - كانتا مكرَّرتين حرفيًا.
- `showAdvisingTimelineDialog`: نافذة "مسار المتابعة" أصبحت جدولاً زمنيًا
  حقيقيًا (نقاط متصلة بخط، آخر حدث مميَّز بحلقة ذهبية) بدل قائمة نصية بسيطة
  - تعرض فقط الحقول المتوفرة فعليًا بـ`HardshipHistoryEntry` (الحالة/
  الملاحظات/مَن حدَّثها/متى)؛ حقلا "الجهة/الدور" بالمواصفة غير مخزَّنين حاليًا
  بقاعدة البيانات فلم يُضافا وهميًا.

**تطبيق فعلي على الشاشات الست**:
- `advising_hub_screen.dart` ("نظرة عامة"): إعادة بناء كاملة - 5 بطاقات KPI
  متساوية (بدل 4 + دائرة تغطية منفصلة كبيرة؛ التغطية أصبحت بطاقة سادسة
  عادية)، ألوان دلالية (طلبة بلا مرشد=0 → أخضر، >0 → أحمر؛ تغطية 100% →
  أخضر، أقل → ذهبي)، شبكة خدمات سريعة تعرض 5 بطاقات بصفّ واحد على الشاشات
  الواسعة (بدل 4+1 معزولة)، حُذفت دائرة `fl_chart` والحسابات الخاصة بها بعد
  استبدالها ببطاقة عادية.
- الخمس الفرعية: أُضيف التنقّل الداخلي + هيدر الصفحة الموحَّد لكل منها.
  "الظروف الخاصة"/"الدعم النفسي" استبدلتا كليًا بالودجت المشتركة الجديدة.
  "متابعة حالات الإرشاد": استُبدل التحذير البرتقالي الصارخ لحد الـ300 صف
  بشريط معلومات محايد، والحالة الفارغة بـ`AdvisingEmptyState`. "بحث عن
  مرشد": أُضيفت حالة بداية احترافية ("ابدأ بالبحث عن مرشد") بدل شاشة شبه
  فارغة قبل الكتابة، وحالة "لا يوجد مرشد مطابق" منفصلة عن حالة عدم وجود
  بيانات إطلاقًا.
- **أُصلح خلل حقيقي بالتنقّل**: شاشة "توزيع فترات الإرشاد" كانت تمرر
  `current: 'tools'` لتبويبات الهيدر العلوي (قيمة لا تطابق أي شرط)، فتبويب
  "لوحة الإرشاد" العلوي لا يظهر مميَّزًا نشطًا عند فتحها - صُحِّح لـ
  `'advising-hub'`.

### توحيد لوحة الإرشاد - المرحلة الثانية + نشر (2026-08-21)

بطلب سليمان الصريح "طبق على جميع صفحات لوحة الإرشاد وانشر"، استُكمل الجزء
الأكبر المتبقي من المواصفة ونُشر العمل فعليًا:

- **"متابعة حالات الإرشاد"**: فُصلت الـ14 بطاقة إحصاء بصريًا لـ"مؤشرات
  أساسية" (أول 5 تبويبات بترتيب سليمان نفسه، حجم أكبر) و"مؤشرات تشغيلية"
  (الباقي، حجم أصغر) بدل حائط بطاقات متساوية الوزن، مع تمييز واضح للبطاقة
  المطابقة للتبويب المفتوح حاليًا (لم يكن هناك أي مؤشر اختيار من قبل).
  استُبدل القصّ الصامت لأول 300 صف بترقيم صفحات حقيقي ("عرض 1-100 من N" +
  أزرار سابق/تالي، 100 صف بكل صفحة) - يصفَّر تلقائيًا عند تبديل التبويب أو
  أي فلتر؛ التصدير (Excel/PDF) بلا تغيير، يبقى بكل السجلات دومًا بلا حد.
- **"توزيع فترات الإرشاد"**: شرائح المرشدين وُحِّدت بودجت مشترك جديد
  `AdvisingAdvisorChip` (ارتفاع/نصف قطر ثابتان) بدل `Chip` القياسي متفاوت
  الحجم، والحالتان الفارغتان تحوَّلتا لـ`AdvisingEmptyState` الموحَّد.
- **متبقٍ صغير** (انظر TODO.md): إعادة تصميم الجدول/الفلاتر الداخلية لـ
  "متابعة حالات الإرشاد" و"بحث عن مرشد" بهوية `DashTokens` بالكامل (لا تزال
  تستخدم `DataTable`/`AppColors` القديم للجدول نفسه لا الإطار المحيط به)،
  واختبار Responsive حي (لا يوجد متصفح بهذه البيئة).

**فُحص الكود قبل النشر**: `flutter analyze` على كل الملفات الثمانية
المعدَّلة (لوحة الإرشاد + مركز التقارير) أعاد نتيجة نظيفة (تحذير أسلوبي
واحد فقط، أُصلح)، ثم `flutter build web --release --pwa-strategy=none` نجح
بلا أخطاء، ونُشر فعليًا عبر `firebase deploy --only hosting` بطلب سليمان
الصريح ("طبق على جميع صفحات لوحة الإرشاد وانشر") - منشور الآن على
https://sss-advising-tu.web.app.

### إعادة تصميم مركز التقارير بجودة احترافية (2026-08-21)

بناءً على مواصفة تفصيلية من سليمان (46 بندًا)، رُفعت جودة `reports_hub_screen.dart`
("مركز التقارير") دون تغيير البنية الأساسية المعتمَدة (بحث ↓ فلترة رئيسية ↓
فلترة فرعية ↓ شبكة بطاقات) ودون تكبير البطاقات أو الخطوط:

- **بحث محسَّن**: عنوان توضيحي مبسّط + نص مساعد بأمثلة، تطبيع عربي بسيط
  (إزالة تشكيل، توحيد الألف/الهمزة/الياء/التاء المربوطة) عبر `_normalizeArabic`،
  البحث يغطي الآن العنوان والوصف والتصنيف والنوع الفرعي + كلمات مفتاحية إضافية
  (مثال: "مرشد" يطابق بطاقات تذكر "المرشد الأكاديمي"/"المرشدين"). تأخير خفيف
  (200ms) بدل تحديث فوري بكل ضغطة.
- **فصل بصري بين مستويي الفلتر**: تسميتان صغيرتان "التصنيف"/"نوع المحتوى"
  فوق كل صفّ، ووزن بصري مختلف (فلاتر التصنيف أثقل قليلًا: ارتفاع/خط أكبر) عبر
  ودجت جديدة `_FilterGroup`/`_FilterChip` محلية للملف (لم تُعدَّل الودجت
  المشتركة `DashActionCard`/`DashSectionHeader` بـ`dashboard_tokens.dart` حتى
  لا يتأثر `advising_hub_screen.dart`/`academic_services_hub_screen.dart`).
- **حالة الاختيار غير معتمدة على اللون فقط**: خلفية + حدّ + وزن خط + علامة ✓.
- **عدّاد نتائج** ("N نتيجة"/"نتيجة واحدة") + **ملخّص فلاتر نشطة** + زر
  "مسح الفلاتر" يظهر فقط عند وجود بحث/فلتر فعلي.
- **بطاقات أوضح**: تباين حدّ/ظل أعلى قليلًا، البطاقة كاملةً `InkWell` واحد
  (كانت كذلك أصلًا عبر `DashActionCard`)، أُضيف تأثير hover خفيف (رفع 2px +
  ظل أقوى + حدّ ملوَّن) وسهم اتجاه صغير يوضّح أنها تفتح شيئًا، مع حلقة تركيز
  ظاهرة للوحة المفاتيح.
- **شبكة استجابة صريحة عبر `LayoutBuilder`**: 4 أعمدة (≥1400)، 3 (≥1000)،
  2 (≥640)، عمود واحد أقل من ذلك - بدل الاعتماد الضمني على
  `maxCrossAxisExtent` فقط.
- **حالة عدم وجود نتائج محسَّنة**: أيقونة + رسالة + سبب مقترح + زر مسح الفلاتر
  بدل نص وحيد بلا سياق.
- لم يُغيَّر: منطق فتح أي تقرير، مصدر البيانات (لا يزال قائمة ثابتة بالكود لا
  API)، ولا GoRouter/query params (المشروع لا يستخدم GoRouter إطلاقًا - أُبقي
  خارج النطاق حتى لا يزعزع الاستقرار).
- **لم يُختبَر بصريًا حيًّا بعد** (لا توجد أداة `flutter` بهذه البيئة) ولم
  يُنشَر - بانتظار مراجعة سليمان [[feedback_no_deploy_until_all_notes]].

### توحيد الهوية البصرية (DashTokens) - جارٍ (2026-08-20)

سليمان لاحظ أن "لوحة الإرشاد" و"تقارير" و"خدمات أكاديمية" لا تطابق هوية
"الحذف والإضافة" (بطاقات ملوَّنة بالكامل بدل بطاقات بيضاء + فراغات بيضاء
كبيرة أسفل الصفحة) وطلب توحيد الهوية فعليًا بكل الموقع، لا صفحة بصفحة.

- اكتُشف أن ألوان "الحذف والإضافة" (`_Tokens` بـ`ticket_action_stats_panel.dart`)
  كانت موثَّقة بالكود عمدًا كنظام **مستقل** عن `AppColors` العام - القرار
  الآن: توحيدها كهوية واحدة فعلية.
- أُنشئ `lib/theme/dashboard_tokens.dart` (`DashTokens`/`DashKpiCard`/
  `DashCardShell`/`DashActionCard`/`DashSectionHeader`) كمرجع مشترك قابل
  لإعادة الاستخدام، بدل تكرار القيم بكل ملف.
- طُبِّق على `advising_hub_screen.dart` (+ دائرة "التغطية الإرشادية" الجديدة
  بنفس أسلوب دائرة الإنجاز بلوحة الحذف والإضافة)، `academic_services_hub_screen.dart`،
  و`reports_hub_screen.dart`.
- أُصلح أثناء العمل خلل حقيقي: حذف قائمة "عرض" المنسدلة بلوحة "متابعة حالات
  الإرشاد" (طلب سابق بنفس الجلسة) كان قد قطع الوصول عن 4 تبويبات كاملة
  ("تقرير نصاب الإرشاد"، "خطة إعادة التوزيع العادل"، "إحصائيات الإرشاد"،
  "حركات الإرشاد") - أُضيفت لها بطاقات إحصاء بنفس هوية بقية التبويبات
  (أصبحت 14 بطاقة بدل 10).
- **متبقٍ (انظر TODO.md)**: صفحة المنسوبين وتفريعاتها، بوابات المنسّقين
  الثلاث، حالات الظروف الخاصة/الدعم النفسي، وبقية صفحات الموقع.
- **لم يُنشَر بعد** - العمل مستمر على فرع `preview/forms-import-fix` بانتظار
  جمع كل ملاحظات سليمان قبل النشر.

### ربط لوحة الإدارة الرئيسية ببيانات Firestore حقيقية (2026-08-20)

بعد اكتمال اختبار دورة الرفع الشاملة، طلب سليمان ربط لوحة الإدارة (كانت
Mock Data معتمدة UI-Frozen منذ 2026-08-19) ببيانات حقيقية - **بلا أي تغيير
بصري/هيكلي** عن التصميم المعتمد، فقط استبدال مصدر الأرقام:

- **عائقان تقنيان حقيقيان اكتُشفا وعُرضا على سليمان قبل أي كود** (بدل
  الافتراض): (أ) لا يوجد أي تاريخ/وقت مسجَّل بأي تذكرة إطلاقًا - فلا يمكن
  حساب "التأخر" بلا نقطة بداية زمنية معروفة. (ب) قسم "آخر النشاطات" يحتاج
  سجل حركات لحظي (من فعل ماذا ومتى) لا يحتفظ به النظام - قاعدة البيانات
  تخزّن الحالة الأخيرة فقط لا تاريخ التغييرات.
- سليمان أوضح القاعدة الفعلية: **العدّ يبدأ من لحظة تنزيل الملف** (لا رفع
  الفورمز) - مهلة يوم واحد للمرشد من تنزيله هو، ويوم إضافي لمنسّق القسم من
  تنزيله هو، ومنسّق الكلية **بلا مهلة إطلاقًا** ("ليس له وقت ولا تأخير").
- **`StageDownloadService` جديد** (`lib/services/stage_download_service.dart`)
  يسجّل أول تنزيل فقط (Idempotent) لكل مرحلة/قسم/شطر بمجموعة Firestore
  جديدة `stage_downloads` - مربوط من نقطتَي التنزيل الحقيقيتين معًا: شاشة
  المنسّق الفعلية (`coordinator_workspace_screen.dart`) والمسار الاحتياطي
  الشامل بصفحة "رفع وتنزيل الملفات". أُضيفت قواعد أمان جديدة بـ`firestore.rules`
  (نمط مطابق لـ`stage_snapshots` الموجود أصلاً).
- **"آخر النشاطات" تبقى Mock مؤقتًا بموافقة سليمان صراحةً** (بعد توضيح
  السبب بعدة محاولات) - كل بقية أقسام اللوحة (المؤشرات الأربعة، متابعة سير
  العمل 6 مراحل، أداء الأقسام العلمية، حالة الإنجاز حسب النوع، تحتاج تدخل
  إدارة الوحدة) مربوطة الآن ببيانات حقيقية عبر `_DashboardData.compute()`
  الجديدة (تصنّف كل إجراء حسب `advisor_status`/`coordinator_status`/
  `college_status` - القيمة المرجعية `'تم الإنجاز'`، وتحسب التأخر بمقارنة
  `stage_downloads` بالوقت الحالي).
- "حالات مصعدة لإدارة الوحدة" تُحتسَب من ذوي الإعاقة/المتوقع تخرجهم
  (تعريف تقريبي واقعي - لا يوجد حقل "تصعيد" صريح بعد بنموذج البيانات).
- شريط "آخر تحديث" استُبدل بمؤشر "بيانات حيّة" بدل تاريخ ثابت وهمي (كان
  Mock صريحًا، لا معنى له بعد أن أصبحت الصفحة تُحدَّث لحظيًا فعليًا).

### إعادة هيكلة صفحة "رفع وتنزيل الملفات" إلى 3 أقسام + إصلاح خلل تخطيط + إحصائية مقررات (2026-08-20)

بطلب سليمان صراحةً: أُعيد بناء الصفحة لتحوي 3 أقسام واضحة (حذف/إضافة -
كما كانت، المقررات الدراسية، الإرشاد الأكاديمي) بدل الشبكة القديمة
4 بطاقات كبيرة:

- قسم "المقررات الدراسية" و"الإرشاد الأكاديمي" الآن شريطان أخضران بنفس
  هوية شريط "الملف الأساسي" - الإرشاد بلا زر رفع (عنوان فقط) وتحته 3
  صفوف Compact (حالات الإرشاد/ذوو الإعاقة/مواعيد الإرشاد) بنفس أسلوب
  صفوف قسم الحذف/الإضافة.
- **خلل تخطيط حرج اكتُشف أثناء المعاينة المحلية**: استخدام `IntrinsicHeight`
  لمساواة ارتفاع القسمين داخل `LayoutBuilder` تسبّب بانهيار الصفحة بالكامل
  (`LayoutBuilder does not support returning intrinsic dimensions`) لأن
  القسمين داخل `SingleChildScrollView` بارتفاع غير محدود. أُصلح باستبدال
  `IntrinsicHeight` بـ`Row` عادي مع محاذاة علوية.
- توحيد شكل المربعين (بطلب سليمان: "نفس الحجم والشكل والتصميم"): كلاهما
  الآن بطاقة بيضاء بحدّ ذهبي فاتح ونصف قطر موحَّد تحوي الشريط الأخضر
  بالأعلى.
- إضافة قيمة فعلية تحت شريط "المقررات الدراسية" (بطلب سليمان): إحصائية
  عدد شعب المقررات المرفوعة لكل شطر (الطلاب/الطالبات) بجانب بعضهما.
- حذف الكود الميت (`_uploadCard`/`_dropzone`/`_DashedBorderPainter`) فعليًا
  بعد استبداله بالتصميم الجديد - لا إعادة تسمية.
- **تصحيح لاحق بطلب سليمان**: كان `Expanded(flex: 4)`/`Expanded(flex: 6)`
  يجعل عرض مربع الإرشاد أكبر فعليًا من مربع المقررات رغم أن الشكل توحَّد -
  سليمان لاحظ الفرق بالعين مباشرة وسأل صراحةً. صُحِّح لـ`Expanded` بلا
  `flex` (عرض متساوٍ 1:1 تمامًا) وأُعيد النشر.
- **تصحيح ثانٍ (نفس اليوم)**: الارتفاع أيضًا لم يكن متساويًا (كل مربع
  يأخذ ارتفاعه الطبيعي حسب محتواه) - وسليمان أرسل لقطة شاشة حقيقية وسأل
  صراحةً. أُضيف `_EqualHeightRow` (ودجت جديد بأسفل الملف): يقيس الارتفاع
  الفعلي لكلا المربعين بعد الرسم عبر `GlobalKey`/`RenderBox` ثم يطبّق
  الأطول كحدّ أدنى (`minHeight`) للطرفين - بديل عن `IntrinsicHeight` الذي
  يتعارض مع `LayoutBuilder` المتداخل داخل `_greenBanner` (نفس الخلل
  المذكور أعلاه). أُضيفت أيضًا رموز تعبيرية (👨‍🎓/👩‍🎓) بجانب عدّاد كل
  شطر بطلب سليمان، وصُحِّح نص الشريط العلوي الكبير للصفحة (كان لا يزال
  "رفع ملفات الإرشاد الأكاديمي" رغم أن اسم الصفحة تغيّر لـ"رفع وتنزيل
  الملفات" منذ عدة جولات - سليمان لاحظ التناقض ونبَّه عليه صراحةً).
- **زر "رفع دفعي سريع" الشامل فُصِّل لثلاث بطاقات مستقلة بطلب سليمان**:
  كان زرّ واحد "ملفات المعالجة" يخدم مرحلتَي المرشدين ومنسّقي الأقسام معًا
  (الدمج فعليًا مستقل عن المرحلة - `mergeAllProcessedRows` يطابق كل صف
  بمفتاحه الخاص بغض النظر عن مصدره)، لكن سليمان فضَّل فصلهما فعليًا في
  الواجهة "حتى لا يلتبس الأمر" - أُضيفت دالة رفع مستقلة
  `_pickAndUploadCoordinatorProcessedFilesAll` (نفس منطق الدمج، حالة/تتبّع
  منفصلَين) وأصبحت 3 بطاقات متساوية الحجم (مرشدون/منسّقو أقسام/منسّق
  كلية)، كل بطاقة بشارة أيقونة ملوَّنة مميّزة (أخضر/ذهبي/أزرق) - إعادة
  تصميم من `_bulkStageCard` القديمة (أفقية بخلفية رمادية) لبطاقة عمودية
  بيضاء بحدّ ملوَّن وظل خفيف. حُذف الكود الميت الناتج (`_goldIconBadge`
  ومحاولة "مستطيل مقسوم نصفين" مؤقتة لم تُعتمَد) فعليًا.

### إصلاح خلل حرج: مطابقة حالات "حذف" كانت تفشل دائمًا منذ إنشاء الميزة (2026-08-20)

أثناء اختبار شامل لدورة الحذف/الإضافة كاملة (ملفات تجريبية مولَّدة لكل
المراحل - انظر `المرفقات/تجربة الموقع/`)، اكتُشف **خلل إنتاج حقيقي** لم يكن
معروفًا سابقًا:

- **`FirestoreTicketService._ticketActionSection`** كانت تستخدم
  `required_section ?? current_section ?? ''` لتحديد رقم الشعبة بمفتاح
  مطابقة كل إجراء (`uid|النوع|المقرر|الشعبة`). لكن `ExcelParserService`
  يكتب `required_section: ''` صراحةً (نص فارغ لا `null`) لكل إجراء **حذف** -
  وعامل `??` في Dart لا يتخطى النص الفارغ، يتخطى فقط `null`. النتيجة: كانت
  الدالة تُرجع `''` دائمًا لأي إجراء حذف بدل الرجوع فعليًا لـ`current_section`،
  فيفشل مفتاح المطابقة **دائمًا** مهما كانت بيانات الملف المرفوع صحيحة تمامًا.
  **أي رفع لملف معالج يحمل حالة "حذف مقرر" لم يكن يُطابَق منذ إنشاء هذه
  الميزة على الأرجح.** أُصلحت بفحص فراغ النص صراحةً بدل الاعتماد على `??`.
- **رفع "ملف الفورم الأساسي"** (`upload_hub_screen.dart` و`home_shell.dart`
  لتطبيق الجوال) كان بلا `try/catch` إطلاقًا - أي خطأ أثناء التحليل يوقف
  التنفيذ بصمت تام قبل إنشاء أي حالة بقاعدة البيانات وقبل أي رسالة، فيبدو
  الرفع "ناجحًا بصمت" بينما لم تُنشأ أي حالة فعليًا. أُضيفت معالجة أخطاء
  صريحة بحوار خطأ واضح.
- تحقّقتُ من الإصلاحين باستدعاء دوال الموقع الحقيقية مباشرة (سكربت Dart
  مؤقت يستدعي `ExcelParserService`/`ProcessedFileParserService` فعليًا، لا
  محاكاة) على ملفات اختبار حقيقية: تحسّنت النتيجة من 4/6 إلى 6/6 حالات
  متطابقة (تضمّنت حالتَي حذف) بعد الإصلاح.
- **ملفات اختبار شاملة جديدة**: `المرفقات/تجربة الموقع/` - 23 ملف Excel
  (ملف فورم أساسي + ملفات معالجة مرشدين/منسّقي أقسام/منسّق كلية) لاختبار
  دورة الحذف/الإضافة كاملة من البداية للنهاية، مع فهرس "اقرأني أولًا.md".
  وُلِّدت برمجيًا وتحقَّق منها فعليًا بكود الموقع الحقيقي (لا افتراضًا).

### اعتماد نهائي لتصميم لوحة الإدارة الرئيسية ونشره (2026-08-19)

بعد 10 جولات مراجعة متتالية (مفصّلة أدناه)، اعتمد سليمان تصميم لوحة الإدارة
**نهائيًا** وطلب النشر الفوري. تم:

- بناء نسخة الإنتاج (`flutter build web --release --pwa-strategy=none`)
  ونشرها على Firebase Hosting (`https://sss-advising-tu.web.app`).
- Commit للتغييرات على فرع `preview/forms-import-fix` (6 ملفات: الشاشة
  نفسها، `admin_nav.dart`، `portal_header.dart`، `test_switcher_screen.dart`،
  README.md، TODO.md).
- **التصميم الآن UI Frozen** - قاعدة ثابتة جديدة مسجَّلة بالذاكرة الدائمة
  (`feedback_ui_freeze_after_approval`): أي عمل قادم على هذه الصفحة يقتصر
  على استبدال بيانات Mock ببيانات Firestore حقيقية داخل نفس بنية الـWidgets
  المعتمدة حرفيًا - بلا أي تعديل بصري/هيكلي من تلقاء نفسي إلا بموافقة صريحة
  مسبقة. القاعدة تنطبق على كل صفحات المشروع مستقبلاً، لا لوحة الإدارة فقط.

### تنقيح لوحة الإدارة الرئيسية - الجولة العاشرة (رأس أعمدة موحَّد + لمسات أخيرة) (2026-08-19)

الجزء العلوي (المؤشرات + سير العمل) اعتُمد نهائيًا هذه الجولة ولم يُمَس؛
التركيز انتقل لمنتصف/أسفل الصفحة:

- **"أداء الأقسام العلمية" أصبح برأس أعمدة واحد** (`_DepartmentGridHeader`:
  الإجمالي/قيد المعالجة/متأخرة/مكتملة/نسبة الإنجاز) بدل تكرار المسميات
  الأربعة داخل كل صف من الأصناف الخمسة - كان يزيد الارتفاع بلا فائدة. كل صف
  الآن أرقام فقط محاذاة تحت رأس الأعمدة (نفس توزيع flex)، بارتفاع أدنى 42px
  بدل الصف المزدوج السابق. **ترتيب الأقسام العلمية نفسه لم يتغيّر** (تحقّقتُ
  أنه لا يزال يتبع ترتيب `_kDepartmentPerf` الثابت المتَّفق عليه، لا فرز حسب
  الأداء أو الاسم).
- **Badge "عدد الحالات" أصبح ملاصقًا لعنوان "تحتاج تدخل إدارة الوحدة"**
  مباشرة (بدل النظام العام `_SectionTitle.trailing` الذي كان يدفعه لأقصى
  الطرف الآخر من الحاوية) - عنوان مخصَّص لهذا القسم فقط بنفس شكل بقية
  العناوين + Badge بجانبه مباشرة.
- **دائرة Donut بقسم "حالة الإنجاز حسب نوع الإجراء" كُبِّرت ~15%** (34→39px)
  بلا زيادة ارتفاع البطاقة نفسها.
- تقليل بسيط إضافي (Padding رأسي 8→6) لحاوية "متابعة سير العمل" - آخر لمسة
  مطلوبة بهذا القسم.
- **لا تغيير على المؤشرات الأربعة العليا** ولا مسار سير العمل ولا الترتيب
  العام للصفحة - كلها معتمدة نهائيًا هذه الجولة.
- **بانتظار اعتماد سليمان النهائي**.

### تنقيح لوحة الإدارة الرئيسية - الجولة التاسعة (توازن Typography لسير العمل) (2026-08-19)

سليمان لاحظ أن خط "متابعة سير العمل" أصغر وأخفت من بقية اللوحة رغم رفع
عناوين الأقسام - طلب رفعًا بسيطًا إضافيًا **مع تقصير الحاوية لا تكبيرها**:

- اسم الدور بالمسار: 14→15px. عنوان القسم: 18→19px. النص التوضيحي: 10.5→12px
  (رمادي أفتح، يبقى Secondary). وصف المرحلة: 12→12.5px (رمادي أفتح قليلاً).
- **الحاوية أُنقصت لا كُبِّرت**: Padding رأسي 12→8، الفراغ بين العنوان
  والتوضيح والمسار مُقلَّص (4-4px بدل 6-8)، الحشو الداخلي لكل عقدة مسار
  خُفِّض (5→3px رأسيًا) - خط أوضح داخل حيّز أضيق.
- بطاقات المؤشرات الأربع العليا **لم تُمس إطلاقًا** بهذه الجولة (معتمدة).
- **بانتظار اعتماد سليمان النهائي**.

### تنقيح لوحة الإدارة الرئيسية - الجولة الثامنة (إصلاح Overflow + رفع الخطوط + هوية بصرية) (2026-08-19)

سليمان رصد خطأ Overflow ظاهر فعليًا (BOTTOM OVERFLOWED BY 4.0 PIXELS)
ناتجًا عن `height` ثابت مع خط كبّرته الجولة السابقة، وطلب صراحةً عدم حل
المشكلة بتصغير الخط بل برفع ارتفاع البطاقة، مع تقوية حضور الهوية البصرية في
أهم منطقة بالصفحة (المؤشرات + سير العمل) - **لا تزال اللوحة غير معتمدة**:

- **إصلاح Overflow نهائيًا**: بطاقات المؤشرات تستخدم الآن `BoxConstraints
  (minHeight: 86)` بدل `height` ثابت - يستحيل حدوث Overflow مهما زاد
  المحتوى، الحاوية تتمدد تلقائيًا عند الحاجة بدل قصّ النص.
- **رفع الخطوط للحدود التي طلبها سليمان صراحةً** (لا مزيد من التصغير):
  عنوان المؤشر 14px، الرقم الرئيسي 28px، الوصف 12px؛ عناوين الأقسام
  (`_SectionTitle` المشترك لكل أقسام اللوحة) من 14.5 إلى 18px؛ اسم الدور
  بمسار سير العمل 14px، الرقم 17px، وصف المرحلة 12px.
- **هوية بصرية محسوبة بدل الحياد الكامل**: خط ذهبي رفيع (3px) عند الحافة
  اليمنى لكل بطاقة مؤشر (بديل عن إطار ذهبي كامل حول البطاقة) - يعوّض غياب
  عنوان قسم فوق صف المؤشرات تحديدًا. مرحلتا "إدارة الوحدة" الأولى والأخيرة
  بمسار سير العمل تحملان خطًّا ذهبيًا صغيرًا مائزًا تحت الاسم (تمييز بداية/
  نهاية الدورة بلا تحويلهما لبطاقتين منفصلتين).
- تحقّقتُ من اتجاه الأسهم مجددًا (`Icons.arrow_back` = ← ثابت غير متأثر
  باتجاه النص) - صحيح فعليًا مع مسار يبدأ من أقصى اليمين.
- **لا تغيير بالمحتوى أو الترتيب أو مسار سير العمل** - تعديل بصري/Layout بحت.
- **بانتظار اعتماد سليمان النهائي**.

### تنقيح لوحة الإدارة الرئيسية - الجولة السابعة (إعادة ضبط Layout جذرية) (2026-08-19)

سليمان حدَّد المشكلة بدقة أكبر: لم تعد مسألة حجم Card، بل **توزيع المحتوى
داخل البطاقة نفسها** - أيقونة في طرف ونص في الطرف الآخر مع فراغ كبير بينهما
على الشاشات العريضة (4 أعمدة × ~390px لكل بطاقة). طلب معالجة Layout
Architecture لا تصغيرًا إضافيًا - **لا تزال اللوحة غير معتمدة**:

- **بطاقات المؤشرات الرئيسية**: المحتوى (أيقونة/حلقة + كتلة نص) أصبح
  يُمركَز كوحدة بصرية واحدة عبر `Center` بدل `Expanded` كان يمدّد كتلة النص
  لتلامس الحافة البعيدة تاركًا فراغًا كبيرًا على الحافة الأخرى. كتلة النص
  محدودة بعرض أقصى 230px (`ConstrainedBox`) بدل التمدد الحر. الحلقة/الأيقونة
  أصبحت بصرية بحتة بلا رقم مكرَّر بداخلها (كانت نسبة الإنجاز تظهر مرتين:
  داخل الحلقة وبكتلة النص) - الرقم يظهر مرة واحدة بحجم أكبر (24px) كأبرز
  عنصر في الهرمية البصرية.
- **متابعة سير العمل**: شريط المراحل (Process Strip) أصبح يُمركَز أفقيًا
  كوحدة واحدة (`ConstrainedBox(minWidth: عرض الحاوية)` + `Center`) بدل
  الالتصاق بحافة البداية وترك فراغ كبير على الحافة المقابلة في الشاشات
  العريضة - يبقى قابلاً للتمرير أفقيًا لو ضاقت الشاشة عن احتواء المسار كاملاً.
- تحقّقت من اتجاه الأسهم في RTL فعليًا: `Icons.arrow_back` رمز ثابت (←) لا
  يتأثر باتجاه النص (`matchTextDirection` غير مفعَّل لأيقونات Material
  الافتراضية)، وبما أن الشريط يبدأ من أقصى اليمين (أول عنصر في Row داخل
  Directionality.rtl)، فالسهم يقود العين فعليًا يمينًا→يسارًا بشكل صحيح.
- **لم يتغيّر أي محتوى**: نفس المؤشرات الأربعة، نفس مسار سير العمل الست
  مراحل، نفس الترتيب العام للصفحة - Refinement على مستوى Layout فقط.
- **بانتظار اعتماد سليمان النهائي**.

### تنقيح لوحة الإدارة الرئيسية - الجولة السادسة (اتساق حسابي للـMock Data) (2026-08-19)

سليمان اكتشف تعارضات حسابية حقيقية بين أقسام اللوحة رغم كون كل البيانات
وهمية: المؤشر الرئيسي كان يقول "47 من أصل 60" بينما مجموع "أداء الأقسام
العلمية" الفعلي كان 45 من 60 (75% لا 78%)، ومجموع "حالة الإنجاز حسب نوع
الإجراء" كان 65 طلبًا (لا 60) بـ32 مكتملًا (لا 47) - **حتى البيانات الوهمية
يجب أن تحكي نفس القصة رقميًا** - **لا تزال اللوحة غير معتمدة**:

- **`_kDepartmentPerf` أصبح مصدر Mock Data المركزي الوحيد** لإجمالي/مكتمل
  الطلبات - نُقل من داخل `_DepartmentPerformanceSectionState` إلى مستوى
  الملف، مع `_kTotalRequests`/`_kTotalCompleted` كدالتين مُشتقّتين تجمعان
  القيم من نفس القائمة مباشرة.
- **بطاقة "نسبة الإنجاز"** بالمؤشرات الرئيسية تقرأ الآن `$rate%`/`$completed
  من أصل $total طلبًا` من هاتين الدالتين بدل رقم منفصل مكتوب يدويًا - يستحيل
  الآن أن تتعارض مع مجموع أداء الأقسام العلمية لأنهما نفس المصدر حرفيًا.
- **أرقام "حالة الإنجاز حسب نوع الإجراء" أُعيد ضبطها يدويًا** لتطابق نفس
  الإجمالي (31+18+11=60) ونفس المكتمل (24+15+8=47) - نفس الـ60 طلبًا مصنَّفة
  بحسب القسم العلمي من جهة وبحسب نوع الإجراء من جهة أخرى.
- **"حالة تحتاج قرارًا إداريًا" → "حالة تحتاج قرارًا من إدارة الوحدة"** في
  كل الملف (حالتَي إدارة الوحدة بقسم "تحتاج تدخل إدارة الوحدة" + تبويب
  التنبيهات) - نفس منطق تجنّب لبس "الإدارة" مع "قسم الإدارة".
- **Tabs الشطر أصغر وعرضها طبيعي** (`MainAxisSize.min` بدل `Expanded` بعرض
  نصف الحاوية) - أقرب لـCompact Segmented Control حقيقي.
- تقليل بسيط إضافي لـPadding حاوية "متابعة سير العمل" الخارجية (6px بدل 8px
  رأسيًا) - Refinement أخير لا تصغير جديد.
- **جميع الأرقام الآن متسقة حسابيًا عبر اللوحة بالكامل**: 60 طلبًا إجماليًا،
  47 مكتملًا، 78% نسبة إنجاز - في المؤشرات الرئيسية، أداء الأقسام العلمية،
  وحالة الإنجاز حسب نوع الإجراء معًا. **بانتظار اعتماد سليمان النهائي**.

### تنقيح لوحة الإدارة الرئيسية - الجولة الخامسة (نمط عرض جديد + تصحيح اسم قسم) (2026-08-19)

بعد أن أكّد سليمان أن مشكلة الأحجام تكرّرت رغم التصغير التدريجي، طلب هذه
المرة **تغيير نمط العرض نفسه** لا مجرد تخفيضًا إضافيًا، مستخدمًا تصميمًا
قديمًا (شاشة "الحذف والإضافة" الحالية) كمرجع بصري فقط للحجم والتوازن -
**لا تزال اللوحة غير معتمدة**:

- **بطاقات المؤشرات الرئيسية**: أُعيد بناؤها كـCompact Metric Tile أفقي
  حقيقي (أيقونة/حلقة + عمود نص) بدل الأعمدة الثلاثة المكدَّسة رأسيًا -
  ارتفاع ثابت **76px** (كان 94px). أُزيلت الحدود الحمراء الكاملة حول بطاقة
  "حالات مصعدة لإدارة الوحدة" (كانت تحوّل البطاقة لصندوق تحذير كامل) -
  الإشارة الآن تقتصر على لون الأيقونة فقط.
- **متابعة سير العمل → Process Strip حقيقي**: لا Cards منفصلة بحدود وخلفيات
  إطلاقًا - عُقَد نصية مضغوطة ("الدور | العدد" + وصف صغير) داخل شريط أفقي
  واحد قابل للتمرير عند الضيق، بلا حدود أو خلفية لكل عقدة (Hover فقط عند
  التفاعل). أُضيفت ملاحظة صغيرة واحدة أسفل العنوان توضح أن الأرقام تمثّل
  "عدد الطلبات الموجودة حاليًا في كل مرحلة" بدل تكرار الشرح عند كل رقم.
- **تصحيح خطأ اسم قسم علمي حقيقي في كل الملف**: "قسم الاقتصاد والتمويل
  والتأمين" → **"قسم الاقتصاد والتمويل"** (لا تُضاف كلمة "التأمين" لاسم
  القسم - 5 أقسام علمية فقط: الإدارة، المحاسبة، التسويق، نظم المعلومات
  الإدارية، الاقتصاد والتمويل). لاحظت أن مصدرًا موحدًا لتوحيد أسماء الأقسام
  موجود أصلاً بالمشروع (`lib/data/academic_department_names.dart`) لأغراض
  مطابقة بيانات الطلاب/المرشدين - **لم يُستخدَم هنا بعد** لأن غرضه مطابقة لا
  عرض قوائم Mock؛ تعميم مصدر موحّد لعرض أسماء الأقسام (فلاتر/تقارير/لوحة
  الإدارة) يبقى تحسينًا مستقبليًا مسجَّلًا بـTODO.md.
- **"تحتاج تدخل إدارة الوحدة" → Compact Structured Grid**: عرض الأقسام
  العلمية داخل الشطر أصبح جدولًا منظَّمًا بعمود لكل تصنيف (عاجلة/متأخرة/
  مراجعة/الإجمالي) مع صف رؤوس أعمدة، بدل صف نصي حر باسم القسم في طرف وعدد
  الحالات في الطرف الآخر بفراغ كبير بينهما.
- **حالة الإنجاز حسب نوع الإجراء**: الوحدات الثلاث توزَّع الآن بالتساوي عبر
  `Expanded` بدل `Wrap` بعرض ثابت كان يجمّعها يسارًا ويترك فراغًا كبيرًا على
  الشاشات العريضة.
- **جميع البيانات لا تزال وهمية بالكامل**، والترتيب العام للأقسام ومسار سير
  العمل (6 مراحل) ثابتان بلا أي تغيير. **بانتظار اعتماد سليمان النهائي**.

### تنقيح لوحة الإدارة الرئيسية - الجولة الرابعة (حدود حجم صريحة) (2026-08-19)

سليمان أكّد أن ملاحظة تضخّم البطاقات تكررت عبر الجولات السابقة دون معالجة
كافية، وطلب هذه المرة **حدودًا رقمية صريحة** بدل "تصغير" عام - **لا تزال
اللوحة غير معتمدة**:

- **بطاقات المؤشرات الرئيسية**: ارتفاع ثابت **94px** (`Container(height: 94)`
  بدل الاعتماد على المحتوى) مع `MainAxisAlignment.spaceBetween` بدل فراغات
  يدوية بين الصفوف الثلاثة - يطابق الحد المطلوب (90-100px) حرفيًا.
- **بطاقات متابعة سير العمل**: ارتفاع ثابت **68px** (بدل `minHeight: 66`
  سابقًا) + Padding الحاوية الخارجية مخفَّض إلى 16×12 (بدل 14 على كل الجهات).
- **صياغة أرقام سير العمل**: "13 طلبات" كانت تحمل خطأ تصريف عربي (لا تصح
  لكل الأرقام) - استُبدلت بصيغة ثابتة "الطلبات: 13" لا تتأثر بتصريف العدد.
- نفس المبدأ طُبِّق على "تحتاج تدخل إدارة الوحدة": "3 حالة" → "عدد الحالات: 3"
  (على مستوى القسم العلمي وعلى شارة إجمالي الحالات بعنوان القسم).
- **أداء الأقسام العلمية**: أُضيف Hover خفيف (`hoverColor`) لصف القسم، مع
  إبقاء التصميم والفلتر (الكل/شطر الطلاب/شطر الطالبات) كما هما دون إعادة
  تصميم - طُلب Refinement بسيط لا صفريًا.
- **حالة الإنجاز حسب نوع الإجراء**: تقليل إضافي للـPadding والفراغ بين
  الدائرة والنص (Compact أكثر قليلاً، لا إعادة تصميم).
- **دمج "آخر تحديث للبيانات" مع "إدارة البيانات"** في سطر واحد متجاور (كانا
  في طرفَي الصف فيبدوان غير مرتبطين).
- **مسار سير العمل الست مراحل ثابت** (تحقّقتُ حرفيًا من كل تسمية مرحلة
  مطابقة لما اعتمده سليمان)، وترتيب الصفحة العام لم يتغيّر.
- **جميع البيانات لا تزال وهمية بالكامل**. **بانتظار اعتماد سليمان النهائي**.

### تنقيح لوحة الإدارة الرئيسية - الجولة الثالثة (Compact حقيقي) (2026-08-19)

بعد الجولة الثانية، راجع سليمان النسخة مرة أخرى وأكّد أن مشكلة تضخّم
الأحجام والفراغات لا تزال قائمة، وطلب Refinement دقيق (لا إعادة تصميم من
الصفر) على النسخة الحالية تحديدًا - **لا تزال اللوحة غير معتمدة**:

- **متابعة سير العمل**: الرقم بمفرده كان غير مفهوم (لا يُعرف إن كان تراكميًا
  أم حاليًا) - أُضيفت كلمة "طلبات" ملاصقة للرقم بكل بطاقة. البطاقات صُغِّرت
  أكثر (`minHeight` من 72 إلى 66، Padding من 8×6 إلى 6×8، أحجام الخط
  مخفَّضة). عنوان المرحلتين الأخيرتين صُحِّح إلى "المراجعة والاعتماد"
  (منسّق الكلية) و"المراجعة والإغلاق" (إدارة الوحدة) بدل "مراجعة نهائية"/
  "مكتمل" - المسار الإداري نفسه (6 مراحل) ثابت بلا تغيير.
- **أداء الأقسام العلمية**: فُصل شريط نسبة الإنجاز بصريًا عن أعمدة الأرقام
  (كان يمر تحتها فيخلق تشتتًا) - الآن كل صف قسم بخلفية بطاقة مستقلة، وشريط
  الإنجاز بصف خاص بتسمية "الإنجاز" + النسبة ملاصقة له، بعد خط تقسيم رفيع.
  الفلتر (الكل/شطر الطلاب/شطر الطالبات) وقابلية النقر لكل صف باقيان كما هما.
- **حالة الإنجاز حسب نوع الإجراء**: البطاقات كانت تتمدد بعرض الشاشة الكامل
  فيظهر فراغ كبير بين الدائرة والنص - أصبحت بعرض ثابت (300px) تتجمّع طبيعيًا
  عبر `Wrap` بدل التمدّد القسري، مع تصغير الحشو والدائرة والنص، وإضافة إجمالي
  كل نوع بعنوان البطاقة ("طلبات الإضافة — 35 طلبًا").
- **"تحتاج تدخل إدارة الوحدة" - إعادة هيكلة كبرى**: كان يعرض بطاقتَي شطر
  كبيرتين ثم فراغًا كبيرًا (المستوى الثاني لم يكن يظهر تلقائيًا). الآن:
  شطر الطلاب مُحدَّد افتراضيًا (Segmented Control صغير بدل بطاقتين ضخمتين)،
  والأقسام العلمية التابعة للشطر تظهر مباشرة أسفله كصفوف Compact واحدة
  (`قسم X — N حالة • تفاصيل` بارتفاع ~40px بدل بطاقات كبيرة)، والنقر على أي
  قسم ينقل لتفاصيل حالاته (المستوى الثالث) - أسماء الطلبة لا تظهر إلا بعد
  هذا المستوى.
- **النشاطات والتنبيهات → تبويبان مستقلّان**: "التنبيهات" (الافتراضي) |
  "آخر النشاطات" بدل قائمة مختلطة تخلط بين ما يحتاج انتباهًا وما هو سجل
  عادي. أُزيلت شارة "تنبيه" البعيدة عن النص (لم تعد لازمة - التبويب نفسه
  يفرّق النوعين). كل صف الآن سطر واحد مضغوط (أيقونة + نص + وقت) بارتفاع
  طبيعي غير مفروض عليه مساواة ارتفاع "تحتاج تدخل إدارة الوحدة" المجاور
  (توزيع 70/30 بينهما ثابت كما هو).
- **جميع البيانات لا تزال وهمية بالكامل (Mock Data)**، والترتيب العام
  للأقسام (المؤشرات ← سير العمل ← أداء الأقسام العلمية ← حالة الإنجاز حسب
  نوع الإجراء ← تحتاج تدخل إدارة الوحدة/النشاطات) ثابت بلا تغيير بطلب
  سليمان صراحةً. **بانتظار اعتماده النهائي**.

### تنقيح لوحة الإدارة الرئيسية - الجولة الثانية (2026-08-19)

بعد أول إعادة تصميم (أدناه)، راجع سليمان النسخة حيًّا وطلب تعديلات جوهرية
إضافية قبل الاعتماد النهائي - **لم تُعتمد اللوحة بعد**:

- **توحيد المصطلحات**: "حالات مصعدة للإدارة"→"حالات مصعدة لإدارة الوحدة"،
  "تحتاج تدخل الإدارة"→"تحتاج تدخل إدارة الوحدة"، "أداء الأقسام"→"أداء
  الأقسام العلمية"، وكل قسم علمي يُكتب بصيغة "قسم X" (قسم الإدارة/قسم
  المحاسبة/...) لتجنّب اللبس بين "إدارة الوحدة" و"القسم العلمي للإدارة".
  **يبقى مطلوبًا تعميم هذه المصطلحات على بقية شاشات النظام مستقبلاً** - هذا
  التعديل غطّى لوحة الإدارة فقط حتى الآن.
- **تكثيف الأحجام (Compact)**: تقليل Padding/ارتفاع كل البطاقات (KPI، سير
  العمل، أداء الأقسام، الحالات، النشاطات) بما يتناسب مع حجم المعلومات
  الفعلي بدل الاعتماد على بطاقات ضخمة لعرض رقم وعنوان فقط.
- **مسار سير العمل الحقيقي (6 مراحل لا 5)**: إدارة الوحدة (توزيع) → منسّق
  القسم العلمي (إحالة للمرشد) → المرشد الأكاديمي (تنفيذ) → منسّق القسم
  العلمي (مراجعة) → منسّق الكلية (مراجعة نهائية) → إدارة الوحدة (إغلاق) -
  بدل "جديد → المنسق → المرشد → الإدارة → مكتمل" غير الدقيق تشغيليًا. ألوان
  المراحل الآن تعبّر عن الحالة (محايد أثناء المعالجة، أخضر عند الإغلاق) لا
  عن الدور.
- **أداء الأقسام العلمية نُقل مباشرة بعد سير العمل** (كان آخر الصفحة) + فلتر
  شطر جديد (الكل/شطر الطلاب/شطر الطالبات) بأعلى القسم.
- **قسم جديد "حالة الإنجاز حسب نوع الإجراء"** (إضافة/حذف/تعديل شعبة) بمؤشرات
  دائرية (Donut) مصغَّرة - استعادة للأسلوب البصري من التصميم الأول الذي أعجب
  سليمان، بحجم Compact بدل الشريط الأخضر الكبير السابق. أُضيف أيضًا مؤشر
  دائري مصغَّر لبطاقة "نسبة الإنجاز" بالمؤشرات الرئيسية.
- **"تحتاج تدخل إدارة الوحدة" أصبح هرمًا تدرّجيًا** (StatefulWidget بحالة
  محلية): الشطر ← القسم العلمي ← تفاصيل الحالات - بدل عرض أسماء الطلبة
  مباشرة بالصفحة الرئيسية. أسماء الطلبة تظهر فقط بعد اختيار القسم العلمي.
  Breadcrumb أعلى القسم يسمح بالرجوع لأي مستوى.
- **النشاطات والتنبيهات**: تُعرض آخر 5 فقط + زر "عرض جميع النشاطات"، بارتفاع
  طبيعي غير مفروض عليه مساواة ارتفاع القسم المجاور (سبَّب فراغًا كبيرًا
  سابقًا)، مع شارة "تنبيه" صغيرة تفرّق التنبيه عن النشاط العادي.
- **توزيع الصف الأخير 70/30** ("تحتاج تدخل إدارة الوحدة" / "النشاطات
  والتنبيهات") عبر `Expanded(flex: 7)`/`Expanded(flex: 3)`.
- إزالة رابط "رفع ملفات" المكرِّر بجانب "آخر تحديث للبيانات"، واستبداله
  برابط نصي خافت باسم "إدارة البيانات".
- ترتيب الصفحة النهائي المتّبع: المؤشرات الرئيسية ← متابعة سير العمل ← أداء
  الأقسام العلمية ← حالة الإنجاز حسب نوع الإجراء ← (تحتاج تدخل إدارة
  الوحدة + النشاطات والتنبيهات).
- إصلاح خطأ تخطيط حقيقي (`BoxConstraints forces an infinite height`) كان
  يُسقط الصفحة بالكامل على الشاشات الضيقة - سبَّبه استخدام `Expanded` داخل
  `Column` غير محدود الارتفاع بقسم سير العمل بالنسخة الأولى.
- إصلاح فيض بصري (`RenderFlex overflowed`) بالهيدر المشترك
  (`portal_header.dart`) - `PortalHeader.preferredSize` كان أصغر من الارتفاع
  الفعلي للمحتوى (56 مطلوب فعليًا مقابل 52 محجوزة). أُضيف أيضًا خط سفلي
  ذهبي رفيع يميّز التبويب النشط بشريط التنقّل، وتقليص Padding/حجم أيقونات
  التبويبات لتخفيف الازدحام.
- **جميع البيانات لا تزال وهمية بالكامل (Mock Data)** بطلب سليمان، ولا
  ربط فعلي بقاعدة بيانات. **بانتظار اعتماده النهائي** قبل الربط الفعلي أو
  الانتقال لتصميم صفحة أخرى.

### إعادة تصميم كاملة للوحة الإدارة الرئيسية (Executive Dashboard) (2026-08-19)

النسخة السابقة (`admin_executive_dashboard_screen.dart`) كانت 4 بطاقات إحصائية
عامة + قسم "روابط سريعة" مكرِّر لشريط التنقل العلوي + مساحة فارغة كبيرة -
غير معتمدة صراحةً من سليمان لأنها لا تعكس مفهوم لوحة إدارة حقيقية
(Monitoring + Decisions + Actions لا Links). أُعيدت الصفحة بالكامل:

- **حذف قسم "روابط سريعة" نهائيًا** - محتواه مكرَّر أصلاً بشريط التنقل العلوي.
- **4 بطاقات تشغيلية جديدة**: طلبات تحتاج إجراء، حالات مصعدة للإدارة، طلبات
  متأخرة، نسبة الإنجاز - بدل الأرقام العامة السابقة.
- **قسم "متابعة سير العمل"**: خط مراحل تفاعلي (جديد ← لدى المنسّق ← لدى
  المرشد ← لدى الإدارة ← مكتمل)، كل مرحلة قابلة للنقر.
- **قسم "تحتاج تدخل الإدارة"**: قائمة حالات (طالب/قسم/نوع الحالة/الحالة
  الحالية/سبب التنبيه/مدة الانتظار/زر عرض).
- **قسم "أداء الأقسام"**: صف لكل قسم (الإدارة، المحاسبة، التسويق، نظم
  المعلومات الإدارية، الاقتصاد والتمويل والتأمين) بإجمالي/قيد المعالجة/
  متأخرة/مكتملة/نسبة الإنجاز مع شريط تقدّم.
- **قسم "النشاطات والتنبيهات"**: قائمة أحداث زمنية.
- شريط علوي صغير: "آخر تحديث للبيانات" + رابط نصي لصفحة "رفع ملفات" (بلا
  تكرار وظيفة الرفع نفسها هنا).
- إعادة تسمية "تجربة الصفحات" إلى **"الوصول التشغيلي"** في شريط التنقل
  (`admin_nav.dart`) وعنوان الصفحة (`test_switcher_screen.dart`) - الاسم
  الجديد يعكس الوظيفة الفعلية (تمكين صاحب الصلاحية من تنفيذ أعمال دور آخر
  عند غيابه، لا مجرد "تجربة").
- **جميع بيانات هذه المرحلة وهمية (Mock Data) عمدًا** بطلب سليمان صراحةً،
  لتقييم شكل الصفحة عند امتلائها قبل الربط بـFirestore فعليًا - لم يتم أي
  ربط بقاعدة بيانات بعد.
- **بانتظار اعتماد سليمان قبل الربط الفعلي بالبيانات الحقيقية أو الانتقال
  لتصميم أي صفحة أخرى** (بطلبه صراحةً).

### إعادة بناء قارئ ملف Microsoft Forms لطلبات الحذف/الإضافة/تعديل الشعب (2026-08-19)

النموذج نفسه أُعيد بناؤه بالكامل في Microsoft Forms (لم يعد "5 طلبات متكررة"،
بل ثلاثة أقسام مستقلة داخل نفس الاستجابة: إضافة حتى 3 مقررات، حذف حتى
مقررين، تعديل شعبة حتى مقررين - كل قسم بعده سؤال نعم/لا يحدد ظهوره)، فأُعيد
بناء `ExcelParserService.parseTickets` (`lib/services/excel_parser_service.dart`)
بالكامل لمطابقة البنية الجديدة:

- **اكتشاف حدود كل قسم بعمودي بوابة**: عناوين "المقرر الأول ورقم الشعبة"
  و"يرجى توضيح الظروف الخاصة" تتكرر حرفيًا بين الإضافة/الحذف والحذف/التعديل
  على الترتيب في الملف الحقيقي - يستحيل تمييزها بالاسم وحده، فأُضيفت
  `_findColumnsInRange` تحصر البحث ضمن نطاق فهرس كل قسم (بين بوابتيه).
- **بوابة الإضافة مُفرَّعة 10 مرات** (مثل أعمدة المرشد العشرة): سؤال "هل
  لديك طلب إضافة مقرر؟" يقع داخل تفريع القسم/الشطر فيُصدَّر بعشر نسخ شبه
  متطابقة (لاحقة تعداد تلقائية من Excel)، والقيمة الفعلية بنسخة واحدة فقط
  حسب فرع الطالب - القراءة الجديدة تفحص كل النسخ معًا (`.any`) بدل الاكتفاء
  بأول عمود مطابق (كان سيُسقِط طلبات أي قسم غير الأول بصمت).
- تحليل نص المقرر الحر (`"<رمز> - <اسم> - <شعبة>"`) وتعديل الشعبة
  (`"... - الحالية: N - المطلوبة: M"`) بدوال أنماط نصية جديدة.
- شكل عنصر `action` الناتج (`action_type/required_section/current_section/
  course/reason/reason_detail`) بقي كما هو تمامًا، فكل الشاشات/خدمات
  التصدير والتقارير التي تستهلكه لم تحتَج أي تعديل.
- **تصحيح المرشد تلقائيًا** (`lib/services/advisor_correction_service.dart`
  جديد): المرشد الفعلي المسجَّل بتقرير الإرشاد الرسمي (`AdvisingReportKind
  .allColleges`) هو من يُعتمد ويستلم الطلب دائمًا بصرف النظر عمّا اختاره
  الطالب في Forms (قد يختار بالخطأ)؛ اختيار الطالب يُحفظ فقط تحت
  `selected_advisor` للمراجعة، ويُعلَّم `advisor_corrected` عند الاختلاف.
  مطابقة الأسماء تستخدم `normalizeAdvisorNameForMatch` الموجودة مسبقًا.
  وُصِّل هذا في نقطتي الرفع (`admin_workspace_screen.dart` و`home_shell.dart`).
- الرقم الجامعي يبقى يُستخرَج من البريد الجامعي (الأدق) كما كان، مع حفظ
  الرقم المكتوب يدويًا تحت `typed_university_id` للمقارنة فقط دون أن يوجّه
  أي منطق.
- بُنيت أداة توليد (Node + exceljs) لملفَي اختبار اصطناعيين شاملين بنفس بنية
  الأعمدة الـ65 الحقيقية، باستخدام أسماء مرشدين ومقررات حقيقية من ملفات
  المرفقات: `ملفات اختبار الفورم الجديد/synthetic_add_delete_change_responses.xlsx`
  (60 حالة تغطي الأقسام الخمسة × الشطرين، أعداد إجراءات من صفر حتى 7، حالات
  تخرج/إعاقة، تطابق وعدم تطابق مرشد متعمَّد، ورقم جامعي مطبوع خطأً) و
  `sample_advising_report.xlsx` (تقرير إرشاد مطابق لنفس أرقام الطلاب لاختبار
  التصحيح فعليًا). بانتظار رفع سليمان لهما على النظام الفعلي للتأكيد.

### إصلاح خطأ "Transaction too big" عند اعتماد ملف "كل الكليات" (2026-08-18)

فشل اعتماد ملف "كل الكليات" الضخم مرتين متتاليتين بخطأ Firestore
`[cloud_firestore/invalid-argument] Transaction too big. Decrease transaction size.`
رغم إصلاح مشابه سابق بتاريخ 2026-08-14 (تقسيم كتابة السجلات لقطع `chunks` على
دفعات Firestore متتالية بدل دفعة واحدة ضخمة). الحد الذي وُضع وقتها (20 قطعة
لكل دفعة، حتى 400 سجل/قطعة) كان لا يزال قريبًا جدًا من سقف حجم الطلب الأقصى
لـFirestore (~10 ميجابايت) مع بيانات الجامعة الكاملة الفعلية - قُلِّص إلى 6
قطع/دفعة (`advising_report_repository.dart`) لهامش أمان أوسع بكثير. نُشر عبر
CI (push لفرع main يشغّل `.github/workflows/deploy.yml` تلقائيًا) بناءً على
تأكيد صريح من سليمان. بانتظار اختبار سليمان الحي لتأكيد الحل.

### توحيد تصميم كل بطاقات صفحة "رفع ملفات" (لا فراغات ممتدة) + تسمية أعم للبطاقات
- سليمان قارن التصميم المنشور بالمرجع الذي اعتمده: بطاقتا "طلاب/طالبات" للمقررات كانتا مطابقتين للمرجع، لكن الثلاث الأخرى (كل الكليات/ذوو الإعاقة/مواعيد الإرشاد) استخدمت نمط مختلف تمامًا (صف ممدود بفراغ أفقي كبير بين التواريخ وزر الرفع) بدل صندوق التصميم الموحَّد - "لم تلتزم بنفس التصميم والطريقة".
- `_uploadSlotBox` عُمِّم ليقبل قائمة تواريخ (بدل تاريخ واحد) ويُستخدَم الآن بلا استثناء لكل بطاقات الرفع الأربعة - نفس الشارة الذهبية، نفس الزر الأخضر الداكن، نفس الفاصل، نفس صف التاريخ (ساعة + تسمية + قيمة) - حذف `_actionRow`/`_dateLine` القديمين نهائيًا.
- عنوان بطاقة "حالات الإرشاد - كل الكليات" وزرّها اختُصرا لـ"حالات الإرشاد"/"رفع حالات الإرشاد" - "كل الكليات" مصطلح داخلي غير احترافي للعرض (بطلب سليمان).
- إصلاح تكرار نصي: تسمية "آخر رفع (أي قسم/شطر) - آخر رفع: ..." أصبحت "أي قسم/شطر: ...".

### إعادة تصميم صفحة "رفع ملفات" بالهوية البصرية + زر تفريغ جدول مواعيد الإرشاد
- سليمان لاحظ غياب زر "تفريغ" لبطاقة "جدول مواعيد الإرشاد" تحديدًا (لم يكن لـ`AdvisingScheduleRepository` أصلًا دالة تفريغ جماعية) - أُضيفت `clearAll()` جديدة (تحذف كل مستندات قسم×شطر دفعة واحدة) وزر تفريغ مطابق لبقية البطاقات.
- سليمان أرسل تصميمًا مرجعيًا واعتمده صراحةً بدل التصميم الأولي "البدائي": رأس مصرَّف أخضر داكن بخط ذهبي علوي/سفلي وشارة ماسية، بطاقات بيضاء بحدود ذهبية وشارة أيقونة ذهبية دائرية لكل قسم، وصندوقا رفع الطلاب/الطالبات جنبًا لجنب كصندوقين مستقلين (فاصل منقّط + تاريخ آخر رفع بأيقونة ساعة) بدل أزرار متراصة بسيطة.
- `upload_hub_screen.dart` أُعيد بناء طبقة العرض بالكامل (`_headerBanner`, `_cardShell`, `_goldIconBadge`, `_uploadSlotBox`, `_actionRow`) بلا أي تغيير بمنطق الرفع/التفريغ/التقرير نفسه.

### تقرير تغييرات جدول المقررات الدراسية (إضافة/حذف شعب، تغيير محاضرين) + حذف كلمة "الحويّة" من الواجهة + زر تفريغ بصفحة رفع الملفات
- سليمان: عند رفع ملف مقررات جديد بتاريخ أحدث، يُستبدَل الجدول القديم بالكامل (`CourseScheduleRepository.saveSchedule` تستخدم `.set()` بلا `merge`) - أراد تقريرًا يوضّح **ماذا تغيّر فعليًا** بين النسختين قبل أن يضيع القديم، تحديدًا: شعب أُضيفت، شعب حُذفت، تغيّر عضو هيئة تدريس لشعبة قائمة - **مع تجاهل تغيّر عدد الطلاب المسجَّلين عمدًا** (تقلّبه طبيعي).
- `course_schedule_change.dart` (نموذج جديد) + `course_schedule_diff_service.dart` (مقارنة نقية: مفتاح المطابقة = رمز المقرر + رقم الشعبة النظرية) + `course_schedule_change_repository.dart` (سجل تراكمي دائم بنفس مبدأ `AdvisorMovementRepository` - لا يُستبدَل، كل رفعة تُضيف تغييراتها).
- `upload_hub_screen.dart`: قبل كل رفعة مقررات، يُحمَّل الجدول القديم ويُقارَن بالجديد تلقائيًا، وتُعرَض التغييرات بنافذة تأكيد الاعتماد + رسالة النجاح، وتُحفَظ بالسجل التراكمي. زر جديد "عرض تقرير التغييرات" أسفل بطاقة المقررات يفتح كل السجل (الأحدث أولًا) بأيقونة ولون لكل نوع.
- كلمة "الحويّة" حُذفت من كل نص مرئي للمستخدم بصفحة رفع الملفات (كانت متبقية من التسمية القديمة رغم تغيير اسم الزر سابقًا) - بطلب سليمان الصريح: "احذف كلمة حوية من قاموسك".
- أُضيف زر "تفريغ" (كان مفقودًا تمامًا) لكل أزرار الرفع الأربعة بصفحة "رفع ملفات" - نفس سلوك صفحاتها الأصلية بالضبط.

### توحيد منطق رفع الملفات الثلاثة المتبقية (كل الكليات/ذوو الإعاقة/مواعيد الإرشاد) بين صفحاتها الأصلية وصفحة "رفع ملفات"
- سليمان لاحظ بعد أول استخدام فعلي لصفحة "رفع ملفات" أن الثلاثة الأصعب (كل الكليات، ذوو الإعاقة، مواعيد الإرشاد) كانت مجرد أزرار "الانتقال لصفحة الرفع" بدل رفع فعلي - اعترض: "لا يجب أن يكون هناك انتقال لصفحة الرفع، المفترض رفع من خلال البوابة". وضّح الحل المطلوب: "نقل كامل بدون مخاطرة، يبقى كما هو بنفس الصفحة الأساسية وينقل إلى صفحة رفع الملفات" - أي مصدر منطق واحد يعمل من مكانين، لا نسخة مكررة ولا اختصار تنقّل فقط.
- `upload_flows.dart` جديد: استخراج `runUploadAllColleges`/`runUploadHealth`/`runUploadAdvisingSchedule` كدوال مشتركة كاملة (بلا أي تبسيط للمنطق الأصلي - نفس مطابقة الأسماء، كشف تعارضات المكاتب، تتبّع حركات المرشدين حرفيًا) تأخذ `context`/`setUploading`/`onSuccess` بدل الاعتماد على حالة صفحة بعينها.
- `upload_dialogs.dart` جديد: حوارات المعالجة/الخطأ المشتركة (كانت خاصة بحالات الإرشاد فقط) - يستخدمها الآن كل مكان يستدعي دوال الرفع المشتركة.
- الصفحات الأصلية (`advising_cases_admin_screen.dart`, `advising_schedule_admin_screen.dart`) عدَّلت أزرارها لاستدعاء الدوال المشتركة بدل نسخها الخاصة المحذوفة الآن - سلوكها الظاهر للمستخدم **لم يتغيَّر إطلاقًا**.
- `upload_hub_screen.dart`: الأزرار الثلاثة أصبحت رفعًا فعليًا كاملاً (لا تنقّل) تستدعي نفس الدوال المشتركة، مع تحديث تواريخ آخر رفع محليًا بعد النجاح.
- الدوال الصغيرة النقية (`_normalizeArabic`/`_isSubsetMatch`/`_findOfficeConflicts`) بقيت بنسختين منفصلتين حيث كانت خاصية Dart (private بحرف تسفيل) تمنع استيرادها عبر ملفات - مقبول لأنها دوال مطابقة نصوص صغيرة مستقرة لا منطق أعمال حسّاس.

### تصحيح فرز "مواد خارج الكلية" بعمود "المستفيد" + دمجها بتصدير فورمز لكل قسم
- سليمان صحّح تعريف "مادة خارج الكلية" مرتين: هي مادة **موجودة فعلًا بخطة الكلية** لكن مصنَّفة فيها "من خارج الكلية" (تُدرَّس عبر كلية أخرى، لا ضمن ملف الحويّة الخاص بأقسامنا) - وليست "غائبة عن الخطة" كما وصفتها أول مرة خطأً. الشرط الكامل لاعتمادها: (أ) كودها ضمن قائمة خطة الكلية المصنَّفة كذلك [outsideCollegeCourses]، (ب) عمود "المستفيد" بملف الحويّة الشامل لشعبتها الفعلية تحديدًا هو "كلية إدارة الأعمال" - وجود الكود بالملف وحده لا يكفي دون الشرط الثاني.
- `docx_schedule_parser_service.dart`: أُضيفت قراءة عمود "المستفيد" (index 2 بترتيب الخلايا) كان مُتجاهَلًا تمامًا سابقًا. `parse()` بقي كما هو للتوافق، وأُضيف `parseSections()` جديد يُرجع `ParsedCourseSection` (السجل + نص المستفيد الخام) لمن يحتاج الفلترة به.
- `course_schedule_admin_screen.dart` - `_uploadOutsideCoursesFor`: أصبح يفلتر أكواد المقررات "المعروضة" على شعب المستفيد منها "كلية إدارة الأعمال" فقط قبل مطابقتها بقائمة `CourseCatalog.outsideCollegeCourses` الثابتة - يمنع اعتماد مادة خطأً لمجرد ظهور كودها بملف كلية أخرى ضمن نفس الملف الشامل.
- تصدير "نسخ مقررات القسم لفورمز" (`_formsExportOptions`) أصبح يُلحق تلقائيًا مواد خارج الكلية لنفس الشطر بنهاية قائمة كل قسم (تتكرر نفس القائمة بكل قسم بطلب سليمان) بدل تصدير منفصل - حُذفت القائمة المنبثقة التي كانت تفصل الخيارين، وأصبح زرًا واحدًا مباشرًا.
- **اكتمل التوحيد**: الأزرار الأربعة (طلاب/طالبات × ملف كلية/ملف خارج الكلية) استُبدلت برفعتين فقط ("رفع ملف الحويّة الشامل - طلاب/طالبات") - سليمان أكّد أن لديه فعليًا ملفين فقط (شطر طلاب، شطر طالبات)، والملف الواحد لا يميّز بينهما فيُرفع كل ملف لشطره يدويًا. كل رفعة تستخرج تلقائيًا من نفس الملف: (أ) شعب كليتنا الخاصة لجدول الشطر (`المستفيد` = كلية إدارة الأعمال وكودها ليس بقائمة "خارج الكلية")، (ب) قائمة "خارج الكلية" المعتمدة (كودها بالقائمة + شعبة فعلية مستفيدها كليتنا) - بنافذة تأكيد واحدة تعرض عدد كل فئة قبل الاعتماد. لو لم توجد أي شعبة بعمود "المستفيد" مطابقة، تظهر رسالة خطأ واضحة بدل اعتماد جدول فارغ بصمت.
- **يحتاج اختبارًا حيًّا بملف حقيقي** قبل الاعتماد النهائي على الميزة - لم تُختبَر بعد بملف "حويّة" فعلي بهذا الشكل الموحَّد.

### إصلاح خلل جذري: كل روابط "المزيد" الجوّالة كانت تُظهر "قيد التطوير" دائمًا + إصدار 1.0.40+43 (2026-08-16، ج60)
- سليمان اختبر تبويب "أدوات إضافية" الجديد (ج59) حيًّا: كل الروابط الخمسة فتحت "قيد التطوير" رغم أنها مبنية فعليًا بصفحات حقيقية.
- **السبب الجذري بـ`mobile_home_screen.dart`**: عنصر `IndexedStack` الخاص بفتحة "المزيد" كان مبنيًا بـ`_ComingSoonBody` **ثابتًا دائمًا** (`if (overflow.isNotEmpty) _ComingSoonBody(...)`) بدل تمرير التسمية المختارة فعليًا عبر نفس دالة `bodyFor()` المستخدَمة لبقية التبويبات المباشرة - خلل موجود من إعادة الهيكلة الأصلية (ج52)، لم يظهر أثره لأن كل عناصر "المزيد" وقتها كانت فعليًا "قيد التطوير" أصلاً، فصار ملحوظًا الآن فقط مع أول رابط حقيقي ("أدوات إضافية").
- الإصلاح: استبدال ذلك السطر بـ`bodyFor(_moreLabel.isEmpty ? overflow.first.$2 : _moreLabel)` - يوجّه الآن لنفس منطق التوجيه الصحيح المستخدَم بكل مكان آخر.
- نُشر الويب + APK جديد عبر Firebase App Distribution.

### إصلاحات من أول اختبار حي على الجوال + توحيد تسمية "الرئيسية" + إصدار 1.0.39+42 (2026-08-16، ج59)
- سليمان اختبر أول APK حيًّا وأرسل 7 لقطات شاشة موسومة بوصف كل خلل. النتائج:
  - **خلل مؤكَّد ومُصلَح - التقويم يفتح صفحة الموقع الكاملة**: زر التقويم الجديد (ج56) كان يفتح `AcademicCalendarPage` الكاملة (`InfoPageScaffold` بشريط تنقّل/تذييل مصمَّم لعرض حاسوب عريض) بدل محتوى خفيف - سبَّب جدولاً لا يتناسب حجم الجوال، وزر "تسجيل الدخول" مكرَّر بداخلها يدفع `PortalRoot` جديدة فوق شاشة الدخول الأصلية (تخبّط بالمسارات عند الرجوع). أُضيفت `PortalAcademicCalendarPage` خفيفة (`Scaffold`/`AppBar` عاديان + `AcademicCalendarContent` فقط) بـ`public_landing_screen.dart`، وحلَّت محل القديمة بكل نقاط الاستدعاء الثلاث (`portal_header.dart`، شاشتَي الدخول).
  - **خلل مؤكَّد ومُصلَح - تسجيل الخروج من الشريط العلوي لا يفعل شيئًا**: زر الحساب المنبثق بـ`mobile_home_screen.dart` كان يستدعي `signOut()` فقط بلا أي تنقّل - أصبح الآن يعيد فهرس الشريط السفلي لـ"الرئيسية" ويفتح شاشة الدخول مباشرة، مطابقًا لما طلبه سليمان.
  - **"لوحة الإرشاد" فارغة**: سلوك متوقَّع وموثَّق مسبقًا (لا تزال "قيد التطوير" جوّالًا - انظر TODO.md)، ليس خللاً جديدًا.
  - **"الرئيسية"/"لوحة الإدارة" فارغتان بلا إحصائيات**: على الأرجح APK الأول اختُبر قبل أن تصل بعض التعديلات (الكود الحالي يعرض بطاقات إحصاء دائمًا حتى بلا بيانات، نص "قيد الرصد التلقائي" بدل فراغ) - يحتاج تأكيدًا بعد تثبيت APK الأحدث الصادر من هذه الدفعة.
- **توحيد تسمية زر "الرئيسية"**: زر "الموقع العام" بشريط تنقّل الإدارة/منسّقي الأقسام/الكلية (يعيد للصفحة العامة) أصبح "الرئيسية" بكل الأماكن الثلاثة (`admin_nav.dart`، `coordinator_nav.dart`، `college_coordinator_workspace_screen.dart`) - يطابق الآن تسمية نفس الوجهة بالموقع العام نفسه، بطلب سليمان صراحةً بعد مقارنة لقطتَي شاشة.
- **قاعدة تنقّل جوّال جديدة ثابتة** (وثِّقت بالذاكرة الدائمة): أي وجهة داخل البوابة (بعد الدخول) → الشريط السفلي (مباشرة أو عبر "المزيد")؛ أي وجهة بالصفحة العامة (قبل الدخول) → الشريط العلوي؛ الاستثناء الوحيد: "تسجيل خروج" يبقى إجراءً بقائمة الحساب، لا تبويبًا.
- **"أدوات إضافية" - تبويب جديد بالشريط السفلي جوّالًا (إدارة/مدير عام فقط)**: طبَّقتُ القاعدة أعلاه على قائمة "المزيد" المنبثقة الموجودة أصلًا بصفحة الإدارة على الويب (`admin_workspace_screen.dart`) - أُضيف تبويب رابع "أدوات إضافية" بالشريط السفلي جوّالًا (يُفعِّل "المزيد" تلقائيًا لأنه رابع قسم) يحوي: دليل تشغيل البوابة، بيانات منسقي الأقسام (مدير عام فقط)، قائمة مرشدي القسم (مدير عام فقط)، الحسابات وكلمات المرور (مدير عام فقط)، خريطة صفحات الموقع.
  - **ثلاثة روابط من نفس القائمة الأصلية مؤجَّلة عمدًا** (لا تزال "قيد التطوير" جوّالًا فقط): "تقارير متابعة الحذف والإضافة" و"تنزيل ملفات الحالات" - منطقهما مبني داخل حالة شاشة الويب الخاصة (`_AdminWorkspaceScreenState`) وليس كودجت مستقل بعد؛ و"تفريغ البيانات" - إجراء حذف نهائي حسّاس يحتاج مراجعة سلامة إضافية على شاشة جوّال ضيقة قبل نقله. التفاصيل بـTODO.md.
- نُشر الويب + APK جديد عبر Firebase App Distribution (نفس آلية ج58).

### توزيع APK تلقائيًا عبر Firebase App Distribution + إصدار 1.0.38+41 (2026-08-16، ج58)
- سليمان: "نكمل تثبيت التطبيق بأفضل طريقة" - كانت طريقة التثبيت اليدوية (تنزيل ZIP من GitHub Actions Artifacts → استخراج → نقل للجوال) تعمل لكنها عدة خطوات. أفضل بديل بما أن المشروع أصلًا على Firebase: **Firebase App Distribution** - رابط تثبيت مباشر بلا ZIP ولا نقل ملفات يدوي.
- `.github/workflows/deploy.yml`: خطوة جديدة بعد بناء APK - `appdistribution:distribute` ترفعه تلقائيًا لتطبيق "CBA Advising" المسجَّل أصلًا بمشروع Firebase (App ID: `1:843787630334:android:01b9f5a9d7ea7712ea97af`)، بملاحظة إصدار تلقائية (آخر رسالة Commit)، للمُختبِر `salfawaz50@gmail.com` حاليًا (يمكن إضافة مُختبِرين آخرين لاحقًا بتعديل `--testers`).
- خطوة GitHub Artifacts (ZIP) أُبقيت كما هي كخيار احتياطي، بلا حذف.
- **يحتاج من سليمان بعد أول تشغيل**: فتح البريد `salfawaz50@gmail.com` (يصل إشعار من Firebase App Distribution برابط تثبيت مباشر)، أو تنزيل تطبيق "App Tester" من Google Play لفتح الروابط تلقائيًا مستقبلاً.

### "رقم المنسوب" أصبح شاشة الدخول الوحيدة بالموقع أيضًا + إصدار 1.0.38+41 (2026-08-16، ج57)
- سليمان: "اجعل 'رقم المنسوب' الدخول الوحيد بالموقع أيضًا" - كانت `PortalLoginScreen` القديمة (بريد/كلمة سر + اختيار صلاحية من قائمة) لا تزال نقطة دخول الموقع الافتراضية، بينما التطبيق الجوّال وحده انتقل حصريًا لنظام رقم المنسوب (ج45). أكَّد سليمان أن الموقع لا يزال بطور التجربة بلا أي مستخدم حقيقي فعليًا، فلا خطر من الاستبدال الفوري.
- `portal_root.dart`: `loginBuilder` أصبح `StaffNumberLoginScreen` بدل `PortalLoginScreen` - نقطة دخول الموقع والجوال أصبحتا موحَّدتين تمامًا الآن. `PortalLoginScreen` نفسها لم تُحذف من المشروع (تبقى تُصدِّر `BrandPanel` المشتركة، ورابطها الداخلي "الدخول برقم المنسوب" لا يزال موجودًا لمن يفتحها مباشرة لو احتاجها لاحقًا).
- الحسابات الثابتة القديمة (`admin@`, `ameen@`, `secretary@`, منسّقو الأقسام/الكليات...) لم تُحذف ولا تزال تعمل تقنيًا - حقل "رقم المنسوب" بالويب مجرد بريد نصي عادي (لوحة مفاتيح فعلية بلا قيد رقمي حقيقي، القيد الرقمي فعلي فقط بلوحة مفاتيح الجوال الافتراضية)، فيمكن كتابة الجزء الأول من البريد القديم به مباشرة عند الحاجة.
- نُشر الويب.

### زر "التقويم الجامعي" بكل صفحات البوابة الداخلية + صفحتَي الدخول + بناء APK تلقائي بـCI + إصدار 1.0.37+40 (2026-08-16، ج56)
- سليمان: "التقويم ارغب ان يظهر في جميع البوابات من الادمن حتى اي صفحه دخول" - كان التقويم متاحًا فقط بالصفحة العامة (`PublicLandingScreen`) وبتبويب علوي جديد بتطبيق الجوال (ج55)، لا داخل صفحات العمل الفعلية (إدارة/منسّقين/عرض) ولا قبل تسجيل الدخول.
- `portal_header.dart`: زر جديد `_PortalCalendarButton` (أيقونة تقويم) أُضيف كجزء **ثابت** من `PortalHeader` نفسه (بجانب زر البحث السريع/التحديث) - يظهر تلقائيًا بكل صفحة تستخدم `PortalScaffold`/`PortalHeader` بلا استثناء (لوحة الإدارة، لوحة المنسّق، منسّق الكلية/الوحدة/المسار، عرض...) دون الحاجة لتعديل كل شاشة على حدة، لأنه لا يعتمد على `navItems` الخاصة بكل شاشة. يفتح نفس `AcademicCalendarPage` المستخدَمة بالصفحة العامة.
- `portal_login_screen.dart` و`staff_number_login_screen.dart` (شاشتا الدخول قبل المصادقة، ويب وجوال): زر مطابق أُضيف بالـ`AppBar` لكل منهما - يغطي الجزء "قبل الدخول" من طلب سليمان.
- `.github/workflows/deploy.yml`: خطوتان جديدتان بعد نشر الويب - `flutter build apk --release --flavor advising -t lib/main_advising_app.dart` ثم رفع الناتج كـ"Artifact" باسم `app-advising-release` (يُحفَظ 30 يومًا، يُنزَّل مباشرة من صفحة تشغيل الوركفلو بـGitHub). يحل مشكلة موثّقة من فترة طويلة (عدم توفر أداة `flutter` بالبيئة المحلية لبناء APK يدويًا) - من الآن كل دفعة لـmain تبني الويب **وAPK معًا تلقائيًا** بلا أي تدخل يدوي.
- نُشر الويب عبر الدفع لـmain (يُفعِّل GitHub Actions). **APK الجديد الآن جزء من نفس عملية النشر التلقائي** - ينزَّل من تبويب Actions بمستودع GitHub بعد اكتمال التشغيل.

### "لوحة الإدارة" مطابقة كاملة للموقع + تبويب "التقويم" الجامعي جديد بالتطبيق + إصدار 1.0.36+39 (2026-08-16، ج55)
- سليمان: "أبغى أشاهد صفحة الأدمن كاملة متكاملة مثل الموقع تمامًا، وفيها نفس التبويبات بالإضافة إلى التقويم" - طلبان: (١) مطابقة كاملة لصفحة لوحة الإدارة، (٢) تقويم جامعي جديد بالشريط العلوي "مع التواصل" بالتطبيق (كان موجودًا بالموقع فقط عبر الشريط العلوي هناك أيضًا).
- `mobile_admin_dashboard_screen.dart`: أُعيد بناؤها بالكامل لتستخدم **نفس مكوّنات الموقع الحقيقية** بلا تبسيط - `ReportDataService`/`FirestoreTicketService`/`AdvisorRosterService` لبطاقات الإحصاء الثلاث (إجمالي الحالات/الأقسام النشطة/نسبة الإنجاز)، ثم `FollowUpChart` **نفسه** (لا نسخة مختصرة) لأنه مبني أصلًا بتخطيط متجاوب (`LayoutBuilder`/`Wrap`) يتكيّف مع عرض جوال ضيق - رسم متابعة الإنجاز الدائري + فلاتر شطر/قسم + ترتيب المرشدين، تمامًا كالموقع.
- `public_landing_screen.dart`: استُخرج محتوى جدول "التقويم الجامعي" (شريط الأشهر + الجدول + الملاحظات) لودجت عام جديد `AcademicCalendarContent` (كان محصورًا داخل `AcademicCalendarPage` فقط) - يُعاد استخدامه الآن مباشرة من التطبيق بلا تكرار البيانات. الجدول نفسه أصلًا أفقي التمرير (`SingleChildScrollView` بعرض أدنى 900) فيبقى قابلاً للتصفح كاملاً على شاشة ضيقة.
- `mobile_home_screen.dart`: تبويب "التقويم" جديد بالشريط العلوي (تاسع تبويب، بجانب "التواصل" كما طلب سليمان).
- نُشر الويب فعليًا (يظهر أثر استخراج `AcademicCalendarContent` بصفحة التقويم العامة بالموقع، بلا تغيير بصري). **أثر الجوال (لوحة الإدارة الكاملة + تبويب التقويم) لا يظهر إلا بعد بناء APK جديد يدويًا من سليمان**.

### إعادة هيكلة جذرية: شريطان علوي/سفلي ثابتان بـIndexedStack + إصلاح "المزيد" الزائد + إصدار 1.0.35+38 (2026-08-16، ج54)
- سليمان اختبر حيًّا 3 مشاكل حقيقية بعد بناء "لوحة الإدارة": (أ) الصفحة ظهرت بعنوان "إحصائيات سريعة" فقط وبقية المحتوى فارغ أبيض، (ب) الشريط السفلي أظهر تبويب "المزيد" رغم أن العدد الكلي (الرئيسية + 3 أقسام) لا يتجاوز أربعة - طلب صراحةً: "إذا كان عدد التبويبات أربعة لا يكون هناك تبويب المزيد، تظهر الأربعة كما هي"، (ج) التنقّل بين تبويبات الشريط السفلي كان عبر `Navigator.push` فيتطلّب زر رجوع بدل تبديل مباشر - طلب: "يبقى التبويب بالأسفل ثابت... لا يتوجب الرجوع بالسهم للخلف".
- **إعادة هيكلة جذرية**: `mobile_home_screen.dart` أصبح غلافًا واحدًا ثابتًا (شريط علوي + شريط سفلي لا يتغيّران أبدًا) حول `IndexedStack` يبدّل المحتوى الأوسط فقط حسب فهرس حالة (`_bottomIndex`) بدل دفع صفحات منفصلة - يحلّ (ج) جذريًا. "لوحة الإدارة" (`mobile_admin_dashboard_screen.dart`) أُعيد بناؤها كمحتوى بلا `Scaffold`/`AppBar` خاص بها (`MobileAdminDashboardBody`) لتُضمَّن مباشرة بـ`IndexedStack`.
- قاعدة "المزيد" صُحِّحت: تظهر فقط لو تجاوزت أقسام الدور 3 (أي: الرئيسية + أكثر من 3 = أكثر من 4 إجمالًا) - يحلّ (ب). أقسام الإدارة الثلاثة (لوحة الإدارة/تقارير/لوحة الإرشاد) تظهر الآن مباشرة بلا "المزيد" إطلاقًا.
- أُضيف عرض نص الخطأ الفعلي عند فشل تحميل إحصائيات "لوحة الإدارة" (بدل صمت يُبقي الأرقام صفرًا بلا تفسير) - يكشف سبب (أ) في التجربة القادمة إن تكرر.
- نُشر الويب فقط (تحقّق بناء - هذه الشاشة جوّالة حصرًا). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### أول قسم جوّال حقيقي: "لوحة الإدارة" لحساب المدير العام + إصدار 1.0.34+37 (2026-08-16، ج53)
- سليمان: "اعمل ما يكون مناسب بحيث تحتوي في حساب salfawaz لديك كل الصلاحيات" - أول قسم فعلي (لا "قيد التطوير") يُبنى بالخطة الجديدة.
- `mobile_admin_dashboard_screen.dart` جديد: شاشة جوّالة أصيلة (لا `AdminWorkspaceScreen` العريضة) - إحصائيات حية (مرشدون لهم/بدون طلاب، طلاب لهم/بلا مرشد) من نفس مصدر `AdvisingHubScreen` بالضبط (`AdvisingCaseAnalyzer`، بلا إعادة حساب أي منطق)، ثم شبكة 2×3 لبقية أقسام الإدارة (لوحة الإرشاد، تقارير، خدمات أكاديمية، المنسوبين، الحسابات، تجربة الصفحات) - كل قسم غير مبني بعد يبقى "قيد التطوير".
- مربوطة بتبويب "لوحة الإدارة" بالشريط السفلي **لحساب المدير العام (`salfawaz`/`super_admin`) تحديدًا** - بقية الأدوار (بما فيها حساب "الإدارة" العادي) لا تزال "قيد التطوير" لنفس هذا التبويب، بانتظار توسيع النطاق تدريجيًا.
- الدخول المخفي لحساب `salfawaz` (ضغطة مزدوجة، بلا أثر ظاهر) تحقَّقتُ أنه لا يزال يعمل كما هو من الإصدار السابق - لا تعديل لازم.
- نُشر الويب فقط (تحقّق بناء - هذه الشاشة جوّالة حصرًا). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### قائمة حساب منبثقة بدل صفحة كاملة + "المزيد" بالشريط السفلي لأقسام كل دور + إصدار 1.0.33+36 (2026-08-16، ج52)
- سليمان: الأمر الوحيد غير المُرضي بعد كل الإصلاحات كان أن أيقونة الحساب (لحساب مسجَّل دخوله) تفتح صفحة كاملة منفصلة لمجرد تغيير كلمة المرور/الخروج - أصبحت الآن `PopupMenuButton` منبثقة مباشرة من نفس الأيقونة (بلا أي تنقّل صفحة) تحوي الإجراءين. غير المسجَّل دخوله يبقى يُدفَع لصفحة الدخول كما هي (نموذج متعدد الحقول يستحق صفحة كاملة).
- اقتراح سليمان: "أربع اختيارات كما هي، على أن يكون الخيار الأخير المزيد ويفتح قائمة نختار منها" - الشريط السفلي أصبح يعرض حتى تبويبين مباشرين فقط لكل دور (بعد "الرئيسية")، وأي أقسام إضافية (مثال: حساب الإدارة له 3 أقسام) تظهر عبر تبويب "المزيد" الأخير (قائمة سفلية Bottom Sheet) بدل تكديسها كلها كتبويبات مباشرة - يفسح مجالًا لإضافة أقسام مستقبلية بلا ازدحام الشريط.
- نُشر الويب فقط (تحقّق بناء - هذه الشاشة جوّالة حصرًا). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### ترتيب الجامعة قبل الكلية + أيقونة دخول واضحة بدل أيقونة تُقرأ كخروج + إصدار 1.0.32+35 (2026-08-16، ج51)
- سليمان: "الجامعة أعم وأشمل من الكلية دائمًا" - رأس الصفحة الرئيسية كان يعرض "وحدة الإرشاد الأكاديمي والخريجين" أولًا (بخط كبير) والبطاقة الخضراء تحته "كلية إدارة الأعمال - جامعة الطائف". أُعيد الترتيب: العنوان الرئيسي الآن "جامعة الطائف - كلية إدارة الأعمال"، وتحته بخط أصغر "وحدة الإرشاد الأكاديمي والخريجين" - البطاقة الخضراء أصبحت "أهلاً بك / نتمنى لك يومًا موفقًا" بدل تكرار اسم الجامعة/الكلية.
- أيقونة "تسجيل الدخول" (`Icons.login`) كانت تُقرأ بصريًا كأيقونة خروج (سهم يخرج من صندوق) - استُبدلت بـ`Icons.person_add_alt_1_outlined` (شخص واضح المعنى كدخول/حساب) حين لا توجد جلسة، وتبقى `Icons.account_circle` حين توجد جلسة.
- نُشر الويب فقط (تحقّق بناء - هذه الشاشة جوّالة حصرًا). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### تصغير شعار شاشة الدخول الجوّالة + تحديث عبارة التعريف القديمة + إصدار 1.0.31+34 (2026-08-16، ج50)
- سليمان لاحظ بلقطة شاشة: صندوق الشعار العلوي بشاشة الدخول الجوّالة كبير جدًا (يُظهر شريط تمرير) - قلَّصه ~2 سم. كما لاحظ أن عبارة التعريف السفلية "بوابة رفع وتنزيل ملفات متابعة طلبات الإضافة والحذف" وصف قديم جدًا (من أول العمل) لا يعكس نطاق العمل الفعلي الحالي (إرشاد/متابعة/إحصائيات/تقارير).
- `BrandPanel` (`portal_login_screen.dart`، مشتركة بين الويب والجوال): أُضيف معامل `compact` جديد (دائرة/خطوط/تباعد أصغر، بلا سطر التعريف السفلي) - `staff_number_login_screen.dart` (شاشة دخول الجوال) يستخدمه الآن بارتفاع صندوق مخفَّض من 280 إلى 205 بكسل منطقي. النسخة الكاملة (الويب) غير متأثرة (تبقى `compact: false` افتراضيًا).
- عبارة التعريف حُدِّثت لكل الاستخدامات (ويب وجوال) إلى "بوابة الإرشاد الأكاديمي - متابعة وإحصائيات وتقارير".
- نُشر الويب فعليًا (يظهر أثره على شاشة دخول الويب الآن مباشرة). **أثر الجوال لا يظهر إلا بعد بناء APK جديد يدويًا من سليمان**.

### إصلاح خلل: تبويب "الرئيسية" بالشريط السفلي بلا أثر + إصدار 1.0.30+33 (2026-08-16، ج49)
- سليمان أرسل لقطة من تبويب "الأهداف" العلوي: الضغط على "الرئيسية" بالشريط السفلي لا يفعل شيئًا. السبب: `onTap` كان فارغًا عمدًا بافتراض خاطئ أن المستخدم بتبويب "الرئيسية" العلوي أصلًا دائمًا - لا صحيح إن تصفّح لتبويب آخر (نبذة/الأهداف/...).
- أُصلح: يستدعي الآن `_tabController.animateTo(0)` فيعيد تبويبات الصفحة العلوية لـ"الرئيسية" فعليًا بغضّ النظر عن أي تبويب مفتوح.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### إعادة مدخل الدخول المخفي للمدير العام بشاشة رقم المنسوب + إصدار 1.0.29+32 (2026-08-16، ج48)
- سليمان لاحظ: خانة "رقم المنسوب" الجديدة تفرض لوحة مفاتيح رقمية (`keyboardType: TextInputType.number`)، فيتعذّر كتابة `admin`/`salfawaz` (حروف) بها مباشرة على الجوال - نجح الدخول فقط بلصق النص من مكان آخر. فضّل استرجاع أسلوب الدخول المخفي القديم (ضغطتان) بدل الاعتماد على حقل رقمي لحساب نصّي.
- `staff_number_login_screen.dart`: أُضيفت نفس نقطة الدخول المخفية الشفافة أسفل يمين الشاشة (ضغط مزدوج) الموجودة أصلًا بـ`portal_login_screen.dart` - تفتح `HiddenAdminLoginScreen` (اسم مستخدم/كلمة مرور نصّيان، بلا قيد رقمي) للمدير العام تحديدًا. حسابات المنسوبين العادية تبقى تستخدم حقل "رقم المنسوب" الرقمي كالمعتاد.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### الشريط السفلي أصبح خاصًا بدور الحساب - المرحلة 2 (تسميات فقط) + إصدار 1.0.28+31 (2026-08-16، ج47)
- سليمان: "إظهار كل صفحة حسب دورها وأهميتها في التبويب السفلي" - كان الشريط ثابتًا عامًا (الرئيسية/إدارة الطلبات/التقارير) بغضّ النظر عن الدور.
- `portal_role_gate.dart`: دالة جديدة `PortalRoleGate.resolveSimple(User?)` تحسم الدور بلا أي بوابة UI (لا شاشة تحميل، لا فحص إجبار تغيير كلمة مرور) - لإعادة استخدام نفس منطق حسم الدور الموحَّد من صفحة عامة لا تريد حجب نفسها.
- `mobile_home_screen.dart`: الشريط السفلي الآن `StreamBuilder<User?>` + `FutureBuilder<PortalResolvedRole?>` يبني عناصره حسب الدور فعليًا: "الرئيسية" أولًا دائمًا، ثم أقسام بوابة هذا الدور بالضبط (إدارة: لوحة الإدارة/لوحة الإرشاد/تقارير - أمين: تقارير - منسّق وحدة: رفع الملفات - منسّق قسم/كلية: متابعة الطلبات/متابعة الإنجاز - منسّق مسار: حالات المسار) - غير مسجَّل الدخول يرى "الرئيسية" فقط.
- **نطاق هذه المرحلة**: التسميات/الترتيب فقط - محتوى كل قسم (غير "الرئيسية") لا يزال "قيد التطوير" (بلا دفع شاشات الويب العريضة، بعد أن ثبت فشل ذلك سابقًا هذه الجلسة). بناء كل قسم كشاشة جوّالة أصيلة حقيقية عمل قادم منفصل واحدًا تلو الآخر.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### إصلاح بريد "التواصل" + تبويب "مواقع مهمة" جديد + إصدار 1.0.27+30 (2026-08-16، ج46)
- سليمان لاحظ أن تبويب "التواصل" يعرض بريد سليمان الشخصي مُصنَّفًا خطأً كـ"البريد الرسمي" (كان placeholder سريع أثناء البناء) - صُحِّح: "البريد الرسمي للوحدة" الآن `cba.ag@tu.edu.sa` (نفس بريد الموقع)، وبريد سليمان بقي لكن بتصنيفه الصحيح "الدعم الفني للموقع" - مطابق تمامًا لصفحة "تواصل معنا" بالموقع.
- تبويب "مواقع مهمة" جديد (السادس): نفس الروابط الخارجية الست بصفحة الموقع (جامعة الطائف، الكلية، وحدة الإرشاد بموقع الكلية، المنظومة الخارجية/الداخلية، بلاك بورد) بشبكة بطاقات 2×3 قابلة للنقر - تفتح المتصفح الخارجي.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.
- **ملاحظة سليمان الأهم من هذه الجلسة (لم تُنفَّذ بعد - الخطوة التالية)**: الشريط العلوي يجب أن يبقى ثابتًا بكل صفحة، والشريط السفلي يجب أن يعكس تبويبات كل حساب فعليًا (مثال: حساب الإدارة يرى نفس تبويبات بوابة الإدارة بالموقع: لوحة الإدارة/تقارير/لوحة الإرشاد/خدمات أكاديمية/المنسوبين) بدل التبويبات العامة الثابتة الحالية (الرئيسية/إدارة الطلبات/التقارير) - يتطلب حسم دور الحساب بالصفحة الرئيسية نفسها (لا فقط حالة الدخول) وتبديل عناصر الشريط السفلي ديناميكيًا. هذا هو العمل التالي المخطَّط ضمن خطة إعادة بناء تطبيق الجوال.

### تبويب "الرئيسية" بإحصائيات حقيقية + "الهيكل التنظيمي" الرسمي + الدخول برقم المنسوب حصرًا + إصدار 1.0.26+29 (2026-08-16، ج45)
- سليمان لاحظ بعد التجربة الحية: (أ) التطبيق يفتح على تبويب "نبذة" لا تبويب إحصائيات كما بالموقع، (ب) التبويبات منسوخة من نسخة الموقع القديمة - لا يوجد "الهيكل التنظيمي" (الرسم البياني الفعلي)، فقط "أعضاء الوحدة" (قائمة مسطّحة) - أكّد لاحقًا أن "أعضاء الوحدة" بشكله الحالي (مع البريد) مقبول ويبقى كما هو، فقط يحتاج إضافة "الهيكل التنظيمي" بجانبه، (ج) الدخول يجب أن يكون حصرًا بنظام رقم المنسوب الجديد لا شاشة البريد القديمة.
- `mobile_home_screen.dart`: أُضيف تبويب "الرئيسية" أولاً (افتراضي) بإحصائيات حية من `public_stats/summary` (نفس مصدر قسم "أرقامنا" بالموقع، عبر `PortalStatCard`)، وتبويب "الهيكل التنظيمي" منفصل (الرسم الكامل: رئيس/نائب، منسّقو المسارات، القيادة الإدارية، منسّقو الكلية، جدول منسّقي الأقسام - نفس بنية `_OrgChartSection` بالموقع، أُعيد بناؤها محليًا بعناصر عامة الاسم `_Org*` لتفادي الاعتماد على كلاسات خاصة بملف آخر) - "أعضاء الوحدة" بقي كما هو بلا تغيير. ترتيب التبويبات النهائي: الرئيسية، نبذة، الرؤية والرسالة، الأهداف، الهيكل التنظيمي، أعضاء الوحدة، التواصل.
- `mobile_account_screen.dart`: الدخول أصبح عبر `StaffNumberLoginScreen` (نظام رقم المنسوب) حصرًا بدل `PortalLoginScreen` القديمة (بريد/كلمة مرور حرفي) - الشاشة القديمة نفسها لم تُحذف من المشروع (لا تزال تخدم حسابات الويب القديمة المشتركة)، فقط لم تعد تُستخدَم من مسار الجوال.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### الصفحة الرئيسية أصبحت عامة بلا بوابة دخول (تسجيل الدخول اختياري) + إصدار 1.0.25+28 (2026-08-16، ج44)
- سليمان وضّح النموذج الصحيح (مطابق تمامًا لسلوك الموقع): "طبيعي عندما أدخل الموقع يفتح على الصفحة الرئيسية، كذلك التطبيق - بعدها لي الخيار أن أبقى بالصفحة الرئيسية أو أذهب لتسجيل الدخول. إذا سجلت دخول يبقى مسجّل دخول، لكن فتح التطبيق مجددًا يظهر الصفحة الرئيسية دائمًا" - هذا لم يكن متناقضًا مع طلبه السابق كما ظننت، بل نموذج مختلف كليًا لم أفهمه صح من المرة الأولى.
- إعادة هيكلة: `mobile_advising_root.dart` لم يعد يفحص حالة الدخول إطلاقًا - يعرض `MobileHomeScreen` (عامة بالكامل، بلا `PortalRoleGate`) دائمًا كأول صفحة. أُضيفت أيقونة حساب بأعلى الصفحة (تتغيّر بين "تسجيل الدخول"/"حسابي" حسب حالة الجلسة عبر `authStateChanges()`) تفتح `mobile_account_screen.dart` الجديدة (بطاقة الاسم/الدور + تغيير كلمة المرور + خروج، أو شاشة الدخول إن لم تكن هناك جلسة) - نقطة الدخول الوحيدة الآن لـ`PortalRoleGate`.
- الجلسة المحفوظة تبقى كما هي طبيعيًا (لا تسجيل خروج تلقائي) - لكن الصفحة الأولى تبقى دائمًا الرئيسية العامة، لا انتقال تلقائي لأي لوحة.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### إزالة تسجيل الخروج الإجباري - فتح التطبيق يذهب للصفحة الرئيسية مباشرة + إصدار 1.0.24+27 (2026-08-16، ج43)
- تراجُع عن قرار سابق (ج39): طلب سليمان حينها "اطلب كلمة المرور دائمًا عند فتح التطبيق" فأُضيف `signOut()` تلقائي بشاشة البداية. بعد التجربة الفعلية، وضّح أن هذا يتعارض مع ما كرّره مرارًا: يريد فتح التطبيق يذهب للصفحة الرئيسية مباشرة بلا شاشة دخول إن كانت الجلسة محفوظة.
- أُزيل `FirebaseAuth.instance.signOut()` من `mobile_splash_screen.dart` - الآن: شاشة الترحيب تظهر دائمًا، ثم صفحة الدخول فقط إن لم تكن هناك جلسة محفوظة أصلًا، أو الصفحة الرئيسية مباشرة إن كانت هناك جلسة سابقة (سلوك Firebase Auth القياسي، لا تدخّل إضافي).
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### دمج محتوى "عن الوحدة" كتبويبات داخل الصفحة الرئيسية بدل صفحة منفصلة + إصدار 1.0.23+26 (2026-08-16، ج42)
- سليمان صرّح: "لا أرغب أن تضيف شاشات وخلافه، أرغب ما أراه بالصفحة الرئيسية [بالموقع] أراه بالتطبيق بالصفحة الرئيسية مثل التبويبات" - حُذفت `mobile_about_unit_screen.dart` (المُضافة بج41) نهائيًا.
- `mobile_home_screen.dart`: أُضيف `TabBar`/`TabBarView` **داخل نفس الصفحة الرئيسية** (لا `Navigator.push`) بخمسة تبويبات تطابق محتوى الصفحة العامة بالموقع (`public_landing_screen.dart`): نبذة، الرؤية والرسالة، الأهداف، أعضاء الوحدة (تشكيل الوحدة الحي من `UnitCommitteeRepository`)، التواصل. النصوص منقولة حرفيًا من نفس مصدر الموقع.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### تسجيل خروج إجباري بكل فتح + شاشة "عن الوحدة" جوّالة جديدة + إصدار 1.0.22+25 (2026-08-16، ج41)
- سليمان اختبر الخطوة 1 حيًّا ولاحظ مشكلتين حقيقيتين: (أ) الجلسة المحفوظة (سلوك Firebase Auth القياسي) تجعله يدخل مباشرة على حساب المدير العام بلا كلمة مرور عند كل فتح - أراد صراحةً "اطلب كلمة المرور دائمًا عند فتح التطبيق"، (ب) لا وصول إطلاقًا للهيكل التنظيمي/صفحة التواصل من التطبيق - قرار سابق (2026-08-07) افترض خطأً أن موظف الوحدة لا يحتاجها.
- `mobile_splash_screen.dart`: أُضيف `FirebaseAuth.instance.signOut()` قبل الانتقال لـ`MobileAdvisingRoot` - يضمن طلب كلمة المرور في كل فتح فعلي للتطبيق (لا يؤثر على الجلسة أثناء الاستخدام العادي داخل التطبيق نفسه).
- `mobile_about_unit_screen.dart` جديد: شاشة "عن الوحدة" جوّالة أصيلة (لا الهيكل التنظيمي متعدد الأعمدة المصمَّم لحاسوب) - قائمة تشكيل الوحدة بتخطيط عمودي بسيط من نفس مصدر بيانات الصفحة العامة (`UnitCommitteeRepository`)، مع زر مراسلة بريد لكل عضو. مرتبطة من `mobile_home_screen.dart`.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### إعادة بناء تطبيق الجوال من الصفر - شاشة رئيسية واحدة نظيفة لكل الأدوار + إصدار 1.0.21+24 (2026-08-16، ج40)
- سليمان اختبر المرحلة 2 حيًّا وأرسل صورًا حقيقية: شاشات الموقع العريضة (`AdminWorkspaceScreen`, `CoordinatorWorkspaceScreen`, رسم "متابعة الإنجاز" الدائري...) تظهر مكسورة تمامًا على الجوال - رسم بياني يفيض بحجم ضخم يغطي الشاشة، شريط علوي مزدحم بخمس أيقونات، صفحات شبه فارغة. السبب: هذه الشاشات مبنية حصرًا لعرض حاسوب ولم تُختبَر على عرض جوال ضيق من قبل إطلاقًا (المرحلة 1 هي أول مرة تصل فيها حسابات القسم/الكلية الجديدة لها على الجوال أصلًا).
- **قرار سليمان الصريح**: "إعادة بناء التطبيق من الصفر حتى يتوافق مع الموقع" + "يبدأ بالصفحة الرئيسية فقط". أُعيد بناء `mobile_advising_root.dart` بالكامل: **لا يُدفَع (`Navigator.push`) أي من شاشات الموقع العريضة على الجوال بعد الآن إطلاقًا** لأي دور.
- `mobile_home_screen.dart` جديد: شاشة رئيسية جوّالة أصيلة واحدة موحَّدة لكل الأدوار السبعة (تحية باسم الدور فقط يختلف) - بطاقة ترحيب + شريط `MobileBottomNavBar` (من المرحلة 2، لا يزال صالحًا) بتبويب "الرئيسية" فقط نشطًا، والتبويبات الأخرى ("إدارة الطلبات"/"التقارير") تعرض "قيد التطوير" بدل فتح شاشة مكسورة. حُذف `mobile_advising_home_screen.dart` القديم (لم يعد مستخدَمًا).
- كل وجهة حقيقية قادمة (تقارير، إدارة طلبات...) ستُبنى جوّالة أصيلة من الصفر بجلسات منفصلة، واحدة تلو الأخرى مع اختبار حي بعد كل واحدة - لا دفعة شاشات معًا كما حدث الليلة.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### شاشة ترحيب/شعار ثابتة عند فتح تطبيق الجوال + إصدار 1.0.20+23 (2026-08-16، ج39)
- سليمان لاحظ (بمقطع فيديو): التطبيق يفتح مباشرة على شاشة الدخول بلا أي مقدمة - أراد شاشة ترحيب/شعار تظهر **دائمًا** أول ما يُفتح التطبيق (بغضّ النظر عن وجود جلسة محفوظة).
- `mobile_splash_screen.dart` جديد: شاشة بخلفية خضراء داكنة + شعار التطبيق + اسم الوحدة/الكلية، تظهر 1.4 ثانية ثم تنتقل تلقائيًا لـ`MobileAdvisingRoot` (الذي يحسم الدخول/الوجهة كالمعتاد). أصبحت هي `home:` بـ`main_advising_app.dart` بدل `MobileAdvisingRoot` مباشرة.
- نُشر الويب فقط (تحقّق بناء). **لا يظهر أثره إلا بعد بناء APK جديد يدويًا من سليمان**.

### كشف سبب "الدوّار اللانهائي" بشاشتي منسّق القسم/الكلية على الجوال + إصدار 1.0.19+22 (2026-08-16، ج38)
- بعد اختبار سليمان الحي للمرحلة 2 على جهازه الفعلي: منسّقو الكلية والأقسام يواجهون دوّار تحميل بلا نهاية (لا يتوقف أبدًا)، بينما صفحة الأمين/السكرتير تعمل ممتازة - يؤكد أن توجيه المرحلة 1 يعمل صحيحًا، لكن هاتين الشاشتين تحديدًا تفشلان بصمت.
- **السبب الجذري**: `coordinator_workspace_screen.dart`/`college_coordinator_workspace_screen.dart` يفحصان `!snapshot.hasData` فقط بـ`FutureBuilder` - لا يميّزان "لا يزال يحمّل" عن "فشل فعليًا" (مثال Firestore: `PERMISSION_DENIED`)، فيبقى الدوّار ظاهرًا للأبد بدل كشف الخطأ الحقيقي.
- تحقّقتُ من أداة إنشاء الحسابات (`manage_staff_accounts.js`) وقواعد الأمان (`firestore.rules`) ولم أجد خللاً واضحًا بالقراءة الساكنة - **الإصلاح المرحلي**: أضفت فرع `snapshot.hasError` بالشاشتين يعرض نص الخطأ الفعلي بدل الدوّار الصامت - يكشف السبب الحقيقي (على الأرجح صلاحية/مستند مفقود) في تجربة سليمان القادمة مباشرة بدل تخمين أعمى بلا سجلات جهاز.
- **بقية ملاحظات سليمان من نفس الاختبار الحي** (لم تُعالَج بعد): (1) الشريط السفلي الجديد يظهر فقط بصفحة الإدارة الرئيسية لا كل الصفحات - نطاق متعمَّد بالمرحلة 2 (انظر ج37 أدناه)، (2) لا شاشة رئيسية جوّالة لغير حساب الإدارة - سلوك حالي مقصود، (3) "المنسوبين" يحمّل بلا نتائج - يحتاج تحقيقًا منفصلاً، (4) تصميم منسّق الوحدة placeholder مؤقت معروف (بانتظار اعتماد رسمي، انظر [[project_new_unit_position_pending]]).

### شريط تنقّل سفلي جوّال جديد لدور الإدارة - المرحلة 2 (نموذج مرجعي) + إصدار 1.0.18+21 (2026-08-16، ج37)
- `mobile_bottom_nav_bar.dart` جديد: شريط تنقّل سفلي بأسلوب صور سليمان المرجعية (خلفية بيضاء، أيقونة + تسمية، مؤشّر Pill أخضر فاتح للتبويب النشط) - قابل لإعادة الاستخدام لأي دور لاحقًا (Phase 3).
- طُبِّق على `mobile_advising_home_screen.dart` (الشاشة الرئيسية الجوّالة لحساب الإدارة): 3 تبويبات (الرئيسية/إدارة الطلبات → `AdvisingHubScreen`/التقارير → `ReportsHubScreen`) + "المزيد" (قائمة سفلية: لوحة الإدارة الكاملة، خدمات أكاديمية والمنسوبين لحساب المدير العام فقط، تغيير كلمة المرور، تسجيل خروج) - بدل شبكة الخدمات الثابتة السابقة.
- **نطاق هذه المرحلة متعمَّد ومحدود**: التنقّل بين التبويبات يتم بالدفع العادي (`Navigator.push`) لا بحفظ حالة كل تبويب (`IndexedStack`) - أبسط وأسرع تنفيذًا، ويطابق بصريًا طلب سليمان. توسيع الآلية لحفظ حالة التبويبات (إن رغب لاحقًا) يحتاج عملًا إضافيًا منفصلاً.
- **نُشر الويب فقط** (السلوك على الويب لا يتأثر - هذه الشاشة جوّالة حصرًا). **لا يظهر أثره على الجوال الفعلي إلا بعد بناء APK جديد ورفعه يدويًا لـGitHub Release `app-latest`** - لا توجد أداة `flutter` بهذه البيئة فلا يمكن بناء/تصوير APK مني مباشرة، بانتظار سليمان لهذه الخطوة قبل المراجعة البصرية والانتقال للمرحلة 3.

### توحيد منطق توجيه الأدوار (ويب/جوال) - المرحلة 1 من إعادة بناء تطبيق الجوال + إصدار 1.0.17+20 (2026-08-16، ج36)
- بداية مشروع أكبر: إعادة بناء تطبيق الجوال بأسلوب نسخة قديمة وصفها سليمان بأنها "خرافية ومميزة" (صور/فيديو أرسلها) - الخطة الكاملة بـ`C:\Users\salfa\.claude\plans\shiny-exploring-whale.md` (4 مراحل)، تُنفَّذ وتُنشَر مرحلة بمرحلة مع مراجعة سليمان بينها.
- **خلل حقيقي اكتُشف واصلح بهذه المرحلة**: `mobile_advising_root.dart` (نقطة دخول الجوال) كان يفحص فقط الأعلام القديمة (`isFullAdmin`/`isViewer`/`isCollegeCoordinator`) - أي حساب من نظام الأدوار الجديد (Custom Claims: منسّق قسم/مسار/وحدة، أمين) لا يُوجَّه لصفحته الصحيحة على الجوال إطلاقًا رغم عمله الصحيح تمامًا على الويب.
- الحل: `portal_role_gate.dart` جديد يستخرج منطق حسم الدور بالكامل (حالة تسجيل الدخول، توكن Custom Claim، إجبار تغيير كلمة المرور، التوافق الرجعي بالبريد الحرفي) لودجت واحد (`PortalRoleGate`) يُستخدَم الآن من `portal_root.dart` (الويب) و`mobile_advising_root.dart` (الجوال) معًا - مصدر وحيد للحقيقة، لا ازدواجية.
- نُشر فعليًا (الويب). **الجوال لا يظهر أثره إلا بعد بناء APK جديد ورفعه لـGitHub Release `app-latest` يدويًا** - غير مؤتمَت بعد.

### زر "يتوفر تحديث" أصفر بالشريط العلوي + إصدار 1.0.15+18 (2026-08-15، ج35)
- سليمان طلب إشارة واضحة داخل الموقع (الويب) توضّح صراحةً وجود إصدار أحدث منشور من الذي يستخدمه المستخدم حاليًا.
- آلية العمل: `web/version.json` (ملف ثابت بجذر الموقع، يُنشَر مع كل بناء) يحمل رقم البناء الفعلي؛ `lib/app_version.dart` (`kAppBuildNumber`) يحمل رقم البناء المضمَّن بالنسخة الحالية المحمَّلة بالمتصفح فعليًا؛ زر جديد بـ`PortalHeader` (`_PortalUpdateButton`) يقارن الاثنين عند فتح أي صفحة، ويظهر أصفر فقط عند وجود رقم أحدث - الضغط عليه يعيد تحميل الصفحة من الخادم مباشرة (`reloadWebApp`، تنفيذ ويب فقط عبر `dart:html`).
- **يجب تحديث `kAppBuildNumber` بـ`app_version.dart` و`web/version.json` معًا مع كل رفع رقم إصدار قادم من هذه الجلسة** - وإلا سيظهر الزر خطأً لكل المستخدمين رغم أنهم على آخر نسخة. نُشر فعليًا.

### حذف أداة "نشر تحديث تطبيق الجوال" من قائمة الإدارة + إصدار 1.0.14+17 (2026-08-15، ج34)
- سليمان طلب أن يكون نشر تحديثات التطبيق حصريًا عبر جلسة العمل هذه (git push → GitHub Actions) لا من داخل التطبيق نفسه.
- حُذفت أداة "نشر تحديث تطبيق الجوال" بالكامل من `admin_workspace_screen.dart` (بند القائمة + الحوار + الدالة `_publishAppUpdateDialog` + الاستيراد غير المستخدَم `cloud_firestore`). نُشر فعليًا.

### إصلاح محاذاة عمود "منسّقو المسارات النوعية" بالهيكل التنظيمي + إصدار 1.0.13+16 (2026-08-15، ج33)
- سليمان لاحظ (بصورة) أن عمود "منسّقو المسارات النوعية" بصفحة "الهيكل التنظيمي" (`public_landing_screen.dart`) يظهر بمحاذاة اليمين (Wrap بلا alignment)، بخلاف باقي أعمدة الهيكل المتوسِّطة.
- أُضيف `WrapAlignment.center` لنفس الـ `Wrap`. نُشر فعليًا.

### تحسين بطاقات شبكة إحصائيات "متابعة حالات الإرشاد" + إصدار 1.0.12+15 (2026-08-15، ج32)
- سليمان لاحظ أن بطاقات `PortalStatCard` الأصلية (المضافة بج31 أدناه) كانت ضخمة جدًا مع عشر بطاقات فتُقصّ التسميات الطويلة.
- بطاقة مصغَّرة مخصَّصة جديدة (`_CompactStatTile`) بنفس لغة التصميم (أيقونة ملوَّنة + رقم + تسمية) بحجم أصغر بكثير - صفّان ثابتان (5+5) على الشاشات الكبيرة، وشبكة تلتف تلقائيًا (Wrap) على الجوال بدل GridView الصارم.
- رُفع رقم الإصدار إلى **1.0.12+15** ونُشر فعليًا عبر GitHub Actions (Deploy Web، نجح).

### شبكة إحصائيات (10 بطاقات) بأعلى "متابعة حالات الإرشاد" (2026-08-15، ج31)
- سليمان أرسل صورتين: قائمة تصنيفات "عرض" النصية المجرَّدة (بلا أرقام)، وشريط إحصائيات لوحة الإرشاد (تصميم بطاقات احترافي بأيقونات وألوان) - وطلب عرض التصنيفات التسعة الأولى بنفس تصميم البطاقات، بترتيب 5 أعلى + 5 أسفل.
- أُضيفت شبكة `PortalStatCard` جديدة (نفس المكوّن المستخدم بلوحة الإرشاد) أعلى شاشة "متابعة حالات الإرشاد" - كل بطاقة تعرض عددًا حيًّا من نفس مصدر التحليل الموجود أصلًا، وقابلة للنقر لتبديل قسم العرض مباشرة. قائمة "عرض" المنسدلة بقيت كما هي لتغطية التبويبات الأربعة الإضافية غير القابلة للعدّ (تقرير النصاب/خطة التوزيع/الإحصائيات/الحركات).
- نُشر فعليًا.

### إضافة رقم منسوب المرشد لملفات إعادة تسكين الطلبة المصدَّرة (2026-08-15، ج30)
- سليمان لاحظ: ملف Excel المصدَّر من تبويبي "طلاب على غير مرشدهم" و"طلبة ذوي الإعاقة على غير مرشدهم" لا يظهر رقم منسوب المرشد - وأوضح أن الهدف الفعلي من هذا الملف هو إرساله لمسؤول الإرشاد بالقبول والتسجيل لتسكين الطلبة على مرشدهم الصحيح؛ بلا رقم المنسوب لا يمكنه تحديد المرشد فعليًا (الاسم قد يتكرر)، فيتعطّل العمل بالكامل.
- أُضيف عمود "رقم منسوب المرشد" (الحالي و/أو المفترض حسب التبويب) للتبويبات الثلاثة المتعلقة بإعادة التسكين - البيانات كانت موجودة أصلًا بالتحليل (`CollegeRosterMember.staffNumber`) ولم تكن تُعرَض بالملف المصدَّر فقط.
- نُشر فعليًا.

### شاشة "تجربة الصفحات" لحساب المدير العام (2026-08-15، جزء من ج29)
- سليمان يحتاج تجربة كل صفحات النظام الجديد (لا الرعاية الطلابية فقط) بحساب واحد بدل إنشاء حساب حقيقي منفصل لكل دور.
- `test_switcher_screen.dart` جديدة: زر "تجربة الصفحات" بشريط تنقّل الإدارة يفتح قائمة بكل الأدوار (إدارة، عرض، منسّق وحدة، منسّق كلية × شطرين، منسّق قسم × 5 أقسام × شطرين، منسّق مسار × 5 مسارات) - لمنسّقي الكلية/القسم تُكتَب أولًا وثيقة الحساب المطابقة لحساب المدير العام نفسه (مسموح له بالكتابة أصلًا) بدل تكرار منطق الشاشات الحقيقية.
- **إصلاح مهم اكتُشف أثناء البناء**: عدة شاشات (شريط تنقّل الإدارة، خدمات أكاديمية، المنسوبين...) كانت تُظهر أزرارًا حصرية لحساب `salfawaz@...internal` بمطابقة البريد الحرفي فقط - أي حساب مدير عام جديد بالنظام الجديد (بالمنسوب) لن يراها رغم امتلاكه نفس الصلاحية فعليًا. أضفت `PortalAccounts.currentClaimRole` (يُخزَّن مرة واحدة بـ`PortalRoot` عند كل دخول، يُقرَأ متزامنًا من أي مكان) لحل هذا الفحص في كل المواضع.
- **إصلاح خلل حقيقي قبل استخدامه فعليًا**: `track_case_list_view.dart` (شاشة حالات مسار الرعاية الطلابية) كان يخزّن قيمة الشطر كـ"male"/"female" بدل "شطر الطلاب"/"شطر الطالبات" المستخدمة بباقي النظام - صُحِّح قبل أي بيانات حقيقية.
- **تصحيح تالٍ (بطلب سليمان)**: الزر كان يظهر لأي حساب يصل للوحة الإدارة (admin أو super_admin) - قُيِّد ليظهر لحساب المدير العام (super_admin) حصرًا، لا حساب "الإدارة" العادي.
- نُشر فعليًا.

### بدء إعادة هيكلة الدخول والصلاحيات - المرحلة 1: أداة حسابات فردية بالمنسوب (2026-08-15، ج29)
- سليمان وصف مشكلة حقيقية: توسّع الهيكل التنظيمي (مسارات نوعية جديدة كالرعاية النفسية والصحية) سيتطلّب حسابات دخول أكثر فأكثر، بينما نظام الدخول الحالي يربط كل دور بحساب مشترك ثابت (بريد حرفي مطابَق داخل `firestore.rules`) - لا يتوسّع، ولا يميّز مَن نفّذ أي عمل فعليًا (كل منسّقي قسم معيّن يظهرون كحساب واحد).
- **القرار المعتمد** (بعد نقاش مفصّل): دخول موحّد مستقبلًا بـ"رقم المنسوب" ككل يوزر + كلمة مرور يختارها الشخص بنفسه أول دخول، وتوجيه تلقائي للصفحة المناسبة حسب دوره - **حساب واحد لكل شخص فعلي**، لا حسابات أدوار مشتركة. كل الحسابات الحالية ستُلغى لاحقًا ما عدا `salfawaz`. أُقِرَّت 7 مستويات صلاحية فقط (لا 22 صفحة منفصلة)، وأُضيف مستوى ثامن جديد كليًا: "منسّق مسار" (صفحة واحدة عامة قابلة لإعادة الاستخدام لكل المسارات الخمسة + أي مسار مستقبلي)، ستضم لاحقًا حالات الظروف الخاصة والدعم النفسي/الاجتماعي (نُقِلتا من صفحة منسّق القسم - انظر أدناه) بالإضافة إلى إرشاد شامل كل الأقسام (بخلاف منسّق القسم الذي يبقى مقتصرًا على قسمه) وتقرير خاص بنطاقه. سيُضاف لاحقًا سجل حركات (Activity Log) يُظهر من نفّذ أي عملية فعليًا لحساب `salfawaz`.
- **نُفِّذت المرحلة 1 فقط الآن** (إضافية بحتة، لا تُعطّل شيئًا حاليًا): أداة سطر أوامر جديدة `tool/admin_cli/manage_staff_accounts.js` تعمل على جهاز سليمان فقط (بلا Cloud Functions ولا خطة Blaze - نفس مبدأ `manage_users.js` الحالية) تُنشئ حسابًا لكل موظف (بريد داخلي `<رقم_المنسوب>@sss-advising-tu.internal`، كلمة مرور مبدئية = رقم المنسوب، دور يُخزَّن Custom Claim + وثيقة `portal_users/{uid}`). قاعدة Firestore جديدة (منشورة فعلًا) تسمح لكل شخص بقراءة وثيقته الخاصة فقط.
- تمهيدًا لصفحة منسّق المسار، حُذف تبويبا "حالات الظروف الخاصة" و"الدعم النفسي والاجتماعي" من شريط تنقّل منسّق القسم (الشاشتان الفعليتان لم تُحذَفا، بانتظار إعادة استخدامهما بالصفحة الجديدة).
- **المرحلة 2 اكتملت أيضًا (2026-08-15)**: `firestore.rules` تقبل الآن الأدوار الجديدة (Custom Claims) إضافيًا بجانب البريد الحرفي القديم (نُشرت فعليًا) - منسّقو الأقسام/الكلية الجدد يُكتَب لهم تلقائيًا مستند `coordinator_accounts`/`college_coordinator_accounts` (نفس الآلية القديمة المُختبَرة، بدل تكرارها)، ومنسّق المسار دور جديد كليًا (`isTrackCoordinatorFor`) جاهز لصفحته القادمة.
- **المرحلة 3 اكتملت ونُشرت (2026-08-15)**: شاشة دخول جديدة مستقلة برقم المنسوب (`staff_number_login_screen.dart`) - رابط "الدخول برقم المنسوب (تجريبي)" بجانب النموذج القديم بدل استبداله. `portal_root.dart` يقرأ الآن الدور من التوكن (Custom Claim، بتحديث إجباري) ويوجّه تلقائيًا؛ النظام القديم يبقى مسارًا احتياطيًا لمن لم يُنقَل بعد. شاشة تغيير كلمة مرور إجبارية (`force_change_password_screen.dart`) تظهر تلقائيًا لأي حساب جديد ولا يمكن تجاوزها. صفحة "منسّق مسار" عامة (`track_coordinator_workspace_screen.dart`) حاليًا Placeholder بانتظار بناء وظائفها الفعلية. اختُبر فعليًا بحساب سليمان (رقم منسوب 4280579).
- سليمان اختبر الدخول الجديد حيًّا وأكّد نجاحه - كان فيه خلل حقيقي (الشاشة الجديدة مدفوعة `Navigator.push` فوق `PortalRoot` بنفس المكدّس، فتبقى ظاهرة تغطّي الصفحة الموجَّهة إليها رغم نجاح الدخول فعليًا) أُصلح بإرجاع المستخدم لجذر المكدّس بعد الدخول مباشرة، ونُشر.
- **وظائف صفحة "منسّق مسار" الفعلية بدأت**: مسار "الرعاية الطلابية" أصبح فعّالًا بالكامل - تبويبا حالات الظروف الخاصة والدعم النفسي (منقولان من صفحة منسّق القسم، عبر كل الأقسام معًا لا قسم واحد) عبر عرض قائمة مشترك جديد (`track_case_list_view.dart`) يخدم النوعين بنفس الكود. قاعدة Firestore جديدة تمنح هذا المسار تحديدًا صلاحية القراءة/الإنشاء/التحديث عبر كل الأقسام. بقية المسارات (الإرشاد الأكاديمي، البيانات والجودة والتطوير، الخريجين، الموهوبين) لا تزال Placeholder - الإرشاد الشامل معطَّل أصلًا حتى بصفحة منسّق القسم بانتظار بيانات كاملة، فلا فائدة من تفعيله بصفحة المسار الآن أيضًا.
- شاشة الدخول الجديدة أصبحت بنفس الهوية البصرية للوحدة تمامًا (لوح أخضر تعريفي + شعار) بدل كونها شاشة بيضاء بسيطة - بطلب سليمان صراحةً.
- نُشر الكل فعليًا (build + hosting + firestore rules).
- **المرحلة القادمة (لم تبدأ بعد)**: بعد استقرار الدخول الجديد لفترة كافية - حذف الحسابات المشتركة القديمة (ما عدا salfawaz) وتفعيل سجل الحركات (Activity Log).

### توسيع صفحة "الهيكل التنظيمي" + إعادة تصميم "التقويم الجامعي" كجدول رسمي (2026-08-15، ج28)
- سليمان لاحظ بعد النشر أن أسماء منسّقي الأقسام بصفحة "الهيكل التنظيمي" تظهر مقصوصة (مثل "شطر الط.." و"أكرم مد..") لأن `PageSection` (الحاوية المشتركة لكل صفحات الموقع) كانت تُحدَّد بعرض أقصى 820px ثابت لكل الصفحات - عرض غير كافٍ لأربع بطاقات هيكلية. أضفتُ معامل `maxWidth` اختياريًا لـ `PageSection` (envault 820 كالسابق لبقية الصفحات) واستخدمت 1180px لصفحة الهيكل التنظيمي تحديدًا.
- سليمان طلب أيضًا إعادة تصميم صفحة "التقويم الجامعي" لتطابق تصميم البوستر الرسمي الذي أرسله بالضبط: شريط أشهر زمني (أغسطس 2026 → يناير 2027) بنقاط ذهبية، جدول حقيقي برأس أخضر وصفوف متبادلة الألوان وشارة تصنيف ملوَّنة لكل حدث (م | التصنيف | الحدث | تاريخ البداية | تاريخ النهاية)، وصندوق "ملاحظات مهمة" أسفله - بدل قائمة البطاقات البسيطة السابقة.
- **نُشر (2026-08-15)**.

### صفحة "التقويم الجامعي" الجديدة + أيقونة وصول مخصَّصة بالشريط العلوي (2026-08-15، ج27، نُشر)
- سليمان أرسل صورة التقويم الجامعي الرسمي للفصل الدراسي الأول 1448هـ (17 حدثًا: محطات رئيسية، إجراءات أكاديمية، إجازات، اختبارات) وطلب إضافته بأيقونة في مكان مناسب بالموقع.
- صفحة جديدة `AcademicCalendarPage` تعرض كل حدث ببطاقة (تصنيف ملوَّن + أيقونة + تاريخ البداية/النهاية هجري وميلادي) - نص حر منسوخ حرفيًا عن التقويم الرسمي (لا يُقرأ من بيانات حية، يُحدَّث يدويًا عند صدور تقويم جديد من الجامعة).
- أيقونة تقويم مخصَّصة بالشريط العلوي (بجانب "تواصل") + بند بالقائمة المنسدلة على الجوّال.
- **نُشر (2026-08-15)**.

### صفحة "الهيكل التنظيمي" الجديدة - مخطط هيكلي بصري بدل قائمة "أعضاء الوحدة" المسطّحة (2026-08-15، ج26، نُشر)
- سليمان أرسل رسمًا تقريبيًا ثم مثالًا فعليًا لهوية وحدة الشراكات الاستراتيجية بالجامعة (بطاقات برأس أخضر + أيقونة، خطوط توصيل بزوايا، شعار الجامعة، شريط رسالة ذهبي)، وطلب تصميمًا مطابقًا لتشكيل وحدة الإرشاد بنفس الهوية البصرية - صُمِّمت المسودة كاملة كـ Artifact (حوالي 20 جولة تعديل: أحجام، توسيط، تكرار نصوص، ترتيب أعمدة، ألوان الخطوط) قبل اعتمادها.
- **استُبدلت صفحة "أعضاء الوحدة" بالكامل** (كانت قائمة مسطّحة مقسَّمة هيئة إدارية/منسّقين/أعضاء) بصفحة "الهيكل التنظيمي" الجديدة (`MembersPage` في `public_landing_screen.dart`، القائمة العلوية والمنسدلة حُدِّثتا بالمسمّى الجديد). التصنيف تلقائي بالكامل من نص الدور المخزَّن في `public_unit_committee` (يحتوي "مسار"/"قسم"/"الكلية" أو مطابقة صريحة لرئيس/نائب/أمين/منسّق/سكرتير الوحدة) - **لا اسم واحد مكتوب يدويًا بالكود**، فيبقى مطابقًا لتشكيل الوحدة تلقائيًا مهما تغيّر شاغلو المناصب.
- الهيكل: رئيس ونائب أعلى (متصلان بخط ذهبي)، ثم أربع بطاقات بنفس الترتيب المعتمد من سليمان (المسارات النوعية، القيادة الإدارية، منسّقو الكلية، منسّقو الأقسام العلمية) - منسّقو الأقسام معروضون كجدول حقيقي (القسم | شطر الطلاب | شطر الطالبات) بدل تكرار العناوين لكل قسم. توقيع مائي خفيف "S/A" بطلب سليمان صراحةً.
- **نُشر (2026-08-15)** عبر `flutter build web --release --pwa-strategy=none` ثم `firebase deploy --only hosting,firestore:rules`.

### تشكيل وحدة الإرشاد يُقرأ الآن حيًّا من ملف منسوبي الكلية بدل أسماء ثابتة بالكود (2026-08-15، ج25)
- سليمان طلب إضافة "منسّق الوحدة للشؤون الإدارية" (يوسف علي عبدالله الشهراني) لصفحة "تواصل معنا"، ثم أوضح أن أي تغيير مستقبلي بأي منصب بالوحدة يجب أن ينعكس تلقائيًا على الموقع عند رفع ملف منسوبي الكلية - دون كتابة كود جديد في كل مرة، ودون التأثير على منصب أي منسوب آخر بالملف.
- **الحل المعتمد**: ورقة عمل خامسة مستقلة تمامًا اسمها "تشكيل الوحدة" أُضيفت لملف "قالب البيانات النهائي.xlsx" (بجراحة XML مباشرة عبر AdmZip حفاظًا على تنسيق الورقتين الأصليتين - `tool/admin_cli/add_unit_committee_sheet.js`)، معبَّأة بأعضاء التشكيل الـ22 (الاسم/البريد/القسم العلمي/العضوية) كما في الهيكل التنظيمي المعتمد. تحقَّقتُ أن عدد صفوف "منسوبو الكلية" (164) و"الإداريين" (25) لم يتغيّر بعد الإضافة.
- **الآلية**: نموذج `UnitCommitteeMember` وخدمة `UnitCommitteeRepository` (وثيقة Firestore عامة القراءة `public_unit_committee/current`، قاعدة كتابة للإدارة فقط) + دالة قراءة جديدة `CollegeRosterParserService.parseUnitCommittee()`. شاشة إدارة منسوبي الكلية تحفظ الآن ورقة "تشكيل الوحدة" تلقائيًا (إن وُجدت بالملف المرفوع) بجانب حفظها لبيانات المنسوبين المعتادة، بنفس ضغطة زر الاعتماد. صفحة "تواصل معنا" وقسم "أعضاء الوحدة" بالموقع العام أصبحا يقرآن البيانات حيًّا (StreamBuilder) بدل قائمة `_Member` ثابتة كانت مكتوبة يدويًا بالكود.
- نُشرت قواعد Firestore الجديدة ودُفعت بيانات التشكيل الأولية للإنتاج (بإذن صريح من سليمان). **لم يُنشر الموقع (Hosting) بعد** - الكود المُحدَّث بانتظار البناء والنشر.

### إصلاح جذري: ملفات Excel المصدَّرة تحتاج "إصلاحًا" في سطح المكتب (تسحب معه القوائم المنسدلة) (2026-08-15، ج24)
- سليمان لاحظ: نموذج "توزيع فترات الإرشاد" يعمل بلا مشكلة عبر إكسل أونلاين/المتصفح، لكن عند فتحه بإكسل سطح المكتب تظهر رسالة "وجدنا مشكلة في المحتوى" - وبعد قبول الإصلاح تختفي القوائم المنسدلة والترتيب.
- **التشخيص (بالتحقق الفعلي، لا التخمين)**: ولَّدت ملفًا حقيقيًا عبر الخدمة نفسها وفحصت أرشيف ZIP/XML مباشرة. السبب: مكتبة `excel` تُصدر تلقائيًا بكل ملف تولّده (حتى بلا أي صورة) "شبح رسم" فارغ (`xl/drawings/drawing1.xml` + علاقة تربطه بالشيت) **لا يستخدمه الشيت الفعلي إطلاقًا** - تناقض حقيقي بين ملف العلاقات والمحتوى يرفضه محلِّل إكسل الصارم لسطح المكتب، بينما يتجاهله إكسل أونلاين بتساهل.
- **الإصلاح**: `ExcelProtectionService.protect()` يحذف الآن "شبح الرسم" هذا (الملف + علاقته + إدخال Content_Types) تلقائيًا من كل ملف يمرّ عبرها - يشمل **5 ملفات بالموقع** تستخدم نفس الخدمة (نموذج توزيع فترات الإرشاد، ملفات المنسّقين، ذوي الإعاقة، التصعيد، البريد). تحقَّقتُ بتوليد ملف حقيقي بعد الإصلاح: "شبح الرسم" اختفى تمامًا وترتيب عناصر XML (mergeCells قبل dataValidations قبل pageMargins) بقي صحيحًا.
- **لم يُنشر بعد**.

### إعادة ترتيب وتسمية تبويبات "متابعة حالات الإرشاد" بالكامل + توسيع قائمة "عرض" (2026-08-15، ج23)
- سليمان أرسل ترتيبًا وتسميات جديدة كاملة لكل التبويبات الأربعة عشر (عبر صورة استخرجتها كنص + رسالة توضيحية بالأرقام لتصحيح ترتيب فرعي). طُبِّق الترتيب/التسميات بالضبط: الكل، طلبة تابعين لمرشد، طلبة تابعين لمرشد – ذوي الإعاقة، طلبة بلا مرشد، طلبة على غير مرشدهم، طلبة على غير مرشدهم – ذوي الإعاقة، مرشدون خارج الكلية ← طلابنا، مرشدون داخل الكلية ← طلاب خارج الكلية، مرشدون معفون ولهم نصاب، مرشدون بلا نصاب، تقرير نصاب الإرشاد، خطة إعادة التوزيع العادل، إحصائيات الإرشاد، حركات الإرشاد.
- **قائمة "عرض" وُسِّعت من 260 إلى 420** (حقل + قائمة منسدلة معًا) - التسميات الجديدة أطول من السابقة وكانت تُقتَص بالعرض القديم.
- **نُشر (2026-08-15)**.

### زر تفريغ لحركات الإرشاد (2026-08-15، ج22)
- سليمان لاحظ أن تبويب "حركات الإرشاد" (سجل تراكمي دائم) لا يملك زر "تفريغ البيانات" أصلاً، خلافًا لبقية التقارير - لأنه سجل مُشتَق تلقائيًا من المقارنة بين رفعتين لا رفعة مباشرة. أُضيفت `AdvisorMovementRepository.clear()` (حذف مجموعة `advisingAdvisorMovements` بالكامل، بدفعات صغيرة متتالية كبقية عمليات الحذف الكبيرة) + زر بنفس نمط بقية أزرار التفريغ.
- **نُشر (2026-08-15)**.

### إزالة ألقاب الأسماء (د./أ.) من كل نقاط العرض + حفظ القاعدة بالذاكرة الدائمة + تعديلات صغيرة (2026-08-15، ج21)
- سليمان لاحظ بإحباط أنه يُضطر لتكرار طلب إزالة "د."/"أ." من أمام الأسماء كل مرة. تتبَّعت كل نقاط عرض الأسماء الخام (`advisorNameRaw`/`advisor.name`/`member.name`) بشاشتي "متابعة حالات الإرشاد" و"بحث عن مرشد" - كانت عشرات الأماكن (خلايا جداول، تسميات فلتر المرشد، تسميات القوائم المنسدلة، حركات الإرشاد، إعادة التوزيع، إحصائية المرشدين...) تعرض الاسم الخام مباشرة بلا `displayName()` رغم وجودها ومستخدَمة داخليًا لأغراض أخرى فقط. لُفّت كل نقاط العرض الآن.
- **حُفظت القاعدة بالذاكرة الدائمة لأول مرة** (`feedback_strip_name_titles_everywhere.md`) - لن يحتاج سليمان لتكرارها مستقبلاً بأي مشروع/جلسة.
- **حذف عمود "التخصص"** من صفحة "بحث عن مرشد وقائمة طلابه" (غير مفيد - كل طلاب المرشد الواحد عادة بنفس القسم) - سيُستبدَل مستقبلاً بعمودي "المعدل"/"النطاق" حين تكتمل بيانات الطلبة الأكاديمية.
- **تسمية ملف Excel/PDF المصدَّر باسم المرشد** حين يكون فلتر "المرشد" محدَّدًا بعضو واحد (بدل عنوان التبويب العام غير المفيد في هذه الحالة).
- **نُشر (2026-08-15)**.

### إضافة خيار "الكل" + إعادة تصميم صفحة "بحث عن مرشد" (2026-08-15، ج20)
- **خيار "الكل - كل الطلاب بغض النظر عن وضعهم"** أُضيف كأول عنصر بقائمة "عرض" بشاشة "متابعة حالات الإرشاد" - بطلب سليمان الصريح: يجمع طلاب التصنيفات الخمسة (على مرشدهم/بلا مرشد/على غير مرشدهم/مرشد خارجي/طلاب خارجيون) بجدول واحد مع عمود "الوضع" الإضافي.
- **إعادة تصميم كاملة لصفحة "بحث عن مرشد وقائمة طلابه"** - سليمان لاحظ التصميم السابق (بطاقات `ExpansionTile` بلا أي تصدير) غير احترافي ولا يمكن طباعته. استُبدل بنفس الهوية البصرية لبقية جداول الموقع (جدول أخضر منسَّق، رأس متوسِّط، صفوف متبادلة اللون) + زرّا **Excel**/**PDF-طباعة** فعليين لأول مرة بهذه الصفحة.
- **نُشر (2026-08-15)**.

### تحسينات جدول عام: توسيط الرأس + فرز بالنقر + إصلاح بحث المرشد (2026-08-15، ج19)
- **توسيط رأس الجدول**: كانت طريقة `Center` السابقة لا تتوسّط فعليًا (قيد Flutter داخلي بمكوّن `DataTable`) - استُبدلت بـ`Expanded` + `textAlign: center`، النمط المعتمَد لهذه الحالة تحديدًا.
- **فرز بالنقر على أي عمود**: أُضيف للجدول العام المشترك بكل التبويبات (`_dataTableFromRows`) - النقر على أي رأس عمود (الرقم الجامعي/التخصص/المرشد/أي عمود آخر) يفرز الصفوف تصاعديًا/تنازليًا (رقميًا لو كانت القيم أرقامًا، وإلا نصيًا).
- **إصلاح خلل حقيقي بالبحث**: "لا أستطيع البحث باسم المرشد" - البحث كان مقارنة نصية حرفية بلا توحيد صور الهمزة، فيفشل لو اختلفت صورة الهمزة بين ما يكتبه المستخدم والاسم المخزَّن (مثال: "احمد" بلا همزة لا يطابق "أحمد" بها). أُضيف توحيد الهمزة/التاء المربوطة قبل المقارنة.
- **نُشر (2026-08-15)**.

### إصلاح جذري نهائي: استبعاد كليات أخرى حقيقية بلا التضحية بطارق حلمى (2026-08-15، ج18)
- سليمان لاحظ 168 حالة بتبويب "مرشد خارجي ← طلابنا" وشكّ أنها طلاب كلية تطبيقية. تحقَّقتُ مباشرة (`tool/admin_cli/check_external_advisors.js`): ثلاثة "مرشدين" (محمد عبدالله حسين زميع/قمراء راجح الحميدي السبيعي/إيمان عبدالعزيز أحمد جان طاشكندي) - **لا أحد منهم موجود بملف منسوبي كليتنا**، ولكل واحد عشرات الطلاب موزَّعين على تخصصات كلية تطبيقية واضحة (الإعلان والاتصال التسويقي/السكرتارية التنفيذية/تقنية وأمن الشبكات...)، **وبعض تلك التخصصات فقط يحمل نصًا مطابقًا حرفيًا لأحد أقسامنا الخمسة** (مثال: "قسم المحاسبة" - تخصص بنفس الاسم موجود بالكلية التطبيقية أيضًا)، فتُخطئ `isKnownBachelorDepartment` (مقارنة نصية فقط) في تمييزه عن قسمنا الحقيقي.
- **الحل النهائي**: أُعيد استبعاد "أي كلية لا تذكر إدارة الأعمال" (كما بج10 سابقًا)، **لكن مع استثناء ذكي هذه المرة**: يُستثنى من الاستبعاد أي كتلة اسم مرشدها موجود فعليًا بملف منسوبي الكلية (`knownAdvisorNameKeys`، يُمرَّر من الشاشة بعد تحميل الروستر مسبقًا لغرض آخر أصلاً) - يحلّ حالة طارق حلمى (معروف بالملف رغم تسمية كليته الإدارية التاريخية المختلفة) وحالة المرشدين الحقيقيين من كليات أخرى (غير معروفين بالملف إطلاقًا) معًا بلا تعارض، بخلاف محاولتي ج10 (عامة جدًا، استبعدت طارق) وج16 (تراجع كامل، أعاد فتح الباب للكلية التطبيقية).
- **نُشر (2026-08-15)** - يحتاج رفعة جديدة لملف "كل الكليات" والتحقق: طارق حلمى يظهر ببياناته الكاملة، والـ168 حالة المشبوهة تختفي (أو تنخفض للحالات الحقيقية فقط).

### إعادة تصميم "إحصائية المرشدين" - احترافية وليست نسخة إكسل حرفية (2026-08-15، ج17)
- سليمان أوضح: البيانات يجب أن تُقرأ من الموقع حصريًا (كانت كذلك فعلاً - ملف الإكسل استُخدم مرة واحدة فقط يدويًا أثناء التطوير لفهم منهجية الحساب، لا كمصدر بيانات وقت التشغيل)، ولا داعي لالتزام كل أعمدة الإكسل حرفيًا - يجب أن تكون الصفحة احترافية ومفيدة وفيها فلترة.
- **فلتر جديد خاص بالتبويب**: "حالة النصاب" (فوق النصاب/دون النصاب/متوازن/معفى-مجمَّد/**معفى وله طلاب - يحتاج مراجعة** كحالة شاذة مميَّزة) - يعمل مع فلاتر الشطر/القسم/المرشد/البحث المشتركة.
- **الجدول اختُصر** من 10 أعمدة (نسخة إكسل الحرفية) إلى 8 أعمدة عملية (حُذف عمود "عدد الطلبة بالمنظومة" المكرر).
- **بطاقة الملخص أعيد تصميمها**: مربعات مستقلة لكل رقم بدل نص متلاصق، مع تمييز أحمر تلقائي لحالة "طلاب لدى معفَين" الشاذة إن وُجدت.
- **فرز تلقائي بالأهمية**: كل قسم يُرتَّب من الأكثر تجاوزًا للنصاب إلى الأقل بدل الترتيب الأبجدي.
- **نُشر (2026-08-15)**.

### تبويب "إحصائية المرشدين" الجديد + إصلاح وزن أمين القسم بالنصاب + تراجع عن استبعاد "الكلية التطبيقية" الواسع (2026-08-14، ج16)
- **تراجع عن إصلاح ج10 (استبعاد أي كلية لا تذكر "إدارة الأعمال")**: سبّب رجوعًا حقيقيًا خطيرًا - طارق حلمى عبدالنبي علي (عضو حقيقي بملف منسوبي كليتنا، قسم نظم المعلومات الادارية، نصاب كامل) اختفت كتلته بالكامل (~101 طالبًا) من "مرشدون بلا طلاب" لأن سطر "الكلية" أسفل كتلته تاريخيًا لا يذكر "إدارة الأعمال" لسبب إداري معروف مسبقًا. تحقَّقتُ مباشرة من قاعدة البيانات (لا وجود لسجل واحد له بتقرير "كل الكليات" المخزَّن) فتأكَّد الشك. رجع الاستبعاد لحالة الفروع الثلاثة فقط (الخرمة/تربة/رنية) - الحماية من كليات أخرى حقيقية (كالكلية التطبيقية) تبقى قائمة عبر `classifyAllColleges` (تخصص الطالب/المرشد غير معروف).
- **إصلاح وزن أمين القسم بحساب النصاب العادل**: سليمان قدَّم ملفه المرجعي الخاص ("تقرير الإرشاد النهائي.xlsx") فتأكَّد أن أمين القسم له وزن **0.5** فعليًا (مثل نصاب 50%)، لا صفر كما كان بالكود (`_weightOf`) - كان يُستبعَد كليًا من حساب النصاب العادل للقسم فيرتفع نصيب البقية خطأً.
- **تبويب "إحصائية المرشدين" جديد بالكامل** (بين "إعادة التوزيع" و"حركات الإرشاد" كما طلب تحديدًا) - يطابق تصميم ملف سليمان المرجعي حرفيًا: كتلة مستقلة لكل قسم/شطر (رأس ملخَّص: إجمالي المرشدين/كامل النصاب/نصاب 50%/المعفيون/عدد المرشدين المكافئ/حالات المستحقين/النصاب العادل الكامل والـ50%/إجمالي الحالات الفعلية) + جدول تفصيلي لكل عضو (م/الاسم/الرقم/عدد بالمنظومة/العدد الفعلي/نوع النصاب/الوزن/النصاب المستهدف/الفرق/الحالة). **النصاب المستهدف يُقرَّب للأعلى (ceiling) لا تقريبًا عاديًا** - تحقَّقتُ حسابيًا أن هذا يطابق منهجية ملف سليمان بالضبط (مثال حقيقي: 901 حالة ÷ 12.5 وزن = 72.08 يظهر 73 بملفه، تماشيًا مع ceiling لا round).
- **تحقَّقتُ من صحة أرقام "قسم الإدارة" التي شكّك بها سليمان** مباشرة من قاعدة البيانات (`tool/admin_cli/check_advising_record.js` وسكربتات مؤقتة) - الحساب سليم رياضيًا؛ الفارق الظاهري بين الأرقام الحية والملف المرجعي القديم سببه اختلاف تاريخ لقطة البيانات (الملف المرجعي أقدم بكثير)، لا خطأ حسابي.
- **نُشر (2026-08-14)**.

### إصلاح ازدواج ظاهري: نفس الطالب يظهر مرتين (شطر الطلاب والطالبات معًا) (2026-08-14، ج15)
- سليمان لاحظ "فيصل نادر محمد أحمد محمد" مكرَّرًا صفّين متطابقين (شطر الطلاب وشطر الطالبات) بتبويب "مرشد خارجي ← طلابنا".
- **السبب**: صف تعذّر تحديد شطره وقت القراءة (مرشده "عبدالله معتق سعيد الذيباني" من خارج كليتنا، فلا خريطة شطر مرشدين تحدِّد جنسه، ولا عمود جنس بالتقرير) يُضاف لكلا الشطرين معًا كحل احتياطي موثَّق مسبقًا - عادة تُتجاهَل نسخة الشطر الخطأ بصمت لاحقًا أثناء الدمج بشطر واحد، لكن تصنيفات `classifyAllColleges` (كل تبويبات "التصنيفات الأربعة") تُغذَّى من دمج الشطرين الخامين معًا مباشرة بلا تلك المطابقة الوسيطة، فتظهر النسختان معًا فعليًا.
- **الإصلاح**: إزالة تكرار بالرقم الجامعي (لا يمكن أن يتكرر فعليًا) قبل التصنيف مباشرة بـ`classifyAllColleges` - يحتفظ بنسخة واحدة فقط لكل رقم جامعي.
- **نُشر (2026-08-14)**.

### تصحيح إصلاح انزياح أعمدة PDF - كان يفحص الحقل الخطأ فلا يكتشف الحالة إطلاقًا (2026-08-14، ج14)
- بعد نشر إصلاح ج11 وإعادة رفع سليمان للملف، الخلل استمر بالضبط بنفس الشكل. بدل التخمين مجددًا، **تحقَّقتُ مباشرة من البيانات المخزَّنة في Firestore** بسكربتين تشخيصيين جديدين (`tool/admin_cli/check_roster_names.js`، `tool/admin_cli/check_advising_record.js`) - نفياني أولاً فرضية "متصفح مخبَّأ" (تحقَّقت سليمان بنافذة متصفح متخفٍّ - نفس النتيجة)، ثم كشفت السجل الفعلي المخزَّن: `studentId: "1"` (رقم تسلسل)، `studentName: "44206645"` (الرقم الجامعي الحقيقي!)، `department: "صباء ...الإدارة"` (اسم+تخصص مدمَجين).
- **الخلل الحقيقي**: إصلاح ج11 كان يفحص هل **الاسم** رقم قصير (`^\d{1,3}$`) ليكتشف الانزياح - لكن الانزياح الفعلي معكوس تمامًا: **الرقم الجامعي** هو من يصير رقمًا تسلسليًا قصيرًا، والاسم يصير الرقم الجامعي الحقيقي (طويل). فحص إصلاح ج11 لم يتطابق أبدًا مع الحالة الحقيقية فلم يعمل إطلاقًا رغم نشره.
- **الإصلاح المصحَّح**: الشرط الآن يفحص الحقلين معًا بشكل صحيح - رقم جامعي مقروء قصير (1-3 أرقام) **و** اسم مقروء يبدو رقمًا جامعيًا حقيقيًا (6-10 أرقام) - عندها يُستعاد الرقم الجامعي الحقيقي من حقل "الاسم"، ويُفصَل الاسم الحقيقي عن التخصص من الحقل المدمَج.
- **لم يُختبَر فعليًا بعد** - يحتاج رفعة جديدة والتحقق أن حالة "صباء" (والحالات المشابهة) تظهر ببياناتها الصحيحة الآن.

**نُشر (2026-08-14)**: كل الإصلاحات من ج7 إلى ج13 أدناه (مطوي القيد، تفاصيل تبويبي الحالات الصحية، أمين القسم، الكلية التطبيقية، انزياح أعمدة PDF + تسامح ±1 + توزيع تلقائي، فلتر المرشد الشامل، استبعاد المرشد غير المعروف من إعادة التوزيع) نُشرت معًا دفعة واحدة على https://sss-advising-tu.web.app بعد موافقة سليمان الصريحة. بانتظار اختباره الحي لكل حالة (خصوصًا تحتاج رفعة جديدة لملف "كل الكليات" لتظهر إصلاحات الفروع/انزياح الأعمدة/الكلية التطبيقية فعليًا - الإصلاحات بالكود فقط لا تُصحِّح بيانات مخزَّنة مسبقًا).

### إصلاح: إعادة التوزيع كانت تقترح نقلاً لحالات "مرشد غير موجود بكليتنا إطلاقًا" (2026-08-14، ج13)
- سليمان لاحظ أسماء غريبة بعمود "المرشد السابق" بتقرير إعادة التوزيع (عمر محمد حميدة، لجين زهير، ايمان عبدالعزيز أحمد جان طاشكندي) وشكّ أنها لا تتبع الكلية. تحقَّقتُ مباشرة من قاعدة البيانات الحية (سكربت تشخيصي جديد `tool/admin_cli/check_roster_names.js`) - **الأسماء الثلاثة غير موجودة بملف منسوبي الكلية إطلاقًا**، فتأكَّد الشك.
- **السبب**: تصنيف "طلاب على غير مرشدهم" (`wrongDept`) يشمل حالتين مختلفتين تمامًا: (أ) مرشد من كليتنا فعليًا لكن بقسم مخالف (إعادة توزيع داخلية منطقية)، و(ب) اسم مرشد في تقرير الجامعة الرسمي **لا يطابق أي عضو بملف منسوبينا إطلاقًا** (خطأ ربط بمنظومة الجامعة، يحتاج تحقُّقًا يدويًا). حلقة "إعادة التوزيع" كانت تعامل الحالتين بنفس الطريقة وتقترح نقلاً تلقائيًا حتى للحالة (ب)، موحية خطأً أنها إعادة توزيع داخلية عادية.
- **الإصلاح**: حلقة اقتراحات النقل بـ`advising_case_analyzer.dart` تستبعد الآن صراحةً حالة "المرشد غير معروف إطلاقًا" (`c.advisor == null`) - تبقى ظاهرة فقط بتبويب "طلاب على غير مرشدهم" (تُعرَض بوضوح "غير موجود بملف منسوبي الكلية")، بلا اقتراح نقل مضلِّل.
- **لم يُختبَر فعليًا بعد**.

### فلتر المرشد أصبح يشمل كل مرشد بقاعدة البيانات (حتى خارج كليتنا) (2026-08-14، ج12)
- كانت قائمة فلتر "المرشد" مقصورة على من يطابق اسمه عضوًا بملف منسوبي الكلية فقط - فتعذّر الفلترة على مرشدين ظاهرين بتبويبي "مرشد خارجي ← طلابنا"/"مرشدنا ← طلاب خارجيون" تحديدًا (هم بالتعريف من خارج ملف منسوبينا). بطلب سليمان الصريح: القائمة تُبنى الآن من السجلات الخام غير المفلتَرة (كل الكليات) بلا قيد وجود بملف منسوبي الكلية.
- **لم يُختبَر فعليًا بعد**.

### إصلاح شامل: انزياح أعمدة PDF + تسامح ±1 بالنصاب/إعادة التوزيع + توزيع تلقائي بدل "يلزم قرار يدوي" (2026-08-14، ج11)
- **إصلاح جذري لخلل تكرَّر الإبلاغ عنه 5 مرات** - "مرشدنا ← طلاب خارجيون" أظهر أرقام تسلسل (1، 13...) بعمود اسم الطالب، والاسم الفعلي مندمجًا مع التخصص بعمود واحد. **السبب الجذري**: استخراج جداول PDF (`PdfTableRowsExtractor`) يُعيد بناء الأعمدة هندسيًا من مواضع الكلمات (لا بنية جدول حقيقية) - لو قلّت الفجوة الأفقية بين عمودي "الاسم" و"التخصص" عن العتبة المعتمدة **لصف معيّن فقط** (يعتمد على طول الاسم/الخط لذلك الصف)، يندمج العمودان في خلية واحدة فتنزاح كل الخلايا اللاحقة يسارًا. هذا سبب معماري وليس خطأ كلمة مفتاحية كسابقيه - لذلك لم يُحلّ بالإصلاحات السابقة. أُضيف كشف وإصلاح تلقائي (`_splitMergedNameAndDepartment` بـ`advising_report_parser_service.dart`): اسم يتكوّن من أرقام صرفة فقط + خلية تخصص طويلة (4+ كلمات) = علامة انزياح، يُعاد فصلها بمطابقة لاحقة تخصص معروف.
- **تسامح ±1 طالب بتقرير النصاب**: بطلب سليمان الصريح ("لا يمكن أن يكون الرقم مطابقًا 100%") - فارق طالب واحد فقط بين العدد الفعلي والحصة العادلة يُعتبَر الآن "متوازن" لا فوق/دون الحصة.
- **نفس التسامح يشمل "إعادة التوزيع"**: فارق طالب واحد لا يُذكر كاقتراح نقل إطلاقًا (طلب سليمان صراحةً) - لأن قائمة `overloaded` المغذِّية لإعادة التوزيع تستخدم نفس الشرط الآن.
- **توزيع تلقائي بدل "يلزم قرار يدوي"**: حين تُستهلَك كل سعة الاستقبال الرسمية (دون الحصة العادلة) بالقسم، كان النظام يتوقف عن الاقتراح ("يلزم قرار يدوي") - سليمان طلب صراحةً اقتراحًا حتى في هذه الحالة، فأُضيف دوران بالتساوي (round-robin) على كل الأعضاء النشطين بالقسم بدل التوقف.
- **لم يُختبَر فعليًا بعد** - يحتاج رفعة جديدة لملف "كل الكليات" (PDF) والتحقق: الأسماء تظهر صحيحة لا كأرقام تسلسل، فوارق الطالب الواحد لا تُصنَّف كمشكلة، و"إعادة التوزيع" تقترح مرشدًا لكل حالة بلا استثناء (ما لم يكن القسم بأكمله بلا أعضاء نشطين).

### إصلاح جذري: كليات أخرى غير كليتنا (كالكلية التطبيقية) كانت تدخل النظام (2026-08-14، ج10)
- سليمان أبلغ عن طالب من **الكلية التطبيقية** (قسم "تطبيقات الأعمال" - كلية مختلفة تمامًا، لا علاقة لها بكلية إدارة الأعمال) ظهر ضمن بيانات النظام، مرشده "محمد عبدالله حسين زميع".
- **السبب**: استبعاد فروع المحافظات (الخرمة/تربة/رنية) بـ`AdvisingReportParserService` كان الاستبعاد الوحيد المطبَّق على سطر "الكلية" أسفل كل كتلة مرشد - أي كلية أخرى بالجامعة غير هذه الفروع الثلاثة (وغير كليتنا) كانت **تمر بلا استبعاد إطلاقًا** لأن الفحص كان "هل الاسم أحد الفروع الثلاثة؟" لا "هل هذه كليتنا فعلاً؟". فلو تصادف اسم مرشد بكلية أخرى مع اسم عضو حقيقي بملف منسوبي كليتنا، تُنسَب طلابه لكليتنا خطأً (تظهر بتبويب "مرشدنا ← طلاب خارجيون" أو أسوأ).
- **الإصلاح**: عُكس الفحص بالكامل - عُدّت الكتلة استبعادًا إلا إذا ذكر سطر "الكلية" كليتنا ("إدارة الأعمال") صراحةً وبلا اسم فرع محافظة. أي كلية أخرى بالجامعة (الكلية التطبيقية وغيرها) تُستبعَد الآن تلقائيًا بنفس آلية الفروع الثلاثة، دون الحاجة لتعداد كل الكليات المحتملة بالجامعة يدويًا.
- **لم يُختبَر فعليًا بعد** - يحتاج رفعة جديدة لملف "كل الكليات" والتحقق أن حالة الطالب المذكورة (وأي حالة مشابهة) اختفت من كل التبويبات.

### إصلاح: إظهار خاطئ لطلاب أمين القسم كـ"على غير مرشدهم" (2026-08-14، ج9)
- إصلاح الجلسة السابقة (ج4، جعل المعفى/المجمَّد/"حالات خاصة فقط" ينتقلون لـ"على غير مرشدهم") شمل خطأً **"حالات خاصة فقط" (أمين قسم)** أيضًا - فظهر طلاب حسن الزبير (أمين قسم التسويق) وغيره من أمناء الأقسام بتبويب "على غير مرشدهم" رغم تطابق قسمهم تمامًا مع قسم الطالب، لاحظه سليمان بلقطة شاشة.
- **صحّح سليمان الفهم**: أمين القسم له نصاب إرشاد فعلي **50%** (لا صفر كما كان مفترَضًا بالكود) - وجود طلاب من نفس قسمه وضع طبيعي متوقَّع، **بشرط أن يكونوا من ذوي الإعاقة تحديدًا** ضمن هذا الـ50%. أُزيلت `specialCasesOnly` من شرط الاستبعاد بـ`classifyAllColleges` - تبقى `exempt`/`frozen` فقط كما هي.
- **ملاحظة مفتوحة لم تُنفَّذ بعد**: التحقُّق الفعلي من أن طلاب أمين القسم (ضمن نصابه 50%) هم فعلاً من ذوي الإعاقة (لا طلاب عاديين) - يحتاج تأكيد سليمان إن رغب بتقرير/تنبيه منفصل لهذه الحالة تحديدًا.

### إعادة تفاصيل تبويبي الحالات الصحية بطلب سليمان (2026-08-14، ج8)
- تراجعت عن تبسيط تبويبي "طلبة ذوي الإعاقة على مرشدهم/على غير مرشدهم" إلى قوائم أسماء مجرَّدة (ج3 أعلاه) - سليمان طلب صراحةً إعادتها لجدول تفصيلي: اسم الطالب/ة، رقمه الجامعي، تخصصه، نوع الإعاقة، والمرشد. تبويب "على غير مرشدهم" يضيف عمودين معًا للمقارنة: **المرشد الحالي** (المخالف) و**المرشد المفترض** (حسب قاعدة أمين/منسّق القسم).
- **نُشر (2026-08-14)**.

### إكمال إصلاح "مطوي القيد" - ثلاثة فحوصات مكرَّرة كانت لا تزال تفوّتها (2026-08-14، ج7)
- سليمان لاحظ ببشرى جمال بكر عمر تحديدًا: تظهر بشارة "الحالة مجمّدة" الرمادية العامة بدل "غير متواجد" (نفس شارة عبدالله ضاوي الذيابي، معار)، **ولا تُخفى تلقائيًا** رغم عدم تفعيل "إظهار الكل" - رغم إصلاح `_frozenKeywords` سابقًا بـ`advising_load_rules.dart`.
- **السبب**: إصلاح الجلسة السابقة أضاف "مطوي" لمكان واحد فقط (`AdvisingLoadRules._frozenKeywords`، يتحكم بعبء الإرشاد)، لكن ثلاثة فحوصات نصية **مستقلة تمامًا** بأماكن أخرى تحدِّد الإخفاء الافتراضي/الشارة/ترتيب الفرز كانت لا تزال تفتقد "مطوي": `_isAbsentMember` و`_absenceLabel` (college_roster_admin_screen.dart)، `_isAbsentRosterMember` (course_schedule_admin_screen.dart)، و`FacultySortOrder.absenceTierIndex` (faculty_sort_order.dart). أُضيفت "مطوي" لكل الأربعة.
- **النتيجة المتوقَّعة بعد النشر**: بشرى تُخفى تلقائيًا من قائمة منسوبي الكلية (كعبدالله ضاوي تمامًا) ما لم يُفعَّل "إظهار الكل"، وتظهر عندها بشارة "غير متواجد" الرمادية الموحَّدة، وتُرتَّب أسفل القائمة مع بقية غير المتواجدين.
- **لم يُختبَر فعليًا بعد** - يحتاج اختبار سليمان الحي بعد النشر.

### استبدال شريط التبويبات الأفقي بقائمة منسدلة + توسيع البحث للمرشدين (2026-08-14، ج6)
- **شريط التبويبات الاثني عشر (TabBar) كان يفيض عن عرض الشاشة بمظهر غير احترافي** كما لاحظ سليمان صراحةً بلقطة شاشة. استُبدل بالكامل بقائمة منسدلة واحدة ("عرض") من نفس تصميم بقية الفلاتر (شطر/قسم/مرشد)، وُضعت **أول الفلاتر قبل "الشطر"** كما اقترح - أخف بصريًا وتستوعب أقسامًا مستقبلية بلا فيضان أفقي. أُزيل `TabController`/`TabBar`/`TabBarView` نهائيًا لصالح متغيّر حالة بسيط (`_sectionIndex`).
- **خانة البحث وسِّعت لتشمل المرشدين وأرقامهم** بجانب الطلبة وأرقامهم - بطلب سليمان الصريح. `_matchesSearch` أصبحت تقبل اسم/رقم المرشد اختياريًا (تُحل رقم المنسوب تلقائيًا من ملف منسوبي الكلية عبر الاسم)، وطُبِّقت على كل التبويبات الاثني عشر - شملت أيضًا تبويبات لم يكن بها بحث نصي أصلاً (معفَون ولهم طلاب/مرشدون بلا طلاب/تقرير النصاب/إعادة التوزيع).
- **لم يُختبَر فعليًا بعد** - يحتاج اختبار سليمان الحي: القائمة المنسدلة الجديدة تبدّل المحتوى بصريًا بشكل صحيح، والبحث يجد المرشد باسمه أو رقمه في كل التبويبات.

### توسيع "إعادة التوزيع" ليشمل مرشدي الحالة المجمَّدة (2026-08-14، ج5)
- بسؤال سليمان: هل طلاب مرشد أصبح "مجاز" يتوزعون تلقائيًا على بقية مرشدي نفس القسم كما يحدث مع المعفى كليًا؟ - تبيّن أن الإجابة كانت **لا** (فجوة حقيقية): محرّك اقتراحات "إعادة التوزيع" (`transferSuggestions`) كان يشمل فقط المرشد المعفى كليًا (`exempt`) والمرشد الفائض عن حصته، ويتجاهل تمامًا طلاب أي مرشد بحالة مجمَّدة (`frozen`: معار/مجاز/مبتعث/موقوف الراتب/مطوي القيد) - يظهرون فقط بتبويب "على غير مرشدهم" بلا مرشد بديل مقترَح.
- بتأكيد سليمان الصريح: أُضيفت قائمة `frozenWithStudents` منفصلة عن `exemptWithStudents` (لا تظهر بتبويب "معفَون ولهم طلاب" - التجميد مؤقت لا إعفاء دائم) لكنها تُدخَل الآن معها في حلقة توليد اقتراحات إعادة التوزيع لكل قسم (`advising_case_analyzer.dart`) - طلاب أي مرشد مجمَّد يُقتَرح نقلهم تلقائيًا لبقية مرشدي نفس القسم دون الحصة العادلة، تمامًا كالمعفى كليًا.
- **لم يُختبَر فعليًا بعد** - يحتاج رفعة جديدة (عضو بحالة مجاز/معار وله طلاب فعليًا) والتحقق أن اقتراحات إعادة التوزيع الجديدة تشمله.

### إصلاح جذري: أعضاء "مطوي القيد" كانوا يُحذَفون كليًا من الموقع + طلاب الأعضاء المعفَين/المجمَّدين ينتقلون تلقائيًا لـ"على غير مرشدهم" (2026-08-14، ج4)
- **خلل حقيقي اكتُشف من حالة بشرى جمال بكر عمر**: `CollegeRosterParserService` كان يفترض أن حالة الموظف "مطوي القيد" تعني الوفاة، فيحذف العضو **كليًا** من كل الأوراق عند القراءة (لا يظهر حتى مع "إظهار الكل") - افتراض خاطئ صحّحه سليمان: بشرى حيّة، حالتها تغيّرت إداريًا فقط. أُزيل الحذف التام؛ "مطوي" أُضيف بدلاً منه لقائمة الكلمات المفتاحية "المجمَّدة" (`_frozenKeywords` بـ`advising_load_rules.dart`، نفس فئة معار/مجاز/مبتعث) - العضو يبقى ظاهرًا بكل الشاشات لكن بلا إسناد إرشاد فعليًا.
- **قاعدة جديدة عامة بطلب سليمان الصريح**: أي عضو أصبحت حالته معفاة (`exempt`)، مجمَّدة (`frozen`: معار/مجاز/مبتعث/موقوف الراتب/مطوي القيد)، أو حالات خاصة فقط (`specialCasesOnly`: أمين قسم) - طلابه في تقرير "كل الكليات" ينتقلون تلقائيًا إلى تبويب **"على غير مرشدهم"** بدل الاستمرار في الظهور خطأً ضمن "على مرشدهم" لمجرد تطابق القسم. عُدِّلت `AdvisingCaseAnalyzer.classifyAllColleges` (advising_case_analyzer.dart) لتفحص `advisor.advisingLoad` إلى جانب مطابقة القسم.
- **لم يُختبَر فعليًا بعد** - يحتاج رفعة جديدة لملف منسوبي الكلية (بحالة بشرى الجديدة) وملف "كل الكليات"، ثم تحقُّق: بشرى تظهر بالقوائم بحالتها الصحيحة، وطلابها (وطلاب أي عضو آخر معفى/مجمَّد له طلاب) يظهرون بتبويب "على غير مرشدهم".

### إعادة تسمية/ترتيب/تبسيط تبويبي الحالات الصحية بطلب سليمان (2026-08-14، ج3)
- **تبويب "ذوو الإعاقة" انتقل ليكون الثاني** (مباشرة بعد "على مرشدهم") وأُعيدت تسميته **"طلبة ذوي الإعاقة على مرشدهم"** - محتواه تغيّر جوهريًا من "كل حالات ذوي الإعاقة" إلى الطلبة **الموزَّعين فعليًا حسب القاعدة المتَّفَق عليها** فقط (حقل جديد `healthCasesWithAmin` بـ`AdvisingCaseAnalyzer.analyze`، عكس `healthCasesNotWithAmin` تمامًا).
- **تبويب "حالات صحية غير موزَّعة" أُعيدت تسميته "طلبة ذوي الإعاقة على غير مرشدهم"** ونُقل ليكون التبويب الثالث مباشرة بعده، للتماثل مع الأول.
- **كلا التبويبين صار عرضهما قائمة أسماء فقط** (عمود واحد "الطالب/ة" + عدد بالعنوان) بدل جدول تفصيلي كامل - بطلب سليمان الصريح: المهم من هو الطالب المخالف/الملتزم للقاعدة، لا تفاصيله.
- **لم يُختبَر فعليًا بعد** - يحتاج اختبار سليمان الحي: الترتيب الجديد للتبويبات، والقائمتان تُظهران الأسماء الصحيحة (موزَّع/غير موزَّع حسب قاعدة أمين/منسّق القسم).

### إصلاحات إضافية بطلب سليمان من الاختبار الحي - "متابعة حالات الإرشاد" (2026-08-14، ج2)
- **استبعاد كامل لطلاب "إدارة الأعمال التنفيذي" من كل الإحصائيات والإرشاد**: بطلب سليمان الصريح، أي طالب/ة تخصصه "إدارة الأعمال التنفيذي" (برنامج ماجستير تنفيذي) يُستبعَد الآن كليًا من `AdvisingCaseAnalyzer.classifyAllColleges` (لا يظهر حتى بتبويب "مرشدنا ← طلاب خارجيون")، أيًا كان مرشده أو وضعه - أُضيفت `isExecutiveMbaProgram` بـ`academic_department_names.dart`.
- **إصلاح خلل فرز حقيقي بتبويبات التصنيف الأربعة** ("على مرشدهم"/"بلا مرشد"/"مرشد خارجي ← طلابنا"/"مرشدنا ← طلاب خارجيون"): `_classification` (خلافًا لبقية جداول الشاشة) كانت تُعرَض بترتيب الملف الخام بلا أي فرز - لاحظ سليمان الأرقام الجامعية تظهر عشوائية (60، 44880384، 1، 13...) بلقطة شاشة. أُضيف فرز موحَّد حسب الشطر ثم اسم الطالب.
- **تقرير "إعادة التوزيع" أصبح إحصائيًا لا تفصيليًا**: بطلب سليمان الصريح - "لا يهم مين الطالب، المهم كم طالب من مرشد أ ينتقل إلى مرشد ب" - استُبدل سرد كل طالب بالاسم بجدول مجمَّع (قسم/مرشد سابق/مرشد موصى به/**عدد الطلاب المنقولين**) مرتب حسب القسم ثم الأكبر عددًا أولاً.
- **لم يُختبَر فعليًا بعد** (لا Flutter/Dart بهذه البيئة) - يحتاج اختبار سليمان الحي: استبعاد طلاب الأعمال التنفيذي فعليًا من كل التبويبات، الترتيب صحيح بالأرقام الجامعية، وجدول إعادة التوزيع الإحصائي الجديد يعطي أعدادًا صحيحة.

### تتمة جلسة 2026-08-14: إصلاحات إضافية من الاختبار الحي المتواصل لـ"متابعة حالات الإرشاد"
بعد الإصلاحات الموثَّقة بالقسم التالي، واصل سليمان الاختبار الحي واكتشف معًا سلسلة أخطاء حقيقية إضافية:
- **مطابقة الشطر الحرفية الفاشلة (خلل جوهري)**: عمود "الشطر" بملف منسوبي الكلية نص حر ("طلاب" لا "شطر الطلاب" بالضرورة) - كان يُقارَن بالمطابقة الحرفية التامة في `facultyInScope` (advising_case_analyzer.dart)، فتفشل دومًا فعليًا ويُستبعَد كل الأعضاء بصمت. اكتُشف عبر حالة سليمان نفسه: مسجَّل "معفى من الإرشاد" وله طالبان فعليًا، لكن تبويب "معفَون ولهم طلاب" أظهر صفرًا. أثّر هذا فعليًا على 4 تبويبات معًا (معفَون ولهم طلاب/مرشدون بلا طلاب/تقرير النصاب/حالات صحية غير موزَّعة) - أُصلح بدالة تطبيع متسامحة بدل المطابقة الحرفية.
- **احتساب الإداريين ضمن حسابات النصاب**: أعضاء من نوع "إداري" (`CollegeMemberType.admin`) كانوا يظهرون خطأً ضمن "مرشدون بلا طلاب"/"تقرير النصاب"/يُقترَحون كمستقبِلين بـ"إعادة التوزيع" - إداري لا يمكن أن يكون مرشدًا أكاديميًا؛ أُضيف استبعاد صريح لهم.
- **فروع جامعة الطائف (الخرمة/تربة/رنية)**: اكتشف سليمان أن هذه الفروع لها كليات إدارة أعمال بنفس مسمّيات أقسام كليتنا تمامًا ضمن ملف "كل الكليات" الموحَّد، وبنية جدول مختلفة تنتج بيانات وهمية (نص هامش/ترقيم صفحة يُقرأ كصف طالب). حدَّد سليمان أن سطر "الكلية" يظهر أسفل كل كتلة مرشد (تحت "رقم المرشد") ويذكر اسم الفرع صراحةً حين يكون خارج المقر الرئيسي - أُضيف تتبّع لهذا السطر يستبعد كل كتلة تابعة لأحد الفروع الثلاثة بالكامل، دون التأثير على كتل المقر الرئيسي (شاملة حالات انتداب معروفة كطارق حلمي). **لم يُختبَر حيًّا بعد** - يحتاج رفعة جديدة لملف "كل الكليات" بعد هذا الإصلاح.
- تحسينات أصغر: تقريب "الحصة العادلة" بتقرير النصاب لرقم صحيح (بدل كسر عشري)، إضافة عمود "رقم المنسوب" لتبويب "مرشدون بلا طلاب" وعمود "رقم منسوب المرشد" لتبويب "مرشد خارجي ← طلابنا"، حصر قائمة فلتر المرشد بمرشدي كليتنا الفعليين فقط (كانت تعرض أسماء مرشدين من كليات أخرى)، ربط قائمة فلتر المرشد بفلتر الشطر أيضًا (كانت تتجاهله)، إزالة عبارة "(تفاديًا لتجمّد الصفحة)" من رسائل تقليص الجدول لعرضها فقط عند التجاوز الفعلي (300 صف)، زر "نسخ" صريح لنص أي خطأ.
- **معلَّق بلا حسم**: زر "تفريغ البيانات" لملف "كل الكليات" يفشل حاليًا بـ`[cloud_firestore/permission-denied]` رغم أن القواعد المنشورة صحيحة ومطابقة لبقية المجموعات العاملة - الشك الحالي جلسة دخول منتهية الصلاحية، يحتاج تأكيد سليمان بعد إعادة تسجيل الدخول.

### إعادة تصميم "متابعة حالات الإرشاد" إلى 12 تبويبًا + سجل حركات إرشاد تراكمي دائم + إصلاحات حرجة من الاختبار الحي (2026-08-14)
- بعد الرفع الفعلي الأول لملف "كل الكليات" (الجامعة كاملة)، اكتُشفت وأُصلحت سلسلة أخطاء حقيقية بالتتابع:
  - **صلاحيات Firestore ناقصة**: مجموعتا `advisingAllCollegesReports`/`advisingAllCollegesReportsPrevious` كانتا غائبتين تمامًا من `firestore.rules` فتُرفَض كل كتابة عليهما بصمت (`permission-denied`) - أُضيفتا ونُشرت القواعد فورًا.
  - **فشل حفظ الملف الكبير**: `AdvisingReportRepository.save` كان يحفظ كل شيء (حذف القديم + كتابة الجديد) بدفعة Firestore واحدة - كافية لبيانات كليتنا وحدها سابقًا، لكن حجم/عدد عمليات ملف الجامعة كاملة يتجاوز حدود الدفعة الواحدة (500 عملية/~10 ميجابايت) - قُسِّم الحفظ لعدة دفعات صغيرة متتالية.
  - **ازدواج الطلاب بين الشطرين وأرقام غير منطقية**: خريطة "اسم المرشد ← شطره" (`advisorShatrByName`، ضرورية لأن تقرير "كل الكليات" لا يحوي عمود "الجنس") حُذفت سهوًا أثناء إعادة التصميم الأولى، فتعذّر تحديد شطر أي صف تقريبًا وظهر كل طالب بالشطرين معًا (سلوك احتياطي موجود أصلاً في الكود، بلا خطوة لاحقة تزيله كما كان يحدث سابقًا مع "بيانات الطلبة") - أُعيدت الخريطة (تُبنى من ملف منسوبي الكلية عند كل رفعة) + تنبيه صريح لحالات مرشدي الكليات الأخرى (قيد معروف: لا يمكن تحديد شطرهم بهذه الطريقة).
  - **تجمّد الصفحة عند فتح جداول كبيرة**: `DataTable`/`ListView(children:)` في Flutter يبنيان كل صفوفهما دفعة واحدة قبل الرسم (لا تحميل كسول) - يتحمّلان مئات الصفوف بأمان لا عشرات الآلاف (17548 صفًا لبطاقة "طلاب على مرشدهم" جمَّد المتصفح فعليًا). أُضيف حد أقصى للعرض المرئي (300 صف) مع تنبيه صريح **فقط عند التجاوز فعليًا** (لا رسالة صامتة تحت الحد) - التصدير (Excel/PDF) يبقى بلا أي حد دومًا.
  - **نافذة "جاري المعالجة" لا تظهر فعليًا**: معالجة PDF الثقيلة تحجب خيط المتصفح قبل أن تُتاح فرصة رسمها؛ `WidgetsBinding.endOfFrame` لم تكفِ عمليًا - استُبدلت بـ`Future.delayed` بمدة حقيقية (تمر عبر مؤقّت متصفح فعلي لا مهمة دقيقة) لضمان عودة السيطرة لحلقة الأحداث قبل استئناف المعالجة.
- **إعادة تصميم كاملة بطلب سليمان صراحةً**: البطاقات العشر (تبويب "ضبط عملية الإرشاد") والبطاقتان (تبويب "النتائج والإحصائيات") دُمجت في **12 تبويبًا** مستقلاً - كل تبويب يعرض قائمته مضمَّنة تحته مباشرة (لا نافذة منبثقة)، بشريط فلترة موحَّد أعلى التبويبات (شطر/قسم/**مرشد** جديد/بحث) يؤثر على كل التبويبات فورًا. قائمة المرشد تُبنى تلقائيًا من مرشدي القسم المختار (تُعاد بناؤها عند تغييره).
- **"حركات الإرشاد" أصبح سجلاً تراكميًا دائمًا** (لا مقارنة آخر رفعتين فقط كما كان سابقًا): مجموعة Firestore جديدة `advisingAdvisorMovements` (`AdvisorMovementRepository`) - كل رفعة جديدة تقارن نفسها بما هو مخزَّن حاليًا **قبل** استبداله، وتُضيف أي حركة مكتشَفة كمستندات جديدة (لا تُستبدَل أبدًا) - فتبقى كل الحركات التاريخية متاحة مهما تكررت الرفعات، بطلب سليمان الصريح: "لو عشر مرات تظهر الحركات". الأعمدة: بيانات الطالب، ثم **المرشد الحالي**، ثم **المرشد السابق** (بهذا الترتيب تحديدًا كما طلب)، ثم تاريخ الاكتشاف.
- ترتيب عرض الطلاب تغيّر إلى: اسم المرشد أولاً (أبجديًا) ثم الرقم الجامعي (بدل الشطر/القسم سابقًا) - بطلب سليمان صراحةً.
- قسم "كل الطلاب في النطاق الحالي" (كان يعرض كل طالب على حدة) حُذف نهائيًا لصالح نظام التبويبات الجديد الأخف.
- **لم يُختبَر التصميم الجديد بالكامل فعليًا بعد** (لا Flutter/Dart بهذه البيئة) - يحتاج اختبار سليمان الحي الشامل بعد كل هذه الإصلاحات مجتمعة.

### إعادة هيكلة "متابعة حالات الإرشاد" - رفع مبسّط (أيقونتان) + تقرير حركات الإرشاد (2026-08-14)
- بطلب سليمان صراحةً: "بيانات الطلبة الأكاديمية" (ملف 14 عمودًا يحوي المعدل/الساعات) يصعب الحصول عليها حاليًا من المنظومة خلال فترة الحذف والإضافة - جُمِّد رفعها من هذه الشاشة مؤقتًا (تبقى الخدمات/البيانات القديمة بالمشروع بلا حذف، لحين توفّر الملف لاحقًا).
- **الأولوية الجديدة**: توزيع الطلبة على المرشدين، عبر ملف أسبوعي واحد "كل الكليات" (تقرير "طلاب تابعين لمرشد" الرسمي **غير مفلتَر** - يشمل الجامعة كاملة بدل كليتنا فقط كسابقًا) بدل أربعة تقارير منفصلة (قاعدة/تابعين لمرشد/غير تابعين/على غير مرشدهم). شريط الرفع الآن أيقونتان فقط: "رفع جميع الطلاب (كل الكليات)" و"رفع طلبة ذوي الإعاقة".
- **تصنيف جديد** (`AdvisingCaseAnalyzer.classifyAllColleges`) يقارن قسم المرشد (من ملف منسوبي الكلية، المصحَّح فعليًا لحالات الانتداب مثل طارق حلمي/حنان عامر تلقائيًا بلا استثناء أسماء) بقسم الطالب لإخراج: طلاب على مرشدهم، طلاب على غير مرشدهم (فارغة تمامًا = رسالة "لا يوجد طلبة على غير مرشدهم" بدل قائمة فارغة صامتة، كما طلب سليمان)، مرشدون من خارج الكلية يرشدون طلبة من داخلها، ومرشدون من داخل الكلية يرشدون طلبة من خارجها.
- **تقرير حركات الإرشاد** (`AdvisingCaseAnalyzer.detectAdvisorMovements`): كل رفعة جديدة لملف "كل الكليات" تُقارَن تلقائيًا بالرفعة السابقة (تُحفَظ عبر `AdvisingReportRepository.promoteAllCollegesToPrevious`، بنفس مبدأ نسخة القاعدة السابقة الموجود أصلاً)، فتظهر بطاقة "حركات الإرشاد" بكل طالب تغيّر اسم مرشده بين الرفعتين (من ← إلى).
- بطاقات "ضبط عملية الإرشاد" (تقرير النصاب/إعادة التوزيع/مرشدون معفَون/مرشدون بلا طلاب) بقيت **بلا أي تغيير جوهري بمنطقها** - فقط تغيّر مصدر تغذيتها من الأنبوب القديم (قاعدة+ربط+تعارض) إلى سجلات "كل الكليات" المفلتَرة لكليتنا (تحمل أصلاً اسم/قسم المرشد بلا حاجة دمج).
- بطاقتا "طلاب بحاجة متابعة (المعدل)" و"طلاب مفصولون أكاديميًا" (تعتمدان على `base` المجمَّدة) أُزيلتا مؤقتًا من الشاشة - ستعودان حين يتوفّر ملف "بيانات الطلبة الأكاديمية" الجديد.
- **تبسيط حقيقي**: أُضيفت دالة تحميل مشتركة `AdvisingCaseAnalyzer.loadCollegeScopedStudents()` تستهلكها الآن أربع شاشات كانت تكرّر نفس تسلسل التحميل/الدمج يدويًا (متابعة حالات الإرشاد، لوحة الإرشاد، بحث عن مرشد وقائمة طلابه، صفحة منسّق القسم) - بدل تكرار التعديل أربع مرات بنفس الطريقة.
- **حركات الإرشاد قابلة للطباعة/PDF أيضًا**: بطلب سليمان صراحةً (2026-08-14)، أُضيف زر "PDF/طباعة" لحوار "حركات الإرشاد" (كان Excel فقط) بنفس نمط بقية الحوارات (`AdvisingCasePdfService`). حوارا "طلاب على غير مرشدهم" والتصنيفات الأربعة (على/غير مرشدهم، مرشدون من داخل/خارج الكلية) كانا يدعمان Excel وPDF أصلاً.
- **لم يُختبَر فعليًا بعد** (لا Flutter/Dart بهذه البيئة) - يحتاج رفع ملف "كل الكليات" حيًّا واختبار سليمان: تصنيفات القسم الأربعة صحيحة، رسالة "لا يوجد طلبة على غير مرشدهم" تظهر فعليًا عند عدم وجود حالات، وحركات الإرشاد تُظهر فقط الطلاب الذين تغيّر مرشدهم فعليًا بين رفعتين متتاليتين (Excel وPDF كلاهما).

### تصحيح الأسماء المختصرة وتصميم عمودين لتقرير كل الأقسام (2026-08-13)
- **تصحيح شامل للأسماء**: سليمان لاحظ أن أسماء المرشدين بتقارير PDF تظهر مختصرة (اسم أول وأخير فقط) بدل الاسم الكامل بملف منسوبي الكلية. صُحِّحت كل الملفات العشرة بمطابقة رقم المكتب (لا الاسم نفسه، تفاديًا لتشابه الأسماء) مقابل ملف منسوبي الكلية.
- **خلل حقيقي اكتُشف وأُصلح أثناء التصحيح**: ملف "التسويق_طلاب.xlsx" تبيّن أنه الوحيد من العشرة غير مبني بمكتبة الموقع (Excel حقيقي بجدول SharedStrings منفصل) - أول محاولة تصحيح له أنتجت فهارس خاطئة (خطأ حسابي ضاعف عدّاد الفهرس) ففسد الملف بصمت. أُعيد من النسخة الاحتياطية وأُصلحت الدالة، ثم تحقُّق نهائي: الملفات العشرة كلها تُقرأ بلا خطأ بالعدد الصحيح للصفوف.
- **ملاحظة بيانات جانبية**: التصحيح كشف أن "براء حسن احمد الحازمي" مسجَّل مرتين بصفَّين منفصلين بملف "التسويق_طلاب" (نفس رقم المكتب) - يحتاج مراجعة سليمان.
- **تصميم عمودين لتقرير "كل الأقسام"**: بعد إصلاح تداخل الأقسام (فاصل الصفحة)، لاحظ سليمان أن أقسامًا كبيرة العدد (الاقتصاد والتمويل/طالبات، 27 عضوًا) لا تكتمل بصفحة واحدة. أُضيف تخطيط عمودين جنبًا إلى جنب لجدولي الفترتين (بدل التكديس الرأسي) يقلّل الارتفاع المطلوب للنصف تقريبًا - مع حد أمان (يُطبَّق فقط لفترتين بالضبط و≤70 عضوًا إجمالًا) يعود عنده التصميم للتكديس الرأسي الأصلي القابل للتقسيم بين الصفحات، تفاديًا لخطر تجمّد `pw.Column` اللانهائي الموثَّق سابقًا.
- **لم يُختبَر أي منهما فعليًا بعد** (لا Flutter/Dart بهذه البيئة، ولا اختبار حي بعد) - بانتظار نشر ورفع سليمان.

### إضافة الأعضاء الناقصين لتوزيع فترات الإرشاد + إصلاح تداخل الأقسام بالتقرير (2026-08-13)
- راجعنا ملف `تدقيق_توزيع_فترات_الإرشاد.xlsx` (ورقة "ناقص - له إرشاد ولا فترة") - 8 أعضاء لهم نصاب إرشاد لكن غائبون عن جدول توزيع الفترات. تبيّن أن 3 منهم أعضاء وحدة فعليون (حسن الزبير، صالح العريفي، اميرة الفقيه - لديهم حضور كل الأيام أصلاً فلا يحتاجون توزيعًا)، ولمية الحسناوي طُوي قيدها (لم تعد عضوًا - أُصلح ملف منسوبي الكلية بالفعل). تبقّى **4 أعضاء فعليون** يحتاجون توزيعًا: بشير بابكر، احمد سبحي، شروق الثبيتي، معاذ الذنيبات.
- طبَّقنا قاعدة سليمان (70/30): يومان بالفترة الأولى + يوم واحد بالفترة الثانية لكل عضو، مع توزيع يوم الفترة الثانية بالتساوي على الأيام الثلاثة بين الأعضاء الأربعة (الأحد×2، الاثنين×1، الثلاثاء×1).
- أُضيفوا مباشرة لملفات "نماذج جاهزة للرفع" الموجودة (`الاقتصاد والتمويل_طلاب.xlsx`، `الاقتصاد والتمويل_طالبات.xlsx`، `نظم المعلومات الإدارية_طلاب.xlsx`) عبر سكربت Node.js - **بلا أي تعديل على التوزيع المعتمَد سابقًا لبقية الأعضاء** (نسخ احتياطية قبل كل تعديل، وتحقُّق كامل بعده أن كل الصفوف السابقة سليمة).
- **إصلاح خلل بصري حقيقي بالتقرير الشامل**: سليمان لاحظ في تقرير "توزيع فترات الإرشاد - كل الأقسام" أن جدول قسم الإدارة يلتصق مباشرة بعنوان قسم المحاسبة بلا فاصل، بشكل غير احترافي. السبب: `AdvisingSchedulePdfService.buildAll` كان يُلحِق كل قسم/شطر بنفس اليوم بقائمة عناصر مسطَّحة بلا فاصل صفحة بينها. أُضيف `pw.NewPage()` بين كل قسم وآخر (نفس مبدأ الفاصل بين الأيام الموجود أصلاً) - تحقَّقتُ أنه الموضع الوحيد بالكود بهذا النمط.
- **خلل حقيقي اكتُشف عند أول محاولة رفع فعلية من سليمان**: التعديل الأول على ملفات الإكسيل استخدم مكتبة SheetJS (Node)، التي أفسدت جدول numFmt الداخلي بـstyles.xml عند إعادة حفظ الملف (خطأ "custom numFmtId starts at 164 but found a value of 56") - ففشلت قراءة 3 من 4 ملفات بالموقع (الذي يستخدم مكتبة `excel` الأكثر صرامة بـFlutter). أُصلح جذريًا: استعادة الملفات من النسخ الاحتياطية، ثم إعادة إضافة الأعضاء بتعديل XML الداخلي مباشرة (`add_missing_schedule_members_raw.js` عبر `adm-zip`) بلا لمس `styles.xml` إطلاقًا - تحقُّق: الملف مطابق بايت لبايت للأصل في كل جزء عدا الصفوف المضافة. نُقلت أيضًا النسخ الاحتياطية خارج مجلد الرفع بعد اختيارها بالخطأ ضمن رفعة العشرة ملفات معًا.
- **لم يُنشَر أي كود من هذا على الموقع الحي بعد (إصلاح PDF)** - ملفات الإكسيل المحدَّثة والمُصلَحة جاهزة الآن للرفع اليدوي مباشرة.

### تصحيح بيانات حسن الزبير - منسّق قسم التسويق (2026-08-13)
- سليمان صحّح أن حسن عبد الرحيم حسن الزبير (أمين قسم التسويق) يشغل أيضًا منصب منسّق قسم التسويق لوحدة الإرشاد لشطر الطلاب - معلومة صحيحة لم تكن مسجَّلة إطلاقًا بملف منسوبي الكلية.
- أُضيف "منسق وحدة" لعمود "منصب آخر (احتياطي - إن وجد)" بملف `المرفقات/أعضاء هيئة التدريس/قالب البيانات النهائي .xlsx` مباشرة عبر سكربت Node.js (SheetJS) - نسخة احتياطية محفوظة قبل التعديل. تحقُّق: 164 منسوبًا سليمين، 3 أوراق كاملة.
- **تصنيف إرشاده الفعلي لم يتغيّر عمدًا** (يبقى "حالات خاصة فقط") - قاعدة `advising_load_rules.dart:259` تُعطي أولوية "أمين قسم" حين يكون لنفس قسم العضو، فتبقى صحيحة رغم إضافة المنصب الثاني. التعديل يُصحِّح فقط بيانات الهوية الوظيفية (البحث/العرض).
- قسم التسويق الآن له منسّقان مسجَّلان بشكل صحيح: حسن الزبير (طلاب) واميرة الفقيه (طالبات).
- **لم يُرفَع الملف المُعدَّل على الموقع الحي بعد** - بانتظار رفع سليمان اليدوي عبر شاشة "بيانات منسوبي الكلية".

### بناء ملف "طلاب تابعين لمرشد" الموحَّد (إدارة الأعمال + الحاسبات) (2026-08-13)
- بطلب سليمان: بُني الملف بدل تركه له - سكربتان مؤقتان في `tool/admin_cli/` (خارج بنية Flutter، أدوات جلسة واحدة لا جزء من الموقع):
  - `extract_college_pages.js`: يفتح ملف "جميع الكليات" الضخم (2147 صفحة) بمكتبة `pdfjs-dist` (طبقة نص PDF مباشرة، أسرع بكثير من محرّك الموقع نفسه)، ويحدّد كلية كل صفحة من سطر "الكلية :" (القيمة تسبق التسمية في ترتيب استخراج pdf.js - نفس ملاحظة اتجاه RTL الموثَّقة أصلاً بـ`pdf_table_rows_extractor.dart`)، وخريطة إزالة "أشكال العرض العربية" (`arabic_deshape.js`) منقولة حرفيًا من نفس الملف حتى تُطابِق قراءة الموقع بالضبط. النتيجة: كلية إدارة الأعمال 298 صفحة، كلية الحاسبات وتقنية المعلومات 233 صفحة (فهرس محفوظ بـ`college_pages_index.json`).
  - `merge_college_pages.js`: يستخرج الصفحات المطابقة فقط بمكتبة `pdf-lib` ويدمجها بملف واحد جديد.
- **الناتج**: `المرفقات/صفحه الارشاد/طلاب تابعين لمرشد-كلية إدارة الأعمال والحاسبات 13-08-2026م.pdf` (531 صفحة). تحقّق يدوي: صفحة الانتقال بين الكليتين سليمة، وبلوك طارق حلمي (رقم المرشد 4280812) موجود فعليًا بصفحات الحاسبات بتخصص طلابه "نظم المعلومات الإدارية" بالضبط - سيمرّ تلقائيًا عبر فلتر `_filterToOurCollegeDepartments` المُضاف سابقًا اليوم بلا أي استثناء يدوي.
- **تحديث لاحق نفس اليوم**: الملف الموحَّد (531 صفحة) كان كبيرًا جدًا وعلَّق فعليًا عند سليمان عند فتحه/رفعه. بطلبه، استُخرج بدلاً منه (`extract_tariq_only.js`، سكربت مؤقت جديد بنفس الأسلوب) ملف مصغَّر جدًا يحوي فقط كتلة طارق حلمي (3 صفحات/104 كيلوبايت/101 طالبًا) - `المرفقات/صفحه الارشاد/طلاب تابعين لمرشد-طارق حلمي فقط 13-08-2026م.pdf`. تحقّق كامل من محتواه (كل الطلاب تخصصهم "نظم المعلومات الإدارية"، الكتلة كاملة ونظيفة). القرار معلَّق: رفعه مستقلاً (يحتاج دعم رفع أكثر من ملف واحد، غير موجود حاليًا) أم دمجه مع ملف كلية إدارة الأعمال وحدها فقط.

### بحث عن مرشد وقائمة طلابه (2026-08-13)
- شاشة جديدة `advisor_students_lookup_screen.dart` (من "لوحة الإرشاد") بطلب سليمان صراحةً: مربع بحث باسم المرشد الأكاديمي يعرض قائمة طلابه كاملة (الرقم الجامعي/الاسم/التخصص) داخل بطاقة قابلة للطي لكل مرشد مطابق للبحث.
- **بلا أي قارئ أو مخزن جديد**: تُستهلَك مباشرة بيانات تقرير "طلاب تابعين لمرشد" (`AdvisingReportRepository.load(kind: assigned)`) المرفوعة أصلاً عبر شاشة "متابعة حالات الإرشاد" الموجودة سابقًا - أعيد استخدام القارئ والتخزين المُثبَتين بلا أي تعديل عليهما.
- **قرار مهم اتُّخذ أثناء البناء**: جُرِّب أولًا الاعتماد على ملف "تقرير الارشاد النهائي.xlsx" (3 أوراق مجمَّعة يدويًا) كمصدر لهذه الميزة (نصاب عادل + حالة وظيفية لكل مرشد)، لكن سليمان صحّح الاتجاه صراحةً: "لا تعتمد على ملف الإكسل - الاعتماد يكون على الملفات المصدَّرة من المنظومة مباشرة". حُذف الكود المبني على ذلك الملف (لم يُنشَر) قبل إكماله. بعدها أرسل سليمان ملف PDF مصدَّر مباشرة من المنظومة ("طلاب على مرشديهم-الكلية 13-08-2026م-احدث مع طارق.pdf"، 295 صفحة، مفلتَر لكليتنا فقط ويشمل حالة الانتداب الخاصة لطارق حلمي بشكل صحيح) - بنفس تنسيق تقرير "طلاب تابعين لمرشد" المدعوم أصلاً، فلم تكن هناك حاجة لأي قارئ جديد إطلاقًا.
- **تحديث لاحق نفس اليوم**: سليمان لاحظ أن ملف طارق حلمي المدموج (قسم "طلاب على غير مرشدهم" بشكل جدول مختلف كليًا في نهاية الملف) يخلط شكلين مختلفين بملف واحد. الحل الأنظف: استخراج كليتَي إدارة الأعمال والحاسبات كاملتين من ملف "جميع الكليات" الأصلي بنفس الشكل الموحَّد (كتل مرشد/طلاب)، ثم فلترة تلقائية بعمود "التخصص" تُبقي فقط أقسام كليتنا الخمسة المعروفة (`isKnownBachelorDepartment`) - يشمل هذا "نظم المعلومات الإدارية" تلقائيًا فتمر حالات طارق حلمي بلا أي استثناء يدوي بالكود، بينما يُستبعَد بقية طلاب كلية الحاسبات (علوم حاسب، أمن سيبراني...) تلقائيًا. أُضيف فلتر `_filterToOurCollegeDepartments` في `advising_cases_admin_screen.dart` (يُطبَّق فقط على رفع PDF لتقرير "طلاب تابعين لمرشد")، مع تنبيه صريح بعدد/تخصصات الصفوف المستبعَدة قبل الاعتماد.
- لم يُختبَر بعد فعليًا - لا يوجد Flutter/Dart بهذه البيئة لتشغيله، والاختبار الحي (رفع الملف الموحَّد الجديد عبر "متابعة حالات الإرشاد" ثم فتح شاشة البحث الجديدة) بانتظار سليمان.

### خطة بديلة وقائية: أداء دور المنسّق من لوحة الإدارة (2026-08-13)
- بطلب سليمان صراحةً بعد ملاحظة صعوبة بعض المنسّقين باستخدام الموقع فعليًا: أُضيف لصفحة "تنزيل ملفات الحالات" بلوحة الإدارة (كانت تحتوي فقط "تفريغ الحالة"/"تنزيل ذوي الإعاقة"/"تنزيل ملفات المرشدين") زرّان جديدان لكل قسم/شطر:
  - **"تنزيل ملف مرحلة المنسّق"** (`_downloadStage2OnBehalf`) - نفس ملف `EscalationFileService.buildStage2File` الذي ينزّله المنسّق بنفسه.
  - **"رفع ملف معالج"** (`_pickAndUploadProcessedFileOnBehalf`) - نفس دمج `FirestoreTicketService.mergeProcessedRows` الذي يستخدمه المنسّق، بقسم/شطر يُحدَّدان صراحةً بدل قراءتهما من حساب المسجَّل دخوله.
- الهدف إجراء وقائي احتياطي فقط: تستطيع الإدارة أداء دور أي منسّق قسم بالكامل (تنزيل ورفع) بالتوازي مع استمرار عمل حساب المنسّق نفسه، حتى لا تتعطل دورة العمل الحرجة - لا يستبدل حساب المنسّق ولا يعطّله.
- لم يُنشَر بعد - بانتظار اكتمال بقية الإصلاحات المتفق عليها بالجلسة ثم نشر واختبار حي دفعة واحدة.

### إصلاح مرجَّح لتعليق رفع الملفات المعالجة بصفحة المنسّق بلا نهاية (2026-08-13)
- المشكلة (لاحظها سليمان فعليًا 2026-08-09): زر "رفع الملفات المعالجة" بصفحة المنسّق يعلّق أيقونة الرفع بلا توقف، بلا خطأ ولا نجاح، حتى بعد انتظار طويل - بقية الصفحة تبقى مستجيبة.
- تشخيص جلسة سابقة عبر Admin SDK استبعد بطء الاستعلام/الكتابة نفسها كسبب أساسي. تشخيص هذه الجلسة: العرض يطابق تمامًا مشكلة معروفة بمكتبة Firestore للويب - قناة البث الافتراضية (WebChannel) تُحجَب/تُعلَّق بصمت خلف بعض شبكات الجامعات/الشركات (بروكسي أو فحص TLS) بلا أي خطأ ظاهر للتطبيق.
- **الإصلاح**: فُعِّل بـ`main.dart` عند تشغيل نسخة الويب `FirebaseFirestore.instance.settings = Settings(webExperimentalAutoDetectLongPolling: true)` - يكتشف Firestore تلقائيًا حاجة الشبكة لـLong-Polling كبديل ويحوّل له بدل التعليق الصامت؛ الحل الرسمي الموصى به من Firebase لهذه الحالة.
- **لم يُنشَر بعد** - بانتظار موافقة سليمان ثم اختبار حي فعلي (رفع ملف معالج حقيقي) للتأكد أن التعليق زال قبل اعتباره محسومًا نهائيًا.

### تشخيص وإصلاح جذري حقيقي لتجمّد PDF + تدقيق شامل لمطابقة أسماء توزيع الإرشاد (2026-08-10)
- **تشخيص فعلي بمتصفح حقيقي (Puppeteer) على الموقع الحي**: بعد فشل إصلاحين سابقين مبنيين على تخمين، سُجِّل دخول فعلي وضُغط زر "عرض PDF - شاشات العرض" مع "الكل"، وروقبت مقاييس المتصفح (`page.metrics()`) لأكثر من 20 دقيقة - `ScriptDuration`/`JSHeapUsedSize` يتصاعدان خطيًا بلا توقف بلا نهاية، يؤكد حلقة "إضافة صفحات فارغة بلا نهاية" الشهيرة بمكتبة `pdf` عندما يتعذّر على `MultiPage` وضع Widget في أي صفحة.
- **السبب الجذري الحقيقي**: `pw.Column` **لا يدعم التقسيم بين صفحات PDF إطلاقًا** (بخلاف `pw.Table` المصمَّم لذلك تحديدًا) - أي قسم أكاديمي كبير (حتى 26 مرشدًا بفترتين بخط شاشات العرض الضخم) مُجمَّع داخل Column واحد يتجاوز ارتفاعه أي صفحة منفردة، فيدخل محرّك التخطيط بالحلقة اللانهائية. إصلاحان سابقان (إزالة `pw.Container`، إزالة إجبار صفحة مستقلة) عالجا أعراضًا جانبية فقط. **الإصلاح الحقيقي**: تسطيح كامل للبنية - كل عنصر (شريط عنوان القسم، عنوان كل فترة، جدول كل فترة) يُضاف مباشرة لقائمة محتوى الصفحة بلا أي Column يجمعها.
- **تدقيق شامل جديد**: ملف `المرفقات/تدقيق_توزيع_فترات_الإرشاد.xlsx` (3 أوراق: مقارنة كاملة لكل الأعضاء الـ164، تعارضات معفى-وله-فترات، ناقصون-لهم-إرشاد-ولا-فترة) يقارن تصنيف الإرشاد الفعلي (بنفس منطق `AdvisingLoadRules.classify` بالضبط) مع من يظهر فعليًا بملفات توزيع الإرشاد. بالطريق اكتُشفت وأُصلحت 3 أخطاء مطابقة أسماء حقيقية بالكود (`_normalizeArabic` بـ`advising_schedule_admin_screen.dart`): توحيد التاء المربوطة/الهاء ("نورة"/"نوره")، ودمج "أبو X" بلا مسافة (بجانب "عبد X" المُصلَح سابقًا اليوم) - رفعت نسبة المطابقة من 106 إلى 133 من أصل 164 عضوًا. النتيجة النهائية: صفر تعارضات (لا معفى ظهر له فترات)، و14 حالة "ناقصة" متبقية (تحتاج مراجعة سليمان)، إحداها خطأ إملائي مؤكَّد بالملف المصدر لا بالمطابقة ("يراء" بدل "براء" الحازمي).

### ترتيب تقرير PDF الشامل حسب اليوم أولًا + عرض الأسماء عند اختيار "الكل" (2026-08-10)
- **PDF الشامل** (`advising_schedule_pdf_service.dart`, `buildAll`): كان يبني صفحة/صفحات كاملة لكل قسم (بكل أيامه) ثم ينتقل للقسم التالي؛ الآن يبني صفحة لكل **يوم** (الأحد فالاثنين فالثلاثاء)، وداخل كل يوم قسمًا فرعيًا مرتَّبًا لكل (قسم × شطر) له بيانات بذلك اليوم - بطلب سليمان صراحةً ليطابق ترتيب الجداول الرسمية المعتادة.
- **شاشة "توزيع فترات الإرشاد"**: عند اختيار "الكل" للقسم و/أو الشطر كانت الشاشة تعرض نصًا فقط بلا أي بيانات فعلية (لازم اختيار قسم وشطر محدَّدين لرؤية الأسماء). أُضيفت معاينة كاملة (`_buildFilteredPreview`) بنفس منطق ترتيب PDF (يوم ← قسم/شطر ← فترة) تعمل مباشرة على الشاشة لأي تركيبة فلترة.

### إصلاح جذري لتوزيع فترات الإرشاد (أسماء أيام حقيقية) + بناء نماذج الرفع من ملفات الأقسام + استكمال أرقام المكاتب (2026-08-10)
- **خلل جذري مُصلَح**: نموذج توزيع فترات الإرشاد (`advising_schedule_excel_service.dart`) كان يسجّل "اليوم الأول/الثاني/الثالث" (تسمية عامة مجرَّدة) بدل اسم اليوم الفعلي (الأحد/الاثنين/الثلاثاء) - هذا سبب ظهور صفحة العرض والطباعة بشكل غير منظَّم لا يعتمد على اليوم/الفترة الحقيقيين (لاحظه سليمان). الإصلاح: `dayColumnLabels` صارت أسماء الأيام الحقيقية مباشرة (تحقَّقت أن كل الأقسام الخمسة تستخدم نفس الأيام الثلاثة فعليًا)، مع ترتيب صريح جديد بصفحة العرض (`advising_schedule_admin_screen.dart`) وملف PDF (`advising_schedule_pdf_service.dart`) حسب ترتيب الأسبوع الفعلي ثم الفترة - بدل ترتيب الظهور العشوائي بالبيانات المرفوعة.
- **10 نماذج إكسل جاهزة للرفع** بُنيت من ملف `ملف_الإرشاد_الأكاديمي_جميع_الأقسام_محدث.xlsx` (5 أقسام × شطرين) بنفس صيغة نموذج الموقع بالضبط - محفوظة بـ`المرفقات/المرشدين/توزيع فترات الارشاد/توزيع فترات الارشاد محدث/تحديث/نماذج_جاهزة_للرفع/`. بالطريق اكتُشف خلل تجميع حقيقي: مرشدة واحدة ("البندري الشهراني") انقسمت لصفّين منفصلين بسبب اختلاف لقبها "د."/"أ." بين يوم وآخر بالمصدر - أُصلح بالتجميع على الاسم بلا لقب.
- **استكمال 90 رقم مكتب فارغًا** (من أصل 102) بملف منسوبي هيئة التدريس، بمطابقة الأسماء تلقائيًا مع ملف توزيع الإرشاد (مطابقة جزئية تتحمّل اختلاف الألقاب وترتيب الأسماء) - 12 عضوًا تعذّرت مطابقتهم آليًا (انظر TODO.md).
- تحققت أولاً أن تحليل ملفات Word الخام للأقسام (عبر `AdvisingScheduleParserService` غير المفعَّل أصلاً بالشاشة الحالية) يعاني قصورًا حقيقيًا (أسماء مقطوعة، فشل كامل لقسم بلا ألقاب) - جهّز سليمان بدلاً منه ملف إكسل موحَّد نظيف لكل الأقسام، فاعتُمد عليه مباشرة بدل إصلاح ذلك المُحلِّل.

### فصل عمودي "نصاب الإرشاد" و"النصاب التدريسي" + حالة "مجمّدة" + قوائم منسدلة بملف الكلية (2026-08-10)
- **الخلل المكتشف**: ملف "قالب البيانات النهائي بعد تحديث المبتعثين" حدّث عمود "نصاب الإرشاد" (تصنيف صريح للإرشاد: بدون تخفيض نصاب/50%/معفي من الارشاد/حالات خاصة) لـ137 عضوًا، لكن الكود كان يقرأ حقلًا واحدًا فقط (`quotaReductionNote`) يُعطي الأولوية لعمود "قرار تخفيض النصاب" (خاص بالنصاب **التدريسي** لا الإرشاد) ثم يمرّره خطأً لتصنيف الإرشاد أيضًا - فتصنيف الإرشاد لمعظم الأعضاء كان يعتمد فعليًا على عمود النصاب التدريسي بدل عمود الإرشاد الفعلي.
- **الإصلاح**: حقل جديد مستقل `advisingQuotaNote` (`college_roster_member.dart`، `college_roster_parser_service.dart`) يُقرأ حصرًا من عمود "نصاب الإرشاد" ويُمرَّر لـ`AdvisingLoadRules.classify` بدل `quotaReductionNote` (الذي بقي للنصاب التدريسي فقط).
- **حالة جديدة `AdvisingLoad.frozen`** ("الحالة مجمّدة"، رمادي) في `advising_load_rules.dart`: أولوية مطلقة تتغلّب حتى على قيمة عمود "نصاب الإرشاد" لمن كانت حالة الموظف موقوف الراتب/معار/مبتعث/مجاز - لا يُسند له إرشاد ولا يُحتسَب "مرشد بلا طلاب" (`advising_case_analyzer.dart`)، بصفته تجميدًا مؤقتًا لا قرار إعفاء دائم.
- بطلب سليمان: أُعيدت تسمية عمود "قرار تخفيض النصاب" في ملف الإكسل الفعلي إلى **"النصاب التدريسي"** (أوضح دلاليًا)، مع إبقاء الاسم القديم كمسار احتياطي في القارئ لأي ملف لم يُحدَّث بعد.
- **محاولة قائمة منسدلة (Data Validation) داخل ملف الإكسل فشلت**: جُرِّبت لعمودي "نصاب الإرشاد" و"النصاب التدريسي" لمنع أخطاء الكتابة اليدوية، لكن تبيّن أنها تكسر قراءة الملف بمكتبة `excel` (v4.0.6) المستخدَمة فعليًا بالموقع (رسالة "تعذّر قراءة الملف" عند الرفع) - أُزيلت تمامًا وأُعيد بناء الملف نظيفًا. **البديل المعتمَد**: تحقّق داخل التطبيق نفسه عند الرفع (`_invalidQuotaValues` في `college_roster_admin_screen.dart`) يعرض تنبيهًا بأي قيمة غير معروفة بالعمودين ضمن حوار تأكيد الاعتماد، مقابل قوائم القيم الصحيحة الموثَّقة في `AdvisingLoadRules.validAdvisingQuotaValues`/`TeachingLoadRegulation.validQuotaReductionValues`.
- **تنظيف مجلد "المرفقات/أعضاء هيئة التدريس"**: حذف كل النسخ الاحتياطية والملفات القديمة غير المتتبَّعة في git (كانت تسبب لخبطة في تحديد أي ملف يُرفَع) - أبقي فقط الملف الرسمي الحالي.

### فرز جدول منسوبي الكلية بالضغط على رأس العمود (2026-08-09)
- طلب سليمان صراحةً إضافة فرز بالضغط على رأس العمود لجدول "منسوبي الكلية" (`college_roster_admin_screen.dart`) على أعمدة: الاسم، القسم/الجهة، المنصب، عبء الإرشاد، العبء الدراسي - بنفس أسلوب المواقع الاحترافية (ضغطة تفرز تصاعديًا، ضغطة أخرى تعكسها).
- التطبيق: `DataColumn.onSort` القياسي بـFlutter مع حالة `_sortKey`/`_sortAscending` تتغلّب على الفرز الافتراضي (`FacultySortOrder`) عند اختيار المستخدم فرزًا يدويًا، وترقيم `sortColumnIndex` يُحسَب ديناميكيًا حسب الأعمدة الظاهرة فعليًا (تتغيّر حسب فلاتر القسم/الشطر النشطة). لا تعديل على جدول الإداريين المنفصل (`_buildAdminTable`).
- **بطلب سليمان صراحةً، هذا النمط محفوظ ليُطبَّق لاحقًا على جداول "بيانات الطلبة" فور بنائها.**

### إصلاح احتساب النصاب التدريسي المخفَّض للمناصب الإدارية (وكيل/عميد/رئيس قسم كلية) (2026-08-09)
- المشكلة: صالح قاضي (وكيل كلية) ظهر نصابه بالأحمر رغم مطابقته فعليًا للحد الأدنى (3 ساعات) - المطابقة كانت تبحث عن تطابق تام لحقل المنصب، فتفوت الصياغة الطبيعية "وكيل كلية إدارة الأعمال"/"عميد كلية...".
- الإصلاح (`lib/data/teaching_load_regulation.dart`): مطابقة ببادئة النص لعميد/عميدة/وكيل/وكيلة/رئيس قسم/رئيسة قسم/رئيس مركز البحوث والاستشارات/مكلف بالحد الأدنى (مستشار استُثنيت عمدًا وبقيت تطابقًا تامًا لتفادي الخلط مع لقب "مستشار عميد شؤون الطلاب" الشرفي)، مع تفسير نص "الحد الأدنى" الحر بملاحظات تخفيض النصاب كـ3 ساعات مباشرة.
- تحقّق فعلي ضد كل الحالات المشابهة الـ12 بقاعدة البيانات الحية (بلا رجعية على حالات مُصلَحة سابقًا مثل الأستاذ نفسه وآلاء بارفعه).

### تحديث تطبيق الجوال Android إلى 1.0.11+12 (2026-08-09)
- بناء ونشر إصدار جديد من تطبيق الجوال (نكهة `advising`) يشمل كل إصلاحات هذه الجلسة (الصفحة البيضاء، دورة عمل المنسّق، خلل حفظ حالات الإنجاز، تسجيل الخروج التلقائي، كلمات المرور، إلخ) - رُفع لـGitHub Release (`salfawaz15/SSS`, tag `app-latest`) وحُدِّث `apk_url`/رقم الإصدار بمستند Firestore `app_config` تلقائيًا.

### جلسة طارئة: إصلاح "الصفحة البيضاء" لمنسّقي الأقسام + إعادة تصميم دورة العمل + خلل جذري بعدم حفظ حالات الإنجاز (2026-08-09 ليلاً)
- **المشكلة الأصلية**: صفحة منسّق القسم تظهر فارغة تمامًا (فراغ أبيض تحت الهيدر) على جهاز سليمان الفعلي فقط - قبل اجتماع عاجل مع إدارة الكلية. تشخيص مطوَّل جدًا (اختبار آلي بمتصفح حقيقي عبر Puppeteer + حساب اختباري مؤقت، مقارنة شجرة التخطيط/الوصولية بالبكسلات الفعلية) أثبت أن المحتوى يُبنى بشكل صحيح تمامًا لكن لا يُرسَم بصريًا (paint) لسبب لم يُحسَم نهائيًا - **ليس قاعدة عامة**: `PortalStatCard`/`PortalIconTileCard` نجحا بصفحات أخرى (منسّق الكلية، لوحة الإرشاد) وفشلا بهذه الصفحة تحديدًا مرتين متتاليتين حتى بعد نسخ نمط ناجح حرفيًا. **الحل النهائي المعتمَد**: `Card`/`ListTile`/`CircleAvatar`/`ElevatedButton.styleFrom` (مكوّنات Material قياسية) بدل أي `Container`/حواف مدوّرة مخصَّصة - مؤكَّد يعمل بعد اختبار حي متكرر. راجع الذاكرة `feedback_material_rounded_corners_blank_render` لتفاصيل التشخيص الكامل قبل أي تصميم جديد لأي بوابة.
- **إعادة تصميم دورة عمل منسّق القسم بالكامل** (`coordinator_workspace_screen.dart`، `coordinator_nav.dart`): سليمان صرّح أن تصميم التبويبات المخفية "صعب حتى عليّ أنا اللي أبني الموقع معك". الصفحة الرئيسية الآن تعرض دورة العمل الكاملة كخطوات مرقَّمة واضحة مباشرة (بطاقة `Card` لكل خطوة، بعنوان ورقم ووصف): (1) تنزيل ملف القسم، (2) رفع ملفات المرشدين المعالَجة + تثبيت مرحلتهم، (3) تنزيل الملف المدمج لمرحلة المنسّق نفسه (يظهر فقط بعد تثبيت مرحلة 2)، (4) تثبيت مرحلة المنسّق وإرسالها لمنسّق الكلية. "حالات الظروف الخاصة" و"الدعم النفسي والاجتماعي" بشريط التنقّل العلوي مباشرة.
- **خلل جذري مُصلَح**: `FirestoreTicketService._mergeProcessedRowsIntoDocs` كانت تكتب لحقول قديمة غير مستخدَمة (`status`/`notes`/`completed_by`) بدل الحقول الثلاثة الفعلية المنفصلة (`advisor_status`/`coordinator_status`/`college_status` وملاحظاتها) التي يقرأها فعليًا `effectiveStatus`/`effectiveCompletedBy` بـ`report_data_service.dart` - **كل حالات الإنجاز المرفوعة من ملفات المرشدين كانت تُهمَل بصمت بلا أي أثر** منذ إضافة الأعمدة الثلاثة المنفصلة. الإصلاح: تحديث كل حقل فقط لو وُجدت له قيمة فعلية بالصف المرفوع (بلا مسح حقل جهة أخرى).
- **إصلاحات إضافية بنفس الجلسة**: تسجيل خروج تلقائي بعد 15 دقيقة خمول (`inactivity_auto_logout.dart` جديد، يلفّ `PortalRoot`)؛ إصلاح فقدان الجلسة عند تحديث الصفحة (F5) بجعل `Persistence.LOCAL` دائمًا بدل الاعتماد على "تذكرني"؛ تحديث كل كلمات مرور الحسابات إلى `00000000` ما عدا `salfawaz` (سكربت `tool/admin_cli/reset_all_passwords.js` جديد) + تحديث ملف Word "جدول_حسابات_البوابة.docx"؛ حساب "منسّق الوحدة" أُنشئ فعليًا بـFirebase Auth وصفحته استُبدلت برسالة ترحيبية مؤقتة فقط بطلب سليمان؛ توحيد ألوان بطاقات صفحة "تقارير" حسب تصنيف كل بند بدل ألوان عشوائية؛ تصحيح حساب "عدد الشعب غير المسكَّنة" بصفحة "خدمات أكاديمية" ليعتمد على وجود محاضر مُسنَد للشعبة بدل موعد الجدول (كان يساوي صفر دائمًا خطأً)؛ تقليل عدد كتابات Firestore بدمج الملفات المعالَجة (تُكتب فقط التذاكر المتأثرة فعليًا) + مهلة 25 ثانية تُظهر خطأ واضح بدل تعليق أبدي عند رفع الملفات.
- مشكلة معلَّقة (لم تُحسَم): تعليق "رفع الملفات المعالجة" أحيانًا بلا خطأ ولا اكتمال - اشتُبه أنه بطء/انقطاع بجانب اتصال سليمان بـFirestore (استعلام تجريبي عبر Admin SDK أخذ ~11 ثانية لـ6 مستندات فقط، بطء غير طبيعي)، لم يُحسَم نهائيًا - راجع TODO.md.

### إعادة هيكلة شاشتَي منسّق القسم ومنسّق الكلية: من تبويبات نصية إلى شبكة أيقونات + رسم بياني ثابت (2026-08-09)
- طلب سليمان صراحة (مراجعة ليلية لإعادة تصميم أُنجزت سابقًا بنفس الليلة): تبويب "الرئيسية" زائد لوجود شريط تنقّل مماثل أعلى الصفحة أصلاً، وبقية الوجهات يجب أن تصير أيقونات بنفس هوية `PortalIconTileCard` (أيقونة أعلى + عنوان تحتها، خلفية صلبة ملوّنة) التي اعتمدها صراحة كمثال "تسكين المقررات الدراسية/الجدول الدراسي/المنسوبين"، مع رسم بياني `FollowUpChart` ثابت أسفل الشبكة يملأ الفراغ بدل الحاجة لفتحه من وجهة منفصلة.
- **`lib/web_portal/coordinator_workspace_screen.dart`**: حُذف تبويب "الرئيسية" والـ`TabController`/`TabBar` بالكامل. الصفحة الجديدة: بطاقتا إحصاء (عدد الحالات، نسبة الإنجاز) + تذكير المواعيد كما كانا، ثم **شبكة 5 أيقونات** `PortalIconTileCard` (متجاوبة عبر `SliverGridDelegateWithMaxCrossAxisExtent` كأسلوب `reports_hub_screen.dart`): (1) متابعة حالات الظروف الخاصة، (2) متابعة حالات الدعم النفسي والاجتماعي (كانتا بطاقتين داخل تبويب "الرئيسية" المحذوف)، (3) الرفع والتنزيل، (4) متابعة الإنجاز، (5) مسار التصعيد. الثلاث الأخيرة (كانت تبويبات نصية) تُفتَح الآن كصفحة فرعية كاملة (`PortalScaffold` بزر رجوع، عبر دالة مساعدة جديدة `_openSection`) بنفس المحتوى والمنطق بالضبط (تنزيل/رفع/تفريغ/تجميد مراحل - لا حذف لأي وظيفة). أسفل الشبكة مباشرة: لوحة "متابعة العمل" الثابتة بنفس `FollowUpChart` المستخدَم داخل صفحة "متابعة الإنجاز" (بلا أزرار طباعة/تصدير هنا، تلك بقيت في أيقونتها).
- **`lib/web_portal/college_coordinator_workspace_screen.dart`**: نفس المبدأ بالضبط. حُذفت التبويبات الثلاث ("الرفع والتنزيل"، "متابعة الحالات"، "الأداء والتقارير")؛ شريط الإحصائيات العلوي بقي كما هو، تحته **شبكة أيقونتين** (الرفع والتنزيل، متابعة الحالات) تفتحان كصفحة فرعية بنفس آلية `_openSection`، ثم لوحة "الأداء والتقارير" (كانت التبويب الثالث بـ`FollowUpChart` وفلترة الأقسام) ثابتة أسفل الشبكة دائمًا بلا حاجة لفتحها من مكان منفصل.
- `dart analyze` نظيف تمامًا على الملفين المعدَّلين وعلى المشروع كاملاً (نفس 18 ملاحظة `info` سابقة الوجود غير متعلقة بهذا التعديل). لا commit ولا نشر من أي نوع.

### ميزة جديدة "بحث سريع" (Command Palette) بأيقونة ثابتة بالهيدر (2026-08-09)
- طلب سليمان: تسهيل التنقّل بدل تصفّح القوائم يدويًا - يكتب المستخدم كلمة مثل "جداول" فيحصل على نتيجة قابلة للضغط تنقله مباشرة، بنفس أسلوب أيقونة البحث بموقع سدايا المرجعي (ثابتة بكل صفحة، اقتراحات جاهزة كشرائح قبل الكتابة، نتائج قابلة للفلترة بالكتابة).
- ملف جديد **`lib/web_portal/quick_search_index.dart`**: كلاس `QuickSearchEntry {title, keywords, icon, onTap}` بدالة `matches(query)` (تطابق العنوان أو أي كلمة مفتاحية، بلا حساسية لحالة الأحرف/المسافات)، ودالة `buildQuickSearchEntries(context, {role, isSuperAdmin, uid, shatr, department})` تُعيد فقط عناصر بحث حقيقية (`Navigator.push` لشاشة موجودة فعليًا) لدور المستخدم الحالي - لا منطق جديد، إعادة استخدام كاملة لنفس وجهات `admin_workspace_screen.dart`/`admin_nav.dart`/`reports_hub_screen.dart`.
- **`lib/web_portal/portal_header.dart`**: أيقونة `Icons.search` أُضيفت مباشرة بجانب `_PortalAccountMenu` (نفس الموضع بكلا تخطيطي isWide/narrow) فتظهر بكل صفحات البوابة تلقائيًا بلا تعديل كل شاشة. الضغط عليها يحسم دور المستخدم أولًا (`PortalAccounts` + قراءة `coordinator_accounts` لمنسّق القسم بنفس آلية `reports_hub_screen.dart`) ثم يفتح حوار بحث: مربع نص، اقتراحات جاهزة كشرائح `ActionChip` (أول 8 عناصر) قبل الكتابة، وقائمة نتائج مطابقة فور الكتابة - الضغط على أي عنصر يُغلق الحوار وينفّذ `onTap` فورًا.
- **تغطية العناصر حسب الدور**: المدير العام (غير المدير العام الخارق) 8 عناصر (تقارير متابعة الحذف والإضافة، مركز التقارير، متابعة حالات الإرشاد، لوحة الإرشاد، حالات الظروف الخاصة، الدعم النفسي والاجتماعي، دليل تشغيل البوابة، خريطة الموقع)؛ المدير العام الخارق (salfawaz) +5 إضافية (تسكين المقررات، الجدول الدراسي، المنسوبين، توزيع فترات الإرشاد، خدمات أكاديمية) = 13؛ حساب "عرض فقط" عنصر واحد؛ منسّق الكلية عنصران؛ منسّق القسم 4 عناصر (لوحة المنسّق، تقارير، حالات الظروف الخاصة، الدعم النفسي والاجتماعي)؛ منسّق الوحدة صفر عناصر عمدًا (دوره حصرًا رفع الملفات).
- `dart analyze` نظيف تمامًا على الملفين الجديد/المعدَّل، وعلى المشروع كاملاً (نفس 18 ملاحظة `info` سابقة الوجود غير متعلقة بهذا التعديل). لا commit ولا نشر من أي نوع.

### شاشة موحّدة جديدة "تقارير" (مركز تقارير مُفلتَر بالبحث والدور) (2026-08-08)
- طلب سليمان: كل مخرجات الموقع القابلة للطباعة/التصدير كانت مشتَّتة بين شاشات مختلفة - المطلوب مكان واحد بفلترة/بحث بدل هذا التشتّت، دون إعادة بناء منطق أي تقرير من الصفر.
- ملف جديد **`lib/web_portal/reports_hub_screen.dart`**: شاشة `PortalScaffold` بعنوان "تقارير" فيها شريط بحث نصي (يبحث بالعنوان + وصف قصير لكل بند)، فلتر تصنيف رئيسي (الكل/تقارير/ملفات عمل/أدلة ونماذج) عبر `ChoiceChip`، وفلتر نوع فرعي (تقرير إرشاد/جدول دراسي/نصاب تدريسي/حالات خاصة/بيانات منسوبين/دليل تشغيلي/نموذج ورقي). النتائج تُعرض كبطاقات `PortalActionCard` بشارة تصنيف ملوّنة أعلى كل بطاقة.
- **القائمة مبنية بالكامل ديناميكيًا حسب دور المستخدم الحالي** (`FirebaseAuth.instance.currentUser?.email` مع `PortalAccounts.isFullAdmin`/`isCollegeCoordinator`/`isUnitCoordinator`/`viewerEmails`؛ ولمنسّق القسم الافتراضي تُقرأ شطره/قسمه من مستند `coordinator_accounts` بنفس آلية `CoordinatorWorkspaceScreen` تمامًا): المدير العام يرى كل تقارير الإدارة (17 بند تقرير)، حساب "العرض فقط" يرى تقريره الشامل المبسّط فقط، منسّق الكلية يرى ملف تنزيل كل الأقسام المدمج، منسّق القسم الافتراضي يرى تقرير متابعة قسمه وملفات العمل الثلاثة الخاصة به، ودليل/نماذج تصنيف 3 (6 بنود) ظاهرة للجميع بلا استثناء. منسّق الوحدة (رفع فقط) لا يرى أي تقرير كالمخطَّط.
- **كل بطاقة تفتح الشاشة الأصلية نفسها** (`Navigator.push`) بلا أي إعادة كتابة لمنطق التوليد - كل الـ22 بندًا المذكورة بالمواصفة رُبطت فعليًا (بفتح شاشتها الأصلية الكاملة، لأن كل تقرير مبني بعمق داخل شاشة ذات حالة معقّدة يصعب فصلها بأمان: `AdminReportsScreen`، `AdvisingCasesAdminScreen`، `CourseScheduleAdminScreen`، `AdvisingScheduleAdminScreen`، `CoordinatorWorkspaceScreen(uid)`، `CollegeCoordinatorWorkspaceScreen(uid)`، `ViewerReportsScreen`، `UnitGuidePage`، `PortalOperationsGuidePage`، `GuidesHubPage`، `FormsPage`) - لا رابط تعذّر ربطه.
- تبويب "تقارير" جديد أُضيف لشريط التنقّل العلوي في `admin_nav.dart` (`buildAdminNavItems`)، `coordinator_nav.dart` (`buildCoordinatorNavItems`)، و`college_coordinator_workspace_screen.dart` (قائمة تنقّله الخاصة) - بمفتاح تحديد `current: 'reports-hub'` (مختلف عمدًا عن `'reports'` المستخدَم مسبقًا لتقرير "متابعة الحذف والإضافة" بلوحة الإدارة، حتى لا يتصادم تظليل التبويب النشط بين الاثنين).
- `dart analyze` نظيف تمامًا على كل الملفات الجديدة/المعدَّلة، وعلى المشروع كاملاً (نفس 18 ملاحظة `info` سابقة الوجود غير متعلقة بهذا التعديل). لا commit ولا نشر من أي نوع.

### بنية تقنية جديدة لدور "منسّق الوحدة" (رفع الملفات فقط) - بلا ربط بشخص فعلي بعد (2026-08-08)
- طلب سليمان صراحة: إنشاء **البنية التقنية العامة فقط** لدور جديد اسمه "منسّق الوحدة" - منصب قيد الموافقة الرسمية، بقيد صارم: لا يُضاف أي اسم شخص حقيقي لأي ملف/قائمة/شاشة بالموقع، ولا يُنشأ حساب Firebase Auth فعلي.
- آلية الأدوار المكتشَفة: لا custom claims - كل دور يُحدَّد ببريد Firebase Auth ثابت داخلي (`xxx@sss-advising-tu.internal`) يُطابَق في `lib/web_portal/portal_accounts.dart`، ثم `PortalRoot` (`lib/web_portal/portal_root.dart`) يوجّه حسب البريد بعد الدخول، وقواعد Firestore (`firestore.rules`) تكرّر نفس فحص البريد مباشرة (`request.auth.token.email`) لأدوار بلا تفريعات (كالمدير العام)، أو عبر مستند حساب منفصل (`coordinator_accounts`/`college_coordinator_accounts`) للأدوار المفرَّعة بشطر/قسم.
- **`lib/web_portal/portal_accounts.dart`**: أُضيف بريد ثابت `unitCoordinatorEmail = 'unit-coordinator@sss-advising-tu.internal'` ودالة `isUnitCoordinator(email)` - بلا أي خريطة أسماء أشخاص (بريد واحد فقط، بعكس منسّقي الأقسام المفرَّعين بـ11 حسابًا).
- **`lib/web_portal/portal_root.dart`**: فرع توجيه جديد يفحص `isUnitCoordinator` قبل فرع "المنسّق الافتراضي" مباشرة (لتفادي وقوعه خطأً ضمن منسّق قسم عادي لأنه بلا مستند `coordinator_accounts`).
- **`lib/web_portal/portal_login_screen.dart`**: قيمة رابعة لـ`_LoginMode` باسم `unitCoordinator` ("منسّق الوحدة") - دخول مباشر بلا أي حقل شطر/قسم إضافي.
- **ملف جديد `lib/web_portal/unit_coordinator_workspace_screen.dart`**: شاشة `PortalScaffold` بلا `navItems` (لا يرى بقية أقسام البوابة) وبلا أي قراءة/عرض بيانات - فقط شبكة `PortalActionCard` لكل نقطة رفع موجودة فعليًا بالموقع: الملف الأساسي (طلبات الحذف والإضافة)، جدول الطلاب/الطالبات الدراسي، مواد خارج الكلية للشطرين، بيانات منسوبي الكلية، توزيع فترات الإرشاد، والتقارير الخمسة لمتابعة حالات الإرشاد (القاعدة/تابعين لمرشد/غير تابعين/الحالة الصحية/غير مرشدهم). كل بطاقة تستدعي غلاف رفع رفيع خاص بالشاشة يستدعي نفس خدمات التحليل والتخزين الفعلية (`ExcelParserService`، `FirestoreTicketService`، `DocxScheduleParserService`، `CourseScheduleRepository`، `OutsideCourseRepository`، `CollegeRosterParserService`/`Repository`، `AdvisingScheduleExcelService`/`Repository`، `AdvisingReportParserService`/`PdfParserService`/`Repository`) المستخدَمة فعليًا في شاشات الإدارة الأصلية - بلا تكرار منطق التحليل نفسه، وزر "تسجيل الخروج" واضح بالأعلى (بما أن الشاشة بلا `navItems` ولا قائمة "⋮").
- **`firestore.rules`** (محليًا فقط - **لم يُنشَر**): دالة `isUnitCoordinator()` تفحص بريد `unit-coordinator@sss-advising-tu.internal` مباشرة، مع صلاحية على نفس المسارات التي يكتب إليها الأدمن/المدير العام عند الرفع فقط: `tickets`، `courseSchedules`، `outsideCourses`، `collegeRoster`، `advisingSchedules`، وكل تقارير `advisingReports*` الستة (بما فيها `advisingReportsPrevious` التي يحرّكها اعتماد تقرير جديد، ومجموعات `chunks` الفرعية لكل منها بقاعدة صريحة منفصلة). **ملاحظة تقنية مهمة**: مُنحت قراءة+كتابة لا كتابة فقط على هذه المسارات تحديدًا، لأن منطق الرفع نفسه (الذي أُعيد استخدامه من الشاشات الأصلية بلا تعديل) يقرأ من نفس المسار قبل الكتابة في كل الحالات (تحقّق تاريخ آخر رفعة/دمج تراكمي/ترحيل النسخة السابقة) - بلا هذه القراءة يفشل الرفع نفسه بخطأ صلاحيات. لا قراءة مُنحت على أي مسار آخر (تقارير الظروف الخاصة، حسابات المنسّقين، إلخ).
- **ضمان عدم ظهور أي اسم شخص**: لا تعديل على أي ملف `.xlsx` تحت "المرفقات/"، ولا إضافة أي اسم لأي شاشة - البريد الثابت والتسمية "منسّق الوحدة" هما فقط ما أُضيف، وكلاهما تسمية دور عامة لا اسم شخص. لم يُنشأ أي حساب Firebase Auth فعلي (لا `createUserWithEmailAndPassword` استُدعي لهذا البريد) - الكود الجاهز فقط يتيح إنشاءه لاحقًا بنفس الآلية المستخدَمة أصلاً بالمشروع (`reset_user_password_screen.dart`).
- `dart analyze` نظيف على كل الملفات المعدَّلة/الجديدة (والمشروع كاملاً - 18 ملاحظة `info` سابقة الوجود غير متعلقة بهذا التعديل). لا commit ولا نشر ولا `firebase deploy` من أي نوع.

### إعادة تصميم شاشة "لوحة الإرشاد" بشريط إحصائيات + شبكة إجراءات 2×2 (2026-08-08)
- طلب سليمان صراحة: الشكل الحالي (قائمة رأسية من `PortalActionCard` فقط، بلا أي رقم) غير مُرضٍ، وأعطى مخططًا تقريبيًا (شريط إحصائيات أفقي أعلى الصفحة ثم شبكة إجراءات 2×2 تحته).
- `lib/web_portal/advising_hub_screen.dart` أُعيد بناؤه بالكامل (تحوَّل من `StatelessWidget` إلى `StatefulWidget`) بلا حذف أي وظيفة موجودة: أعلى الصفحة شريط أفقي من 4 بطاقات `PortalStatCard` (نفس مكوّن لوحة الإدارة) - "مرشدون لديهم طلاب"، "مرشدون بدون طلاب"، "طلاب لهم مرشد"، "طلاب بلا مرشد" - ثم تحته شبكة `GridView.count` 2×2 من `PortalActionCard` (عمود واحد على الشاشات الضيقة) لأربع صفحات: متابعة حالات الإرشاد (حصريًا للمدير العام كسابقًا)، متابعة حالات الظروف الخاصة، متابعة حالات الدعم النفسي والاجتماعي، وأُضيفت لأول مرة هنا "توزيع فترات الإرشاد" (`AdvisingScheduleAdminScreen`) التي كانت معزولة سابقًا (متاحة فقط من لوحة الإدارة العامة) رغم أنها إجراء إرشاد أساسي.
- الأرقام **ليست** محسوبة بمنطق جديد: تُقرأ من نفس تحليل `AdvisingCaseAnalyzer.analyze` (وخطوات الدمج `mergeAdvisorLinks`/`mergeGapsFromMismatchReport`/`mergeHealthConditions`/`mergePreviousGpa`) المستخدَم فعليًا في `AdvisingCasesAdminScreen`، مطبَّقًا على نفس تقارير `AdvisingReportRepository` (القاعدة/الربط/تعارض القسم/الحالة الصحية) وملف منسوبي الكلية (`CollegeRosterRepository`) لكلا الشطرين ثم جمعهما. إن لم تُرفَع أي تقارير بعد، تُعرض الأرقام صفرًا بهدوء بدل رسالة خطأ.
- `dart analyze` نظيف على الملف المعدَّل (والمشروع كاملاً - 18 ملاحظة `info` سابقة الوجود غير متعلقة بهذا التعديل).

### إعادة تصميم شاشة منسّق القسم بتبويبات واضحة (2026-08-08)
- طلب سليمان صراحة: "صفحة مبعثرة جدًا جدًا جدًا" - يجد هو نفسه صعوبة فيها فما بالك بمنسّق يستخدم البوابة لأول مرة، رغم أن عمل المنسّق معروف (رفع/تنزيل ملفات وإضافة أشياء).
- `lib/web_portal/coordinator_workspace_screen.dart` أُعيد بناؤه بالكامل بلا حذف أي وظيفة موجودة (تحقَّق أولاً من كل مستدعٍ للشاشة عبر grep: `mobile_advising_home_screen.dart` و`portal_root.dart` - كلاهما يستدعيها بـ`uid` فقط، فبقي توقيع `CoordinatorWorkspaceScreen({uid})` كما هو تمامًا): شريط إحصائيات علوي (`PortalStatCard`) بعدد حالات القسم ونسبة الإنجاز، ثم أربع تبويبات (`TabController`/`TabBar` عبر `PortalScaffold.bottom`، بنفس نمط شاشة منسّق الكلية): "الرئيسية" (تذكير المواعيد + بطاقتا الدخول لحالات الظروف الخاصة والدعم النفسي والاجتماعي)، "الرفع والتنزيل" (تنزيل ملف القسم، رفع الملفات المعالجة، تنزيل ملف ذوي الإعاقة إن وُجد، تفريغ حالة القسم كإجراء تحذيري منفصل بالأسفل)، "متابعة الإنجاز" (نفس قسم المتابعة والرسم البياني كما كان)، و"مسار التصعيد" (نفس تجميد المراحل كما كان). كل زر في تبويب "الرفع والتنزيل" أصبح داخل بطاقة شرح موحَّدة (`_buildActionExplainer`) بسطر توضيحي بسيط قبل الزر.
- `dart analyze` نظيف على `coordinator_workspace_screen.dart` و`coordinator_nav.dart` (لم يحتج تعديلاً).

### إعادة تصميم شاشة منسّق الكلية بتبويبات واضحة (2026-08-08)
- طلب سليمان صراحة (وكرَّره لمنسّق الكلية بعد أن قاله عن منسّق القسم): الصفحة كانت "مبعثرة جدًا" - أزرار متتالية بلا تجميع منطقي يصعب على منسّق كلية جديد فهمها.
- `lib/web_portal/college_coordinator_workspace_screen.dart` أُعيد بناؤه بالكامل بلا حذف أي وظيفة: شريط إحصائيات علوي (`PortalStatCard`) بأرقام فعلية من بيانات الشطر (إجمالي الحالات، عدد الأقسام، نسبة الإنجاز العامة)، ثم ثلاث تبويبات (`TabController`/`TabBar` عبر `PortalScaffold.bottom`) بنفس فكرة تبويبات منسّق القسم: "الرفع والتنزيل" (تنزيل الملف المدمج + رفع الملفات المعالجة)، "متابعة الحالات" (بطاقة عدد الحالات + تثبيت تقرير مرحلة منسّق الكلية + آخر تقرير مجمَّد)، و"الأداء والتقارير" (`FollowUpChart` كاملاً بما فيه ترتيب منسّقي الأقسام ومنسّقي الكلية أنفسهم عبر `showCollegePerformance`). كل زر أصبح تحته شرح سطر واحد يوضّح غرضه.
- `dart analyze` نظيف على الملف. لا تغيير في توقيع `CollegeCoordinatorWorkspaceScreen({uid})` فبقي التوافق مع مستدعِييه (`mobile_advising_home_screen.dart`، `portal_root.dart`) كما هو.

### تلوين حالات النصاب التدريسي حسب طلب سليمان (2026-08-08)
- الكود كان يفرّق أصلاً بين نوعين من الزيادة: `overWithinRounding` (فارق تقريب غير قابل للتحكم لأن المقررات 3 ساعات - مثلاً نصاب 10 لا يتحقق بالضبط فيُؤخذ 12) و`over` (زيادة حقيقية فوق ذلك الهامش) - لم يكن مستغَلاً بصريًا بشكل يعكس الفرق.
- طلب سليمان تلوينًا واضحًا: دون النصاب = أحمر، مطابق = أخضر، زائد إجباري (فارق التقريب) = برتقالي، زائد اختياري (الزيادة الحقيقية) = لون مميز بتصميم شارة مستقلة كشارة "عبء الإرشاد" (`_loadChip` في `college_roster_admin_screen.dart`) بدل نص ملوّن عادي.
- التنفيذ (`course_schedule_admin_screen.dart`): `_quotaColor` حُدِّثت للتوزيع الجديد (أحمر/أخضر/برتقالي/أرجواني)، وأُضيفت `_optionalOverageBadge` بنفس تصميم حبة "عبء الإرشاد" (خلفية وحدود بلون مميز) تُستخدم لحالة الزيادة الاختيارية في كل من بطاقة عضو هيئة التدريس (`_quotaNoteBox`) وجدول تقرير النصاب.

### إضافة عضوة فعلية ساقطة من ملف منسوبي الكلية + تصحيح درجتين علميتين (2026-08-08)
- بمتابعة مقارنة ملف "للتأكيد من البيانات.xlsx" الرسمي: تبيّن أن "بشرى جمال بكر عمر" (4300198، معيد، الاقتصاد والتمويل) غير موجودة بأي ورقة بملف منسوبي الكلية رغم أن حالتها الرسمية "على رأس عمله" (وليست "مبتعث خارجي" كبقية الحالات المشابهة المكتشَفة بنفس المقارنة) - أُضيفت لورقة "منسوبو الكلية" مباشرة (181 عضوًا الآن) مع إعادة ترتيب كامل الورقة بنفس منطق `FacultySortOrder`.
- صُحِّحت الدرجة العلمية لعضوين حسب تأكيد سليمان: أشرف حسن أحمد الحاج ومزمل عوض طه أحمد من "أستاذ مساعد" إلى "أستاذ مشارك" (تطابقًا مع الملف الرسمي). تنبيه تم تفاديه أثناء التنفيذ: أول محاولة طابقت أيضًا "أشرف كمال عثمان إبراهيم" (شخص مختلف تمامًا) بمطابقة نصية فضفاضة على كلمة "اشرف" - اكتُشف فورًا وأُعيد لدرجته الصحيحة "أستاذ مساعد".
- **قرار سليمان صريح**: أرقام موظفي 10 مبتعثين ظهروا "مختلفين" عن الملف الرسمي بالمقارنة (رعد الغامدي، شيماء القرشي، هند الثبيتي، أروى الثقفي، لانا الشريف، أريج الغامدي، رهام القثامي، ندى الزهراني، لينا الربيعي، روان الحارثي) - أرقام ملفنا **سليمة تمامًا ومعتمدة**، لا تُعدَّل (انظر TODO.md للتفاصيل).

### إضافة "تاريخ التعيين" لترتيب الأعضاء عند تساوي الدرجة العلمية + مقارنة ملف التأكيد الرسمي (2026-08-08)
- **مقارنة**: قورن ملف "للتأكيد من البيانات.xlsx" (مصدر رسمي جديد من منظومة الجامعة، 180 صفًا) بالملف الرئيسي "قالب البيانات النهائي..." عبر رقم الموظف (بعد توحيد الأرقام العربية الهندية والإنجليزية، لأن الملف الجديد يكتبها هندية بالكامل فتفشل أي مطابقة نصية مباشرة). النتائج: 19 عضوًا في الملف الجديد غير مطابقين في ورقة "منسوبو الكلية" (أغلبهم مبتعثون موجودون فعليًا بورقة "المبتعثون" المنفصلة، و12 مبتعثًا جديدًا فعليًا غير موجودين بأي ورقة بالملف الرئيسي بعد)، و3 أعضاء بالملف الرئيسي غير موجودين بالجديد (طارق حلمي - موقوف الراتب، ميسر الجبول - مطوي قيده/متوفى، ورقية بدوي الزبيدي بفارق رقم موظف ظاهري 4310115 بالجديد مقابل 4311115 بالرئيسي - **سليمان أكّد بصورة من المنظومة أن 4311115 هو الرقم الصحيح**، فالخطأ المطبعي في ملف "للتأكيد من البيانات" الجديد وليس بملفنا، لا حاجة لأي تصحيح). اختلافات القسم بين 161 صفًا مشتركًا كانت غالبًا صياغية فقط (مثل "الاقتصاد و التمويل" مقابل "الاقتصاد والتمويل")، مع استثناءين حقيقيين بالدرجة العلمية (أستاذ مشارك بالجديد مقابل أستاذ مساعد بالرئيسي) يحتاجان تحققًا يدويًا من العمادة.
- **عمود جديد**: أُضيف عمود "تاريخ التعيين" لنهاية أعمدة ورقة "منسوبو الكلية" بالملف الرئيسي، مُعبَّأ من عمود "تاريخ الإلتحاق بالجامعة" (نص هجري) بالملف الجديد عبر مطابقة رقم الموظف - 161 عضوًا مُعبَّأ، 3 بلا مطابقة بقوا فارغين كما هم (لا تاريخ مُخترَع). أُخذت نسخة احتياطية أولاً (`...xlsx.backup-2026-08-08-before-appointment-date.xlsx`). ورقتا "الإداريين" و"المبتعثون" لم تُلمَسا.
- **قاعدة ترتيب جديدة (بطلب سليمان)**: عند تساوي الدرجة العلمية بين عضوين، الأقدم تعيينًا (تاريخ التحاق أبكر بالجامعة) يتقدَّم على من التحق لاحقًا - أُضيف حقل `appointmentDate` لـ`CollegeRosterMember`، وقراءته بـ`college_roster_parser_service.dart`، ومقارنته بـ`faculty_sort_order.dart` (`compareMembers`) بعد الدرجة العلمية وقبل رقم المنسوب (الذي بقي fallback أخير). التاريخ الهجري "يوم-شهر-سنة" يُحوَّل لقيمة عددية قابلة للمقارنة (سنة×10000+شهر×100+يوم) ويتحمّل الأرقام العربية والإنجليزية معًا.
- ملفات مُعدَّلة: `lib/models/college_roster_member.dart`، `lib/services/college_roster_parser_service.dart`، `lib/data/faculty_sort_order.dart`. `dart analyze` نظيف على الثلاثة.

### إصلاح حساب نصاب التدريس المخفَّض وحساب الساعات الزائدة الحقيقية (2026-08-08)
- المشكلة: ملف بيانات منسوبي الكلية أعاد تنظيم أعمدة المنصب، وأصبح "قرار تخفيض النصاب" (50%، الحد الأدنى...) بعمود منفصل بينما `college_roster_parser_service.dart` كان لا يزال يبحث عن عمود باسم "ملاحظات تخفيض النصاب" (اسم قديم غير موجود بالملف الجديد) - فسقط نصاب من له تكليف خارجي (مثل سالم آل قظيع، خليل الرويتعي، فؤاد جعماني) للحد الكامل بدل المخفّض، فظهرت رسالة "نصاب غير مكتمل" خطأً رغم استيفائهم فعليًا.
- الحل: أُضيف اسم العمود الفعلي "قرار تخفيض النصاب" لقائمة الأسماء المقبولة في `college_roster_parser_service.dart`.
- **قاعدة عمل جديدة (بطلب سليمان)**: الساعات "الناقصة" تُحسب دائمًا مقابل النصاب الفعلي المخفّض (كالسابق)، لكن الساعات "الزائدة" الحقيقية (التي تُصرف ماليًا) يجب أن تُقاس حصرًا مقابل نصاب الدرجة العلمية الكامل بدون أي تخفيض - تخفيض النصاب لمنصب إداري يعفي العضو من جزء من العبء لكن لا يخفّض سقف الزيادة المدفوعة. مثال: نصاب مخفّض 7 (50% من 14) و9 ساعات فعلية = مكتمل النصاب فقط، وليس له ساعتان زائدتان (لأن 9 لم تتجاوز الـ14).
- ملفات مُعدَّلة: `lib/services/college_roster_parser_service.dart`، `lib/web_portal/course_schedule_admin_screen.dart` (`_quotaCompare` يقبل الآن `fullRankMaxHours` منفصلاً عن `maxHours` المخفّض، ويُستخدم حصرًا في حساب حالة/ملاحظة الزيادة).

### تقسيم عمود "حالة الإنجاز" لثلاث مراحل (مرشد/منسق قسم/منسق كلية) + مؤشر أداء المنسّقين (2026-08-08)
- المشكلة: كل ملفات Excel المصدَّرة (المرشد، منسّق القسم، منسّق الكلية، بريد منسّق القسم، ملف ذوي الإعاقة) كانت تشترك بعمود واحد "حالة الإنجاز" + "ملاحظات" + "أُنجز من قبل" رغم مرورها بـ3 جهات مختلفة عبر مسار التصعيد - فكل جهة كانت تكتب فوق ما كتبته الجهة السابقة، فتُفقد ملاحظات/حالة المرحلة السابقة عند التصعيد.
- الحل: 6 أعمدة بدل 3 - "حالة الإنجاز من قبل المرشد الأكاديمي"/"ملاحظات المرشد الأكاديمي"، ثم نفس الزوج لمنسّق القسم، ثم لمنسّق الكلية. كل ملف مُصدَّر (`advisor_zip_service`, `disability_file_service`, `escalation_file_service` بمرحلتيه، `mail_service`) يفتح للتعديل عمودَي مرحلته فقط، ويُبقي أعمدة المراحل السابقة مقفلة كمرجع للقراءة - فتُحفَظ كل المراحل بنفس الملف بدل التكاتب. حُذف عمود "أُنجز من قبل" اليدوي - يُستنتَج تلقائيًا الآن (أول عمود من الثلاثة يحمل "تم الإنجاز" تصعيديًا).
- **مقياسان منفصلان تمامًا** بطلب سليمان: (1) "هل الحالة محلولة فعليًا" (`effectiveStatus` - يعتمد آخر حالة غير فارغة تصعيديًا: مرشد ← منسّق قسم ← منسّق كلية، يُستخدم بالإجمالي العام وتقارير الحالات المعلّقة وPDF). (2) "هل الشخص اشتغل على حالاته أم لا" (`StatusCounts.addWorked` - أي قيمة مكتوبة = اشتغل، فارغ = لم يشتغل، بصرف النظر عن نوعها؛ حتى لو كل حالات المرشد "لم يتم" فنسبة أدائه 100%) - يُستخدم حصرًا لترتيب/عدّادات أداء المرشد ومنسّق القسم ومنسّق الكلية بلوحة "متابعة الإنجاز" (`follow_up_chart.dart`)، وهو مؤشر جديد كليًا لأداء المنسّقين (لم يكن موجودًا سابقًا - سؤال سليمان الذي بدأ هذه المهمة).
- ملفات مُعدَّلة: `excel_export_service.dart` (أعمدة/فهارس)، `excel_protection_service` (استدعاءات القفل/الدروب-داون بالملفات الأربعة)، `processed_file_parser_service.dart` (قراءة الأعمدة الستة من الملف العائد)، `ticket_repository.dart` (دمج الحقول الستة)، `report_data_service.dart` (`effectiveStatus`/`effectiveCompletedBy`/`addWorked`/`DepartmentReport.coordinatorCounts`/`ShatrReport` الجديد/`rankedCoordinators`/`rankedCollegeCoordinators`)، `follow_up_chart.dart` (قسمان جديدان لأداء منسّقي الأقسام والكلية)، `report_filter_service.dart`/`admin_reports_screen.dart`/`report_pdf_service.dart` (تحويل قراءة الحالة المباشرة لـ`effectiveStatus`).
- **صلاحية العرض**: سليمان لاحظ أن منسّق القسم لا يجب أن يرى قسم "أداء منسقي الكلية" (مستوى أعلى منه) بلوحة متابعة الإنجاز، رغم أن حساب الإدارة يجب أن يرى كل المستويات. أُضيف `showCollegePerformance` (افتراضيًا `true`) لـ`FollowUpChart` ويُعطَّل فقط عند استدعائه من `coordinator_workspace_screen.dart` - شاشة منسّق الكلية والإدارة يريان كل شيء كالمعتاد.
- **اختُبر فعليًا** بتسجيل دخول حساب منسّق قسم تجريبي - القسمان الجديدان (أداء منسقي الأقسام/الكلية) ظهرا صحيحين بأسماء القسم/الشطر الفعلية.
- **إصلاح جانبي مكتشَف أثناء الاختبار**: تحذير "BOTTOM OVERFLOWED" كان يظهر بشريط تنقّل البوابة الداخلية (`portal_header.dart`) عند ضيق نافذة المتصفح - صف أزرار التنقّل (Row عادي بلا التفاف/تمرير) كان يفيض عن ارتفاع الشريط الثابت لو لم تتسع كل الأزرار بسطر واحد. الإصلاح: صف الأزرار الآن داخل `SingleChildScrollView` أفقي ضمن `Expanded` بدل Row مباشر - يمرَّر بدل أن يفيض، بلا تغيير أي شيء آخر بالشريط. خلل موجود مسبقًا لا علاقة له بتعديلات اليوم على الأعمدة، اكتُشف بالصدفة أثناء اختبار حساب المنسّق.

### واجهة جوّال أصلية لتطبيق CBA Advising (2026-08-08)
- سليمان أرسل لقطة شاشة تُظهر تطبيق "CBA Advising" على الجوال بمظهر مكسور وغير احترافي (شريط علوي متراكب/مقطوع، مساحة بيضاء فارغة ضخمة بمنتصف الصفحة) - السبب: التطبيق كان يُحمّل `PublicLandingScreen`/`AdminWorkspaceScreen` بتصميمهما العريض المخصَّص للويب مباشرة داخل التطبيق، وبعض عناصرهما تفشل بصمت بنسخة Release على شاشة جوال ضيقة.
- بالمقارنة، أرسل سليمان لقطة من تطبيق "سليمان" (نكهة أخرى بنفس المشروع) بتصميم جوّال أصلي احترافي (بطاقة ترحيب، بحث، شبكة خدمات، تنقّل سفلي) وطلب صراحةً أخذها كمرجع.
- أُنشئت شاشة رئيسية جوّالة جديدة (`mobile_advising_home_screen.dart`) بنفس الروح: بطاقة ترحيب متدرّجة الألوان + شبكة خدمات (لوحة الإدارة، لوحة الإرشاد، خدمات أكاديمية، المنسوبين - حسب الصلاحية) بهوية الوحدة البصرية (أخضر/ذهبي)، وشاشة دخول جوّالة (أعيد استخدام `PortalLoginScreen` الموجودة أصلًا - متجاوبة بالفعل). نقطة الدخول الجديدة `MobileAdvisingRoot` تحل محل `PortalRoot`/`PublicLandingScreen` داخل تطبيق الجوال فقط (الموقع لم يتأثر - لا يزال بتصميمه الأصلي)، مع تفريع `kIsWeb` بالمسار السري لحساب المدير العام (`hidden_admin_login_screen.dart`) ليقود للواجهة الصحيحة حسب المنصّة.
- بُني APK جديد (1.0.5+6) وموقع مُحدَّث ونُشرا معًا.

### توحيد بطاقات التصميم (أيقونات/أحجام موحّدة) عبر البوابة + إصلاح خطأ بناء APK (2026-08-07)
- أُنشئ ملف مشترك `portal_cards.dart` بمكوّنين: `PortalStatCard` (بطاقة إحصاء - مربع أيقونة يمين ملاصق للحافة) و`PortalActionCard` (بطاقة إجراء تُضغط للتنقّل - أيقونة يمين + عنوان ووصف + سهم اتجاه). يحلّان محل 5 نسخ مكرَّرة يدويًا كانت متفرّقة بملفات مختلفة (`public_landing_screen.dart`, `admin_workspace_screen.dart`, صفحات الوسيطة الجديدة) - اكتُشف التكرار عمليًا حين طلب سليمان تبديل موضع الأيقونة واحتجنا تعديل ملفين منفصلين لنفس النتيجة.
- توحيد حجم الأيقونات بقاعدة ثابتة (`kPortalCardIconSizeSmall`/`Large` = 22/28) بدل قيم متفرّقة (22-30) بلا سبب واضح - البطاقات الصغيرة/الشبكية 22، البطاقات الرئيسية والعناصر العائمة (شارة تحميل الأندرويد) 28.
- **خطأ اكتُشف وأُصلح قبل التوزيع**: بناء APK نكهة "advising" الأول (بهذي الجلسة) استخدم نقطة الدخول الافتراضية الخاطئة (`lib/main.dart` → `HomeShell`، تطبيق دردشة قديم منفصل) بدل `lib/main_advising_app.dart` (نقطة الدخول الفعلية لتطبيق "CBA Advising" - نسخة كاملة من بوابة الويب). أُعيد البناء بالأمر الصحيح (`flutter build apk --release --flavor advising -t lib/main_advising_app.dart`) قبل أي توزيع فعلي - لو وُزِّع الملف الخاطئ كان المستخدمون سيرون تطبيقًا غير ذي علاقة بدل بوابة الإرشاد.

### إعادة هيكلة شريط تنقّل لوحة الإدارة + إصلاح خلل تنقّل حقيقي (2026-08-07)
- **خلل حقيقي انحل**: زر "لوحة الإدارة" كان يستخدم `popUntil((r) => r.isFirst)` ظنًا أن لوحة الإدارة هي أول صفحة بمكدّس التنقّل دائمًا - خطأ عند الدخول العادي (لا الرابط السرّي) لأن الصفحة العامة تبقى تحت `PortalRoot` بالمكدّس، فكان الزر يرجع أبعد من اللازم لغاية الصفحة العامة (يبدو للمستخدم وكأنه "رجع لتسجيل الدخول"). الحل: مسار `PortalRoot` يحمل الآن اسمًا ثابتًا (`kPortalRootRouteName` بـ`portal_root.dart`) يُمرَّر عند كل دفع له، و`popUntil` يستهدفه بالاسم مباشرة بدل الافتراض الموضعي الخاطئ - يعمل صحيحًا من أي نقطة دخول. نفس الإصلاح استُنسخ لشريط منسّق القسم (`coordinator_nav.dart`).
- **إعادة ترتيب الشريط** (بطلب سليمان): "الموقع العام" أولًا (كالمواقع الاحترافية) ثم "لوحة الإدارة" - نفس الترتيب استُنسخ لمنسّق القسم ومنسّق الكلية.
- **تبويبان جديدان وسيطان** يجمعان صفحات متفرّقة بدل تبويب مستقل لكل منها: "لوحة الإرشاد" (`advising_hub_screen.dart` - متابعة حالات الإرشاد + الظروف الخاصة + الدعم النفسي) و"خدمات أكاديمية" (`academic_services_hub_screen.dart` - تسكين المقررات الدراسية + الجدول الدراسي، حصري لحساب المدير العام). أُضيف "المنسوبين" كتبويب مباشر (بدل "بيانات منسوبي الكلية"، حصري أيضًا لحساب المدير العام).
- إعادة تسمية: "تسكين المقررات وشعبها" → "تسكين المقررات الدراسية"، "الجدول الدراسي لأعضاء هيئة التدريس" → "الجدول الدراسي".
- "تنزيل ملفات الحالات" و"التقارير" انتقلتا من شريط الإجراءات السريعة لتكونا مباشرة مع قسم "لوحة متابعة الإنجاز"؛ "التقارير" أُعيدت تسميتها "تقارير متابعة الحذف والإضافة" لتمييزها عن بقية تقارير البوابة.

### بطاقة تحميل تطبيق الجوال بلوحة الإدارة + رفع نسخة APK جديدة (2026-08-07)
- رفع رقم إصدار التطبيق من 1.0.4+5 إلى 1.0.5+6 وبناء نسخة APK جديدة (نكهة `advising` - CBA Advising) بكل تعديلات الجلسة.
- أضيفت بطاقة "تطبيق الجوال متاح الآن" أعلى لوحة الإدارة (`admin_workspace_screen.dart`) - تظهر فقط بنسخة الموقع (لا معنى لها داخل التطبيق نفسه)، بأيقونة أندرويد ونص احترافي وزر تحميل مباشر، وتقرأ رابط ورقم آخر نسخة منشورة حيًّا من نفس مستند Firestore (`app_config/cba_advising`) المستخدَم أصلًا للتحقق من التحديثات داخل التطبيق (`AppUpdateService.getLatestRelease` الجديدة) - أي تحديث لاحق للتطبيق (برفع APK جديد وتحديث المستند بطريقة سليمان المعتادة) ينعكس بالبطاقة تلقائيًا بلا أي تعديل كود إضافي. تختفي البطاقة تلقائيًا لو لم يوجد رابط منشور بعد.

### بطاقتا تحقّق مقارَنة في متابعة حالات الإرشاد (2026-08-07)
- طلب سليمان وسيلة تكشف صحة اكتمال ملف "بيانات الطلبة الأكاديمية" فور رفعه: بطاقة أولى تعرض إجمالي عدد الطلاب المرفوعين فعليًا من هذا الملف (بتفصيل طلاب/طالبات)، وبطاقة ثانية تعرض إجمالي حالات الإرشاد المحلَّلة بنفس تفصيل طلاب/طالبات (على مرشدهم + بلا مرشد + على غير مرشدهم + مفصولون أكاديميًا - كل طالب نشط يقع في واحدة منها بالضبط) - الرقمان يجب أن يتطابقا منطقيًا إن كان الملف المرفوع شاملاً فعلًا لكل طلاب الكلية، وأي فرق بينهما (إجمالي أو لكل شطر لحاله) مؤشر مباشر (بتنبيه لوني برتقالي) على نقص ببيانات المصدر لا خلل بالتحليل.
- ظهر هذا الاحتياج بعد أن أكّد سليمان يدويًا أن ملف "بيانات الطلبة الأكاديمية" المستخرَج من المنظومة الداخلية لا يشمل كل طلاب الكلية فعليًا (ناقص، لا خلل برمجي) - البطاقتان أداة تحقّق سريعة لأي رفعة قادمة، مستقلة عن أي فلتر بالواجهة.

### شفافية الاستبعاد عند رفع تقارير الإرشاد (2026-08-07)
- كانت بعض صفوف الملفات المرفوعة (Word/PDF) تُستبعَد بصمت من الأعداد النهائية دون أي إشعار: صفوف بلا اسم/رقم جامعي، صفوف دراسات عليا (ماجستير/دكتوراه)، وصفوف بقسم لا يطابق أحد الأقسام الخمسة المعروفة - هذا كان يفسّر لماذا لا يتطابق أحيانًا عدد السجلات المعروض بالإحصائيات مع عدد صفوف الملف الخام (مثال: رفع ملف فيه 6565 سجلًا لا يعني بالضرورة ظهور 6565 بالبطاقات، لأسباب استبعاد مشروعة لكنها كانت خفية).
- الإصلاح: `advising_report_parser_service.dart` و`advising_report_pdf_parser_service.dart` يُعيدان الآن عدد الصفوف المستبعَدة لكل سبب (`exclusionCounts`)، وتعرضها `advising_cases_admin_screen.dart` صراحةً في نافذة تأكيد الاعتماد (سبب الاستبعاد وعدده) قبل أن يضغط المستخدم "اعتماد" - بلا أي تغيير في منطق الفلترة نفسه، فقط شفافية إضافية.

### صفحة متابعة حالات الإرشاد (متابعة حالات الإرشاد) - قلب المشروع
- إنشاء الصفحة كاملة: رفع تقارير المنظومة الجامعية (بيانات الطلبة، مرشدون بلا طلاب، طلاب على غير مرشدهم، الحالة الصحية، تقرير المطابقة الرسمي)، وتحليل تلقائي لعدة حالات:
  - طلاب بلا مرشد، طلاب لدى مرشد من قسم آخر، مرشدون معفون لديهم طلاب، مرشدون بلا طلاب، مرشدون محمّلون فوق طاقتهم مقابل النصيب العادل، طلاب في نطاق مخاطر (المعدل التراكمي)، حالات صحية غير مسندة لأمين القسم.
  - تقرير "إعادة التوزيع" يسمّي الطلاب المطلوب نقلهم مع المرشد المستقبِل الموصى به.
  - تسوية بين التقرير الرسمي (طلاب على غير مرشدهم) والانتدابات المصحَّحة يدويًا في ملف هيئة التدريس (حالتا حنان عامر وطارق حلمي).
  - ترتيب موحّد في كل الجداول والتقارير: شطر ← قسم ← عضو.
  - بطاقات إحصائية بتصميم شبكي 4 في كل صف.
  - رفع موحّد لكلا الشطرين لكل التقارير الخمسة (يُفرز داخليًا تلقائيًا عبر عمود الجنس إن وُجد).

### بطاقة "طلاب على مرشدهم" الجديدة
- أُضيفت بطاقة إحصائية جديدة (أول بطاقة بقسم "ضبط عملية الإرشاد") تعرض عدد الطلاب المسكَّنين صح لدى مرشد من نفس قسمهم - الوضع السليم المستهدف، بخلاف "طلاب بلا مرشد" (لا مرشد إطلاقًا) و"طلاب على غير مرشدهم" (مرشد موجود لكن خطأ/غير موثَّق). حقل جديد `AdvisingCaseAnalysis.studentsCorrectlyAssigned`.

### قارئ PDF مباشر لتقرير "طلاب تابعين لمرشد"
- خانة رفع هذا التقرير تحديدًا تقبل PDF الرسمي مباشرة (بجانب docx) دون الحاجة لتحويله يدويًا إلى Word أولًا كبقية تقارير الإرشاد.
- `advising_report_pdf_parser_service.dart`: يعيد بناء صفوف/أعمدة الجدول يدويًا من مواضع الكلمات (Syncfusion لا يفعل ذلك تلقائيًا - يجمع النص عمودًا عمودًا لا صفًا صفًا)، مع معالجة خطيرتين اكتُشفتا أثناء التحقق:
  - النص المستخرَج من هذا الملف يستخدم "أشكال العرض العربية" (Arabic Presentation Forms-B، ‪U+FE70‬–‪U+FEFC‬) بدل الحروف القياسية رغم تطابقهما بصريًا تمامًا - أُضيف جدول تطبيع كامل وإلا فشلت كل مطابقة نصية بصمت.
  - `extractTextLines` لكل صفحة على حدة كان يستغرق نحو 32 دقيقة لملف 315 صفحة - استدعاء واحد لكل الصفحات معًا خفّضه إلى ~78 ثانية (مع تعويض `top` بمضاعف لرقم الصفحة لأن الإحداثي نسبي لكل صفحة).
- أُعيد استخدام نفس منطق الأعمدة/الفرز/الاستبعاد الحالي (`AdvisingReportParserService.parseRows`) دون تكراره - قارئ PDF يغذّيه بصفوف بنفس الشكل بدل صفوف docx.
- تحقّق فعلي على الملف الحقيقي (315 صفحة، 6855 سجل): تطابق كامل للاسم/الرقم/القسم (شمل التحقق من تعيين تخصصات اقتصاد/تمويل/تأمين لقسم الاقتصاد والتمويل تلقائيًا)/المرشد.
- إصلاحان إضافيان اكتُشفا أثناء تجربة الرفع الفعلي من الموقع:
  - المعالجة كانت تُجمِّد تبويب المتصفح بالكامل (لا مجرد بطء) لأنها متزامنة بالكامل على خيط الواجهة - نُقلت لـ Web Worker منفصل عبر `compute()` (`AdvisingReportPdfParserService.parseInBackground`) فلا تتجمّد الصفحة مهما طال وقت المعالجة.
  - `AdvisingReportRepository.save/load/clear`: مستند Firestore محدود بـ1 ميجابايت، وتقرير يغطي الكلية كاملة (آلاف السجلات) يتجاوزه بسهولة - السجلات تُقسَّم الآن لدفعات (`chunks`) في مجموعة فرعية تحت مستند كل شطر بدل مصفوفة واحدة. يتطلّب هذا قاعدة أمان صريحة للمجموعة الفرعية بـ`firestore.rules` (قاعدة المستند الأب لا تشملها تلقائيًا) - أُضيفت ونُشرت لكل أنواع تقارير الإرشاد الستة.
- إصلاح جذري ثالث (بعد التحقق الميداني مع سليمان بتتبّع طالب محدد): أي صف مرشده غير موجود بملف منسوبي الكلية كان **يختفي بصمت** من كل تحليل (تعذّر تحديد شطره فيُستبعَد كليًا) بدل تصنيفه "طلاب على غير مرشدهم" رغم أن له مرشدًا فعليًا. الإصلاح: الصف يُضاف الآن لكلا الشطرين معًا - خطوة الدمج (تربط برقم الطالب داخل شطر واحد فقط) تُفلتِر تلقائيًا للشطر الصحيح فتتجاهل النسخة الخطأ بلا أثر - مع تنبيه صريح للمستخدم بقائمة/عدد الحالات المتأثرة (`unresolvedShatrRows`) بدل الاختفاء الصامت. النتيجة الفعلية: "طلاب بلا مرشد" هبط من 439 إلى 357.
- **إصلاح جذري رابع وأخطر ما اكتُشف بالجلسة، بالكود القديم نفسه (`advising_report_parser_service.dart`) وليس بقارئ PDF الجديد فقط**: اكتُشف بتتبّع نفس الطالب (44254234) الذي بقي "بلا مرشد" رغم إصلاح كل ما سبق - منطق اكتشاف "صف العناوين" يفحص فقط هل **أي خلية تحوي جزئيًا** كلمة "اسم"، وأي طالب اسمه يبدأ بـ"اسم" (مثال حقيقي: "اسماعيل") يُلبِس هذا الفحص، فيُعامَل صف بياناته خطأً كصف عناوين جديد، فتفشل إعادة اكتشاف الأعمدة (فهارس -1)، **فتختفي بصمت كل الصفوف التالية بنفس الصفحة/الكتلة** (اسم/رقم فارغين). الإصلاح: لا يُعتمَد الصف كعناوين إلا إذا نجح فعليًا في تحديد عمودي الاسم والرقم معًا (لا التخمين الجزئي وحده). **الأثر الفعلي المقيس**: إجمالي سجلات "طلاب تابعين لمرشد" المستخرَجة قفز من 6855 إلى **9082** (+2227 سجل، ~25% من الملف) - كان هذا الخلل يُسقط ربع البيانات صامتًا في كل رفعة سابقة لهذا التقرير عبر أي مسار (PDF أو Word)، منذ إنشاء `advising_report_parser_service.dart` الأصلي.

### الجداول الدراسية (الحويّة) وصفحات أخرى
- إصلاح تقارير Microsoft Forms: إزالة أرقام الخطة، إعادة تصميم الشريط لصف واحد، دعم المقررات الاختيارية المشتركة بين أقسام متعددة.
- رفع "مواد خارج الكلية" عبر ملف Word، ومطابقتها تلقائيًا مع كتالوج المقررات.
- إصلاح ترتيب أعمدة PDF (كان معكوسًا خطأً)، إخفاء "نوع الخطة" دائمًا، إخفاء عمود "القسم" تلقائيًا عند تفعيل فلتر قسم محدد، توسيط كل خلايا الجداول.
- إصلاح جذري: مطابقة أسماء الأقسام غير الدقيقة (فراغات/همزات) في `college_roster_parser_service.dart`.
- إصلاح: خانة "الشطر" الفارغة كانت تُقصي أعضاء هيئة التدريس من فلاتر الشطر المحدد.
- إضافة عمود "العبء الدراسي" الفعلي (من الجداول المجدولة) في صفحة الهيئة التدريسية، بجانب "عبء الإرشاد".
- إضافة مربعات بحث في الجداول الرئيسية (اختيار المقرر، حالات الإرشاد).
- إضافة أزرار "تفريغ البيانات" بجانب كل زر رفع في التطبيق لتسهيل إعادة الاختبار.

### تعديل حقيقي على ملف بيانات منسوبي الكلية (2026-08-08)
- بإذن صريح من سليمان: أُضيف عضوان فعليان لورقة "منسوبو الكلية" بملف "المرفقات/أعضاء هيئة التدريس/قالب البيانات النهائي بعد تحديث المبتعثين 06-08-2026م.xlsx" - رقية بدوي أحمد آل أحمد الزبيدي (4311115، معيد، قسم الإدارة، طالبات) وإبرار سعد محمد الحربي (4380038، معيد، قسم الاقتصاد والتمويل، طالبات) - كلتاهما تبيّن أنهما منسوبتان فعليتان للكلية (لهما جدول تدريسي فعلي) لكنهما ساقطتان سهوًا من ملف منسوبي الكلية، وهما مصدر جزء من حالات "طلاب على غير مرشدهم" (انظر TODO.md للتفاصيل الكاملة والتحقق الميداني).
- أُخذت نسخة احتياطية كاملة من الملف قبل التعديل (`...xlsx.backup-2026-08-08.xlsx` بنفس المجلد).
- التنفيذ عبر سكربت Dart مؤقت (حُذف بعد الانتهاء) استخدم فعليًا `CollegeRosterMember`/`CollegeRosterMember.fromRaw`/`FacultySortOrder.compareMembers` من المشروع نفسه لإعادة فرز كل صفوف الورقة (162 صف أصلي + 2 جديدين) بنفس منطق الترتيب الرسمي المستخدم بالتطبيق (الشطر ثم القسم ثم المنصب/النصاب ثم الدرجة العلمية ثم رقم المنسوب)، مع إعادة توليد صيغة عمود "نصاب عضو هيئة التدريس" (formula مرتبطة بأرقام صفوف Excel) لكل صف بعد الفرز حتى تبقى صحيحة.
- الحقول الناقصة (منصب/بريد جامعي/جوال/مكتب) تُركت فارغة للعضوين الجديدين بموافقة صريحة من سليمان.
- تحقّق نهائي بعد الحفظ: صفوف ورقة "منسوبو الكلية" زادت بالضبط من 162 إلى 164، ورقتا "الإداريين" (25) و"المبتعثون" (17) لم تُمَسّا إطلاقًا، والاسمان الجديدان ظهرا في مكانهما الصحيح ضمن قسمهما وشطرهما بعد الفرز.

### تفعيل "بيانات الطلبة الأكاديمية" (المعدل/الساعات) - المصدر أصبح الأصل (2026-08-25)
- وصل سليمان أخيرًا ملفَي إكسل رسميين "بيانات الطلبة الأكاديمية" (طلاب/طالبات، منتظم) بأعمدة: الرقم الجامعي، اسم الطالب، القسم، المعدل التراكمي، ساعات الخطة، الساعات المتبقية، الوضع في الفصل - بعد أن كان هذا مؤجَّلًا منذ 2026-08-07 بانتظار ملف مناسب من عمادة القبول والتسجيل.
- أُضيفت حقول `planHours`/`remainingHours` (+ `completedHours` مُشتَقّ) لنموذج `AdvisingCaseRecord`، وقارئ جديد `academic_data_excel_parser_service.dart` (يعتمد أسماء الأعمدة، يقرأ أول ورقة بالملف بلا افتراض اسم ثابت لها).
- رفع منفصل لكل شطر (الملفان منفصلان أصلاً) من تبويب "رفع وتنزيل الملفات" ضمن قسم "الإرشاد الأكاديمي" - يستخدم بنية تخزين `AdvisingReportKind.base`/`basePrevious` كانت جاهزة وغير مستخدَمة منذ فترة.
- **تغيير معماري جوهري بطلب سليمان صراحةً**: أصبح ملف الإكسل هو **الأصل** الذي يحدّد قائمة طلبة كليتنا بدالة `AdvisingCaseAnalyzer.classifyAllColleges` (شاشة "متابعة حالات الإرشاد" بلوحة الإدارة فقط حاليًا) - أي طالب موجود بملف الإكسل ولم يظهر بتقرير "كل الكليات" (طلاب تابعين لمرشد) أو ظهر بلا مرشد، يُصنَّف "طالب بلا مرشد" فورًا. تقرير "كل الكليات" ظل الأصل فقط لحالتي "مرشد خارجي ← طلابنا"/"مرشدنا ← طلاب خارجيون" (طلاب/مرشدون من خارج كليتنا، لن يظهروا بملف الإكسل أصلاً).
- أُضيفت أعمدة "المعدل"/"الساعات المجتازة"/"الساعات المتبقية" لتبويب "كل الطلاب" بشاشة الإدارة، ولصفحة "بحث عن مرشد وقائمة طلابه" - وهذه الأخيرة أصبحت قائمة كل مرشد مرتَّبة تنازليًا حسب المعدل (كما تظهر بالمنظومة الداخلية)، وعند تساوي المعدل: الرقم الجامعي الأصغر رقميًا (الأقدم) أولاً.
- لم يُختبَر بعد برفع حي فعلي (لا `flutter` بهذه البيئة) - التفاصيل والمتبقي بـTODO.md.
- تصميم الرفع أُعيد تعديله بنفس اليوم (2026-08-26) ليطابق هوية "منسوبي الكلية"/"المقررات الدراسية" بصريًا: مربع مستقل رابع بنفس الصف (شريط أخضر + زر ذهبي واحد "رفع الملف" يسأل عن الشطر أولًا + بطاقتا إحصائية لعدد طلبة كل شطر + زر مسح أحمر) بدل الصفَّين المدمجين داخل مربع "الإرشاد الأكاديمي".

### قاعدة "الطلبة المستجدون" في إحصاءات الإرشاد (2026-08-26)
- بطلب سليمان صراحةً: الطالب المستجد (رقمه الجامعي يبدأ برمز سنة القبول الحالية - `448` لعام 1448هـ) غير ملزم بوجود مرشد أكاديمي، فلا يُحتسب ضمن مؤشر "طلبة بلا مرشد" مهما كانت حالته.
- أُضيف مؤشر مستقل "الطلبة المستجدون" (`CollegeAdvisingClassification.newStudents`) بشاشة "متابعة حالات الإرشاد" - تبويب خامس عشر جديد + بطاقة إحصائية خاصة (لون برتقالي، أيقونة 🆕).
- الاستبعاد يحدث **قبل** فحص المرشد (لا بطرح الأعداد لاحقًا) - طالب مستجد له مرشد فعليًا يبقى مصنَّفًا "مستجد" حصرًا، لا يُضاف لـ"طلبة على مرشدهم".
- بطاقة "الكل" تعرض الآن ملاحظة "المستهدفون بالإرشاد: X" (= كل الحالات الأربع − المستجدون تلقائيًا، لأنهم مستبعَدون من مصدرها أصلاً).
- **رمز السنة `AdvisingCaseAnalyzer.currentAdmissionYearPrefix` ثابت يدوي بالكود يحتاج تحديثًا كل عام دراسي** عند قبول فصل أول جديد - لا يوجد بالمشروع تحويل تقويم هجري تلقائي موثوق.

### إصلاح خلل "بلا مرشد" الخاطئ بعد رفع تقرير إرشاد جديد (2026-08-26)
- سليمان رصد أن رفع تقرير "كل الكليات" بتاريخ 26-08-2026 صنَّف 432 طالبًا "بلا مرشد"، رغم أن 429 منهم كان لهم مرشد صحيح بتقرير 25-08-2026 السابق (تخرُّج/تغيّر حالة/سهو من الجامعة - لا فقدان مرشد فعليًا). آلية الحذف/الاستبدال بـ`AdvisingReportRepository.save` صحيحة أصلًا (استبدال كامل ذري، لا دمج) - العلة كانت بمنطق **التصنيف** بـ`AdvisingCaseAnalyzer.classifyAllColleges`: يحكم فقط على أساس تقرير "كل الكليات" **الحالي**، بلا رجوع للنسخة السابقة المحفوظة أصلًا (`allCollegesPrevious`).
- أُضيف معامل `allCollegeRecordsPrevious` لـ`classifyAllColleges`. **بعد توضيح سليمان لاحقًا (2026-08-26) صار التصنيف النهائي حالتين فقط**: `studentsWithoutAdvisor` ("بلا مرشد" - يشمل الآن حالتين مدمجتين: طالب ظهر بالتقرير الحالي فعليًا وحقل مرشده فارغ فيه صراحة، **أو** طالب غائب عن التقرير الحالي والسابق معًا فلا يوجد أي مصدر يثبت وجود مرشد له) و`studentsMissingFromCurrentReport` ("غائب عن الحالي فقط لكنه ظهر بالسابق" - **عدّاد داخلي فقط بلا تبويب** بطلب سليمان الصريح: "لا نحتاجها إطلاقًا ولا يجب أن يظهروا في بلا مرشد"، إذ الأرجح أنهم تخرَّجوا بعد نهاية الفصل الصيفي 25-08-2026). لا تبويب "يحتاج تحقق من الإسناد" منفصل - أُلغي ودُمج بالكامل مع "بلا مرشد".
- تبويب "الكل" لم يتغيّر - لا عمود/صفوف إضافية للحالة الجديدة (مخفية عمدًا، عدّاد داخلي فقط).
- تحديث نقاط الاستدعاء الثلاث (`advising_cases_admin_screen.dart`/`advising_hub_screen.dart`/`advising_overview_stats_service.dart`) لتمرير `allCollegesMalePrevious`/`FemalePrevious` (كانت مُحمَّلة أصلًا من `loadCollegeScopedStudents` بلا استخدام).
- **قاعدة دائمة سجّلها سليمان صراحةً (2026-08-26) لتطبيقها عند رفع عمود "الحالة" الأكاديمية مستقبلاً**: أي طالب حالته أي شيء غير "منتظم" (منسحب/مطوي قيده/مفصول/مؤجل) لا يدخل نطاق حسابات "له مرشد"/"بلا مرشد" إطلاقًا - كأنه غير متواجد بهذه القوائم تحديدًا، لكن بياناته تبقى محفوظة حتى يُصحَّح وضعه الأكاديمي (يعود "منتظم" فيظهر تلقائيًا). **استثناء مهم**: يبقى مُحتسَبًا كرقم ضمن حساب توازن التوزيع/نصاب المرشدين (لأنه لا يزال مسنَدًا فعليًا لمرشده، وقد يعود "منتظم" في أي لحظة). لم يُنفَّذ بعد - بانتظار الملف الفعلي بعمود الحالة (تفاصيل بـTODO.md).
- `AdvisingReportRepository.save` يقبل الآن `sourceFileName` اختياريًا (يُحفَظ بالمستند الرئيسي)، ودالة جديدة `currentSourceFileName()`؛ الرفعات الثلاث (كل الكليات/ذوو الإعاقة/بيانات أكاديمية) تمرره الآن من اسم الملف المرفوع فعليًا.
- رسائل نجاح الرفع الثلاث تعرض الآن "حُذف X سجلًا قديمًا وأُضيف Y سجلًا جديدًا" بدل عدد السجلات الجديدة فقط.
- استبعاد الماجستير التنفيذي من كل الحسابات كان مُطبَّقًا فعلًا (`isExecutiveMbaProgram`) - تحقُّق فقط، لا تعديل كود. قاعدة طارق حلمي الدائمة تعتمد على تصحيح قسمه بشاشة "منسوبي الكلية" (بيانات، لا كود) - يحتاج تحقق ميداني لاحق أن قسمه مضبوط فعليًا "نظم المعلومات الإدارية".
- **نُشر على قناة المعاينة ثم اعتمده سليمان بعد مراجعة، ونُشر على الموقع الفعلي (2026-08-26)** عبر فرع `preview/advising-without-advisor-fix` (fast-forward إلى `main`، Deploy Web نجح).

### ترتيب ملف "طلبات القسم" حسب أولوية التخرّج بدل نوع الإجراء (2026-08-27)
- بطلب سليمان الصريح: ملف الإكسل الذي يستلمه المرشد الأكاديمي (`ExcelExportService.buildDepartmentWorkbook` - يُستخدَم أيضًا لملفات ذوي الإعاقة/التصعيد/البريد) كان يُرتِّب صفوف الطلبات حسب **نوع الإجراء** (كل الإضافات معًا، ثم الحذف، ثم التعديل) - فتتشتت طلبات الطالب الواحد بين ثلاث كتل منفصلة بالملف، ما يضطر المرشد للبحث عنه أكثر من مرة بدل تعديل كل احتياجاته دفعة واحدة.
- **الترتيب الجديد**: متوقعو التخرّج (30 ساعة متبقية فأقل) أولًا، ثم القريبون من التخرج (31 أو 32 ساعة)، ثم بقية الطلبة (33 فأكثر أو مجهول الساعات - المجهول يوضَع بآخر القائمة لا أولها، فالأولوية للمؤكَّد قرب تخرّجهم لا للمجهولين)؛ داخل كل فئة: الساعات المتبقية تصاعديًا ثم الرقم الجامعي، مع إبقاء كل طلبات نفس الطالب متتابعة كحزمة واحدة (إضافة ثم حذف ثم تعديل) بدل تشتتها بين كتل حسب نوع الإجراء كما كان سابقًا. **لا عمود "أولوية" منفصل** (أُضيف ثم حُذف بطلب سليمان صراحةً بعد المراجعة على المعاينة - الترتيب البصري + التلوين كافيان).
- **تمييز لوني تلقائي** حسب الساعات المتبقية (يطغى على التلوين المتناوب العادي): صف كامل بخلفية **وردية فاتحة** لمتوقعي التخرّج، و**كهرمانية فاتحة** للقريبين من التخرج، بلا تمييز لبقية الطلبة. عمود "خريج متوقع" نفسه يعرض النص المحسوب تلقائيًا ("متوقع تخرجه"/"قريب من التخرج") بدل قيمة "نعم/لا" المُبلَّغ عنها ذاتيًا عند انطباق إحدى الفئتين - القيمة الذاتية الأصلية تبقى فقط لمن ساعاته 33 فأكثر أو مجهولة.
- أُضيف نص توضيحي بشريط التعليمات أعلى ملف المرشد يشرح الفئتين اللونيتين ويطلب البدء بمعالجة متوقعي التخرّج أولًا حسب الترتيب الظاهر.
- أُضيف عمودان جديدان (**الساعات المتبقية**، **المعدل**) يُعرَضان قبل عمود "الشطر" - يُقرآن من "بيانات الطلبة الأكاديمية" (`AdvisingReportKind.base`) بمطابقة الرقم الجامعي، عبر دالة جديدة `ExcelExportService._loadAcademicLookup()` (لا تُفشِل بناء الملف كاملاً لو تعذّر تحميلها - يُكمَل ببيانات مجهولة تنزل تلقائيًا لأسفل الترتيب).
- **قرار سليمان الصريح: لا دمج بصري لخلايا الطالب (Excel merge)** - القيم (الاسم/الرقم/المرشد...) تتكرر بكل صف كما كانت، تفاديًا لخطر كسر `ProcessedFileParserService` (يقرأ الرقم الجامعي من كل صف؛ الخلايا المدموجة تصبح فارغة فعليًا بكل الصفوف عدا الأولى، فتُفقَد إجراءات تلك الصفوف عند عودة الملف من المرشد).
- **تعديل جذري بتوقيع الدالة**: `buildDepartmentWorkbook` أصبحت `Future<Uint8List>` (كانت متزامنة) - لأنها تحتاج تحميل بيانات أكاديمية من Firestore أولًا. انتشر هذا التغيير لأعلى بشكل متسلسل بلا كسر أي منطق: `AdvisorZipService.buildZip`/`buildAdvisorFiles`، `DisabilityFileService.buildFile`، `EscalationFileService.buildStage2File`/`buildStage3File` أصبحت جميعها `Future<...>` أيضًا، مع إضافة `await` في كل نقاط الاستدعاء (~11 موقعًا عبر `admin_workspace_screen.dart`/`coordinator_workspace_screen.dart`/`college_coordinator_workspace_screen.dart`/`upload_hub_screen.dart`/`upload_flows.dart` - كلها كانت أصلًا داخل دوال `async` فلم تتطلب تعديلًا هيكليًا، فقط إضافة الكلمة).
- فهارس الأعمدة الثابتة (`shatrColumnIndex`، `advisorStatusColumnIndex`... إلخ المستخدَمة لإخفاء/تفعيل قوائم منسدلة بملف المرشد) انزاحت جميعها +2 لاستيعاب العمودين الجديدين (كانت +3 مؤقتًا أثناء وجود عمود "الأولوية" المحذوف لاحقًا) - عُدِّلت كلها بالتوازي، `flutter analyze` نظيف على كامل المشروع.
- اختباران قديمان بـ`test/ticket_merge_test.dart` (توقيع تابع لـ`buildDepartmentWorkbook`) عُدِّلا فقط لإضافة `await`/`async` اللازمة للتصحيح - **فشلهما الفعلي (3 اختبارات) موجود مسبقًا قبل هذا التعديل بالأساس** (تحقَّقتُ عبر `git stash`: يفشل بنفس الرسائل على الكود الأصلي غير المعدَّل - عدم تطابق مفاتيح `status`/`notes`/`completed_by` المستخدَمة بالاختبار مع `advisor_status`/`advisor_notes` الفعلية بـ`TicketRepository.mergeProcessedRows`) - لم يُصلَح ضمن هذه المهمة (خارج نطاقها).
- **تعديلات إضافية بعد المراجعة على المعاينة (نفس اليوم 2026-08-27)**: (أ) استبدال كلمة "كهرماني" بـ"أصفر" بنص التعليمات (نفس اللون، تسمية أوضح). (ب) الساعات المتبقية/المعدل تُكتبان أرقامًا فعلية (`IntCellValue`/`DoubleCellValue`) لا نصًا - يصح الفرز الرقمي لاحقًا لو فرزها المرشد يدويًا. (ج) خلايا حالة/ملاحظات المرشد ومنسق القسم/الكلية الفارغة تُكتب الآن بلا قيمة إطلاقًا (`null`) بدل نص فارغ - النص الفارغ كان لا يزال "مستخدَمًا" تقنيًا فيمنع اكتشاف الحالات غير المعالَجة. (د) تفعيل اتجاه الورقة RTL، وتسميتها "طلبات المرشد" بدل "Sheet1" الافتراضي. (هـ) التفاف نص + ارتفاع أكبر لصف عناوين الأعمدة (كان مضغوطًا بلا التفاف). (و) إزالة الفاصلة المنقوطة الزائدة بنهاية "سبب الطلب" (خلل تصدير Microsoft Forms المعروف للاختيار المتعدد). (ز) تثبيت (Freeze Panes) أول `headerRowCount` صف عبر تعديل XML مباشر بـ`ExcelProtectionService._freezeHeaderRows` (المكتبة لا تدعمه أصلًا) - تبقى صفوف التعليمات/العناوين ظاهرة أثناء التمرير. (ح) عمود "خريج متوقع" أصبح "تصنيف أولوية التخرج" (يناسب القيم الثلاث الفعلية). (ط) شريط التعليمات دُمج بعمودَي B-O فقط (لا كل الأعمدة) ليظهر كاملاً بمجرد فتح الملف بلا تمرير أفقي.
- سليمان راجع كل هذا على قناة المعاينة وأكّد نجاحه ("نجح الآن اكتمل العمل") - لم يُنشر بعد على الموقع الفعلي، بانتظار تأكيده الصريح للنشر [[feedback_no_deploy_until_all_notes]].

## ملاحظات تقنية دائمة
- أي رفع ملف جديد: افتراضيًا رفعة واحدة موحّدة للشطرين تُفرز داخليًا (استثناء حالي: جداول المقررات/الحويّة تبقى منفصلة لحين تأكيد إمكانية سحب تقرير موحّد من الجامعة).
- `pw.TableHelper.fromTextArray`: ترتيب الأعمدة في المصفوفة هو ترتيب بكسل LTR حرفي، ولا ينعكس تلقائيًا مع `textDirection: rtl`.
- تحليل ملفات Word: مطابقة الأعمدة بأسماء الترويسة (Header) وليس بالفهرس الثابت.
