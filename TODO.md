# مهام معلّقة

- [ ] **بانتظار اختبار سليمان الحي - ملف منسّق الكلية عاد ملفًا واحدًا مدمجًا مفروزًا حسب حالة منسّق القسم، بعد إصلاح خلل الفرز (2026-09-05)**: طلب سليمان العودة لملف مدمج واحد لكل شطر (بعد تجربة 5 ملفات ZIP بـ2026-09-03) مع فرز ثلاثي: "تم التنفيذ" ← "لم يتم التنفيذ" ← "لم يعمل عليها" (فارغة)، بالاعتماد على **حالة منسّق القسم** (`coordinator_status`) لأن الحالات هنا مُصعَّدة أصلاً منه. **أول نسخة كان بها خلل**: قارنت `coordinator_status` بنص خيارات المرشد الخاطئ ("تم/لم يتم التنفيذ") بدل خيارات منسّق القسم الفعلية ("تم الإنجاز"/"جزئي"/"لم يتم" - `ExcelProtectionService.statusOptions`)، فوقعت كل الحالات غير الفارغة بفئة واحدة خطأً - اكتشفه سليمان بلقطة شاشة فعلية. **أُصلح**: `تم الإنجاز`=0، `جزئي`/`لم يتم`=1، فارغ=2 (`excel_export_service.dart`، معامل `isCollegeStage`). القسم يبقى معيار فرز ثانٍ، والمسارات الثلاثة (شاشة منسّق الكلية/صفحة رفع وتنزيل الملفات/زر تنزيل الكل) كلها تنزّل `.xlsx` مدمج لا ZIP مُقسَّم. **يحتاج تأكيد سليمان الآن** أن الفرز صحيح فعليًا بالملف المُنزَّل.

- [ ] **متابعة بعد نشر ربط بطاقتَي "الطلبة المستفيدون"/"المرشدون الأكاديميون" بالرئيسية العامة (نُشر 2026-09-02)**: التفاصيل الكاملة بـREADME.md. **متبقٍ**: تأكيد سليمان أن الرقمين الظاهرين فعليًا على الموقع (https://sss-advising-tu.web.app) يطابقان الواقع. بطاقتا "الخريجون" و"الخدمات والمبادرات" تبقيان "— قيد ربط البيانات" عمدًا حتى تتوفر مصادر بيانات فعلية لهما.

- [ ] **بانتظار اختبار سليمان الحي - سجل تقديمات Forms المتكررة + معاينة بطاقة PDF بالتطبيق (2026-09-02)**: التفاصيل الكاملة بـREADME.md. الكود جاهز و`flutter analyze` نظيف على كل الملفات المتأثرة (الموقع + التطبيق). **لم يُنشَر بعد**. المتبقي: (1) اختبار حي برفع ملف يحتوي رقمًا جامعيًا سبق ظهوره - التأكد أن التذكرة القديمة لا تتأثر وأن "سجل التقديم" يظهر ببطاقة حالة الطالب/ة (بالموقع والتطبيق معًا) بتاريخين. (2) اختبار أن تعديل قرار المرشد على نفس الإجراء مرتين يظهر بسجل قرارات متعدد أسفل كل إجراء. (3) بالتطبيق: التأكد حيًّا أن الضغط على "تنزيل PDF/مشاركة" بتبويب "بحث عن طالب/ة" يفتح المعاينة المنبثقة الجديدة بشكل صحيح على جهاز حقيقي (لم يُختبَر إلا بالتحليل الساكن فقط، لا تجربة فعلية بعد) وأن زر التنزيل داخلها يُصدِّر نفس ملف PDF المعتاد.

- [ ] **إلغاء قسم "النموذج الورقي" من ملف المرشد بعد انتهاء فترة الحذف والإضافة الحالية (بدأ 2026-09-02)**: بتأكيد سليمان الصريح - الميزة استثنائية مؤقتة لهذه الدورة فقط بسبب توجيه إداري (صعوبة بعض الطلبة بالتقديم عبر Forms)، وليست ميزة دائمة. حاليًا `AdvisorZipService.buildAdvisorFiles` يمرّر `includePaperFormRows: true` دومًا لـ`buildDepartmentWorkbook` (بلا شرط زمني/دورة) - عند انتهاء هذه الدورة، بدّل القيمة إلى `false` (أو اربطها بشرط دورة صريح) حتى يعود ملف المرشد لشكله المعتاد بلا القسم الإضافي. لا تحذف الكود نفسه (`includePaperFormRows`، `paperFormExtraRowCount`، إلخ بـ`excel_export_service.dart`) - قد يُطلب تفعيله مجددًا بدورة مستقبلية مشابهة.

- [x] ~~خلل حقيقي بملف "بطاقة حالة طالب/ة" PDF: شارة ذهبية `borderRadius: circular(999)` تُفسِد الصفحة كاملة~~ - **أُصلح ونُشر (2026-08-31)**. سليمان أرسل لقطة فعلية للـPDF المُصدَّر تُظهر مثلثًا ذهبيًا ضخمًا يغطي أغلب الصفحة ويُخفي لوحتي "تقرير المرشد"/"الطلب المقدَّم". اكتُشف عدم وجود `flutter`/`dart` بهذه البيئة سابقًا كان خطأً - وُجد فعليًا بـ`C:\Users\salfa\AppData\Local\flutter\bin` (+ `pdftoppm`/poppler لترجمة PDF إلى صورة للمعاينة)، فأُعيد إنتاج نفس المستند خارج Flutter (سكربت Dart مستقل يقرأ الخط/الشعار من القرص مباشرة بدل `rootBundle`) وأُثبت السبب الجذري تجريبيًا: شارة "إضافة/حذف" الصغيرة داخل بطاقة الطلب استخدمت `pw.BorderRadius.circular(999)` (نمط "حبة دواء" شائع بـFlutter العادي) لكن حزمة `pdf` (لا Flutter) تُفسِد رسم أي `Container` بهذا القطر الكبير نسبيًا لحجمه الفعلي - أُثبت بعزل العنصر (إزالته أزال الخلل تمامًا، استبداله بـ`circular(4)` أصلح الشكل بالكامل مع بقاء الشارة الذهبية الصغيرة سليمة). **لن يتكرر**: لا تُستخدَم `BorderRadius.circular` بقيمة كبيرة (تريد شكل "حبة دواء" مستديرة الطرفين) داخل أي PDF بحزمة `pdf` - استخدم رقمًا صغيرًا (4-8) بدل الاعتماد على قيمة كبيرة تتجاوز نصف الارتفاع.
- [ ] **بانتظار اختبار سليمان الحي - "بطاقة حالة طالب/ة" (2026-08-31، بالموقع والتطبيق معًا)**: بالموقع: شاشة "بطاقة حالة طالب/ة" داخل "لوحة الإرشاد" (تفاصيل كاملة بـREADME.md)، PDF أُعيد إنتاجه وتحقَّق بصريًا عبر Dart+poppler مباشرة قبل النشر. بالتطبيق: تبويب ثالث "بحث عن طالب/ة" بـ"لوحة الإدارة" الجوّالية، بنفس محتوى الطلب/الملاحظة/تنزيل PDF - `flutter analyze` نظيف على كل الملفات لكن **لم يُبنَ APK فعليًا ولم يُختبَر على جهاز حقيقي بعد** (بناء APK يستغرق وقتًا طويلاً، لم يُجرَّب محليًا؛ سيصل تلقائيًا عبر Firebase App Distribution بعد أول نشر CI). **متبقٍ**: تجربة سليمان الحية بكليهما - بالموقع: البحث برقم طالب/ة حقيقي وتنزيل الملف من الواجهة نفسها؛ بالتطبيق: فتح "لوحة الإدارة" ← تبويب "بحث عن طالب/ة" والتأكد من ظهور القسم الجديد وعمل زر التنزيل على جهاز حقيقي.

- [ ] **خلل غير مُشخَّص: قارئ CSV لجدول المقررات (الحويّة) يرفض ملفًا حقيقيًا (2026-08-30)**: رفع سليمان ملف "دراسي مستفيد شطرين" بصيغة CSV فظهر خطأ `لم يُعثر على أي شعبة "المستفيد" منها كلية إدارة الأعمال` رغم أن فحص بنية الملف (ترويسة الأعمدة وصفوف بيانات فعلية) تُظهر تطابقًا تامًا مع ما يتوقعه `CsvScheduleParserService` (عمود [21]=المستفيد يحوي فعليًا نص "كلية إدارة الأعمال"). السبب الحقيقي غير مؤكَّد بعد - يحتاج فحص الملف الفعلي (bytes خام، لا نص منسوخ من واجهة الدردشة قد يكون مرّ بإعادة ترميز) للتأكد إن كان الترميز Windows-1256 فعليًا أو شيء آخر. **الحل المؤقَّت**: PDF لنفس البيانات يعمل بنجاح (`PdfScheduleParserService`) - استُخدم كبديل فوري. لم يبدأ التصحيح الجذري بعد؛ عند العودة إليه اطلب من سليمان الملف الأصلي كمرفق حقيقي (لا نص مُلصَق) لفحص البايتات الخام مباشرة.

- [ ] **تفعيل Domain Authentication لنطاق tu.edu.sa بـSendGrid (اكتُشف 2026-08-30)**: عند إرسال بريد "من" salfawaz@tu.edu.sa عبر SendGrid، يرفضه خادم الجامعة (Outlook/EOP) برسالة `550 5.7.509 ... sending domain tu.edu.sa does not pass DMARC verification and has a DMARC policy of reject`. السبب: نطاق الجامعة لديه سياسة DMARC "reject" صارمة، و"Single Sender Verification" (المفعَّل حاليًا وناجح بلوحة SendGrid) لا يكفي - يثبت فقط ملكية الصندوق، لا يُخوِّل خوادم SendGrid بإرسال بريد باسم النطاق. **الحل**: تفعيل "Domain Authentication" الكامل بـSendGrid (Settings → Sender Authentication → Authenticate Your Domain) عبر إضافة 3 سجلات CNAME تعطيها SendGrid إلى DNS نطاق tu.edu.sa - يحتاج تنسيقًا مع عمادة تقنية المعلومات بالجامعة (خارج صلاحية حساب SendGrid العادي). **الحل المؤقَّت المُطبَّق الآن**: أُعيد `SENDER_EMAIL`/`RECIPIENT_EMAIL` (أسرار GitHub Actions) إلى `tualfawaz@gmail.com` (يعمل ويصل فعليًا، تحقَّق سليمان من Activity Feed بـSendGrid) بدل البريد الجامعي حتى يُنجَز التنسيق مع الجامعة.

- [x] ~~تصنيف إجباري/اختياري لمقررات قسم الاقتصاد والتمويل~~ - **أُنجِز (2026-08-27)** بعد أن رفع سليمان ملفَي "تقرير خطط 38.pdf" و"تقرير خطط 47.pdf" الشاملين (`المرفقات/01-الملفات المرفوعة للموقع/`) اللذين يغطيان الكلية كاملة بما فيها البرامج الفرعية الثلاثة (اقتصاد/تمويل/تأمين). راجعتُ الثلاثة فعليًا وحدّثت `lib/data/course_catalog.dart` (`elective: true`) وفق الأرجح عند تعارض التصنيف بين برنامجين فرعيين لنفس الرمز (مثال: 66053103 ظهر إجباريًا ببرنامج التأمين واختياريًا بقائمة برنامج التمويل الخاصة - اعتُمد "اختياري" كخيار أكثر تحفّظًا). **لم يُنشَر بعد** - بانتظار مراجعة سليمان.
- [ ] **متوقَّف مؤقتًا بطلب سليمان (2026-08-27) - "يوجد ما هو أهم"، سيُرتَّب لاحقًا**: مجموعة عمل زر "توزيع فترات الإرشاد" (شاشة `advising_schedule_admin_screen.dart`) توقَّفت في المنتصف. **الحالة الفعلية للكود الآن**: سليمان اختبر زر "تنزيل ملف مضغوط منظّم" حيًّا فجاء الملف **فارغًا** - السبب المكتشَف: كل أزرار ZIP الأربعة بالشاشة (`onSignageImagesZip`/`onSignageImagesByDepartmentZip`/`onPptxZip`/`onDepartmentFoldersZip`) كانت تعتمد على `_filteredData` مخزَّنة قد تكون فارغة/قديمة حين الفلتر الحالي = "الكل" (نفس خلل مكتشَف سابقًا لزر واحد فقط بـ2026-08-25، لم يُعمَّم يومها على البقية). **أُصلح الكود محليًا**: كل الأزرار الأربعة الآن تستدعي `_loadFilteredData()` طازجة دومًا بدل الاعتماد على `_filteredData` المخزَّنة - `flutter analyze` نظيف و`flutter build web` نجح محليًا. **استُبعد عمدًا من نشر 2026-08-27** (سليمان صراحةً: "ماعدا تنزيل فترات الإرشاد سيتم تأجيله لاحقًا") - عبر `git stash` مؤقت لهذا الملف تحديدًا وقت البناء والنشر، ثم أُعيد الملف لشجرة العمل بعد النشر (غير مُلتزَم - uncommitted). عند العودة: انشر مباشرة (الإصلاح جاهز ومُختبَر بالتحليل) ثم اطلب من سليمان إعادة اختبار الزر حيًّا للتأكد أن الملف لم يعد فارغًا.
- [ ] **متوقَّف مؤقتًا بطلب سليمان (2026-08-27) - تقرير "تغييرات الحويّة" PDF**: سليمان جرَّب أول نسخة فظهرت صفحات فارغة ولم يعجبه الشكل ("ليس كعرض الصور") - سيرسل ملف عيّنة PDF يوضّح الشكل المطلوب بالضبط **قبل** أي تعديل. لم يصل الملف بعد. **لا تُعِد بناء الشكل بالتخمين** - انتظر الملف العيّني فعليًا عند العودة لهذا البند. الميزة أصلاً لم تُنشر على الموقع الفعلي بعد (مستبعَدة تلقائيًا من كل نشر ما دامت غير مطلوبة).

- [x] ~~تبويب "بيانات الطلبة" بشاشة "الجداول الدراسية" (تطبيق الجوّال)~~ - **أكّد سليمان تجربته حيًّا ونجاحه، نُشر رسميًا (2026-08-27)**.
- [ ] **طلب مستقبلي من سليمان (2026-08-27)**: إن رُفعت مستقبلاً بيانات "المواد المسجَّلة" لكل طالب (غير متوفرة حاليًا)، يُضاف لبطاقة "بيانات الطلبة" (تبويب الجداول الدراسية) عرض قائمة المقررات المسجَّلة فعليًا لكل طالب عند البحث عنه.

- [x] ~~قارّاءا CSV الجديدان لملفي الإرشاد والحويّة~~ - **أكّد سليمان تجربته حيًّا ونجاحه بعد إصلاح خلل NBSP، نُشر رسميًا (2026-08-27)**.
- [x] ~~"التعليق الكبير" بصفحة "رفع وتنزيل الملفات"~~ - **أكّد سليمان أن الخلل زال فعليًا، نُشر رسميًا (2026-08-27)**.
- [x] ~~خلل عدم انعكاس الفلتر بجدول "بيانات الطلبة الأكاديمية"~~ - **أكّد سليمان أن الخلل زال فعليًا، نُشر رسميًا (2026-08-27)**.
- [x] ~~ترتيب ملف "طلبات القسم" حسب أولوية التخرّج~~ - **أكّد سليمان تجربته حيًّا ونجاحه، نُشر رسميًا (2026-08-27)**.

- [ ] **متابعة بعد نشر إصلاح تصنيف "بلا مرشد" (نُشر على الموقع الفعلي 2026-08-26)**: التفاصيل الكاملة بـREADME.md - سليمان راجع المعاينة واعتمد النشر الفعلي بنفس اليوم. **متبقٍ للتحقق الميداني لاحقًا (لا يمنع الاستخدام)**: التحقق أن سجل طارق حلمي (رقم مرشد 4280812) بشاشة "منسوبي الكلية" قسمه مضبوط فعليًا "نظم المعلومات الإدارية" (القاعدة تعتمد على تصحيح هذا الحقل يدويًا، لا استثناء بالكود).

- [ ] **بانتظار رفع سليمان لبيانات "بيانات الطلبة الأكاديمية" محدَّثة بعد إغلاق الفصل الصيفي (2026-08-27، الآلية تغيّرت)**: بعد إغلاق الفصل الصيفي يوم 25-08-2026، تخرَّج عدد كبير من الطلاب - تقرير "كل الكليات" (الإرشاد) حذفهم فورًا، لكن أصل "بيانات الطلبة" (الذي يحدّد قائمة طلبتنا بـ`classifyAllColleges`) لم يتحدَّث بعد. **تحقَّقتُ ميدانيًا سابقًا**: 429 من أصل 430 حالة "بلا مرشد" الظاهرة فعليًا طلاب اجتازوا كل ساعاتهم (`remainingHours == 0`) - مؤشر تخرّج شبه مؤكَّد؛ لن يُضاف كود يعتمد على `remainingHours == 0` كمؤشر تخرّج بقرار سليمان الصريح ("لا لا سيتم تحديث بيانات الطلبة"). **الآلية المعتمَدة الآن لتحديث هذا الأصل تغيّرت** (انظر البند الأول أعلاه): رفع الملفات الستة CSV الجديدة بدل ملف xlsx القديم - يبقى الحل بانتظار رفع سليمان للبيانات الفعلية المحدَّثة، وحينها يصحّح رقم "بلا مرشد" تلقائيًا بلا أي تعديل إضافي بالكود. الحالة الحقيقية الوحيدة المتبقية المؤكَّدة فعليًا حتى الآن: طالبة رقمها الجامعي 44705627 (91 ساعة متبقية - منتظمة فعليًا، بلا مرشد حقيقةً).

- [ ] **خلل حقيقي مكتشَف: فشل صامت جزئي بحفظ قطع (chunks) رفعات كبيرة (2026-08-26)**: أثناء تشخيص "بلا مرشد"، اكتُشف أن رفعة كبيرة (شطر الطالبات، ~5204 سجل) كتبت 3 قطع فقط من أصل ~35 المتوقَّعة قبل التوقف صامتًا (مستند `studentsCount` الرئيسي يُكتب بالعدد الكامل **قبل** حلقة كتابة القطع، فيبقى يعرض رقمًا صحيحًا رغم فقدان معظم البيانات فعليًا - انظر `AdvisingReportRepository.save()`). أُصلح يدويًا بإعادة رفع الملف (نجحت لاحقًا كاملة: 35/35 قطعة). **لم يُعالَج جذريًا بعد** - يحتاج إما: (أ) دالة تحقّق بعد الحفظ تقارن عدد القطع الفعلي بالمتوقَّع وتُنبّه المستخدم صراحةً إن نقص، أو (ب) كتابة `studentsCount` **بعد** نجاح كل القطع لا قبلها، حتى لا يعطي انطباعًا كاذبًا بالاكتمال عند فشل جزئي. لم يبدأ العمل عليه.

- [ ] **مؤجَّل لغد بطلب سليمان (2026-08-26)**: 103 طالب/ة موجودون بتقرير "طلاب تابعين لمرشد" (لهم مرشد) لكن غائبون عن ملف "بيانات الطلبة الأكاديمية" (الإكسل) - تحقَّقتُ من العدد على البيانات الحية وهو **صحيح تمامًا (103)** بعد تصفيته على أقسام كليتنا الخمسة فقط (275 إجمالاً قبل التصفية، تشمل كليات أخرى). توقُّع سليمان: أغلبهم طلبة مفصولون أو مطويّ قيدهم (سيسحب غدًا تقرير الحالة الدراسية للتأكد)، وسيُضاف لهم أيقونة "طلبة غير منتظمين" إن صحّ ذلك. بانتظار تقرير الحالة الدراسية منه قبل أي تنفيذ.

- [ ] **بانتظار اختبار سليمان الحي بعد النشر - اعتماد رفع PDF مباشرة بالرفع الحقيقي (2026-08-24)**: بطلب صريح من سليمان ("رافع الملف مستقبلاً لن يعرف تحويل PDF إلى Word")، بعد سلسلة مقارنات برمجية (710 شعبة، سكربتات Node بمجلد الـscratchpad) وإصلاحات متعددة بقارئ PDF (`pdf_schedule_parser_service.dart`: لقب المحاضر، "(حضوري)"، اقتطاع "المستفيد" متعدد الكليات بثلاث جولات إصلاح، موعد وهمي إضافي من صف متابعة، انعكاس أرقام "شبكة" مستقل عن خلل Word) + إصلاح خلل حقيقي مستقل بقارئ **Word نفسه** (انعكاس أرقام قاعات "شبكة" غير مُصحَّح سابقًا، `docx_schedule_parser_service.dart`)، أصبحت البيانات الجوهرية (وقت/قاعة/محاضر) دقيقة ~99%. الخلل الوحيد المتبقي ذو الأثر (~1% من الشعب: موعد وهمي إضافي) سببه بنيوي باستخراج PDF نفسه، غير مضمون الحل الكامل - بدل مطاردته أكثر بالكود، `runUploadCourses` (`upload_flows.dart`) الآن **يقبل .docx أو .pdf مباشرة** ويُنبّه تلقائيًا (حوار تأكيد صريح) بأي شعبة تحوي 3 مواعيد أسبوعية أو أكثر (نادر فعليًا، مؤشر خلل استدلال محتمل) قبل الاعتماد النهائي. حقل "المستفيد" (يُستخدَم فعليًا للفلترة) تحقَّقتُ أن اسم الكلية الأساسي يبقى سليمًا بكل الحالات المتبقية (14/710) رغم اقتطاع كوزمتيكي لكليات إضافية لاحقة - **لا أثر فعلي على منطق الفلترة**. كل هذا على فرع preview فقط - يحتاج اختبار رفع PDF حقيقي عبر `runUploadCourses` نفسه (لا صفحة المعاينة المعزولة فقط) والتأكد أن التنبيه التلقائي يظهر بشكل صحيح، قبل أي نشر على الموقع الفعلي [[feedback_no_deploy_until_all_notes]]. تفاصيل المقارنات الكاملة بـ[[project_pdf_vs_docx_schedule_reader_comparison]].

- [ ] **بانتظار رفع سليمان لبيانات حقيقية جديدة بعد التفريغ الشامل (2026-08-23)**: بطلبه الصريح، فُرِّغت Firestore من كل بيانات الاختبار والبقايا اليتيمة المرتبطة بها: `tickets` (40)، `advisingAllCollegesReports`+`advisingAllCollegesReportsPrevious` (شطرين + chunks)، `courseSchedules` (شطرين)، `support_cases` (سجل "محمد احمد")، ثم بعد فحص شامل لكل مجموعات القاعدة (`db.listCollections()`) وتأكيد سليمان: `outsideCourses` (2)، `stage_downloads` (3)، `stage_snapshots` (15)، `advisingAdvisorMovements` (254)، `advisingReportsPrevious` (1، مجموعة قديمة غير مستخدَمة أصلًا بواجهة الرفع الحالية). عبر `tool/admin_cli/clear_selected_data_2026_08_23.js` + أوامر مباشرة إضافية. **تحقَّق أن الباقي (منسوبو الكلية 206 عضوًا، حسابات المنسّقين، جداول مواعيد الإرشاد، `advisingHealthReports`، `hardship_cases` فارغة أصلاً) بيانات مؤسسية حقيقية لا اختبار** - لم تُمسّ. `public_stats` سيتصحّح تلقائيًا عند أول رفع حقيقي جديد (لا يحتاج تدخلًا يدويًا). الرئيسية/لوحة الإدارة/الجداول الدراسية بكل من الموقع والتطبيق ستعرض أصفارًا/فراغًا حتى يرفع سليمان الملفات الحقيقية من جديد.

- [ ] **إضافة عمود "القاعة" لجدول المقررات الدراسية (بدأ 2026-08-23، تصحيح+تحضير جوّالي 2026-08-24)**: **تصحيح**: افتراضي السابق أن "القاعة" غير موجودة بالملف كان خطأً - تحقَّقتُ فعليًا من الملف الحقيقي الذي أرسله سليمان (PDF من المنظومة الداخلية، "دراسي طلاب/طالبات - المستفيد...") وعمود "القاعة" **موجود فعليًا** (قيم مثل "0110(حضوري)"). المشكلة الحقيقية أن `DocxScheduleParserService` لا يقرأه بعد (لا `CourseSectionRecord`/`CourseMeeting` يملكان حقلًا له). **يلزم من سليمان**: تحويل نفس ملف PDF هذا فعليًا إلى **Word (.docx)** وإرساله (لا يمكن تحديد فهرس عمود القاعة بدقة داخل مصفوفة الأعمدة الثابتة بالكود من PDF وحده - الكود الحي يقرأ .docx حصرًا). **جاهز مسبقًا بالتطبيق الجوّالي**: جدول "الجدول الدراسي" بتبويب "الجداول الدراسية" أصبح يعرض عمود "القاعة" فعليًا (بعد الوقت، بلا عمود "النوع" مستقل - شارة نظري/عملي انتقلت لتظهر بجانب اليوم) لكن بقيمة "-" مؤقتة حتى تتوفر البيانات الحقيقية - سيُملأ تلقائيًا فور إضافة الحقل بالموقع. **يلزم أيضًا**: تعديل `DocxScheduleParserService` لاستخراج القاعة + إضافة حقل `room` لـ`CourseMeeting` (القاعة قد تختلف بين مواعيد الأسبوع لنفس الشعبة) + عرضه بجدول الموقع كذلك.
- [ ] **بانتظار اختبار سليمان الحي - زر "تنزيل الملف" بجانب "رفع الملف" (2026-08-23)**: أُضيف زر تنزيل ملف ZIP (10 ملفات Excel: 5 أقسام × شطرين) من "الملف الأساسي - طلبات الحذف والإضافة" بصفحة رفع/تنزيل الملفات (`upload_hub_screen.dart`) - ليتمكّن سليمان من إرساله يدويًا لمنسّق قسم يتعذّر عليه الدخول للموقع. لم يُختبَر حيًّا بعد (لا `flutter` بهذه البيئة). التفاصيل بـREADME.md.
- [ ] **مرحلة ثانية: نظام `StatusCounts`/"جزئي" النصّي المنفصل (2026-08-23)**: بعد إعادة هيكلة "متابعة سير العمل" لثلاث حالات فقط (انظر README.md)، يبقى نظام منفصل تمامًا يخزّن "جزئي" كقيمة نصية حرفية ويغذّي: تصدير Excel/PDF، `stage_snapshot_service.dart`، ولوحة `ticket_action_stats_panel.dart` - لم يُمس عمدًا هذه المرحلة. يحتاج جولة تحقيق مستقلة (تحديد كل نقاط الاستخدام والحساب/الفلاتر المعتمِدة عليه) قبل أي تعديل، ثم تطبيق نفس منطق الثلاث حالات إن كان ملائمًا، مع الانتباه لبيانات قديمة فعلية تحمل "جزئي" (تصنيفها لحالة حقيقية واحدة من الثلاث، لا تحويل عشوائي). لم يبدأ العمل عليه.
- [ ] **تعميم هوية DashTable على بقية جداول دليل المقررات (2026-08-23، نُشر على الموقع الرسمي واعتمده سليمان)**: `course_schedule_admin_screen.dart` - جدول "تسكين الشعب الرئيسي" (`_buildTableTab`/`_recordsDataTable`) حُوِّل بالكامل لـ`DashTable`/`DashTableCard`: عمود "أعلى حد" أصبح شريط تقدّم ملوَّن (`DashProgressCell`)، وعمود "النشاط" أصبح `DashBadgeCell`. جدول "المحاضر" أبقى تصميمه الداخلي باستحسان صريح من سليمان - فقط بطاقته المحيطة وُحِّدت. **متبقٍ تعميم نفس الهوية على**: `_buildSectionsListCard`، `_buildQuotaReportCard` بنفس الملف، و`advising_cases_admin_screen.dart`/`advisor_students_lookup_screen.dart` بملفات أخرى - لم تُطبَّق بعد.
- [ ] **تقرير "بحسب اليوم" للحذف والإضافة (2026-08-21)**: سليمان يريد إحصائيات يومية دقيقة (اليوم 1/2/3: منجز/محوَّل/لم يُعمل عليه) قابلة للتصدير حسب الشطر والقسم لرفعها للجهات العليا. **يحتاج تعديل بنية قاعدة البيانات**: تسجيل تاريخ/وقت لحظة كل تغيير حالة فعلي (متى كتب المرشد "تم التنفيذ"، متى قرّر منسّق القسم...) - غير موجود حاليًا (تُخزَّن الحالة الأخيرة فقط بلا تاريخ). يحتاج أيضًا تحديد "تاريخ بداية الدورة" (نقطة الصفر لحساب رقم اليوم). لم يبدأ العمل عليه - مرحلة قادمة، تفاصيل النقاش الكامل بـREADME.md.

- [ ] **تعميم قاعدة "الخيار الافتراضي بالفلتر = اسم الحقل" على كل صفحات الموقع (2026-08-21)**: طبّقناها فقط على قسم "متابعة سير العمل" بلوحة الإدارة (`admin_executive_dashboard_screen.dart`) - الشطر/القسم العلمي/الحالة تعرض اسم الحقل نفسه بدل "كل X" حين لا فلتر مُختار. سليمان طلب صراحةً تعميمها على **كل الفلاتر بكل الصفحات** (تقارير، متابعة حالات الإرشاد، بحث مرشد، إلخ) - لم يبدأ العمل عليها بعد بغير لوحة الإدارة. التفاصيل والقاعدة الكاملة بـ[[feedback_filter_default_shows_field_name]].
- [ ] **بانتظار اختبار سليمان الحي - ملف المرشد الجديد ولوحة الإدارة (2026-08-21)**: إعادة تصميم ملف المرشد (قائمتان منسدلتان + عمود سبب آخر) + متابعة سير عمل ثلاثية البطاقات بلوحة الإدارة (تفاصيل كاملة بـREADME.md). **نُشر على قناة المعاينة وتأكَّد سليمان بصريًا أن شكل البطاقات الثلاث أوضح من السابق** (https://sss-advising-tu--preview-forms-import-fix-b6z3irkl.web.app). **متبقٍ**: اختبار رفع ملف مُعالَج حقيقي (مرشد يختار تم/لم يتم + سببًا) والتأكد أن الدمج يعمل وتظهر أرقام منطقية بالبطاقات، قبل أي نشر على الموقع الفعلي [[feedback_no_deploy_until_all_notes]].
- [ ] **بقايا توحيد لوحة الإرشاد بعد النشر (2026-08-21، حُدِّث 2026-08-22)**: المرحلتان الأولى والثانية اكتملتا ونُشرتا (انظر README.md) - نظام التصميم المشترك، التنقّل الداخلي، الحالات الفارغة، بطاقات الحالات، نافذة مسار المتابعة، فصل مؤشرات "متابعة حالات الإرشاد" لأساسية/ثانوية، ترقيم صفحات حقيقي بدل القصّ الصامت، تجميع شرائح المرشدين الموحَّد بـ`AdvisingAdvisorChip`. **متبقٍ (نطاق أصغر الآن)**: (أ) إعادة تصميم شريط الفلاتر (الشطر/القسم/المرشد/البحث) وجدول "متابعة حالات الإرشاد" بالكامل بهوية `DashTokens` بدل `AppColors`/`_FilterField`/`_FilterTokens` المحلية القديمة، (ب) إعادة تصميم جدول "بحث عن مرشد" ونتائجه بهوية `DashTokens`. (ج) اختبار Responsive الحي أُنجِز 2026-08-22 وكشف 3 أعطال - **الثلاثة أُصلحت وتحقَّقت حيًّا بنفس اليوم (انظر README.md)**: صفحة "متابعة الحالات" الفارغة (تراجع عن شريط فلاتر مثبَّت غير مستقر)، انهيار الجوال بـ"نظرة عامة" (`Expanded` خاطئ داخل عمود غير مُقيَّد الارتفاع)، وتجاوز 1px ببطاقات المؤشرات.
- [ ] **تثبيت رؤوس أعمدة جدول "متابعة الحالات" (Sticky Column Headers) - مؤجَّل عمدًا لخطورته (2026-08-22)**: كلا الخيارين (إعادة بناء الجدول يدويًا بلا `DataTable`، أو حزمة تجريبية `two_dimensional_scrollables`) يحملان خطورة حقيقية على منطق مشترك بين 14 تبويبًا. جرَّبت شريط فلاتر مثبَّت مبسَّط عبر `SliverPersistentHeader` بنفس اليوم فسبَّب فعليًا فراغًا كاملًا بالصفحة (استثناءات Sliver حقيقية) - تم التراجع عنه فورًا. يحتاج جلسة مستقلة مركَّزة بالكامل مع اختبار حي شامل قبل أي محاولة أخرى.
- [ ] **اختبار حي لتصميم مركز التقارير الجديد (2026-08-21)**: أُعيد تصميم `reports_hub_screen.dart` (بحث محسَّن + فصل مستويي الفلتر + عدّاد نتائج + hover/focus + شبكة استجابة) حسب مواصفة سليمان الكاملة (46 بندًا) - لم يُختبَر بصريًا (لا `flutter` بهذه البيئة) ولم يُنشَر بعد. التفاصيل بـREADME.md.
- [ ] **بنود معلّقة من مواصفة توحيد صفحات الموقع العام (2026-08-20)**: تعذّر تنفيذها ضمن جلسة التوحيد الأخيرة - (أ) عرض/تنزيل صورة التقويم الجامعي الرسمي عالي الدقة بصفحة "التقويم الجامعي": لا يوجد ملف صورة للتقويم الرسمي بالمشروع حتى الآن، يحتاج سليمان رفعه أولًا. (ب) فصل عمودي "يوم البداية/يوم النهاية" عن التاريخ بجدول التقويم: البيانات موجودة فعليًا لكن مدموجة داخل نص التاريخ نفسه (مثال: "الأحد 1448/03/27هـ...")، يحتاج فصلها لعمودين مستقلَّين تعديلًا في بنية `_CalendarEvent` نفسها - لم يُنفَّذ. (ج) اختبار Responsive فعلي على قياسات الشاشة المختلفة (1920/1440/1024/768/430/390): لا يوجد متصفح بهذه البيئة، التعديلات طُبِّقت بمنطق مرن (Wrap/LayoutBuilder) لكن لم تُختبَر بصريًا.
- [ ] **توحيد الهوية البصرية لكل الموقع (بدأ 2026-08-20، عمل جارٍ)**: سليمان طلب صراحةً توحيد هوية "الحذف والإضافة" (كانت موثَّقة بالكود كنظام ألوان مستقل عمدًا) كهوية واحدة فعلية بكل صفحات البوابة، بلا فراغات بيضاء، شاملةً "الخدمات الأكاديمية وتفريعاتها والمنسوبين وتفريعاتها وكل شيء بالموقع". أُنشئ `lib/theme/dashboard_tokens.dart` (DashTokens/DashKpiCard/DashCardShell/DashActionCard/DashSectionHeader - نسخة مُصدَّرة حرفيًا من `_Tokens`/`_KpiCard`/`_CardShell` بـ`ticket_action_stats_panel.dart`) كمرجع مشترك. طُبِّق حتى الآن على: `advising_hub_screen.dart` (لوحة الإرشاد - KPI بشريط أعلى ملوَّن + دائرة تغطية إرشادية + بطاقات إجراء)، `academic_services_hub_screen.dart` (خدمات أكاديمية)، `reports_hub_screen.dart` (تقارير). **متبقٍ (لم يبدأ بعد)**: صفحة "المنسوبين" (`college_roster_admin_screen.dart`) وتفريعاتها، بوابات المنسّقين (`coordinator_workspace_screen.dart`, `college_coordinator_workspace_screen.dart`, `unit_coordinator_workspace_screen.dart`), `hardship_cases_admin_screen.dart`, `support_cases_admin_screen.dart`, وبقية صفحات الموقع. لم يُنشَر أي من هذا بعد - بانتظار بقية ملاحظات سليمان قبل النشر [[feedback_no_deploy_until_all_notes]].
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-20)**: إعادة هيكلة صفحة "رفع وتنزيل الملفات" لـ3 أقسام (حذف/إضافة، المقررات الدراسية، الإرشاد) + إصلاح خلل تخطيط حرج (`IntrinsicHeight` كان يُعطّل الصفحة كليًا) + إحصائية عدد مقررات كل شطر. يحتاج تأكيد أن الصفحة تتّسع فعليًا بلا تمرير على شاشة سليمان الفعلية (الهدف المُعلَن صراحةً)، وأن الإحصائية تعرض الأرقام الصحيحة. التفاصيل بـREADME.md.
- [ ] **مراجعة الحالات السابقة المتأثرة بخلل مطابقة "حذف" (2026-08-20)**: أُصلح خلل حقيقي كان يمنع مطابقة أي حالة "حذف مقرر" عند رفع ملف معالج (انظر README.md) - إن كانت هناك دورة حذف/إضافة حقيقية سابقة تم فيها رفع ملفات معالجة تتضمن حالات حذف، فهذه الحالات على الأرجح **لم تُسجَّل كمكتملة رغم معالجتها فعليًا من المرشد/المنسّق** (ظهرت "غير مطابَقة" حينها بصمت أو تجوهلت). يحتاج سليمان تحديد إن كانت هناك دورة سابقة متأثرة تستدعي مراجعة/تصحيحًا يدويًا لحالات الحذف فيها.
- [ ] **مصدر موحّد لعرض أسماء الأقسام العلمية عبر كل صفحات النظام (2026-08-19)**: يوجد أصلاً `lib/data/academic_department_names.dart` لمطابقة/توحيد أسماء الأقسام من بيانات خام (تقارير الطلاب/ملف المنسوبين)، لكن لا يوجد بعد قائمة عرض واحدة (الأقسام الخمسة بصيغتها النهائية) تُستخدَم في كل مكان تُذكر فيه الأقسام (لوحة الإدارة، الفلاتر، التقارير، الخدمات الأكاديمية...) - حاليًا كل صفحة قد تكتب الاسم يدويًا. سليمان أكّد صراحةً: "أسماء الأقسام بيانات أساسية للنظام وليست نصوصًا تصميمية قابلة للاجتهاد". يشمل أيضًا تصحيح أي ظهور متبقٍ لـ"قسم الاقتصاد والتمويل والتأمين" (الاسم الخطأ) في أي صفحة أخرى غير لوحة الإدارة (صُحِّح هناك فقط حتى الآن).
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-20)**: ربط لوحة الإدارة الرئيسية ببيانات Firestore حقيقية - منجَز ومنشور (التفاصيل الكاملة بـREADME.md)، يحتاج تأكيد سليمان أن الأرقام تعكس واقع بياناته الفعلية بعد استخدام حقيقي (خاصةً "طلبات متأخرة"/"مصعدة لإدارة الوحدة" اللذين يعتمدان على `stage_downloads` الجديدة - لن تظهر أي حالة متأخرة إطلاقًا حتى تُسجَّل أول عملية تنزيل فعلية بعد هذا النشر، فالبيانات القديمة قبل النشر لا تحمل توقيت تنزيل).
- [ ] **"آخر النشاطات" بلوحة الإدارة لا تزال Mock (2026-08-20)**: تحتاج ميزة "سجل حركات لحظي" جديدة كليًا (من فعل ماذا ومتى) - مؤجَّلة بموافقة سليمان صراحةً، منفصلة عن بقية اللوحة المربوطة الآن ببيانات حقيقية. التفاصيل بـREADME.md.
- [ ] **تعريف "حالات مصعدة لإدارة الوحدة" تقريبي حاليًا (2026-08-20)**: يُحتسَب من (ذوو إعاقة + متوقع تخرجهم) فقط لعدم وجود حقل "تصعيد" صريح بنموذج البيانات - يحتاج مراجعة سليمان لاحقًا إن أراد تعريفًا أدق (مثلاً: حالة تجاوزت مهلة منسّق القسم أيضًا لا المرشد فقط).
- [ ] **بانتظار اختبار سليمان الحي بملفَي الاختبار الاصطناعيين (2026-08-19)**: إعادة بناء `ExcelParserService.parseTickets` لبنية النموذج الجديد (إضافة/حذف/تعديل مستقلة بدل 5 طلبات متكررة) + تصحيح المرشد تلقائيًا عبر `AdvisorCorrectionService`. الملفان جاهزان بمجلد "ملفات اختبار الفورم الجديد" بجذر المشروع: يرفع سليمان أولًا `sample_advising_report.xlsx` (تقرير إرشاد) ثم `synthetic_add_delete_change_responses.xlsx` (60 استجابة) من شاشة رفع ملف Forms الحالية، ويتأكد أن: عدد وأنواع العمليات لكل طالب صحيحة، وأن حالات المرشد المختار خطأً عمدًا انتقلت لمرشدها الفعلي الصحيح. لم يُختبَر بعد على النظام الفعلي (لا تتوفر أداة flutter بهذه البيئة). التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-18)**: تقليل حجم دفعة كتابة تقارير "كل الكليات" من 20 إلى 6 قطع/دفعة لحل تكرار خطأ Firestore "Transaction too big" عند الاعتماد. التفاصيل بـREADME.md.
- [ ] **استكمال "أدوات إضافية" الجوّالة - 3 روابط مؤجَّلة (2026-08-16)**: أُضيف تبويب "أدوات إضافية" بالشريط السفلي جوّالًا (إدارة/مدير عام) بـ5 روابط بسيطة، لكن 3 روابط من نفس القائمة الأصلية على الويب لا تزال "قيد التطوير" جوّالًا: (أ) "تقارير متابعة الحذف والإضافة" و(ب) "تنزيل ملفات الحالات" - منطقهما داخل `_AdminWorkspaceScreenState` الخاصة بالويب (`admin_workspace_screen.dart`)، يحتاج استخراجًا لكودجت مستقل قابل لإعادة الاستخدام من الجوال قبل النقل؛ (ج) "تفريغ البيانات" - حذف نهائي لكل الحالات المرفوعة، يحتاج تصميم حوار تأكيد آمن لشاشة جوّال ضيقة (تجنّب الضغط العرضي) قبل عرضه هناك.
- [ ] **تأكيد ظهور إحصائيات "الرئيسية"/"لوحة الإدارة" بعد APK الأحدث (2026-08-16)**: أول اختبار حي أظهر الصفحتين فارغتين بلا بطاقات إحصاء - الكود الحالي يعرض البطاقات دائمًا (نص "قيد الرصد التلقائي" بدل فراغ لو لا بيانات)، فالأرجح أن ذلك APK سبق وصول هذا الكود. يحتاج سليمان تثبيت APK الأحدث (يشمل إصلاحات ج59) والتأكد إن كانت المشكلة حُلَّت تلقائيًا أو لا تزال قائمة فعليًا.
- [ ] **ربط صلاحية الدخول تلقائيًا بالمنصب من ملف "منسوبو الكلية" (خطة مؤجَّلة، اتُّفق عليها 2026-08-16)**: سليمان يريد أن يرتبط منح صلاحية "admin" (رئيس/نائب رئيس الوحدة، عميد الكلية، وكيلة الكلية) - وأيضًا الأدوار الأخرى (أمين/سكرتير الوحدة) - **بمن يشغل المنصب فعليًا بعمود "المنصب" في ملف منسوبي الكلية**، لا بحساب ثابت يُنشأ يدويًا مرة واحدة. الفكرة: لو تغيّر العميد/الوكيلة/رئيس الوحدة مستقبلاً بملف جديد يُرفع، ينتقل الدخول تلقائيًا لمن يحل محله بلا أي تدخل يدوي (لا `manage_staff_accounts.js update-role`).
  - **القرار المتفق عليه بخصوص المصدر**: ملف "منسوبو الكلية" هو المصدر (فيه رقم المنسوب اللازم لربط الحساب) - **لا** ورقة "تشكيل الوحدة" المنفصلة (تُستخدَم لصفحة "تواصل معنا"/الهيكل التنظيمي فقط، بلا رقم منسوب، فلا تصلح لربط دخول تقنيًا).
  - عمود "المنصب" بالملف يدعم أصلًا القيم المعيارية: عميد/عميدة، وكيل كلية/وكيلة كلية، رئيس وحدة/رئيسة وحدة، نائب رئيس وحدة/نائبة رئيس وحدة (`lib/data/position_catalog.dart`). **يحتاج إضافة قيمتين جديدتين**: أمين وحدة/أمينة وحدة، سكرتير وحدة/سكرتيرة وحدة - لتغطية بقية الأدوار (أمين/سكرتير الوحدة) من نفس المصدر أيضًا، بدل الاقتصار على عميد/وكيلة/رئيس/نائب فقط.
  - **آلية العمل المتوقَّعة**: عند الدخول، يُطابَق رقم منسوب الحساب مع حقل `staffNumber` بسجلات ملف منسوبي الكلية المرفوع (`CollegeRosterMember`)، ثم يُقرأ عمود "المنصب" لتحديد الصلاحية تلقائيًا (شبيه بمنطق `CollegeRosterLookupService.deanName`/`unitManagerFor` الموجود أصلًا للعرض فقط - يحتاج تعميمه ليُستخدَم لقرار الصلاحية الفعلي بـ`portal_role_gate.dart`، لا للعرض وحده).
  - **يبقى لازمًا رغم هذا**: إنشاء حساب Firebase فردي (بريد/كلمة سر) لكل شخص عبر `manage_staff_accounts.js create` كالمعتاد - هذا التعديل يُلغي فقط الحاجة لتحديث الدور يدويًا عند تغيّر شاغل المنصب، لا ينشئ الحساب نفسه تلقائيًا.
  - **مؤجَّل بطلب سليمان صراحةً**: "احفظ هذه الطريقة واجل العمل عليها، لدينا ما هو أهم الآن". لم يُنفَّذ أي كود بعد.
- [x] ~~إعادة بناء تطبيق الجوال من الصفر (خطة `shiny-exploring-whale.md`)~~ - **قرار مُستبدَل كليًا (2026-08-23)**: بدل الاستمرار بترقيع "CBA Advising" (`main_advising_app.dart`/`lib/web_portal/mobile_*`)، قرَّر سليمان **تجميده كما هو** وبناء تطبيق جوال جديد كليًا "بوابة الإرشاد" (`main_advising_portal.dart`/`lib/mobile_portal/*`) - راجع README.md لتفاصيل المرحلة الأولى المُنجزة.
- [x] ~~بوابة الإرشاد - تحقيق مصدر بيانات "الإرشاد"~~ - **حُلّ (2026-08-23)**: مصدره الفعلي `AdvisingCaseAnalyzer` + `AdvisingReportRepository` (تقارير Excel مرفوعة + مطابقة منسوبي الكلية)، لا `HardshipCaseService` كما افتُرض أول الأمر. استُخرجت خدمة عامة جديدة `lib/services/advising_overview_stats_service.dart` تستدعي حرفيًا نفس الدوال العامة المستخدَمة بـ`advising_cases_admin_screen.dart` بلا تكرار حساب - **مربوطة الآن فعليًا بالرئيسية** (القسم العلوي "مؤشرات رئيسية لحالات الإرشاد" بأرقام حقيقية 100%، راجع README.md).
- [x] ~~بوابة الإرشاد - تبويب "الإرشاد" الفرعي بلوحة الإدارة~~ - **مُنجز (2026-08-23)**: نفس بطاقات الرئيسية الأربع + بطاقة "الكل" الخامسة (أُعيدت هنا فقط - صفحة تفصيل، خلافًا للرئيسية المختصرة عمدًا)، وجدولا "التوزيع حسب القسم العلمي" (طلاب/طالبات) بعمودي تابعين لمرشد/على غير مرشدهم لكل قسم - عبر دالة جديدة `AdvisingOverviewStatsService.loadDepartmentBreakdown()` (نفس مصدر التصنيف `classifyAllColleges` بلا تكرار حساب).
- [x] ~~بوابة الإرشاد - تبويب "رفع الملفات"~~ - **مُنجز (2026-08-23)**: 3 فئات فقط بطلب سليمان صراحةً (ملف الفورم رفع+تنزيل/المقررات الدراسية "الحويّة"/الإرشاد الكامل "كل الكليات") - الباقي (ذوو الإعاقة/جداول المواعيد) نادر التغيير، مؤجَّل. **استُخرج منطق رفع الفورم والمقررات من `upload_hub_screen.dart` إلى دوال عامة جديدة بـ`upload_flows.dart`** (`runUploadForms`/`runDownloadFormsZip`/`runUploadCourses`) بلا أي تغيير بالسلوك على الموقع (رفوذ نفسها بالضبط، فقط نُقلت المكان)؛ ملف "كل الكليات" كان جاهزًا أصلًا (`runUploadAllColleges`). بلا تكرار منطق رفع إطلاقًا.
- [ ] **بوابة الإرشاد - المراحل التالية بعد المرحلة الأولى (2026-08-23)**: المرحلة الأولى (دخول + رئيسية بمؤشرات حقيقية + تنقّل سفلي) منجزة (انظر README.md). **متبقٍ بالترتيب المقترَح** - كل مرحلة تُبنى وتُعتمَد من سليمان قبل التالية: (١) "المتابعة" (`متابعة سير العمل` بتفاصيل قابلة للفتح لكل مستوى)، (٢) "رفع الملفات" (نفس منطق الرفع الحالي بالموقع - 4 فئات بطاقة واحدة بكل صف)، (٣) "التنبيهات" (عاجل/متابعة/بيانات/نجاح)، (٤) "البحث" و"المزيد" (سجل عمليات، فتح النظام الكامل، حساب المستخدم). تفاصيل كل شاشة بمواصفة سليمان الكاملة (57 قسمًا) المرفقة بجلسة 2026-08-23.
- [ ] **إعادة هيكلة الدخول والصلاحيات (2026-08-15)** - المراحل 1-3 اكتملت ونُشرت واختُبرت حيًّا بنجاح. مسار "الرعاية الطلابية" فعّال بالكامل (حالات الظروف الخاصة والدعم النفسي). المتبقي: (أ) الإرشاد الشامل وتقرير خاص لبقية المسارات (بانتظار بيانات كاملة لميزة الإرشاد أصلًا)، (ب) حذف الحسابات المشتركة القديمة (ما عدا salfawaz) + سجل حركات (Activity Log) بعد استقرار الدخول الجديد. التفاصيل بـREADME.md.
- [ ] **بانتظار موافقة سليمان قبل النشر (2026-08-15)**: إصلاح جذري لمشكلة "إصلاح المحتوى" بإكسل سطح المكتب لملفات Excel المصدَّرة (يشمل 5 ملفات بالموقع) - حذف "شبح رسم" فارغ كانت تُصدره مكتبة excel تلقائيًا. التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-15)**: إعادة ترتيب/تسمية كل تبويبات "متابعة حالات الإرشاد" الأربعة عشر + توسيع قائمة "عرض" لتستوعب التسميات الأطول. التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-15)**: زر "تفريغ البيانات" جديد لتبويب "حركات الإرشاد" (لم يكن موجودًا أصلاً). التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-15)**: إزالة ألقاب الأسماء من كل نقاط العرض (وحُفظت القاعدة بالذاكرة الدائمة أخيرًا) + حذف عمود التخصص من صفحة بحث المرشد + تسمية الملف المصدَّر باسم المرشد. التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-15)**: خيار "الكل" الجديد بشاشة متابعة حالات الإرشاد + إعادة تصميم صفحة "بحث عن مرشد" (جدول احترافي + تصدير Excel/PDF لأول مرة). التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-15)**: توسيط رأس الجدول، فرز بالنقر على أي عمود، وإصلاح خلل حقيقي بالبحث (لا يوحّد صور الهمزة). التفاصيل بـREADME.md.
- [ ] **بانتظار رفعة جديدة واختبار سليمان الحي بعد النشر (2026-08-15)**: إصلاح جذري نهائي لاستبعاد كليات أخرى حقيقية (زميع/السبيعي/طاشكندي - تحقَّقتُ أنهم من كلية تطبيقية) دون التضحية بطارق حلمى - استثناء ذكي بمطابقة اسم المرشد بملف منسوبي الكلية. التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-15)**: إعادة تصميم "إحصائية المرشدين" - فلتر "حالة النصاب" جديد، جدول مختصر لثماني أعمدة، بطاقة ملخص أوضح، فرز تلقائي بالأهمية. التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-14)**: تراجع عن استبعاد "أي كلية لا تذكر إدارة الأعمال" (كان سبَّب رجوعًا حقيقيًا: اختفاء طارق حلمى بالكامل)، إصلاح وزن أمين القسم بحساب النصاب (0.5 لا صفر)، وتبويب "إحصائية المرشدين" الجديد (بين إعادة التوزيع وحركات الإرشاد) بنفس تصميم ملف سليمان المرجعي. التفاصيل بـREADME.md.
- [ ] **بانتظار رفعة جديدة واختبار سليمان الحي (2026-08-14) - نُشر**: تصحيح إصلاح انزياح أعمدة PDF - الإصلاح الأول (ج11) كان يفحص الحقل الخطأ (ظن أن "الاسم" يصير رقمًا قصيرًا، والحقيقة أن "الرقم الجامعي" هو من يصير قصيرًا و"الاسم" يصير الرقم الجامعي الحقيقي) فلم يعمل إطلاقًا رغم نشره - تحقَّقتُ من البيانات الفعلية بـ Firestore مباشرة (سكربتان تشخيصيان جديدان بـ`tool/admin_cli/`) قبل التصحيح هذه المرة. التفاصيل بـREADME.md.
- [ ] **بانتظار رفعة جديدة واختبار سليمان الحي (2026-08-14)**: دفعة إصلاحات كبيرة على "متابعة حالات الإرشاد" - إصلاح انزياح أعمدة PDF (اسم الطالب يظهر كرقم تسلسل)، تسامح ±1 بالنصاب وإعادة التوزيع، توزيع تلقائي (round-robin) بدل "يلزم قرار يدوي"، استبعاد حالات "مرشد غير موجود بكليتنا إطلاقًا" من اقتراحات إعادة التوزيع، توسيع فلتر المرشد ليشمل كل مرشد بقاعدة البيانات. التفاصيل الكاملة بـREADME.md.
- [ ] **بانتظار رفعة جديدة واختبار سليمان الحي (2026-08-14)**: إصلاح دخول كليات أخرى غير كليتنا (كالكلية التطبيقية) للنظام خطأً - سببه فحص استبعاد الفروع كان يستثني فقط 3 أسماء محدَّدة بدل التحقق من كون الكلية فعلاً "إدارة الأعمال". التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-14)**: إكمال إصلاح "مطوي القيد" - أُضيفت لثلاثة فحوصات نصية مكرَّرة كانت لا تزال تفوّتها (شارة "غير متواجد"، الإخفاء الافتراضي، ترتيب الفرز) - بشرى يجب أن تُخفى وتُصنَّف الآن كعبدالله ضاوي (معار) تمامًا. التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي بعد النشر (2026-08-14)**: استبدال شريط التبويبات الأفقي (كان يفيض ومظهره غير احترافي) بقائمة منسدلة "عرض" قبل فلتر الشطر + توسيع خانة البحث لتشمل المرشدين وأرقامهم بجانب الطلبة. التفاصيل بـREADME.md.
- [ ] **بانتظار رفعة جديدة واختبار سليمان الحي (2026-08-14)**: توسيع "إعادة التوزيع" ليشمل مرشدي الحالة المجمَّدة (معار/مجاز/مبتعث/مطوي القيد) - طلابهم يُقترَح نقلهم تلقائيًا لبقية مرشدي نفس القسم الآن، لا يقتصر الأمر على المعفى كليًا. التفاصيل بـREADME.md.
- [ ] **بانتظار رفعة جديدة واختبار سليمان الحي (2026-08-14)**: إصلاح خلل حذف أعضاء "مطوي القيد" كليًا من الموقع (حالة بشرى جمال بكر عمر) + قاعدة جديدة: طلاب أي عضو معفى/مجمَّد (معار/مجاز/مبتعث/مطوي القيد)/حالات خاصة فقط ينتقلون تلقائيًا لتبويب "على غير مرشدهم". التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي (2026-08-14)**: إعادة تسمية/ترتيب/تبسيط تبويبي الحالات الصحية - "طلبة ذوي الإعاقة على مرشدهم" أصبح التبويب الثاني (قائمة أسماء فقط لمن هم فعليًا لدى أمين/منسّق قسمهم)، و"طلبة ذوي الإعاقة على غير مرشدهم" التبويب الثالث بجانبه (قائمة أسماء المخالفين). التفاصيل بـREADME.md.
- [ ] **بانتظار اختبار سليمان الحي (2026-08-14)**: ثلاثة إصلاحات جديدة بـ"متابعة حالات الإرشاد" لم تُختبَر فعليًا بعد - استبعاد طلاب "إدارة الأعمال التنفيذي" كليًا من كل التصنيفات، إصلاح فرز تبويبات التصنيف الأربعة (كانت غير مرتبة إطلاقًا)، وتحويل تقرير "إعادة التوزيع" لجدول إحصائي (عدد الطلاب المنقولين لكل زوج مرشد سابق/موصى به) بدل سرد كل طالب. التفاصيل الكاملة بـREADME.md.
- [ ] **جلسة 2026-08-14 الطويلة - إعادة هيكلة "متابعة حالات الإرشاد" + سلسلة إصلاحات من الاختبار الحي - كلها منشورة، يحتاج استكمال الاختبار بجلسة قادمة**: التفاصيل الكاملة بـREADME.md. ملخص الحالة الحالية:
  - **التصميم**: 12 تبويبًا (بدل بطاقات/نوافذ منبثقة) + شريط فلترة موحَّد (شطر/قسم/مرشد/بحث) أعلاها يؤثر على الكل فورًا. "حركات الإرشاد" سجل تراكمي دائم (`advisingAdvisorMovements`) لا يُستبدَل أبدًا.
  - **إصلاحات مؤكَّدة نجاحها من سليمان مباشرة**: استبعاد الإداريين من حسابات النصاب، تقريب "الحصة العادلة" لرقم صحيح، إضافة عمود رقم المنسوب لتبويب "مرشدون بلا طلاب"، إصلاح مطابقة الشطر الحرفية الفاشلة (كانت تُخفي 4 تبويبات كاملة بصمت)، استبعاد فروع الجامعة (الخرمة/تربة/رنية) من قراءة ملف "كل الكليات" (لم يُختبَر حيًّا بعد - يحتاج رفعة جديدة).
  - **معلَّق دون تأكيد نهائي**: تبويب "مرشدنا ← طلاب خارجيون" - آخر حالة معروفة قبل إصلاح الفروع كانت بيانات وهمية (أرقام صفحات)؛ يحتاج إعادة رفع الملف واختبار بعد إصلاح الفروع تحديدًا.
  - [x] محسوم (2026-08-14): زر "تفريغ البيانات" كان يفشل بـ`[cloud_firestore/permission-denied]` - **السبب المؤكَّد: جلسة دخول منتهية الصلاحية بالمتصفح**، لا خلل بالقواعد (تحقَّقتُ مجددًا وأعدت نشر `firestore.rules` احتياطًا - كانت محدَّثة أصلاً). حُلَّ فور تسجيل خروج/دخول من جديد - أكَّد سليمان نجاح "تفريغ البيانات" بعدها مباشرة. إن تكرر مستقبلاً: إعادة تسجيل الدخول هي الحل أولاً قبل افتراض خلل بالكود.
  - **طلب مؤجَّل بطلب سليمان صراحةً**: دعم رفع ملفي شطر الطلاب/الطالبات لذوي الإعاقة **معًا بضغطة واحدة** (حاليًا يعملان بس بخطوتين منفصلتين متتاليتين، وهذا يعمل بشكل صحيح فعليًا - فقط ليس بضغطة واحدة).
- [ ] مؤجَّل حتى توفّر ملف "بيانات الطلبة الأكاديمية" الجديد (14 عمودًا: رقم/اسم/جنس/درجة علمية/قسم/فصل التحاق/حالة دراسية/وحدات مسجلة ومتبقية/ساعات نجاح وخطة/معدل تراكمي/جوال/مرشد) - سليمان صرّح أن الحصول عليه صعب حاليًا خلال فترة الحذف والإضافة، سيوفّره لاحقًا. عند توفّره: إعادة تفعيل أيقونة رفعه بشاشة "متابعة حالات الإرشاد" (الكود القديم لا يزال موجودًا بالمشروع بلا حذف) وبطاقتي "طلاب بحاجة متابعة (المعدل)"/"طلاب مفصولون أكاديميًا".

- [x] نُشر (2026-08-14): أُضيفت علامة مائية خفيفة "S/A" (رمادي فاتح جدًا D9D9D9، أسفل يسار كل صفحة) لكل ملفات PDF الصادرة عن الموقع - بطلب سليمان صراحةً، على غرار نفس العلامة الموجودة بملفات Word الرسمية للوحدة. أُضيفت مركزيًا في `PdfBrandKit.footer` (يغطي `report_pdf_service.dart` تلقائيًا)، وأُضيفت يدويًا لتذييل كل الخدمات الأخرى: `advising_schedule_pdf_service.dart` (نسختي القسم الواحد وكل الأقسام)، `advising_case_pdf_service.dart`، `course_table_pdf_service.dart`، `instructor_schedule_pdf_service.dart`، `teaching_quota_pdf_service.dart`، `unit_guide_pdf_service.dart` (صفحات الغلاف والمحتوى المتدفق). **استُثني عمدًا**: `buildAddDropManualFormPdf` بنفس الملف الأخير (نموذج ورقي بحدود مرسومة بدقة لكل الصفحة - إضافة العلامة قد تتداخل بصريًا مع تخطيطه الضيق، يحتاج قرارك إن رغبت بإضافتها هناك أيضًا). **لم يشمل ملفات Excel المُصدَّرة** (طلب سليمان ذكر PDF والمخرجات عمومًا - يحتاج توضيح إن كان يقصد Excel أيضًا). لم يُنشَر بعد.
- [x] نُشر (2026-08-14): أعيد تصميم شريط عنوان اليوم بتقرير "كل الأقسام" بطلب سليمان الصريح - الشعار انتقل لأقصى يمين الشريط، اسم الوحدة لليسار، والمنتصف صار يعرض عنوانًا ثابتًا "جدول توزيع فترات الإرشاد الأكاديمي للفصل الدراسي الأول 1448هـ" بدل اسم اليوم. اسم اليوم نفسه ("يوم الأحد" مثلًا) انتقل لمكان العنوان الثابت السابق أسفل الشريط الأخضر. يُطبَّق تلقائيًا على كل صفحات التقرير.
- [x] نُشر (2026-08-14): **إصلاح جذري لمشكلة شعار الوحدة الكبيرة** - `unit_logo_cropped.png` (استُخدمت سابقًا لحل مشكلة الهامش الشفاف الضخم) تبيّن أن خلفيتها **بيضاء صريحة بلا أي شفافية**، فظهر مربع أبيض قبيح حول الشعار فوق أي خلفية ملوَّنة (الشريط الأخضر) - هذه "المشكلة الكبيرة" التي أبلغ عنها سليمان. أرفق سليمان شعارًا جديدًا شفافًا فعليًا (`المرفقات/عام/1111.png`) - قُصَّ يدويًا بسكربت `pngjs` يحسب صندوق الحدود المرئي فعليًا (بدل الاعتماد على قصّ يدوي بمحرر صور)، أُنتِج `assets/images/unit_logo_final.png` (شفافية 100% محقَّقة بالزوايا الأربع). **وُحِّد استخدامه عبر كل خدمات PDF السبع بالموقع** (كان بعضها يستخدم `unit_logo_light.png` وبعضها `unit_logo_transparent.png` - مصادر مختلفة الجودة) - مصدر شعار واحد موحَّد الآن للموقع بالكامل بطلب سليمان الصريح.
- [x] محسوم (2026-08-13): **تصحيح شامل لأسماء المرشدين المختصرة بكل ملفات توزيع فترات الإرشاد العشرة** - سليمان لاحظ أن الأسماء تظهر مختصرة (اسم أول وأخير فقط، مثال: "خالد الغامدي") بدل الاسم الكامل كملف منسوبي الكلية ("خالد احمد مسفر القشيري الغامدي"). صُحِّحت بمطابقة رقم المكتب (أدق من مطابقة الاسم نفسه) مقابل ملف منسوبي الكلية داخل نفس القسم/الشطر - `tool/admin_cli/fix_schedule_names_by_office.js` لتسعة ملفات (نفس تنسيق الموقع، بلا SharedStrings)، و`fix_marketing_boys_schedule_names.js` خاص بملف "التسويق_طلاب.xlsx" (ملف Excel حقيقي مختلف البنية بجدول SharedStrings منفصل - اكتُشف أثناء العمل أنه الوحيد من العشرة غير مبني بمكتبة الموقع). **خلل حقيقي اكتُشف وأُصلح أثناء العمل**: أول محاولة لملف التسويق_طلاب أنتجت فهارس SharedStrings خاطئة (خطأ حسابي في الدالة ضاعف عدّاد الفهرس) ففسد الملف بصمت (قرأ 0 صفوف) - أُعيد من النسخة الاحتياطية وأُصلحت الدالة ثم أُعيد التنفيذ بنجاح. تحقُّق نهائي: كل الملفات العشرة تُقرأ بلا خطأ بعدد الصفوف الصحيح. نسخ احتياطية لكل الملفات قبل التعديل بمجلد `نسخ_احتياطية` (`*.backup-2026-08-13-before-name-fix.xlsx`). **ملاحظة جانبية من التصحيح**: بملف "التسويق_طلاب.xlsx" ظهر أن "براء حسن احمد الحازمي" مسجَّل صفّين منفصلين (نفس رقم المكتب 128) - يبدو خطأ تكرار بالبيانات الأصلية يستحق مراجعتك.
- [ ] **بانتظار رفعك الفعلي (2026-08-13)**: بعد تصحيح الأسماء، الملفات العشرة بمجلد "نماذج_جاهزة_للرفع" جاهزة للرفع الكامل (ليس فقط الثلاثة المتأثرة بإضافة الأعضاء الأربعة سابقًا) - تحتاج إعادة رفع كل العشرة حتى تظهر الأسماء الكاملة الصحيحة بدل المختصرة على الموقع الحي.
- [x] محسوم (2026-08-13): **إعادة تصميم تقرير "كل الأقسام" لاستيعاب أقسام كبيرة العدد بصفحة واحدة** - سليمان: "قسم الاقتصاد شطر الطالبات أعدادهم كبيرة، يجب أن يستوعب الجميع بصفحة واحدة، شرط يوم واحد فيه جدولان (فترة أولى وثانية)". أُضيف تخطيط عمودين جنبًا إلى جنب (`_periodTablesSideBySide` في `advising_schedule_pdf_service.dart`) بدل تكديس الجدولين رأسيًا - يقلّل الارتفاع المطلوب للنصف تقريبًا. **حد أمان مضاف عمدًا**: يُطبَّق فقط حين تكون الفترات فترتين بالضبط وإجمالي الأعضاء ≤70 - وإلا يعود للتكديس الرأسي القابل للتقسيم بين الصفحات (الأصلي)، تفاديًا لخطر تجمّد `pw.Column` اللانهائي الموثَّق سابقًا لو تجاوز المحتوى ارتفاع صفحة واحدة فعليًا. **لم يُختبَر فعليًا** (لا Flutter/Dart بهذه البيئة) - يحتاج اختبارك الحي بعد النشر، خصوصًا لقسم الاقتصاد والتمويل/طالبات (27 عضوًا، أكبر الأقسام عددًا).
- [x] محسوم (2026-08-13): بعد إضافة فاصل الصفحة بين الأقسام، لاحظ سليمان مشكلة جديدة (صفحات شبه فارغة - جدول قسم ينتهي بسطرين فقط أعلى صفحة ثم فراغ كبير، ثم صفحة جديدة كاملة لقسم صغير تاليًا) وطلب: "صغّر الشعار او قلل الفرق بين الاسماء - المهم كل يوم لكل شطر لكل قسم في صفحة وحدة". طُبِّق الاثنان معًا: شعار عنوان اليوم (`_genericHeader`) صُغِّر من 72 إلى 40 نقطة (يتكرر أعلى كل صفحة بتقرير قد يصل عشرات الصفحات)، وحشو كل صف اسم بجدول الفترات (`_cell` عبر `_periodTable`) صُغِّر من 5 إلى 3 - يقلّل هدر المساحة العمودية فيزيد احتمال اكتمال قسم كامل بصفحة واحدة. **لا يضمن 100% عدم انقسام أي قسم كبير جدًا** (حد فعلي لحجم الصفحة)، لكنه يقلّله بشكل كبير.
- [x] محسوم (2026-08-13): إصلاح خلل بصري حقيقي بتقرير "توزيع فترات الإرشاد - كل الأقسام" (`AdvisingSchedulePdfService.buildAll`) - أقسام مختلفة بنفس اليوم كانت تُلصَق ببعضها بلا فاصل صفحة، فلو انتهى جدول قسم قرب أسفل الصفحة يظهر عنوان القسم التالي ملتصقًا به مباشرة بشكل غير احترافي (لاحظه سليمان: "الإدارة مع المحاسبة"). أُضيف `pw.NewPage()` بين كل قسم/شطر داخل نفس صفحات اليوم، نفس مبدأ الفاصل بين الأيام الموجود أصلاً. تحقَّقت أنه الموضع الوحيد بالكود بهذا النمط (لا تقارير أخرى متأثرة). **لم يُنشَر بعد**.
- [x] محسوم (2026-08-13): **خلل حقيقي اكتُشف عند رفعك الفعلي**: أول محاولة إضافة للأعضاء الأربعة عبر مكتبة SheetJS (Node) أفسدت جدول numFmt بملف styles.xml الداخلي (خطأ "custom numFmtId starts at 164 but found a value of 56")، ففشلت قراءة 3 من 4 ملفات بالموقع. أُصلح بإعادة الملفات من النسخ الاحتياطية ثم إعادة إضافة الأعضاء بتعديل XML الداخلي مباشرة (`tool/admin_cli/add_missing_schedule_members_raw.js` عبر `adm-zip`) بلا لمس `styles.xml` إطلاقًا - تحقَّق: `styles.xml` مطابق بايت لبايت للأصل بعد التعديل. نُقلت أيضًا ملفات النسخ الاحتياطية خارج مجلد "نماذج_جاهزة_للرفع" (إلى "نسخ_احتياطية" بجانبه) بعد أن اختارها سليمان بالخطأ ضمن رفعة العشرة ملفات معًا.
- [ ] **بانتظار رفعك اليدوي (2026-08-13)**: أضفت الأعضاء الأربعة الناقصين (بشير بابكر، احمد سبحي، شروق الثبيتي، معاذ الذنيبات) فعليًا لملفات "نماذج جاهزة للرفع" الموجودة (`المرفقات/المرشدين/توزيع فترات الارشاد/توزيع فترات الارشاد محدث/تحديث/نماذج_جاهزة_للرفع/`) - قاعدة 70/30 (يومان فترة أولى + يوم فترة ثانية موزَّع بالتساوي: الأحد×2، الاثنين×1، الثلاثاء×1)، بلا أي تعديل على التوزيع المعتمَد سابقًا لبقية الأعضاء (نسخ احتياطية محفوظة قبل كل تعديل). **الملفات المتأثرة**: `الاقتصاد والتمويل_طلاب.xlsx` (+بشير، احمد سبحي)، `الاقتصاد والتمويل_طالبات.xlsx` (+شروق)، `نظم المعلومات الإدارية_طلاب.xlsx` (+معاذ). يحتاج رفعك اليدوي لهذه الثلاثة عبر شاشة "توزيع فترات الإرشاد" (زر "رفع نموذج معبّأ") حتى تظهر أسماؤهم فعليًا بتقارير الموقع.
- [x] محسوم (2026-08-13): سليمان رفع الملف الموحَّد فعليًا **بصيغة Word (docx)** بعد تحويله يدويًا - اكتُشف خلل حقيقي: فلتر `_filterToOurCollegeDepartments` كان مُطبَّقًا فقط على مسار PDF، لا مسار Word (نفس دالة الرفع لكن فرع مختلف تمامًا بالكود) - فمرّت كل تخصصات كلية الحاسبات غير ذات الصلة بلا استبعاد رغم وجود الفلتر بالكود. أُصلح بتطبيقه على كل المسارات الأربعة الممكنة (PDF/Word × مباشر/بعد اختيار الشطر يدويًا) بدل مسارين فقط.
- [ ] **بانتظار قرار سليمان (2026-08-13)**: الملف الموحَّد الأول (531 صفحة: كليتَي إدارة الأعمال والحاسبات كاملتين) كان كبيرًا جدًا وعلَّق عند سليمان فعليًا. بناءً على طلبه استُخرج بدلاً منه ملف مصغَّر جدًا يحوي فقط كتلة طارق حلمي (`المرفقات/صفحه الارشاد/طلاب تابعين لمرشد-طارق حلمي فقط 13-08-2026م.pdf`، 3 صفحات فقط/104 كيلوبايت، 101 طالبًا، تخصصهم جميعًا "نظم المعلومات الإدارية" - تحقُّق كامل من محتواه). **قرار معلَّق**: هل يُرفع هذا الملف مستقلاً (يتطلب دعم رفع أكثر من ملف واحد بشاشة "متابعة حالات الإرشاد" - غير مدعوم حاليًا)، أم يُدمَج فقط مع ملف كلية إدارة الأعمال وحدها (298 صفحة، بلا كلية الحاسبات كاملة) لتقليل الحجم الإجمالي؟ سكربتات الاستخراج (`tool/admin_cli/extract_college_pages.js`, `merge_college_pages.js`, `extract_tariq_only.js`, `arabic_deshape.js`) جاهزة لإعادة الاستخدام لأي تركيبة يقررها.
- [ ] **بانتظار رفعك الفعلي (2026-08-13)**: أضفت شاشة جديدة "بحث عن مرشد وقائمة طلابه" (`advisor_students_lookup_screen.dart`، من لوحة الإرشاد) بطلبك الصريح - تبحث باسم المرشد وتعرض قائمة طلابه كاملة (رقم/اسم/تخصص). **لا تحتاج أي رفع أو قارئ جديد** - تُبنى مباشرة على بيانات تقرير "طلاب تابعين لمرشد" (`AdvisingReportKind.assigned`) المرفوعة أصلاً عبر شاشة "متابعة حالات الإرشاد" الموجودة. **تحديث الخطة**: بدل الملف المفلتَر لكليتنا فقط + ملف "طلاب على غير مرشدهم" المدموج يدويًا لطارق حلمي (شكلان مختلفان بملف واحد)، قررت استخراج كلية إدارة الأعمال وكلية الحاسبات كاملتين من ملف "جميع الكليات" بنفس الشكل الموحَّد (كتل مرشد/طلاب)، ليصبح الملف متطابقًا بالكامل بلا مزج أشكال. أضفت فلترًا جديدًا (`_filterToOurCollegeDepartments` في `advising_cases_admin_screen.dart`) يُبقي تلقائيًا فقط صفوف "التخصص" المطابقة لأحد أقسام كليتنا الخمسة (يشمل "نظم المعلومات الإدارية" تلقائيًا فتمرّ حالات طارق حلمي بلا استثناء يدوي)، ويستبعد بقية تخصصات كلية الحاسبات (علوم حاسب، هندسة برمجيات...) مع تنبيه صريح بعدد المستبعَدين قبل الاعتماد (نفس نمط الشفافية المعتمَد بكل الموقع). لم يُختبَر بعد فعليًا (لا يوجد Flutter/Dart بهذه البيئة) - يحتاج رفعك للملف الجديد الموحَّد واختبارك الحي.
- [ ] قرار مؤجَّل: ملف "تقرير الارشاد النهائي.xlsx" (3 أوراق، نصاب عادل + حالة وظيفية لكل مرشد) الذي أرسلته أولًا **لن يُعتمَد كمصدر بناء** بقرارك الصريح (2026-08-13: "الاعتماد يكون على الملفات المصدَّرة من المنظومة" لا ملفات مجمَّعة يدويًا) - حُذف الكود المبني عليه (كان قيد الإنشاء، لم يُنشَر). إن احتجت لاحقًا نصاب عادل/حالة وظيفية لكل مرشد بشاشة الموقع، يحتاج ذلك ملفًا مصدَّرًا مباشرة من المنظومة بنفس هذه الحقول - لم يصل بعد.
- [ ] **بانتظار اختبار سليمان الحي (2026-08-13)**: خطة بديلة وقائية - أُضيف لصفحة "تنزيل ملفات الحالات" بلوحة الإدارة زرّان جديدان لكل قسم/شطر: "تنزيل ملف مرحلة المنسّق" (نفس ملف المرحلة 2 الذي يُنزّله المنسّق بنفسه) و"رفع ملف معالج" (نفس دمج `mergeProcessedRows` الذي يستخدمه المنسّق). الهدف: تستطيع الإدارة أداء دور أي منسّق قسم يواجه صعوبة باستخدام الموقع (تنزيل ورفع) دون تعطيل استمرار عمل حساب المنسّق نفسه بالتوازي - إجراء احتياطي فقط، لا يستبدل حساب المنسّق. لم يُنشَر بعد (بانتظار اكتمال بقية الإصلاحات المتفق عليها ثم نشر وموافقة سليمان دفعة واحدة).
- [x] محسوم (2026-08-13): سليمان صحّح أن **حسن عبد الرحيم حسن الزبير** (أمين قسم التسويق) يشغل أيضًا منصب **منسّق قسم التسويق لوحدة الإرشاد لشطر الطلاب** (لم يكن مسجَّلًا بملف منسوبي الكلية إطلاقًا) - أُضيف "منسق وحدة" لعمود "منصب آخر (احتياطي - إن وجد)" بملفه (`المرفقات/أعضاء هيئة التدريس/قالب البيانات النهائي .xlsx`، نسخة احتياطية قبل التعديل: `...xlsx.backup-2026-08-13-hassan-alzubair-unit-coordinator.xlsx`). **مهم**: هذا لا يغيّر تصنيف إرشاده الفعلي (يبقى "حالات خاصة فقط" لأن قاعدة الكود تُعطي أولوية لـ"أمين قسم" حين يكون لنفس قسمه - `advising_load_rules.dart:259`)، فقط يُصحِّح بيانات هويته الوظيفية (يظهر الآن صحيحًا في أي بحث/عرض عن "منسّق قسم التسويق"). بهذا لدى قسم التسويق منسّقان: حسن الزبير (شطر الطلاب) واميرة الفقيه (شطر الطالبات). **لم يُرفَع الملف المُعدَّل على الموقع الحي بعد** - يحتاج رفعك اليدوي عبر شاشة "بيانات منسوبي الكلية" ثم اعتماده.
- [ ] **مراجعة سليمان لملف `المرفقات/تدقيق_توزيع_فترات_الإرشاد.xlsx` (آخر تحديث 2026-08-10 من بيانات الموقع الحي فعليًا بعد رفعة سليمان الأخيرة)**: 11 عضوًا لهم إرشاد (كامل/جزئي/حالات خاصة) حسب تصنيف ملف منسوبي الكلية لكن لا يظهرون بجدول توزيع فترات الإرشاد الحي - القائمة الكاملة بورقة "ناقص - له إرشاد ولا فترة". صفر تعارضات عكسية (لا معفى ظهرت له فترة فعليًا).
- [ ] **بانتظار سليمان لرفع نماذج توزيع الإرشاد (2026-08-10)**: بُنيت 10 ملفات إكسل جاهزة للرفع المباشر (بنفس صيغة نموذج الموقع) في `المرفقات/المرشدين/توزيع فترات الارشاد/توزيع فترات الارشاد محدث/تحديث/نماذج_جاهزة_للرفع/` من ملف `ملف_الإرشاد_الأكاديمي_جميع_الأقسام_محدث.xlsx` الذي جهّزه سليمان - يحتاج مراجعتها ورفعها يدويًا عبر شاشة "توزيع فترات الإرشاد" (زر "رفع نموذج معبّأ") لكل قسم/شطر. تعارضات بسيطة (رقم مكتب مختلف بين الأيام لـ4 مرشدين، وفترتان بنفس اليوم لمرشدة واحدة) حُلَّت تلقائيًا بأخذ القيمة الأكثر تكرارًا - راجعها إن رغبت بتصحيح يدوي.
- [ ] **12 عضوًا بلا رقم مكتب لم يُطابَقوا آليًا مع ملف توزيع الإرشاد (2026-08-10)**: مازن عبدالرحمن المنجومي، احمد حسين سبحي، ايمان سليمان العريفي، هناء محمد سويد، اميره سعد الفقيه، هبه الله عبدالصبور أمين حسن، شروق عايض الثبيتي، سمر كامل حسن عجيب، لمية بنت نورالدين الحسناوي، بشرى جمال بكر عمر، ولاء عمر بارفعه، آلاء عمر أحمد بارفعه - إما غير موجودين إطلاقًا بملف توزيع الإرشاد أو الاسم مختلف الصياغة عمّا بملف منسوبي الكلية. باقي 90 عضوًا (من أصل 102 كانوا فارغين) اكتُملت أرقام مكاتبهم تلقائيًا بمطابقة الأسماء.
- [ ] **بانتظار تأكيد سليمان قبل النشر (2026-08-10)**: تعديلات فصل عمودي "نصاب الإرشاد"/"النصاب التدريسي" وحالة "مجمّدة" الجديدة (`advising_load_rules.dart`، `college_roster_member.dart`، `college_roster_parser_service.dart`، `advising_case_analyzer.dart`، `college_roster_admin_screen.dart`) لم تُختبَر بعد برفع فعلي للملف على الموقع الحي - يحتاج رفع ملف "قالب البيانات النهائي..." المُحدَّث (بعمود "النصاب التدريسي" الجديد الاسم + تنبيه القيم غير المعروفة بدل القائمة المنسدلة) والتأكد أن تصنيفات الإرشاد (كامل/جزئي/معفى/حالات خاصة/مجمّدة) تظهر صحيحة لكل عضو قبل `firebase deploy`. راجع [[project_faculty_columns_meaning]] بالذاكرة للتفاصيل الكاملة.
- [x] محسوم (2026-08-10): القائمة المنسدلة داخل ملف الإكسل جُرِّبت وكسرت قراءة الملف بمكتبة `excel` المستخدَمة بالموقع (تعذّر رفعه) - أُزيلت تمامًا، والملف أُعيد بناؤه نظيفًا. البديل المعتمَد بدلها: تنبيه داخل حوار تأكيد الاعتماد بالموقع نفسه (`_invalidQuotaValues` في `college_roster_admin_screen.dart`) يعرض أي قيمة غير معروفة بعمودي "نصاب الإرشاد"/"النصاب التدريسي" قبل الاعتماد، بلا خطر على الملف المصدر.
- [ ] **بانتظار اختبار سليمان الحي (2026-08-13) - إصلاح مُطبَّق بالكود لم يُنشَر بعد**: تعليق "رفع الملفات المعالجة" بصفحة المنسّق بلا نهاية (بلا خطأ ولا نجاح) على شبكة سليمان تحديدًا يطابق تمامًا عرضًا معروفًا لمكتبة Firestore على الويب: تعتمد افتراضيًا على قناة بث WebChannel تُحجَب/تُعلَّق بصمت خلف بعض شبكات الجامعات/الشركات (بروكسي أو فحص TLS) دون أي خطأ ظاهر. أُضيف بـ`main.dart` (سطر ~29) `FirebaseFirestore.instance.settings = Settings(webExperimentalAutoDetectLongPolling: true)` عند تشغيل نسخة الويب - يكتشف تلقائيًا حاجة الشبكة لـLong-Polling كبديل ويحوّل له عوضًا عن التعليق الصامت؛ الحل الرسمي الموصى به من Firebase لهذه الحالة بالضبط. **لم يُنشَر بعد** (بانتظار موافقتك حسب [[feedback_no_deploy_until_all_notes]]) - يحتاج نشرًا واختبارًا حيًّا فعليًا لرفع ملف معالج حقيقي والتأكد أن التعليق زال قبل اعتباره محسومًا نهائيًا.
- [x] محسوم (تحقُّق 2026-08-13 من كود جلسة سابقة): إعادة تصميم بوابتَي منسّق القسم ومنسّق الكلية أُنجزت فعليًا بالكوميت `7888a14` ("دفعة كبيرة: إعادة تصميم بوابات المنسّقين...") - الشاشتان تستخدمان الآن نفس `PortalScaffold`/`PortalStatCard`/شريط تنقّل علوي بفلسفة `admin_workspace_screen.dart` بالضبط. بوابة "منسّق/أمين الوحدة" تبقى عمدًا رسالة ترحيبية فقط (بلا تصميم كامل) بطلب سليمان الصريح بانتظار اعتماد التشكيل الرسمي - انظر [[project_new_unit_position_pending]]، ليست جزءًا من هذا البند. **متبقٍ فقط**: اختبار حي فعلي على جهاز سليمان (ليس محليًا) للتأكد من عدم تكرار خلل "الصفحة البيضاء" بهذا التصميم الجديد - لم يُؤكَّد بعد.
- [ ] غدًا: سيوفّر سليمان ملف "بيانات الطلبة الأكاديمية" الشامل لكل الطلبة (كان مؤجَّلًا - انظر بند "مؤجَّل بطلب سليمان" أدناه) - عليه العمل على آلية الرفع وبوابة وحدة الإرشاد فور استلامه.
- [ ] متابعة شاشة "تقارير" الجديدة (`reports_hub_screen.dart`، 2026-08-08): كل البطاقات حاليًا تفتح الشاشة الأصلية الكاملة للتقرير (بدل فتح مباشر لنافذة/حوار التصدير بذاته) لأن كل تقرير مبني بعمق داخل شاشة ذات حالة (رفع بيانات/فلاتر/streams). إن رغب سليمان لاحقًا بتقصير الخطوة (مثال: فتح حوار تصدير "التقرير الشامل" مباشرة دون المرور بشاشة `AdminReportsScreen` كاملة)، يحتاج ذلك استخراج منطق كل شاشة لدالة/ويدجت مستقلة قابلة للاستدعاء - عمل إضافي منفصل لكل حالة على حدة.
- [x] محسوم جزئيًا (2026-08-09): حساب "منسّق الوحدة" أُنشئ فعليًا بـFirebase Auth (`unit-coordinator@sss-advising-tu.internal`، كلمة السر `00000000`) والصفحة استُبدلت برسالة ترحيبية فقط ("مرحباً أبو يعقوب" - بانتظار اعتماد التشكيل) بطلب سليمان صراحةً، بلا أي وظيفة رفع فعلية بعد. **متبقٍ**: نشر قواعد `firestore.rules` المعدَّلة محليًا فعليًا (`firebase deploy --only firestore:rules`) - لم تُنشَر بعد رغم إنشاء الحساب، وإعادة وظائف الرفع لاحقًا بعد اعتماد التشكيل الرسمي.
- [ ] **قرار سليمان النهائي بخصوص أرقام المبتعثين (2026-08-08): أرقام ملفنا سليمة تمامًا، لا تُعدَّل إطلاقًا** - الفرق الذي ظهر عند مقارنة ملف "للتأكيد من البيانات.xlsx" الرسمي (رعد الغامدي 4330027، شيماء القرشي 4350149، هند الثبيتي 4310534، أروى الثقفي 4370089، لانا الشريف 4400010، أريج الغامدي 4400007، رهام القثامي 4340133، ندى الزهراني 4330441، لينا الربيعي 4350150، روان الحارثي 4400027) بأرقامهم الأطول بملفنا (مثل 433000227 لرعد) **ليس خطأ بملفنا** - أرقامنا هي المعتمدة. لا حاجة لإضافتهم كصفوف جديدة (موجودون أصلاً) ولا لتصحيح أرقامهم.
  - [x] محسوم: أُضيفت "بشرى جمال بكر عمر" (رقم 4300198، معيد، الاقتصاد والتمويل، طالبات، حالة الموظف "على رأس عمله" كما بالمصدر الرسمي) لورقة "منسوبو الكلية" (181 عضوًا الآن، لا لورقة المبتعثين لاختلاف حالتها). أُعيدت ترتيب كل صفوف الورقة بنفس منطق `FacultySortOrder` فظهرت بجانب زميلاتها بقسم الاقتصاد والتمويل مباشرة. نسخة احتياطية: `...xlsx.backup-2026-08-08-add-bushra.xlsx`.
- [ ] مهمة مستقبلية (سليمان): مراجعة تصميم عرض المحتوى الأقصى (`ConstrainedBox(maxWidth: ...)`) المستخدَم بعشرات المواضع بكل الموقع (720-980px تقريبًا) - على الشاشات العريضة جدًا (2400px+) يترك فراغًا جانبيًا كبيرًا يبدو للوهلة الأولى كعيب. التصميم الحالي مقصود (عمود قراءة مريح) لا خلل برمجي، لكن سليمان يرى أنه يستاهل مراجعة/توسيع لاحقًا - يحتاج قرار تصميم موحّد (رفع الحدود القصوى لقيمة أكبر كـ1200-1400px، أو إبقاءها كما هي) قبل تعديل أي ملف، لأنه يمس شاشات كثيرة.
- [x] محسوم جزئيًا: اختُبرت لوحة "متابعة الإنجاز" فعليًا بحساب منسّق قسم تجريبي - القسمان الجديدان (أداء منسقي الأقسام/الكلية) ظهرا صحيحين، وتأكَّد إخفاء قسم "أداء منسقي الكلية" عن حساب منسّق القسم كما طلب سليمان.
- [ ] متبقٍ من اختبار تقسيم "حالة الإنجاز" لـ3 مراحل: **تحقُّق كود (2026-08-13) بدل الاختبار الحي**: عناوين الأعمدة الستة في `excel_export_service.dart` (`advisorStatusHeader`.. `collegeNotesHeader`) تطابق تمامًا ما يبحث عنه `processed_file_parser_service.dart` بالاسم (لا بالفهرس)، و`_mergeProcessedRowsIntoDocs` في `firestore_ticket_service.dart:195` يحدّث الحقول الستة بشكل صحيح (فقط الحقل المُدخَل فعليًا، بلا مسح حقول جهات أخرى). القفل/الدروب-داون داخل الإكسل نفسه أُزيل عمدًا سابقًا (بند محسوم أعلاه، كسر قراءة المكتبة) فلا ينطبق. **المتبقي الوحيد فعليًا**: رفع ملف معالَج حقيقي على الموقع الحي والتأكد المرئي أن الحقول الثلاثة تُحدَّث صحيحة - يحتاج اختبارك المباشر.
- [x] محسوم جزئيًا (2026-08-25): وصل سليمان أخيرًا ملفا إكسل "بيانات الطلبة الأكاديمية" (قوائم اكسل 481 - طلاب/طالبات - منتظم)، بأعمدة قريبة جدًا من القائمة المطلوبة أدناه (الرقم الجامعي، اسم الطالب، القسم، المعدل التراكمي، ساعات الخطة، الساعات المتبقية، الوضع في الفصل). بُني القارئ (`academic_data_excel_parser_service.dart`)، رفع منفصل لكل شطر بتبويب "رفع وتنزيل الملفات" (يستخدم `AdvisingReportKind.base`/`basePrevious` المجهَّزة سلفًا وغير المستخدَمة)، ودُمجت البيانات بـ`AdvisingCaseAnalyzer.mergeAcademicData`. **تغيير معماري مهم بطلب سليمان صراحةً**: أصبح ملف الإكسل هذا هو **الأصل** الذي يحدّد قائمة طلبة كليتنا في `classifyAllColleges` (لا تقرير "كل الكليات" كما كان سابقًا) - أي طالب بالإكسل لم يظهر بتقرير "كل الكليات" (أو ظهر بلا مرشد) يُصنَّف "بلا مرشد" فورًا؛ تقرير "كل الكليات" يبقى الأصل فقط لحالتي "مرشد خارجي ← طلابنا"/"مرشدنا ← طلاب خارجيون". طُبِّق على شاشة الإدارة فقط الآن (بقرار سليمان) - نظام `analyze` الأقدم (لوحة الإرشاد/صفحة منسّق القسم) لم يُمَس بعد، انظر البند أدناه. أُضيفت أعمدة المعدل/الساعات المجتازة/المتبقية لتبويب "كل الطلاب" بشاشة الإدارة، ولصفحة "بحث عن مرشد وقائمة طلابه" (مرتَّبة تنازليًا حسب المعدل، وعند التساوي: الرقم الجامعي الأصغر أولاً/الأقدم). **لم يُختبَر بعد برفع حي** (لا `flutter` بهذه البيئة) - بانتظار اختبار سليمان الفعلي بعد الرفع.
  - [ ] متبقٍ: تطبيق نفس مبدأ "ملف الإكسل هو الأصل" على نظام `analyze` الأقدم (يغذّي لوحة الإرشاد وصفحة منسّق القسم) - سليمان أجّله عمدًا لجلسة لاحقة بعد التأكد من سلامة شاشة الإدارة أولاً.
  - [ ] متبقٍ: اختبار رفع حي فعلي لملفي الإكسل والتأكد أن أعداد "طلاب بلا مرشد"/"على مرشدهم"/إلخ منطقية بعد التحول لملف الإكسل كأصل (قد تتغيّر الأعداد المعروفة سابقًا لأن نطاق الطلبة المصدر تغيّر).
- [x] محسوم (2026-08-26): قاعدة تصنيف "الطلبة المستجدين" (رقم جامعي يبدأ بـ448 = سنة القبول الحالية 1448هـ) - مؤشر مستقل تمامًا لا يُحسب ضمن "طلبة بلا مرشد"، حتى لو كان له مرشد فعليًا يبقى مصنَّفًا هنا حصرًا. الاستبعاد يحدث **قبل** فحص المرشد (لا بالطرح بعد الحساب). أُضيف تبويب "الطلبة المستجدون" (خامس عشر) بشاشة الإدارة + ملاحظة "المستهدفون بالإرشاد" (= الكل − المستجدون) على بطاقة "الكل". رمز السنة (`AdvisingCaseAnalyzer.currentAdmissionYearPrefix` = '448') **ثابت يدوي يحتاج تحديثًا كل عام دراسي** - لا يوجد بالمشروع تحويل تقويم هجري تلقائي موثوق لاشتقاقه، وسليمان وافق على ثابت موثَّق بدل حساب تلقائي قد يُخطئ بصمت.
  - [ ] متبقٍ: تحديث `currentAdmissionYearPrefix` يدويًا عند كل قبول فصل أول جديد (تذكير سنوي).
  **فكرة مستقبلية لاحظها سليمان**: بعمودي "الساعات المتبقية" و"الساعات المجتازة/ساعات الخطة" (متوفرة الآن فعليًا) يمكن استنتاج هل الطالب على وشك التخرج (أو تخرّج فعليًا) بدل الاعتماد فقط على عمود "الحالة" النصي - لم تُبنَ بعد.
- [x] محسوم: رُفع تطبيق الجوال 1.0.11+12 (يشمل كل إصلاحات جلسة "الصفحة البيضاء") لـGitHub Release وحُدِّث `app_config` بـFirestore - التفاصيل بـREADME.md.
- [ ] متابعة سليمان بعد التثبيت الفعلي: التأكد أن الواجهة الجوّالة الجديدة تظهر صحيحة (بلا انقطاع/بياض) على الجهاز اللوحي الفعلي - لم تُختبَر على جهاز حقيقي، فقط بالكود/التحليل الساكن.
- [ ] بانتظار سليمان: بريد الوحدة الجديد (الرسمي للتواصل) لم يُستلَم بعد - سيُضاف لصفحة "التواصل" (`public_landing_screen.dart`) فور استلامه؛ البريد الحالي (`salfawaz@tu.edu.sa` وغيره بالقائمة) يبقى كما هو بلا تغيير.
- [x] محسوم: طلب سليمان أن تنعكس أعداد الملف المرفوع فعليًا بالإحصائيات (مثال: رفع 6565 سجل). تبيّن أن الفرق الفعلي مصدره استبعادات مشروعة لكنها كانت صامتة (بلا اسم/رقم، دراسات عليا، قسم غير معروف بملف منسوبي الكلية) - لا كاش قديم ولا خلل حساب. أُضيفت شفافية: نافذة تأكيد الرفع تعرض الآن سبب وعدد كل استبعاد صراحةً بدل الاختفاء الصامت.

- [ ] تأكيد ما ظهر عند رفع "بيانات الطلبة الأكاديمية" الموحّد (بعد آخر نشر) - صورة سليمان لم تُقرأ (مشكلة قراءة صور متكررة في الجلسات الطويلة)؛ بانتظار وصف نصي أو تحويل الصورة لـ PDF.
- [ ] تأكيد من سليمان: هل تستطيع منظومة الجامعة سحب تقرير حويّة/جدول دراسي موحّد للشطرين معًا؟ إن نعم -> توحيد رفع المقررات الدراسية مثل بقية التقارير.
- [x] محسوم: خلل حقيقي كان يُسقط بصمت أي صف بمرشد غير موجود بملف منسوبي الكلية (تعذّر تحديد شطره فيُستبعَد كليًا بدل "طلاب على غير مرشدهم") - اكتُشف عبر تتبّع حالة الطالب ٤٤٠٥٠٠٢٨ (موجود ومرتبط صح بالـPDF، لكن غائب تمامًا من نتيجة الدمج). الإصلاح (`advising_report_parser_service.dart`): الصف يُضاف الآن لكلا الشطرين، والدمج (يربط برقم الطالب داخل شطر واحد فقط) يُفلتِر تلقائيًا للشطر الصحيح - مع تنبيه صريح للمستخدم بعدد/أسماء الحالات المتأثرة (500 حالة ظهرت فعليًا) بدل الاختفاء الصامت.
- [x] محسوم نهائيًا - **أخطر خلل اكتُشف بالجلسة، بالكود القديم المشترك (`advising_report_parser_service.dart`) لا بقارئ PDF فقط**: منطق اكتشاف "صف العناوين" كان يفحص فقط هل أي خلية تحوي **جزئيًا** كلمة "اسم" - أي طالب اسمه يبدأ بـ"اسم" (مثال حقيقي اكتُشف بتتبّع الطالب ٤٤٢٥٤٢٣٤: "اسماعيل") يُلبِس الفحص، فيُعامَل صف بياناته خطأً كصف عناوين جديد، فتفشل إعادة اكتشاف الأعمدة، **فتختفي بصمت كل الصفوف التالية بنفس الكتلة**. ما بدا أولًا "قصور مكتبة Syncfusion" (كان الشك الأول قبل التتبّع الدقيق) تبيّن أنه هذا الخلل بالضبط - غير متعلق بمكتبة القراءة إطلاقًا. الإصلاح: لا يُعتمَد الصف كعناوين إلا بعد نجاح تحديد عمودي الاسم والرقم معًا فعليًا. **الأثر المقيس**: "طلاب تابعين لمرشد" ارتفع من 6855 إلى 9082 سجل محليًا (+2227، ~25% من الملف)، وبعد إعادة الرفع الفعلية على الموقع: "طلاب بلا مرشد" هبط من 357 إلى **70**، و"طلاب على غير مرشدهم" من 10 إلى 43، و"طلاب على مرشدهم" من 1739 إلى 2249 (بتاريخ 2026-08-07). كان يُسقط ربع بيانات أي تقرير Word/PDF رفعه صادف اسمًا كهذا، منذ إنشاء الدالة الأصلية.
- [ ] فكرة مستقبلية (سليمان): تعميم قارئ PDF المباشر على بقية تقارير صفحة الإرشاد أيضًا - "طلاب غير تابعين لمرشد"، "الحالات الصحية"، "طلاب على غير مرشدهم" - حاليًا لسا Word فقط، تُبنى فقط إذا احتجناها. الأساس المشترك (`pdf_table_rows_extractor.dart`) أُفرِز فعلًا من قارئ "طلاب تابعين لمرشد" (4 أعمدة فقط) استعدادًا لهذا.
- [ ] **محاولة فاشلة موثَّقة**: جُرِّب بناء قارئ PDF لـ"بيانات الطلبة الأكاديمية" بنفس `PdfTableRowsExtractor` (طلب سليمان توحيد آلية الرفع) - فشل (استخرج سجلين فقط من آلاف)، لأن جدول هذا التقرير أعقد بكثير (14 عمود: رقم/اسم/تخصص/حالة/عمر/جنس/جنسية/درجة علمية/نوع دراسة/3 تواريخ/معدل) مقابل 4 أعمدة بـ"طلاب تابعين لمرشد" - فجوة العمود الثابتة (8 وحدات) غير كافية لتمييز الأعمدة الضيقة المتقاربة (تواريخ/عمر/جنس). حُذف الملف الفاشل عمدًا بدل رفعه (خطر إفساد بيانات صامت). يحتاج معايرة حدود أعمدة مخصَّصة فعلية بجلسة منفصلة إن رغب سليمان بها لاحقًا - "بيانات الطلبة الأكاديمية" تبقى Word حاليًا (تستفيد من إصلاح "اسماعيل" فورًا رغم ذلك).
- [x] محسوم: أعاد سليمان رفع "بيانات الطلبة الأكاديمية" (Word) بالإصلاح الجديد - شطر الطالبات استخرج 812 سجل فقط، بدا للوهلة الأولى خطأً. **تحقّق فعلي ضد ملف PDF المصدر**: 813 رقم جامعي مطابق للنمط بالـPDF الخام مقابل 812 من Word (الفرق 1 = حالة دراسات عليا/موقوفة تأديبيًا استُبعدت عمدًا وصحيحًا) - **812 رقم صحيح فعليًا ومطابق تمامًا للمصدر**، لا خلل ولا فقدان بيانات بالتحويل. النتيجة النهائية بعد رفع كل التقارير بالإصلاح: طلاب على مرشدهم 2249، على غير مرشدهم 43، بلا مرشد 70 (بتاريخ 2026-08-07) - محسوم ونهائي.
- [ ] طلب سليمان (مؤجَّل لجلسة منفصلة): بناء قارئ PDF مخصَّص ومعايَر جيدًا لـ"بيانات الطلبة الأكاديمية" (14 عمودًا) بنفس مبدأ قارئ "طلاب تابعين لمرشد" - المحاولة السريعة اليوم فشلت (انظر البند أعلاه) وحُذفت عمدًا. يحتاج جلسة مخصَّصة لقياس حدود كل عمود بدقة (تواريخ/عمر/جنس/جنسية/درجة علمية متقاربة المسافات) قبل الدفع للإنتاج.
- [ ] **تراجُع عن استنتاج سابق**: 42 حالة "طلاب على غير مرشدهم" مصدرها 4 "مرشدين" غير موجودين بملف منسوبي الكلية - رقية بدوي أحمد آل أحمد الزبيدي، صالح مانع حمد آل سالم، عبدالله معتق سعيد الذبياني، إبرار سعد محمد الحربي. كان الافتراض السابق أنهم غالبًا منسوبون لكلية أخرى أو غير نشطين. **سليمان أكّد بتاريخ 2026-08-08 أن هذا غير صحيح على الأقل لرقية بدوي أحمد آل أحمد الزبيدي**: لديها فعليًا طلاب ترشد عليهم، وهي عضو فعلي بجدول المقررات التدريسية (مسكَّنة بمقرر "إدارة الإنتاج والعمليات")، ما يؤكد أنها منسوبة فعلية للكلية وساقطة سهوًا من ملف منسوبي الكلية (وليست غير نشطة كما افتُرض).
  - بيانات مؤكَّدة من سليمان (مصحَّحة): رقمها الأكاديمي **4311115** (تصحيح لرقم 4310115 المذكور سابقًا بالخطأ)، الدرجة العلمية **معيد**، القسم **الإدارة** (إدارة أعمال)، الحالة الوظيفية "على رأس عمله"، تاريخ 01-03-1431هـ. ملاحظة: الرقم 601332 الظاهر بجدول المقررات التدريسية **غير موثوق** (سليمان صرّح أن تلك البيانات فيها خطأ).
  - **بيانات رقية أصبحت الآن مكتملة كفاية للإضافة** (الدرجة العلمية توفّرت: معيد) - بانتظار تأكيد سليمان النهائي بإضافتها فعليًا لملف منسوبي الكلية، بنفس دفعة إبرار الحربي.
  - يحتاج لاحقًا: (1) استكمال بيانات رقية (الدرجة العلمية على الأقل) ثم إضافتها لملف منسوبي الكلية، (2) التحقق من الأسماء الثلاثة الباقية بنفس الطريقة (هل لهم جدول تدريسي فعلي) قبل الجزم بحالتهم.
  - **تحديث 2026-08-08 (تم التحقق من الثلاثة الباقين)**: بمقارنة أسماء محاضري ملفات "مقررات داخل الكلية" (طلاب/طالبات، الفصل الأول 1448هـ) بملف منسوبي الكلية، **إبرار سعد محمد الحربي لها أيضًا جدول تدريسي فعلي** (تُدرّس "مبادئ الاقتصاد الجزئي" 605201 لشطر الطالبات) وغير موجودة بملف منسوبي الكلية - نفس حالة رقية بالضبط. أما **صالح مانع حمد آل سالم وعبدالله معتق سعيد الذبياني فلم يظهر لهما أي جدول تدريسي** في ملفات "داخل الكلية" - يبقى الافتراض الأصلي (غير نشطين/منسوبون لكلية أخرى) هو الأرجح لهذين الاثنين تحديدًا ما لم يظهر دليل آخر.
  - **بيانات إبرار سعد محمد الحربي المؤكَّدة من سليمان (من سجل آخر لديه)**: الرقم 4380038، الدرجة العلمية **معيد**، القسم **الاقتصاد والتمويل**، الحالة الوظيفية "على رأس عمله"، تاريخ 13-06-1438هـ. بخلاف رقية، بيانات إبرار **مكتملة كفاية للإضافة** (الدرجة العلمية متوفرة) - بانتظار تأكيد سليمان النهائي بإضافتها فعليًا لملف منسوبي الكلية.
  - [x] محسوم بتاريخ 2026-08-08: أضيفت رقية وإبرار فعليًا لملف "قالب البيانات النهائي بعد تحديث المبتعثين 06-08-2026م.xlsx" (ورقة "منسوبو الكلية") بإذن صريح من سليمان - التفاصيل بـREADME.md. باقي الحقول (منصب/بريد/جوال/مكتب) بقيت فارغة كما وافق سليمان. المهمتان الفرعيتان المتبقيتان أعلاه (التحقق من الاثنين الباقيين) ما زالتا مفتوحتين.
- [x] محسوم: خلود راغب والريم الفيصلي كانتا سبب حالات "على غير مرشدهم" المتبقية (مسجَّلتان إدارة بينما طلابهما تسويق فعليًا) - بعد تصحيح قسمهما بملف منسوبي الكلية إلى التسويق ورفعه، وصل العدد لـ0. يؤكد سلامة منطق التوفيق التلقائي لحالات الانتداب بالكود (انظر [[project_kholoud_reem_secondment]] بالذاكرة).
- [ ] "الصفحة لا تستجيب" (تحذير متصفح، ليس تجمّدًا كاملًا) يظهر أحيانًا لثوانٍ فور اختيار ملف PDF كبير قبل وصول المعالجة لخيط `compute()` المنفصل - غير حرج (يزول تلقائيًا) لكن يستاهل تحسين لاحقًا لو تكرر إزعاجه.
